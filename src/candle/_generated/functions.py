"""Auto-generated backward Node classes — DO NOT EDIT.

Generated by tools/autograd/gen_functions.py from derivatives.yaml.
"""
from __future__ import annotations

import math as _math

from ..autograd.node import Node
from .._backends.autograd import (
    _grad_context,
    _strip_autograd_keys,
    _backward_dispatch_keyset,
    _scalar_tensor_like,
)
from .._dispatch.dispatcher import redispatch
from ..autograd.utils import reduce_grad


def _inverse_permutation(dims):
    inv = [0] * len(dims)
    for i, d in enumerate(dims):
        inv[d] = i
    return inv


# ---------------------------------------------------------------------------
# Activation backward helpers (called from generated formulas)
# ---------------------------------------------------------------------------

def _sqrt_backward_helper(grad, result, keyset):
    two = _scalar_tensor_like(result, 2.0)
    return redispatch("div", keyset, grad, redispatch("mul", keyset, two, result))


def _rsqrt_backward_helper(grad, result, keyset):
    half = _scalar_tensor_like(result, -0.5)
    return redispatch("mul", keyset, redispatch("mul", keyset, half, grad), redispatch("pow", keyset, result, 3))


def _exp2_backward_helper(grad, result, keyset):
    ln2 = _scalar_tensor_like(result, _math.log(2.0))
    return redispatch("mul", keyset, redispatch("mul", keyset, grad, result), ln2)


def _sigmoid_backward_helper(grad, result, keyset):
    ones = result._ones_like()
    return redispatch("mul", keyset, grad, redispatch("mul", keyset, result, redispatch("add", keyset, ones, redispatch("neg", keyset, result))))


def _tanh_backward_helper(grad, result, keyset):
    ones = result._ones_like()
    return redispatch("mul", keyset, grad, redispatch("add", keyset, ones, redispatch("neg", keyset, redispatch("mul", keyset, result, result))))


def _threshold_backward_helper(grad, result, threshold, keyset):
    ones = result._ones_like()
    zero = _scalar_tensor_like(result, 0.0)
    mask = redispatch("where", keyset, redispatch("gt", keyset, result, threshold), ones, zero)
    return redispatch("mul", keyset, grad, mask)


def _softplus_backward_helper(grad, input_, beta, threshold, keyset):
    del threshold
    beta_t = _scalar_tensor_like(input_, float(beta))
    return redispatch("mul", keyset, grad, redispatch("sigmoid", keyset, redispatch("mul", keyset, input_, beta_t)))


def _mul_tensor_backward_helper(grad, other, dtype, keyset):
    del dtype
    return redispatch("mul", keyset, grad, other)


def _pow_backward_helper(grad, self_, exponent, keyset):
    """grad * exponent * self**(exponent-1)"""
    if hasattr(exponent, 'shape'):  # tensor exponent
        return redispatch("mul", keyset, grad,
               redispatch("mul", keyset, exponent,
               redispatch("pow", keyset, self_, redispatch("sub", keyset, exponent, 1))))
    exp = float(exponent)
    return redispatch("mul", keyset, grad,
           redispatch("mul", keyset,
           redispatch("pow", keyset, self_, exp - 1), exp))


def _pow_backward_self_helper(grad, self_, exponent, keyset):
    """Backward for pow.Tensor_Tensor w.r.t. self."""
    return redispatch("mul", keyset, grad,
           redispatch("mul", keyset, exponent,
           redispatch("pow", keyset, self_, redispatch("sub", keyset, exponent, 1))))


def _pow_backward_exponent_helper(grad, self_, exponent, result, keyset):
    """Backward for pow w.r.t. exponent: grad * result * log(self)"""
    return redispatch("mul", keyset, grad,
           redispatch("mul", keyset, result,
           redispatch("log", keyset, self_)))


def _div_tensor_self_backward_helper(grad, other, dtype, *extra_and_keyset):
    del dtype
    keyset = extra_and_keyset[-1]
    return redispatch("div", keyset, grad, other)


def _div_tensor_other_backward_helper(grad, self_, other, *extra_and_keyset):
    keyset = extra_and_keyset[-1]
    return redispatch("neg", keyset, redispatch("div", keyset, redispatch("mul", keyset, grad, self_), redispatch("mul", keyset, other, other)))


def _matmul_backward_helper(grad, self_, other, grad_input_mask, keyset):
    grad_self = reduce_grad(redispatch("matmul", keyset, grad, redispatch("transpose", keyset, other, -1, -2)), self_.shape) if grad_input_mask[0] else None
    grad_other = reduce_grad(redispatch("matmul", keyset, redispatch("transpose", keyset, self_, -1, -2), grad), other.shape) if grad_input_mask[1] else None
    return grad_self, grad_other


def _gelu_backward_helper(grad, self_, approximate, keyset):
    del approximate
    return _gelu_grad(grad, self_, keyset)


def _unsqueeze_to_backward_helper(grad, dim, input_sizes, keyset):
    del dim
    return redispatch("reshape", keyset, grad, input_sizes)


def _sum_to_backward_helper(grad, size, keyset):
    target_shape = tuple(size)
    if tuple(grad.shape) == target_shape:
        return grad
    result = grad
    # Reduce extra leading dimensions
    n_extra = len(result.shape) - len(target_shape)
    if n_extra > 0:
        dims = tuple(range(n_extra))
        result = redispatch("sum", keyset, result, dim=dims, keepdim=False)
    # Reduce broadcast dimensions (size-1 dims)
    for i, (g_dim, s_dim) in enumerate(zip(result.shape, target_shape)):
        if s_dim == 1 and g_dim != 1:
            result = redispatch("sum", keyset, result, dim=i, keepdim=True)
    if tuple(result.shape) != target_shape:
        result = redispatch("reshape", keyset, result, target_shape)
    return result


def _permute_backward_helper(grad, dims, keyset):
    inv = [0] * len(dims)
    for i, d in enumerate(dims):
        inv[d] = i
    return redispatch("permute", keyset, grad, inv)


def _select_backward_symint_helper(grad, input_sizes, dim, index, keyset):
    del dim, index
    return redispatch("reshape", keyset, grad, input_sizes)


def _cat_tensors_backward_helper(grad, *args):
    keyset = args[-1]
    del keyset
    # Placeholder: preserve runtime by returning the upstream grad for tensor-list cat cases.
    return grad


def _clamp_backward_helper(grad, self_, min_val, max_val, keyset):
    ones = self_._ones_like()
    zero = _scalar_tensor_like(self_, 0.0)
    mask = ones
    if min_val is not None:
        mask = redispatch("mul", keyset, mask, redispatch("where", keyset, redispatch("ge", keyset, self_, min_val), ones, zero))
    if max_val is not None:
        mask = redispatch("mul", keyset, mask, redispatch("where", keyset, redispatch("le", keyset, self_, max_val), ones, zero))
    return redispatch("mul", keyset, grad, mask)


    one = _scalar_tensor_like(self_, 1.0)
    denom = redispatch("add", keyset, one, redispatch("abs", keyset, self_))
    denom_sq = redispatch("mul", keyset, denom, denom)
    return redispatch("div", keyset, grad, denom_sq)


def _silu_grad(grad, self_, keyset):
    sig = redispatch("sigmoid", keyset, self_)
    ones = self_._ones_like()
    one_minus_sig = redispatch("add", keyset, ones, redispatch("neg", keyset, sig))
    x_mul = redispatch("mul", keyset, self_, one_minus_sig)
    factor = redispatch("mul", keyset, sig, redispatch("add", keyset, ones, x_mul))
    return redispatch("mul", keyset, grad, factor)


def _gelu_grad(grad, self_, keyset):
    sqrt2 = _scalar_tensor_like(self_, _math.sqrt(2.0))
    x_over_sqrt2 = redispatch("div", keyset, self_, sqrt2)
    erf_val = redispatch("erf", keyset, x_over_sqrt2)
    ones = self_._ones_like()
    half = _scalar_tensor_like(self_, 0.5)
    cdf = redispatch("mul", keyset, half, redispatch("add", keyset, ones, erf_val))
    coeff = _scalar_tensor_like(self_, 1.0 / _math.sqrt(2.0 * _math.pi))
    x_sq = redispatch("mul", keyset, self_, self_)
    neg_half_x_sq = redispatch("mul", keyset, _scalar_tensor_like(self_, -0.5), x_sq)
    pdf = redispatch("mul", keyset, coeff, redispatch("exp", keyset, neg_half_x_sq))
    x_pdf = redispatch("mul", keyset, self_, pdf)
    factor = redispatch("add", keyset, cdf, x_pdf)
    return redispatch("mul", keyset, grad, factor)


def _mish_grad(grad, self_, keyset):
    ones = self_._ones_like()
    sp = redispatch("softplus", keyset, self_)
    tanh_sp = redispatch("tanh", keyset, sp)
    tanh_sp_sq = redispatch("mul", keyset, tanh_sp, tanh_sp)
    sech2 = redispatch("add", keyset, ones, redispatch("neg", keyset, tanh_sp_sq))
    sig = redispatch("sigmoid", keyset, self_)
    tail = redispatch("mul", keyset, self_, redispatch("mul", keyset, sech2, sig))
    factor = redispatch("add", keyset, tanh_sp, tail)
    return redispatch("mul", keyset, grad, factor)


def _leaky_relu_grad(grad, self_, negative_slope, keyset):
    pos_mask = redispatch("sign", keyset, redispatch("relu", keyset, self_))
    ones = self_._ones_like()
    nonpos_mask = redispatch("add", keyset, ones, redispatch("neg", keyset, pos_mask))
    slope = _scalar_tensor_like(self_, negative_slope)
    factor = redispatch("add", keyset, pos_mask, redispatch("mul", keyset, nonpos_mask, slope))
    return redispatch("mul", keyset, grad, factor)


def _elu_grad(grad, self_, alpha, keyset):
    pos_mask = redispatch("sign", keyset, redispatch("relu", keyset, self_))
    ones = self_._ones_like()
    nonpos_mask = redispatch("add", keyset, ones, redispatch("neg", keyset, pos_mask))
    alpha_t = _scalar_tensor_like(self_, float(alpha))
    exp_x = redispatch("exp", keyset, self_)
    neg_branch = redispatch("mul", keyset, alpha_t, exp_x)
    factor = redispatch("add", keyset, pos_mask, redispatch("mul", keyset, nonpos_mask, neg_branch))
    return redispatch("mul", keyset, grad, factor)


def _celu_grad(grad, self_, alpha, keyset):
    alpha_t = _scalar_tensor_like(self_, float(alpha))
    small_eps = _scalar_tensor_like(self_, 1e-7)
    ones = self_._ones_like()
    pos_mask = redispatch("sign", keyset, redispatch("relu", keyset,
        redispatch("add", keyset, self_, small_eps)))
    neg_mask = redispatch("add", keyset, ones, redispatch("neg", keyset, pos_mask))
    exp_x_alpha = redispatch("exp", keyset, redispatch("div", keyset, self_, alpha_t))
    deriv = redispatch("add", keyset, pos_mask, redispatch("mul", keyset, neg_mask, exp_x_alpha))
    return redispatch("mul", keyset, grad, deriv)


def _hardtanh_grad(grad, self_, min_val, max_val, keyset):
    min_t = _scalar_tensor_like(self_, float(min_val))
    max_t = _scalar_tensor_like(self_, float(max_val))
    ge_min = redispatch("ge", keyset, self_, min_t)
    le_max = redispatch("le", keyset, self_, max_t)
    mask = redispatch("mul", keyset,
        redispatch("where", keyset, ge_min, self_._ones_like(), _scalar_tensor_like(self_, 0.0)),
        redispatch("where", keyset, le_max, self_._ones_like(), _scalar_tensor_like(self_, 0.0)))
    return redispatch("mul", keyset, grad, mask)


def _hardswish_grad(grad, self_, keyset):
    three = _scalar_tensor_like(self_, 3.0)
    sixth = _scalar_tensor_like(self_, 1.0 / 6.0)
    two = _scalar_tensor_like(self_, 2.0)
    ones = self_._ones_like()
    zero = _scalar_tensor_like(self_, 0.0)
    gt_neg3 = redispatch("gt", keyset, self_, redispatch("neg", keyset, three))
    lt_3 = redispatch("lt", keyset, self_, three)
    inner_mask = redispatch("mul", keyset,
        redispatch("where", keyset, gt_neg3, ones, zero),
        redispatch("where", keyset, lt_3, ones, zero))
    ge_3_mask = redispatch("where", keyset, redispatch("ge", keyset, self_, three), ones, zero)
    two_x_plus_3 = redispatch("add", keyset, redispatch("mul", keyset, two, self_), three)
    inner_grad = redispatch("mul", keyset, two_x_plus_3, sixth)
    dout = redispatch("add", keyset,
        redispatch("mul", keyset, inner_grad, inner_mask), ge_3_mask)
    return redispatch("mul", keyset, grad, dout)


def _hardsigmoid_grad(grad, self_, keyset):
    three = _scalar_tensor_like(self_, 3.0)
    sixth = _scalar_tensor_like(self_, 1.0 / 6.0)
    ones = self_._ones_like()
    zero = _scalar_tensor_like(self_, 0.0)
    gt_neg3 = redispatch("gt", keyset, self_, redispatch("neg", keyset, three))
    lt_3 = redispatch("lt", keyset, self_, three)
    inner_mask = redispatch("mul", keyset,
        redispatch("where", keyset, gt_neg3, ones, zero),
        redispatch("where", keyset, lt_3, ones, zero))
    return redispatch("mul", keyset, grad, redispatch("mul", keyset, inner_mask, sixth))


# Aliases for backward helpers (match _SPECIAL_CALLS transpiler names)
_hardtanh_backward_helper = _hardtanh_grad
_hardswish_backward_helper = _hardswish_grad
_hardsigmoid_backward_helper = _hardsigmoid_grad


def _clamp_backward_min_max_helper(grad, self_, min_val, max_val, grad_input_mask, keyset):
    """Backward for clamp with both min and max tensor."""
    return _clamp_backward_helper(grad, self_, min_val, max_val, keyset), None, None


def _to_padded_tensor_backward_helper(grad, self_):
    raise NotImplementedError("to_padded_tensor backward is not supported for nested tensors")


def _selu_grad(grad, self_, keyset):
    SCALE = 1.0507009873554804934193349852946
    ALPHA = 1.6732631921893986195596513061800
    scale_t = _scalar_tensor_like(self_, SCALE)
    alpha_scale_t = _scalar_tensor_like(self_, SCALE * ALPHA)
    small_eps = _scalar_tensor_like(self_, 1e-7)
    ones = self_._ones_like()
    pos_mask = redispatch("sign", keyset, redispatch("relu", keyset,
        redispatch("add", keyset, self_, small_eps)))
    neg_mask = redispatch("add", keyset, ones, redispatch("neg", keyset, pos_mask))
    exp_x = redispatch("exp", keyset, self_)
    neg_deriv = redispatch("mul", keyset, alpha_scale_t, exp_x)
    deriv = redispatch("add", keyset,
        redispatch("mul", keyset, pos_mask, scale_t),
        redispatch("mul", keyset, neg_mask, neg_deriv))
    return redispatch("mul", keyset, grad, deriv)


def _softmax_grad(grad, self_, dim, keyset):
    s = redispatch("softmax", keyset, self_, dim)
    gs = redispatch("mul", keyset, grad, s)
    gs_sum = redispatch("sum", keyset, gs, dim=dim, keepdim=True)
    return redispatch("mul", keyset, s, redispatch("add", keyset, grad,
        redispatch("neg", keyset, gs_sum)))


def _log_softmax_grad(grad, self_, dim, keyset):
    log_s = redispatch("log_softmax", keyset, self_, dim)
    s = redispatch("exp", keyset, log_s)
    grad_sum = redispatch("sum", keyset, grad, dim=dim, keepdim=True)
    return redispatch("add", keyset, grad,
        redispatch("neg", keyset, redispatch("mul", keyset, s, grad_sum)))


def _prelu_grad_input(grad, self_, weight, keyset):
    pos_mask = redispatch("sign", keyset, redispatch("relu", keyset, self_))
    ones = self_._ones_like()
    nonpos_mask = redispatch("add", keyset, ones, redispatch("neg", keyset, pos_mask))
    factor = redispatch("add", keyset, pos_mask, redispatch("mul", keyset, nonpos_mask, weight))
    return redispatch("mul", keyset, grad, factor)


def _prelu_grad_weight(grad, self_, weight, keyset):
    pos_mask = redispatch("sign", keyset, redispatch("relu", keyset, self_))
    ones = self_._ones_like()
    nonpos_mask = redispatch("add", keyset, ones, redispatch("neg", keyset, pos_mask))
    w_input = redispatch("mul", keyset, nonpos_mask, self_)
    return redispatch("mul", keyset, grad, w_input)


# ---------------------------------------------------------------------------
# Norm backward helpers (called from generated formulas)
# ---------------------------------------------------------------------------

def _norm_stats(x, axes, eps, keyset):
    """Shared: compute mean, diff, inv_std, x_hat for norm backward."""
    mean = redispatch("mean", keyset, x, dim=axes, keepdim=True)
    diff = redispatch("add", keyset, x, redispatch("neg", keyset, mean))
    var = redispatch("mean", keyset, redispatch("mul", keyset, diff, diff), dim=axes, keepdim=True)
    eps_t = _scalar_tensor_like(x, eps)
    inv_std = redispatch("rsqrt", keyset, redispatch("add", keyset, var, eps_t))
    x_hat = redispatch("mul", keyset, diff, inv_std)
    return inv_std, x_hat


def _norm_grad_core(dl_dxhat, x_hat, inv_std, axes, n, keyset):
    """Shared: compute grad_input from dl_dxhat, x_hat, inv_std."""
    n_t = _scalar_tensor_like(x_hat, float(n))
    mean_dl = redispatch("div", keyset,
        redispatch("sum", keyset, dl_dxhat, dim=axes, keepdim=True), n_t)
    mean_dl_xhat = redispatch("div", keyset,
        redispatch("sum", keyset, redispatch("mul", keyset, dl_dxhat, x_hat),
                   dim=axes, keepdim=True), n_t)
    return redispatch("mul", keyset, inv_std,
        redispatch("add", keyset,
            redispatch("add", keyset, dl_dxhat, redispatch("neg", keyset, mean_dl)),
            redispatch("neg", keyset, redispatch("mul", keyset, x_hat, mean_dl_xhat))))


def _layer_norm_grad_input(grad, input_, normalized_shape, weight, eps, keyset):
    norm_shape = tuple(normalized_shape)
    ndim = len(input_.shape)
    n_norm = len(norm_shape)
    axes = tuple(range(ndim - n_norm, ndim))
    inv_std, x_hat = _norm_stats(input_, axes, eps, keyset)
    dl_dxhat = redispatch("mul", keyset, grad, weight) if weight is not None else grad
    n = 1
    for d in axes:
        n *= input_.shape[d]
    return _norm_grad_core(dl_dxhat, x_hat, inv_std, axes, n, keyset)


def _layer_norm_backward_all(grad, input_, normalized_shape, weight, bias, eps, keyset):
    norm_shape = tuple(normalized_shape)
    ndim = len(input_.shape)
    n_norm = len(norm_shape)
    axes = tuple(range(ndim - n_norm, ndim))
    inv_std, x_hat = _norm_stats(input_, axes, eps, keyset)
    dl_dxhat = redispatch("mul", keyset, grad, weight) if weight is not None else grad
    n = 1
    for d in axes:
        n *= input_.shape[d]
    grad_input = _norm_grad_core(dl_dxhat, x_hat, inv_std, axes, n, keyset)
    grad_weight = None
    if weight is not None:
        batch_dims = tuple(range(ndim - n_norm))
        grad_weight = redispatch("sum", keyset, redispatch("mul", keyset, grad, x_hat), dim=batch_dims)
    grad_bias = None
    if bias is not None:
        batch_dims = tuple(range(ndim - n_norm))
        grad_bias = redispatch("sum", keyset, grad, dim=batch_dims)
    return grad_input, grad_weight, grad_bias


def _batch_norm_grad_input(grad, input_, weight, eps, keyset):
    ndim = len(input_.shape)
    axes = (0,) + tuple(range(2, ndim))
    shape_for_stats = [1, input_.shape[1]] + [1] * (ndim - 2)
    inv_std, x_hat = _norm_stats(input_, axes, eps, keyset)
    if weight is not None:
        dl_dxhat = redispatch("mul", keyset, grad, weight.reshape(shape_for_stats))
    else:
        dl_dxhat = grad
    n = 1
    for ax in axes:
        n *= input_.shape[ax]
    return _norm_grad_core(dl_dxhat, x_hat, inv_std, axes, n, keyset)


def _group_norm_grad_input(grad, input_, num_groups, weight, eps, keyset):
    N = input_.shape[0]
    C = input_.shape[1]
    spatial = input_.shape[2:]
    channels_per_group = C // num_groups
    group_size = channels_per_group
    for s in spatial:
        group_size *= s
    reshaped = redispatch("reshape", keyset, input_,
                          (N, num_groups, channels_per_group, *spatial))
    axes = tuple(range(2, len(reshaped.shape)))
    inv_std, x_hat = _norm_stats(reshaped, axes, eps, keyset)
    grad_reshaped = redispatch("reshape", keyset, grad,
                               (N, num_groups, channels_per_group, *spatial))
    if weight is not None:
        w_shape = [1, num_groups, channels_per_group] + [1] * len(spatial)
        w_reshaped = redispatch("reshape", keyset, weight, tuple(w_shape))
        dl_dxhat = redispatch("mul", keyset, grad_reshaped, w_reshaped)
    else:
        dl_dxhat = grad_reshaped
    grad_out = _norm_grad_core(dl_dxhat, x_hat, inv_std, axes, group_size, keyset)
    return redispatch("reshape", keyset, grad_out, input_.shape)


def _rms_norm_grad_input(grad, input_, normalized_shape, weight, eps, keyset):
    norm_shape = tuple(normalized_shape)
    ndim = len(input_.shape)
    n_norm = len(norm_shape)
    axes = tuple(range(ndim - n_norm, ndim))
    x_sq = redispatch("mul", keyset, input_, input_)
    variance = redispatch("mean", keyset, x_sq, dim=axes, keepdim=True)
    eps_t = _scalar_tensor_like(input_, eps)
    rms = redispatch("sqrt", keyset, redispatch("add", keyset, variance, eps_t))
    x_hat = redispatch("div", keyset, input_, rms)
    dl_dxhat = redispatch("mul", keyset, grad, weight) if weight is not None else grad
    n = 1
    for d in axes:
        n *= input_.shape[d]
    n_t = _scalar_tensor_like(input_, float(n))
    dot = redispatch("sum", keyset,
        redispatch("mul", keyset, dl_dxhat, x_hat), dim=axes, keepdim=True)
    return redispatch("div", keyset,
        redispatch("add", keyset, dl_dxhat,
            redispatch("neg", keyset, redispatch("mul", keyset, x_hat,
                redispatch("div", keyset, dot, n_t)))),
        rms)


# ---------------------------------------------------------------------------
# Conv backward helpers (Phase 2)
# ---------------------------------------------------------------------------

class _ConvGradCache:
    """Cache conv backward results to avoid recomputation across grad formulas."""
    _cache = {}

    @classmethod
    def get(cls, key, compute_fn):
        if key not in cls._cache:
            cls._cache[key] = compute_fn()
        return cls._cache[key]

    @classmethod
    def clear(cls):
        cls._cache.clear()


def _conv_backward_all(grad, input_, weight, bias, stride, padding, dilation, groups, name, keyset):
    """Compute all conv gradients, returning (grad_input, grad_weight, grad_bias).

    Delegates to the existing _conv_backward implementation.
    Always returns a 3-tuple (grad_bias is None when bias is None).
    """
    from .._backends.autograd import _conv_backward
    args = (stride, padding, dilation, groups)
    result = _conv_backward(name, grad, input_, weight, bias,
                            input_, weight, bias, keyset, args, {})
    if len(result) == 2:
        return result[0], result[1], None
    return result


# ---------------------------------------------------------------------------
# Pool backward helpers (Phase 2)
# ---------------------------------------------------------------------------

def _max_pool1d_backward_helper(grad, self_, result, kernel_size, stride, padding, dilation, ceil_mode, keyset):
    from .._backends.autograd import _max_pool1d_backward
    args = (kernel_size, stride, padding, dilation, ceil_mode)
    return _max_pool1d_backward(grad, self_, self_, result, keyset, args, {})[0]


def _max_pool2d_backward_helper(grad, self_, result, kernel_size, stride, padding, dilation, ceil_mode, keyset):
    from .._backends.autograd import _max_pool2d_backward
    args = (kernel_size, stride, padding, dilation, ceil_mode)
    return _max_pool2d_backward(grad, self_, self_, result, keyset, args, {})[0]


def _max_pool3d_backward_helper(grad, self_, result, kernel_size, stride, padding, dilation, ceil_mode, keyset):
    from .._backends.autograd import _max_pool3d_backward
    args = (kernel_size, stride, padding, dilation, ceil_mode)
    return _max_pool3d_backward(grad, self_, self_, result, keyset, args, {})[0]


def _avg_pool1d_backward_helper(grad, self_, kernel_size, stride, padding, ceil_mode, count_include_pad, keyset):
    from .._backends.autograd import _avg_pool1d_backward
    args = (kernel_size, stride, padding, ceil_mode, count_include_pad)
    return _avg_pool1d_backward(grad, self_, self_, None, keyset, args, {})[0]


def _avg_pool2d_backward_helper(grad, self_, kernel_size, stride, padding, ceil_mode, count_include_pad, keyset):
    from .._backends.autograd import _avg_pool2d_backward
    args = (kernel_size, stride, padding, ceil_mode, count_include_pad)
    return _avg_pool2d_backward(grad, self_, self_, None, keyset, args, {})[0]


def _avg_pool3d_backward_helper(grad, self_, kernel_size, stride, padding, ceil_mode, count_include_pad, keyset):
    from .._backends.autograd import _avg_pool3d_backward
    args = (kernel_size, stride, padding, ceil_mode, count_include_pad)
    return _avg_pool3d_backward(grad, self_, self_, None, keyset, args, {})[0]


def _adaptive_avg_pool1d_backward_helper(grad, self_, output_size, keyset):
    from .._backends.autograd import _adaptive_avg_pool1d_backward
    args = (output_size,)
    return _adaptive_avg_pool1d_backward(grad, self_, self_, None, keyset, args, {})[0]


def _adaptive_avg_pool2d_backward_helper(grad, self_, output_size, keyset):
    from .._backends.autograd import _adaptive_avg_pool2d_backward
    args = (output_size,)
    return _adaptive_avg_pool2d_backward(grad, self_, self_, None, keyset, args, {})[0]


def _adaptive_avg_pool3d_backward_helper(grad, self_, output_size, keyset):
    from .._backends.autograd import _adaptive_avg_pool3d_backward
    args = (output_size,)
    return _adaptive_avg_pool3d_backward(grad, self_, self_, None, keyset, args, {})[0]


def _adaptive_max_pool1d_backward_helper(grad, self_, result, output_size, keyset):
    from .._backends.autograd import _adaptive_max_pool1d_backward
    args = (output_size,)
    return _adaptive_max_pool1d_backward(grad, self_, self_, result, keyset, args, {})[0]


def _adaptive_max_pool2d_backward_helper(grad, self_, result, output_size, keyset):
    from .._backends.autograd import _adaptive_max_pool2d_backward
    args = (output_size,)
    return _adaptive_max_pool2d_backward(grad, self_, self_, result, keyset, args, {})[0]


# ---------------------------------------------------------------------------
# Upsample backward helpers (Phase 2)
# ---------------------------------------------------------------------------

def _upsample_nearest1d_backward_helper(grad, self_, output_size, keyset):
    from .._backends.autograd import _upsample_nearest1d_backward
    args = (output_size,)
    return _upsample_nearest1d_backward(grad, self_, self_, None, keyset, args, {})[0]


def _upsample_nearest2d_backward_helper(grad, self_, output_size, keyset):
    from .._backends.autograd import _upsample_nearest2d_backward
    args = (output_size,)
    return _upsample_nearest2d_backward(grad, self_, self_, None, keyset, args, {})[0]


def _upsample_bilinear2d_backward_helper(grad, self_, output_size, align_corners, keyset):
    from .._backends.autograd import _upsample_bilinear2d_backward
    args = (output_size, align_corners)
    return _upsample_bilinear2d_backward(grad, self_, self_, None, keyset, args, {})[0]


def _upsample_linear1d_backward_helper(grad, self_, output_size, align_corners, keyset):
    from .._backends.autograd import _upsample_linear1d_backward
    args = (output_size, align_corners)
    return _upsample_linear1d_backward(grad, self_, self_, None, keyset, args, {})[0]


def _upsample_bicubic2d_backward_helper(grad, self_, output_size, align_corners, keyset):
    from .._backends.autograd import _upsample_bicubic2d_backward
    args = (output_size, align_corners)
    return _upsample_bicubic2d_backward(grad, self_, self_, None, keyset, args, {})[0]


# ---------------------------------------------------------------------------
# Binary backward helpers (Batch 2)
# ---------------------------------------------------------------------------

def _add_backward_all(grad, self_, other, keyset):
    grad_self = reduce_grad(grad, self_.shape) if hasattr(self_, 'shape') else None
    grad_other = reduce_grad(grad, other.shape) if hasattr(other, 'shape') else None
    return grad_self, grad_other


def _sub_backward_all(grad, self_, other, keyset):
    grad_self = reduce_grad(grad, self_.shape) if hasattr(self_, 'shape') else None
    grad_other = None
    if hasattr(other, 'shape'):
        grad_other = reduce_grad(redispatch("neg", keyset, grad), other.shape)
    return grad_self, grad_other


def _mul_backward_all(grad, self_, other, keyset):
    if hasattr(other, 'shape'):
        g = redispatch("mul", keyset, grad, other)
        grad_self = reduce_grad(g, self_.shape)
    else:
        g = redispatch("mul", keyset, grad, _scalar_tensor_like(self_, float(other)))
        grad_self = reduce_grad(g, self_.shape)
    if hasattr(other, 'shape'):
        g = redispatch("mul", keyset, grad, self_)
        grad_other = reduce_grad(g, other.shape)
    else:
        grad_other = None
    return grad_self, grad_other


def _div_backward_all(grad, self_, other, keyset):
    if hasattr(other, 'shape'):
        grad_self = reduce_grad(redispatch("div", keyset, grad, other), self_.shape)
        other_sq = redispatch("mul", keyset, other, other)
        g = redispatch("neg", keyset, redispatch("div", keyset, redispatch("mul", keyset, grad, self_), other_sq))
        grad_other = reduce_grad(g, other.shape)
    else:
        # other is a scalar
        grad_self = reduce_grad(redispatch("div", keyset, grad, _scalar_tensor_like(self_, float(other))), self_.shape)
        grad_other = None
    return grad_self, grad_other


def _pow_backward_all(grad, self_, other, keyset):
    from .._backends.autograd import _pow_backward
    return _pow_backward(grad, self_, other, self_, other, keyset)


def _maximum_backward_all(grad, self_, other, keyset):
    from .._backends.autograd import _maximum_backward
    return _maximum_backward(grad, self_, other, self_, other, keyset)


def _minimum_backward_all(grad, self_, other, keyset):
    from .._backends.autograd import _minimum_backward
    return _minimum_backward(grad, self_, other, self_, other, keyset)


def _fmin_backward_all(grad, self_, other, keyset):
    from .._backends.autograd import _fmin_backward
    return _fmin_backward(grad, self_, other, self_, other, keyset)


def _fmax_backward_all(grad, self_, other, keyset):
    from .._backends.autograd import _fmax_backward
    return _fmax_backward(grad, self_, other, self_, other, keyset)


def _hypot_backward_all(grad, self_, other, keyset):
    from .._backends.autograd import _hypot_backward
    return _hypot_backward(grad, self_, other, self_, other, keyset)


def _logaddexp_backward_all(grad, self_, other, keyset):
    from .._backends.autograd import _logaddexp_backward
    return _logaddexp_backward(grad, self_, other, self_, other, keyset)


def _logaddexp2_backward_all(grad, self_, other, keyset):
    from .._backends.autograd import _logaddexp2_backward
    return _logaddexp2_backward(grad, self_, other, self_, other, keyset)


def _atan2_backward_all(grad, self_, other, keyset):
    from .._backends.autograd import _atan2_backward
    return _atan2_backward(grad, self_, other, self_, other, keyset)


def _inner_backward_all(grad, self_, other, keyset):
    from .._backends.autograd import _inner_backward
    return _inner_backward(grad, self_, other, self_, other, keyset)


# ---------------------------------------------------------------------------
# Unary-args backward helpers (Batch 3)
# ---------------------------------------------------------------------------

def _clamp_backward_helper(grad, input_, min_val, max_val, keyset):
    from .._backends.autograd import _clamp_backward
    args = (min_val, max_val) if min_val is not None or max_val is not None else ()
    kwargs = {}
    if not args:
        kwargs['min_val'] = min_val
        kwargs['max_val'] = max_val
    return _clamp_backward(grad, input_, input_, keyset, args, kwargs)[0]


def _clamp_min_backward_helper(grad, input_, min_val, keyset):
    from .._backends.autograd import _clamp_min_backward
    return _clamp_min_backward(grad, input_, input_, keyset, (min_val,), {})[0]


def _clamp_max_backward_helper(grad, input_, max_val, keyset):
    from .._backends.autograd import _clamp_max_backward
    return _clamp_max_backward(grad, input_, input_, keyset, (max_val,), {})[0]


def _gather_backward_helper(grad, input_, dim, index, keyset):
    from .._backends.autograd import _gather_backward
    return _gather_backward(grad, input_, input_, keyset, (dim, index), {})[0]


def _index_select_backward_helper(grad, input_, dim, index, keyset):
    from .._backends.autograd import _index_select_backward
    return _index_select_backward(grad, input_, input_, keyset, (dim, index), {})[0]


def _repeat_backward_helper(grad, input_, repeats, keyset):
    from .._backends.autograd import _repeat_backward
    return _repeat_backward(grad, input_, input_, keyset, (repeats,), {})[0]


def _norm_backward_helper(grad, input_, p, dim, keepdim, keyset):
    from .._backends.autograd import _norm_backward
    return _norm_backward(grad, input_, input_, keyset, (p,), {"dim": dim, "keepdim": keepdim})[0]


def _prod_backward_helper(grad, input_, dim, keepdim, keyset):
    from .._backends.autograd import _prod_backward
    return _prod_backward(grad, input_, input_, keyset, (), {"dim": dim, "keepdim": keepdim})[0]


def _cumsum_backward_helper(grad, input_, dim, keyset):
    from .._backends.autograd import _cumsum_backward
    return _cumsum_backward(grad, input_, input_, keyset, (dim,), {})[0]


def _pad_backward_helper(grad, input_, pad, keyset):
    from .._backends.autograd import _pad_backward
    return _pad_backward(grad, input_, input_, keyset, (pad,), {})[0]


def _roll_backward_helper(grad, input_, shifts, dims, keyset):
    from .._backends.autograd import _roll_backward
    return _roll_backward(grad, input_, input_, keyset, (shifts, dims) if dims is not None else (shifts,), {"dims": dims} if dims is not None else {})[0]


def _tile_backward_helper(grad, input_, dims, keyset):
    from .._backends.autograd import _tile_backward
    return _tile_backward(grad, input_, input_, keyset, (dims,), {})[0]


def _movedim_backward_helper(grad, input_, source, destination, keyset):
    from .._backends.autograd import _movedim_backward
    return _movedim_backward(grad, input_, input_, keyset, (source, destination), {})[0]


def _diagonal_backward_helper(grad, input_, offset, dim1, dim2, keyset):
    from .._backends.autograd import _diagonal_backward
    return _diagonal_backward(grad, input_, input_, keyset, (offset, dim1, dim2), {})[0]


def _threshold_backward_helper(grad, input_, threshold, keyset):
    from .._backends.autograd import _threshold_backward
    return _threshold_backward(grad, input_, input_, keyset, (threshold,), {})[0]


def _instance_norm_backward_helper(grad, input_, weight, bias, running_mean, running_var, use_input_stats, momentum, eps, cudnn_enabled, keyset):
    from .._backends.autograd import _instance_norm_backward
    args = (weight, bias, running_mean, running_var, use_input_stats, momentum, eps, cudnn_enabled)
    return _instance_norm_backward(grad, input_, input_, keyset, args, {})[0]


def _normalize_backward_helper(grad, input_, p, dim, eps, keyset):
    from .._backends.autograd import _normalize_backward
    return _normalize_backward(grad, input_, input_, keyset, (p, dim, eps), {})[0]


def _cumprod_backward_helper(grad, input_, dim, keyset):
    from .._backends.autograd import _cumprod_backward
    return _cumprod_backward(grad, input_, input_, keyset, (dim,), {})[0]


def _repeat_interleave_backward_helper(grad, input_, repeats, dim, keyset):
    from .._backends.autograd import _repeat_interleave_backward
    return _repeat_interleave_backward(grad, input_, input_, keyset, (repeats,), {"dim": dim})[0]


def _rot90_backward_helper(grad, input_, k, dims, keyset):
    from .._backends.autograd import _rot90_backward
    return _rot90_backward(grad, input_, input_, keyset, (k, dims), {})[0]


def _take_backward_helper(grad, input_, index, keyset):
    from .._backends.autograd import _take_backward
    return _take_backward(grad, input_, input_, keyset, (index,), {})[0]


def _take_along_dim_backward_helper(grad, input_, indices, dim, keyset):
    from .._backends.autograd import _take_along_dim_backward
    return _take_along_dim_backward(grad, input_, input_, keyset, (indices, dim), {})[0]


def _hardshrink_backward_helper(grad, input_, lambd, keyset):
    from .._backends.autograd import _hardshrink_backward
    return _hardshrink_backward(grad, input_, input_, keyset, (lambd,), {})[0]


def _softshrink_backward_helper(grad, input_, lambd, keyset):
    from .._backends.autograd import _softshrink_backward
    return _softshrink_backward(grad, input_, input_, keyset, (lambd,), {})[0]


def _logsumexp_backward_helper(grad, input_, dim, keepdim, keyset):
    from .._backends.autograd import _logsumexp_backward
    return _logsumexp_backward(grad, input_, input_, keyset, (dim,), {"keepdim": keepdim})[0]


def _renorm_backward_helper(grad, input_, p, dim, maxnorm, keyset):
    from .._backends.autograd import _renorm_backward
    return _renorm_backward(grad, input_, input_, keyset, (p, dim, maxnorm), {})[0]


def _unfold_backward_helper(grad, input_, dimension, size, step, keyset):
    from .._backends.autograd import _unfold_backward
    return _unfold_backward(grad, input_, input_, keyset, (dimension, size, step), {})[0]


def _var_backward_helper(grad, input_, dim, unbiased, keepdim, keyset):
    from .._backends.autograd import _var_backward
    return _var_backward(grad, input_, input_, keyset, (), {"dim": dim, "unbiased": unbiased, "keepdim": keepdim})[0]


def _std_backward_helper(grad, input_, dim, keepdim, unbiased, keyset):
    from .._backends.autograd import _std_backward
    return _std_backward(grad, input_, input_, keyset, (), {"dim": dim, "keepdim": keepdim, "unbiased": unbiased})[0]


def _amax_backward_helper(grad, input_, dim, keepdim, keyset):
    from .._backends.autograd import _amax_backward
    return _amax_backward(grad, input_, input_, keyset, (dim,) if dim is not None else (), {"dim": dim, "keepdim": keepdim})[0]


def _amin_backward_helper(grad, input_, dim, keepdim, keyset):
    from .._backends.autograd import _amin_backward
    return _amin_backward(grad, input_, input_, keyset, (dim,) if dim is not None else (), {"dim": dim, "keepdim": keepdim})[0]


def _narrow_backward_helper(grad, input_, dim, start, length, keyset):
    from .._backends.autograd import _narrow_backward
    return _narrow_backward(grad, input_, input_, keyset, (dim, start, length), {})[0]


def _select_backward_helper(grad, input_, dim, index, keyset):
    from .._backends.autograd import _select_backward
    return _select_backward(grad, input_, input_, keyset, (dim, index), {})[0]


def _masked_fill_backward_helper(grad, input_, mask, keyset):
    from .._backends.autograd import _masked_fill_backward
    return _masked_fill_backward(grad, input_, input_, keyset, (mask,), {})[0]


def _diff_backward_helper(grad, input_, n, dim, keyset):
    from .._backends.autograd import _diff_backward
    return _diff_backward(grad, input_, input_, keyset, (n, dim), {})[0]


def _trace_backward_helper(grad, input_, keyset):
    from .._backends.autograd import _trace_backward
    return _trace_backward(grad, input_, input_, keyset, (), {})[0]


def _det_backward_helper(grad, input_, keyset):
    from .._backends.autograd import _det_backward
    return _det_backward(grad, input_, input_, keyset, (), {})[0]


def _diag_backward_helper(grad, input_, diagonal, keyset):
    from .._backends.autograd import _diag_backward
    return _diag_backward(grad, input_, input_, keyset, (diagonal,), {})[0]


def _im2col_backward_helper(grad, input_, kernel_size, dilation, padding, stride, keyset):
    from .._backends.autograd import _im2col_backward
    return _im2col_backward(grad, input_, input_, keyset, (kernel_size, dilation, padding, stride), {})[0]


def _col2im_backward_helper(grad, input_, output_size, kernel_size, dilation, padding, stride, keyset):
    from .._backends.autograd import _col2im_backward
    return _col2im_backward(grad, input_, input_, keyset, (output_size, kernel_size, dilation, padding, stride), {})[0]


def _nansum_backward_helper(grad, input_, dim, keepdim, keyset):
    from .._backends.autograd import _nansum_backward
    return _nansum_backward(grad, input_, input_, keyset, (), {"dim": dim, "keepdim": keepdim})[0]


def _nanmean_backward_helper(grad, input_, dim, keepdim, keyset):
    from .._backends.autograd import _nanmean_backward
    return _nanmean_backward(grad, input_, input_, keyset, (), {"dim": dim, "keepdim": keepdim})[0]


def _matrix_power_backward_helper(grad, input_, n, keyset):
    from .._backends.autograd import _matrix_power_backward
    return _matrix_power_backward(grad, input_, input_, keyset, (n,), {})[0]


def _special_logit_backward_helper(grad, input_, eps, keyset):
    from .._backends.autograd import _special_logit_backward
    return _special_logit_backward(grad, input_, input_, keyset, (eps,) if eps is not None else (), {"eps": eps} if eps is not None else {})[0]


def _linalg_norm_backward_helper(grad, input_, ord, dim, keepdim, keyset):
    from .._backends.autograd import _linalg_norm_backward
    return _linalg_norm_backward(grad, input_, input_, keyset, (ord,), {"dim": dim, "keepdim": keepdim})[0]


def _linalg_vector_norm_backward_helper(grad, input_, ord, dim, keepdim, keyset):
    from .._backends.autograd import _linalg_vector_norm_backward
    return _linalg_vector_norm_backward(grad, input_, input_, keyset, (ord,), {"dim": dim, "keepdim": keepdim})[0]


def _linalg_matrix_norm_backward_helper(grad, input_, ord, dim, keepdim, keyset):
    from .._backends.autograd import _linalg_matrix_norm_backward
    return _linalg_matrix_norm_backward(grad, input_, input_, keyset, (ord,), {"dim": dim, "keepdim": keepdim})[0]


def _linalg_pinv_backward_helper(grad, input_, atol, rtol, hermitian, keyset):
    from .._backends.autograd import _linalg_pinv_backward
    return _linalg_pinv_backward(grad, input_, input_, keyset, (atol, rtol, hermitian), {})[0]


def _linalg_cond_backward_helper(grad, input_, p, keyset):
    from .._backends.autograd import _linalg_cond_backward
    return _linalg_cond_backward(grad, input_, input_, keyset, (p,) if p is not None else (), {})[0]


def _linalg_matrix_exp_backward_helper(grad, input_, keyset):
    from .._backends.autograd import _linalg_matrix_exp_backward
    return _linalg_matrix_exp_backward(grad, input_, input_, keyset, (), {})[0]


def _linalg_tensorinv_backward_helper(grad, input_, ind, keyset):
    from .._backends.autograd import _linalg_tensorinv_backward
    return _linalg_tensorinv_backward(grad, input_, input_, keyset, (ind,), {})[0]


def _linalg_vander_backward_helper(grad, x, N, keyset):
    from .._backends.autograd import _linalg_vander_backward
    return _linalg_vander_backward(grad, x, x, keyset, (N,) if N is not None else (), {"N": N})[0]


def _linalg_eigvalsh_backward_helper(grad, input_, UPLO, keyset):
    from .._backends.autograd import _linalg_eigvalsh_backward
    return _linalg_eigvalsh_backward(grad, input_, input_, keyset, (UPLO,), {})[0]


def _sum_to_size_backward_helper(grad, input_, size, keyset):
    from .._backends.autograd import _sum_to_size_backward
    return _sum_to_size_backward(grad, input_, input_, keyset, (size,), {})[0]


def _sum_backward_helper(grad, input_sizes, dim, keepdim, keyset):
    # Expand reduced gradient back to input shape using Candle's existing expand op.
    if dim is None:
        return redispatch("expand", keyset, grad, input_sizes)
    expanded = grad
    dims = dim if isinstance(dim, (list, tuple)) else [dim]
    if not keepdim:
        for d in sorted(dims):
            expanded = redispatch("unsqueeze", keyset, expanded, d)
    return redispatch("expand", keyset, expanded, input_sizes)


def _mean_backward_helper(grad, input_sizes, dim, numel, keepdim, keyset):
    sum_grad = _sum_backward_helper(grad, input_sizes, dim, keepdim, keyset)
    scale = 1.0 / numel if numel else 0.0
    return redispatch("mul", keyset, sum_grad, scale)


def _slice_backward_helper(grad, input_, dim, start, end, step, keyset):
    from .._backends.autograd import _slice_backward
    return _slice_backward(grad, input_, input_, keyset, (dim, start, end, step), {})[0]


def _slice_backward_wrapper_helper(grad, input_sizes, dim, start, end, step, keyset):
    """Backward for slice using input sizes (shape) rather than the input tensor."""
    from .._backends.autograd import _slice_backward
    placeholder = redispatch('zeros', keyset, tuple(input_sizes), dtype=grad.dtype, device=grad.device)
    return _slice_backward(grad, placeholder, placeholder, keyset, (dim, start, end, step), {})[0]


def _as_strided_backward_helper(grad, self_, size, stride, storage_offset, keyset):
    """Backward for as_strided: scatter grad into zeros of self's shape."""
    grad_out = redispatch('zeros', keyset, self_.shape, dtype=grad.dtype, device=grad.device)
    grad_view = redispatch('as_strided', keyset, grad_out, size, stride, storage_offset)
    redispatch('add_', keyset, grad_view, grad)
    return grad_out


def _as_strided_scatter_backward_helper(grad, self_, src, size, stride, storage_offset, keyset):
    """Backward for as_strided_scatter (TensorGeometry args are just self/src)."""
    from .._backends.autograd import _as_strided_scatter_backward
    return _as_strided_scatter_backward(grad, self_, src, self_, src, keyset, (size, stride, storage_offset), {})


def _getitem_backward_helper(grad, self_, key, keyset):
    from .._backends.autograd import _getitem_backward
    return _getitem_backward(grad, self_, self_, keyset, (key,), {})[0]


def _to_backward_helper(grad, input_, keyset):
    from .._backends.autograd import _to_backward
    return _to_backward(grad, input_, input_, keyset, (), {})[0]


def _quantile_backward_helper(grad, input_, q, dim, keepdim, keyset):
    from .._backends.autograd import _quantile_backward
    args = (q,)
    if dim is not None:
        args = (q, dim)
    return _quantile_backward(grad, input_, input_, keyset, args, {"dim": dim, "keepdim": keepdim})[0]


# ---------------------------------------------------------------------------
# Special function backward helpers (Batch 4)
# ---------------------------------------------------------------------------

def _special_erfinv_grad(grad, self_, keyset):
    from .._backends.autograd import _special_erfinv_backward
    return _special_erfinv_backward(grad, self_, self_, keyset)[0]


def _special_erfcx_grad(grad, self_, keyset):
    from .._backends.autograd import _special_erfcx_backward
    return _special_erfcx_backward(grad, self_, self_, keyset)[0]


def _special_ndtr_grad(grad, self_, keyset):
    from .._backends.autograd import _special_ndtr_backward
    return _special_ndtr_backward(grad, self_, self_, keyset)[0]


def _special_ndtri_grad(grad, self_, keyset):
    from .._backends.autograd import _special_ndtri_backward
    return _special_ndtri_backward(grad, self_, self_, keyset)[0]


def _special_sinc_grad(grad, self_, keyset):
    from .._backends.autograd import _special_sinc_backward
    return _special_sinc_backward(grad, self_, self_, keyset)[0]


def _special_entr_grad(grad, self_, keyset):
    from .._backends.autograd import _special_entr_backward
    return _special_entr_backward(grad, self_, self_, keyset)[0]


def _special_log_ndtr_grad(grad, self_, keyset):
    from .._backends.autograd import _special_log_ndtr_backward
    return _special_log_ndtr_backward(grad, self_, self_, keyset)[0]


def _special_i0e_grad(grad, self_, keyset):
    from .._backends.autograd import _special_i0e_backward
    return _special_i0e_backward(grad, self_, self_, keyset)[0]


def _special_i1_grad(grad, self_, keyset):
    from .._backends.autograd import _special_i1_backward
    return _special_i1_backward(grad, self_, self_, keyset)[0]


def _special_i1e_grad(grad, self_, keyset):
    from .._backends.autograd import _special_i1e_backward
    return _special_i1e_backward(grad, self_, self_, keyset)[0]


def _special_xlogy_backward_all(grad, self_, other, keyset):
    from .._backends.autograd import _special_xlogy_backward
    return _special_xlogy_backward(grad, self_, other, self_, other, keyset)


def _special_xlog1py_backward_all(grad, self_, other, keyset):
    from .._backends.autograd import _special_xlog1py_backward
    return _special_xlog1py_backward(grad, self_, other, self_, other, keyset)


def _special_zeta_backward_all(grad, self_, other, keyset):
    from .._backends.autograd import _special_zeta_backward
    return _special_zeta_backward(grad, self_, other, self_, other, keyset)


def _special_gammainc_backward_all(grad, self_, other, keyset):
    from .._backends.autograd import _special_gammainc_backward
    return _special_gammainc_backward(grad, self_, other, self_, other, keyset)


def _special_gammaincc_backward_all(grad, self_, other, keyset):
    from .._backends.autograd import _special_gammaincc_backward
    return _special_gammaincc_backward(grad, self_, other, self_, other, keyset)


def _linalg_inv_grad(grad, self_, keyset):
    from .._backends.autograd import _linalg_inv_backward
    return _linalg_inv_backward(grad, self_, self_, keyset)[0]


def _linalg_svdvals_grad(grad, self_, keyset):
    from .._backends.autograd import _linalg_svdvals_backward
    return _linalg_svdvals_backward(grad, self_, self_, keyset)[0]


# ---------------------------------------------------------------------------
# FFT backward helpers (Batch 5)
# ---------------------------------------------------------------------------

def _fft_c2c_backward_helper(grad, input_, n, dim, norm, inverse_op, keyset):
    return redispatch(inverse_op, keyset, grad, n=n, dim=dim, norm=norm)


def _fft_r2c_backward_helper(grad, input_, n, dim, norm, inverse_op, keyset):
    result = redispatch(inverse_op, keyset, grad, n=n if n is not None else input_.shape[dim if isinstance(dim, int) else -1], dim=dim, norm=norm)
    return result


def _fft_c2r_backward_helper(grad, input_, n, dim, norm, inverse_op, keyset):
    return redispatch(inverse_op, keyset, grad, dim=dim, norm=norm)


def _fft_shift_backward_helper(grad, input_, dim, inverse_op, keyset):
    return redispatch(inverse_op, keyset, grad, dim=dim)


# ---------------------------------------------------------------------------
# Linalg custom backward helpers (Batch 5)
# ---------------------------------------------------------------------------

def _linalg_det_backward_helper(grad, self_, keyset):
    from .._backends.autograd import _linalg_det_backward
    return _linalg_det_backward(grad, self_, self_, keyset)[0]


def _linalg_slogdet_backward_helper(grad, self_, keyset):
    from .._backends.autograd import _linalg_slogdet_backward
    return _linalg_slogdet_backward(grad, self_, self_, keyset)[0]


def _linalg_cholesky_backward_helper(grad, self_, upper, keyset):
    from .._backends.autograd import _linalg_cholesky_backward
    return _linalg_cholesky_backward(grad, self_, self_, keyset, (upper,))[0]


def _linalg_solve_backward_all(grad, self_, other, left, keyset):
    from .._backends.autograd import _linalg_solve_backward
    return _linalg_solve_backward(grad, self_, other, self_, other, keyset, (left,))


def _linalg_solve_triangular_backward_all(grad, self_, B, upper, left, unitriangular, keyset):
    from .._backends.autograd import _linalg_solve_triangular_backward
    return _linalg_solve_triangular_backward(grad, self_, B, self_, B, keyset, (upper, left, unitriangular))


def _linalg_qr_backward_helper(grad, self_, mode, keyset):
    from .._backends.autograd import _linalg_qr_backward
    return _linalg_qr_backward(grad, self_, self_, keyset, (mode,))[0]


def _linalg_svd_backward_helper(grad, self_, full_matrices, keyset):
    from .._backends.autograd import _linalg_svd_backward
    return _linalg_svd_backward(grad, self_, self_, keyset, (full_matrices,))[0]


def _linalg_eigh_backward_helper(grad, self_, UPLO, keyset):
    from .._backends.autograd import _linalg_eigh_backward
    return _linalg_eigh_backward(grad, self_, self_, keyset, (UPLO,))[0]


def _linalg_eig_backward_helper(grad, self_, keyset):
    from .._backends.autograd import _linalg_eig_backward
    return _linalg_eig_backward(grad, self_, self_, keyset)[0]


def _linalg_lu_backward_helper(grad, self_, pivot, keyset):
    from .._backends.autograd import _linalg_lu_backward
    return _linalg_lu_backward(grad, self_, self_, keyset, (pivot,))[0]


def _linalg_lu_factor_backward_helper(grad, self_, pivot, keyset):
    from .._backends.autograd import _linalg_lu_factor_backward
    return _linalg_lu_factor_backward(grad, self_, self_, keyset, (pivot,))[0]


def _linalg_lu_solve_backward_all(grad, LU, pivots, B, left, adjoint, keyset):
    from .._backends.autograd import _linalg_lu_solve_backward
    return _linalg_lu_solve_backward(grad, LU, pivots, B, LU, pivots, B, keyset, (left, adjoint))


def _linalg_lu_solve_backward_helper(grad, LU, pivots, B, left, adjoint, keyset):
    from .._backends.autograd import _linalg_lu_solve_backward
    return _linalg_lu_solve_backward(grad, LU, pivots, B, LU, pivots, B, keyset, (left, adjoint))[2]


def _linalg_multi_dot_backward_helper(grad, tensors, keyset):
    from .._backends.autograd import _linalg_multi_dot_backward
    return _linalg_multi_dot_backward(grad, tensors, keyset)


# ---------------------------------------------------------------------------
# Special custom backward helpers (Batch 5)
# ---------------------------------------------------------------------------

def _special_polygamma_backward_helper(grad, n, self_, keyset):
    return redispatch("mul", keyset, grad, redispatch("special_polygamma", keyset, n + 1, self_))


def _special_multigammaln_backward_helper(grad, self_, p, keyset):
    from .._backends.autograd import _special_multigammaln_backward
    return _special_multigammaln_backward(grad, self_, self_, keyset, (p,))[0]


# ---------------------------------------------------------------------------
# Multi-tensor backward helpers (Batch 6)
# ---------------------------------------------------------------------------

def _lerp_backward_all(grad, self_, other, weight, keyset):
    from .._backends.autograd import _lerp_backward
    return _lerp_backward(grad, self_, other, weight, keyset)


def _addcmul_backward_all(grad, self_, tensor1, tensor2, value, keyset):
    from .._backends.autograd import _addcmul_backward
    return _addcmul_backward(grad, self_, tensor1, tensor2, value, keyset)


def _addcdiv_backward_all(grad, self_, tensor1, tensor2, value, keyset):
    from .._backends.autograd import _addcdiv_backward
    return _addcdiv_backward(grad, self_, tensor1, tensor2, value, keyset)


def _addmm_backward_all(grad, self_, mat1, mat2, beta, alpha, keyset):
    from .._backends.autograd import _addmm_backward
    return _addmm_backward(grad, self_, mat1, mat2, beta, alpha, keyset)


def _baddbmm_backward_all(grad, self_, batch1, batch2, beta, alpha, keyset):
    from .._backends.autograd import _baddbmm_backward
    return _baddbmm_backward(grad, self_, batch1, batch2, beta, alpha, keyset)


def _where_backward_all(grad, condition, self_, other, keyset):
    from .._backends.autograd import _where_backward
    return _where_backward(grad, condition, self_, other, condition, keyset)[1:]


def _embedding_backward_helper(grad, weight, indices, padding_idx, scale_grad_by_freq, keyset):
    from .._backends.autograd import _embedding_backward
    return _embedding_backward(grad, weight, indices, weight, indices, keyset,
                               padding_idx=padding_idx, scale_grad_by_freq=scale_grad_by_freq)[0]


def _cross_backward_all(grad, self_, other, dim, keyset):
    from .._backends.autograd import _cross_backward
    return _cross_backward(grad, self_, other, self_, other, dim, keyset)


def _min_backward_all(grad, self_, other, keyset):
    from .._backends.autograd import _min_backward
    return _min_backward(grad, self_, other, self_, other, keyset)


def _max_backward_all(grad, self_, other, keyset):
    from .._backends.autograd import _max_backward
    return _max_backward(grad, self_, other, self_, other, keyset)


def _dist_backward_all(grad, self_, other, p, keyset):
    from .._backends.autograd import _dist_backward
    return _dist_backward(grad, self_, other, self_, other, keyset, (p,))


def _cdist_backward_all(grad, self_, other, p, keyset):
    from .._backends.autograd import _cdist_backward
    return _cdist_backward(grad, self_, other, self_, other, keyset, (p,))


def _as_strided_scatter_backward_all(grad, self_, src, size, stride, storage_offset, keyset):
    from .._backends.autograd import _as_strided_scatter_backward
    return _as_strided_scatter_backward(grad, self_, src, self_, src, keyset, (size, stride, storage_offset))


def _masked_select_backward_helper(grad, self_, mask, keyset):
    from .._backends.autograd import _masked_select_backward
    return _masked_select_backward(grad, self_, mask, keyset)[0]


def _tensordot_backward_all(grad, self_, other, dims, keyset):
    from .._backends.autograd import _tensordot_backward
    return _tensordot_backward(grad, self_, other, dims, keyset)


def _grid_sample_backward_all(grad, self_, grid, mode, padding_mode, align_corners, keyset):
    from .._backends.autograd import _grid_sample_backward
    return _grid_sample_backward(grad, self_, grid, self_, grid, keyset,
                                 (mode, padding_mode, align_corners), {})


def _affine_grid_backward_helper(grad, theta, size, align_corners, keyset):
    from .._backends.autograd import _affine_grid_backward
    return _affine_grid_backward(grad, theta, size, align_corners, keyset)


def _rrelu_backward_helper(grad, self_, lower, upper, training, keyset):
    from .._backends.autograd import _rrelu_backward
    return _rrelu_backward(grad, self_, self_, keyset, (lower, upper, training), {})[0]


def _ctc_loss_backward_helper(grad, log_probs, targets, input_lengths, target_lengths, blank, reduction, zero_infinity, keyset):
    from .._backends.autograd import _ctc_loss_backward
    return _ctc_loss_backward(grad, log_probs, targets, input_lengths, target_lengths,
                              blank, reduction, zero_infinity, keyset)


def _sort_backward_helper(grad, self_, result1, dim, keyset):
    from .._backends.autograd import _sort_backward
    return _sort_backward(grad, self_, result1, keyset, (dim,), {})[0]


def _topk_backward_helper(grad, self_, result1, k, dim, keyset):
    from .._backends.autograd import _topk_backward
    return _topk_backward(grad, self_, result1, keyset, (k, dim), {})[0]


def _kthvalue_backward_helper(grad, self_, result1, k, dim, keepdim, keyset):
    from .._backends.autograd import _kthvalue_backward
    return _kthvalue_backward(grad, self_, result1, keyset, (k, dim), {"keepdim": keepdim})[0]


def _cummax_backward_helper(grad, self_, result1, dim, keyset):
    from .._backends.autograd import _cummax_backward
    return _cummax_backward(grad, self_, result1, keyset, (dim,), {})[0]


def _cummin_backward_helper(grad, self_, result1, dim, keyset):
    from .._backends.autograd import _cummin_backward
    return _cummin_backward(grad, self_, result1, keyset, (dim,), {})[0]


# ---------------------------------------------------------------------------
# Tensor[] backward helpers (called from generated formulas)
# ---------------------------------------------------------------------------

def _select_backward_symint_helper(grad, input_sizes, dim, index, keyset):
    from .._creation import zeros as _zeros
    out = _zeros(*input_sizes, dtype=grad.dtype, device=grad.device)
    indexer = [slice(None)] * len(input_sizes)
    indexer[dim] = index
    out[tuple(indexer)] = grad
    return out


def _cat_backward_helper(grad, tensors, dim, keyset):
    from .._backends.autograd import _cat_backward
    return _cat_backward(grad, tensors, None, keyset, (dim,), {})


def _stack_backward_helper(grad, tensors, dim, keyset):
    from .._backends.autograd import _stack_backward
    return _stack_backward(grad, tensors, None, keyset, (dim,), {})


def _hstack_backward_helper(grad, tensors, keyset):
    from .._backends.autograd import _hstack_backward
    return _hstack_backward(grad, tensors, None, keyset, (), {})


def _vstack_backward_helper(grad, tensors, keyset):
    from .._backends.autograd import _vstack_backward
    return _vstack_backward(grad, tensors, None, keyset, (), {})


def _dstack_backward_helper(grad, tensors, keyset):
    from .._backends.autograd import _dstack_backward
    return _dstack_backward(grad, tensors, None, keyset, (), {})


def _column_stack_backward_helper(grad, tensors, keyset):
    from .._backends.autograd import _column_stack_backward
    return _column_stack_backward(grad, tensors, None, keyset, (), {})


def _concat_backward_helper(grad, tensors, dim, keyset):
    from .._backends.autograd import _cat_backward
    return _cat_backward(grad, tensors, None, keyset, (dim,), {})


def _concatenate_backward_helper(grad, tensors, dim, keyset):
    from .._backends.autograd import _cat_backward
    return _cat_backward(grad, tensors, None, keyset, (dim,), {})


def _block_diag_backward_helper(grad, tensors, keyset):
    from .._backends.autograd import _block_diag_backward
    return _block_diag_backward(grad, tensors, keyset)


def _pad_sequence_backward_helper(grad, sequences, batch_first, padding_value, padding_side, keyset):
    from .._backends.autograd import _pad_sequence_backward
    return _pad_sequence_backward(grad, sequences, keyset, batch_first, padding_value, padding_side)


class AbsBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='AbsBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("mul", keyset, grad, redispatch("sign", keyset, self_))
        return (grad_self,)

class AcosBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='AcosBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("mul", keyset, grad, redispatch("neg", keyset, redispatch("rsqrt", keyset, redispatch("add", keyset, redispatch("mul", keyset, redispatch("neg", keyset, self_), self_), 1))))
        return (grad_self,)

class AddTensorBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='AddTensorBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_other_idx = None
        self._alpha = None

    def _save(self, *, self_=None, other=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if other is not None:
            self._saved_other_idx = len(tensors)
            tensors.append(other)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_other_idx is not None:
            self._saved_fields['other'] = self._saved_tensors_list[self._saved_other_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        other = _saved[self._saved_other_idx]
        alpha = self._alpha
        with _grad_context(keyset):
            grad_self = _sum_to_backward_helper(grad, self_.shape, keyset)
            grad_other = _sum_to_backward_helper(redispatch("mul", keyset, grad, alpha), other.shape, keyset)
        return (grad_self, grad_other,)

class AddScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='AddScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._other = None
        self._alpha = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        other = self._other
        alpha = self._alpha
        with _grad_context(keyset):
            grad_self = grad
        return (grad_self,)

class AddbmmBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='AddbmmBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_batch1_idx = None
        self._saved_batch2_idx = None
        self._beta = None
        self._alpha = None

    def _save(self, *, batch1=None, batch2=None):
        tensors = []
        if batch1 is not None:
            self._saved_batch1_idx = len(tensors)
            tensors.append(batch1)
        if batch2 is not None:
            self._saved_batch2_idx = len(tensors)
            tensors.append(batch2)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_batch1_idx is not None:
            self._saved_fields['batch1'] = self._saved_tensors_list[self._saved_batch1_idx]
        if self._saved_batch2_idx is not None:
            self._saved_fields['batch2'] = self._saved_tensors_list[self._saved_batch2_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        batch1 = _saved[self._saved_batch1_idx]
        batch2 = _saved[self._saved_batch2_idx]
        beta = self._beta
        alpha = self._alpha
        with _grad_context(keyset):
            grad_self = redispatch("mul", keyset, grad, beta)
            grad_batch1 = redispatch("mul", keyset, redispatch("bmm", keyset, redispatch("expand", keyset, redispatch("unsqueeze", keyset, grad, 0), [batch1.shape[0], batch1.shape[1], batch2.shape[2]]), redispatch("transpose", keyset, batch2, 1, 2)), alpha)
            grad_batch2 = redispatch("mul", keyset, redispatch("bmm", keyset, redispatch("transpose", keyset, batch1, 1, 2), redispatch("expand", keyset, redispatch("unsqueeze", keyset, grad, 0), [batch1.shape[0], batch1.shape[1], batch2.shape[2]])), alpha)
        return (grad_self, grad_batch1, grad_batch2,)

class AddcdivBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='AddcdivBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_tensor1_idx = None
        self._saved_tensor2_idx = None
        self._value = None

    def _save(self, *, self_=None, tensor1=None, tensor2=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensor1 is not None:
            self._saved_tensor1_idx = len(tensors)
            tensors.append(tensor1)
        if tensor2 is not None:
            self._saved_tensor2_idx = len(tensors)
            tensors.append(tensor2)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_tensor1_idx is not None:
            self._saved_fields['tensor1'] = self._saved_tensors_list[self._saved_tensor1_idx]
        if self._saved_tensor2_idx is not None:
            self._saved_fields['tensor2'] = self._saved_tensors_list[self._saved_tensor2_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        tensor1 = _saved[self._saved_tensor1_idx]
        tensor2 = _saved[self._saved_tensor2_idx]
        value = self._value
        with _grad_context(keyset):
            grad_self = grad
            grad_tensor1 = redispatch("mul", keyset, grad, redispatch("div", keyset, value, tensor2))
            grad_tensor2 = redispatch("mul", keyset, redispatch("neg", keyset, grad), redispatch("mul", keyset, value, redispatch("div", keyset, tensor1, redispatch("mul", keyset, tensor2, tensor2))))
        return (grad_self, grad_tensor1, grad_tensor2,)

class AddcmulBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='AddcmulBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_tensor1_idx = None
        self._saved_tensor2_idx = None
        self._value = None

    def _save(self, *, self_=None, tensor1=None, tensor2=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensor1 is not None:
            self._saved_tensor1_idx = len(tensors)
            tensors.append(tensor1)
        if tensor2 is not None:
            self._saved_tensor2_idx = len(tensors)
            tensors.append(tensor2)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_tensor1_idx is not None:
            self._saved_fields['tensor1'] = self._saved_tensors_list[self._saved_tensor1_idx]
        if self._saved_tensor2_idx is not None:
            self._saved_fields['tensor2'] = self._saved_tensors_list[self._saved_tensor2_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        tensor1 = _saved[self._saved_tensor1_idx]
        tensor2 = _saved[self._saved_tensor2_idx]
        value = self._value
        with _grad_context(keyset):
            grad_self = grad
            grad_tensor1 = redispatch("mul", keyset, grad, redispatch("mul", keyset, tensor2, value))
            grad_tensor2 = redispatch("mul", keyset, grad, redispatch("mul", keyset, tensor1, value))
        return (grad_self, grad_tensor1, grad_tensor2,)

class AddmmBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='AddmmBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_mat1_idx = None
        self._saved_mat2_idx = None
        self._beta = None
        self._alpha = None
        self._self_shape = None

    def _save(self, *, mat1=None, mat2=None, self_=None):
        if self_ is not None:
            self._self_shape = self_.shape
        tensors = []
        if mat1 is not None:
            self._saved_mat1_idx = len(tensors)
            tensors.append(mat1)
        if mat2 is not None:
            self._saved_mat2_idx = len(tensors)
            tensors.append(mat2)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_mat1_idx is not None:
            self._saved_fields['mat1'] = self._saved_tensors_list[self._saved_mat1_idx]
        if self._saved_mat2_idx is not None:
            self._saved_fields['mat2'] = self._saved_tensors_list[self._saved_mat2_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        mat1 = _saved[self._saved_mat1_idx]
        mat2 = _saved[self._saved_mat2_idx]
        beta = self._beta
        alpha = self._alpha
        with _grad_context(keyset):
            grad_self = _sum_to_backward_helper(redispatch("mul", keyset, grad, beta), self._self_shape, keyset)
            grad_mat1 = redispatch("mm_mat1_backward", keyset, grad, mat2, mat1.shape, redispatch("sym_strides", keyset, mat1), redispatch("layout", keyset, mat1), alpha)
            grad_mat2 = redispatch("mm_mat2_backward", keyset, grad, mat1, mat2.shape, redispatch("sym_strides", keyset, mat2), redispatch("layout", keyset, mat2), alpha)
        return (grad_self, grad_mat1, grad_mat2,)

class SparseAddmmBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SparseAddmmBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_mat1_idx = None
        self._saved_mat2_idx = None
        self._beta = None
        self._alpha = None

    def _save(self, *, mat1=None, mat2=None):
        tensors = []
        if mat1 is not None:
            self._saved_mat1_idx = len(tensors)
            tensors.append(mat1)
        if mat2 is not None:
            self._saved_mat2_idx = len(tensors)
            tensors.append(mat2)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_mat1_idx is not None:
            self._saved_fields['mat1'] = self._saved_tensors_list[self._saved_mat1_idx]
        if self._saved_mat2_idx is not None:
            self._saved_fields['mat2'] = self._saved_tensors_list[self._saved_mat2_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        mat1 = _saved[self._saved_mat1_idx]
        mat2 = _saved[self._saved_mat2_idx]
        beta = self._beta
        alpha = self._alpha
        with _grad_context(keyset):
            grad_self = redispatch("mul", keyset, grad, beta)
            grad_mat1 = redispatch("mm_mat1_sparse_backward", keyset, grad, mat1, mat2, alpha)
            grad_mat2 = redispatch("mm_mat2_backward", keyset, grad, mat1, mat2.shape, redispatch("sym_strides", keyset, mat2), redispatch("layout", keyset, mat2), alpha)
        return (grad_self, grad_mat1, grad_mat2,)

class AddmvBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='AddmvBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_vec_idx = None
        self._saved_mat_idx = None
        self._beta = None
        self._alpha = None

    def _save(self, *, vec=None, mat=None):
        tensors = []
        if vec is not None:
            self._saved_vec_idx = len(tensors)
            tensors.append(vec)
        if mat is not None:
            self._saved_mat_idx = len(tensors)
            tensors.append(mat)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_vec_idx is not None:
            self._saved_fields['vec'] = self._saved_tensors_list[self._saved_vec_idx]
        if self._saved_mat_idx is not None:
            self._saved_fields['mat'] = self._saved_tensors_list[self._saved_mat_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        vec = _saved[self._saved_vec_idx]
        mat = _saved[self._saved_mat_idx]
        beta = self._beta
        alpha = self._alpha
        with _grad_context(keyset):
            grad_self = redispatch("mul", keyset, grad, beta)
            grad_mat = redispatch("mul", keyset, redispatch("ger", keyset, grad, vec), alpha)
            grad_vec = redispatch("mul", keyset, redispatch("mv", keyset, redispatch("transpose", keyset, mat, 0, 1), grad), alpha)
        return (grad_self, grad_mat, grad_vec,)

class AddrBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='AddrBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_vec2_idx = None
        self._saved_vec1_idx = None
        self._beta = None
        self._alpha = None

    def _save(self, *, vec2=None, vec1=None):
        tensors = []
        if vec2 is not None:
            self._saved_vec2_idx = len(tensors)
            tensors.append(vec2)
        if vec1 is not None:
            self._saved_vec1_idx = len(tensors)
            tensors.append(vec1)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_vec2_idx is not None:
            self._saved_fields['vec2'] = self._saved_tensors_list[self._saved_vec2_idx]
        if self._saved_vec1_idx is not None:
            self._saved_fields['vec1'] = self._saved_tensors_list[self._saved_vec1_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        vec2 = _saved[self._saved_vec2_idx]
        vec1 = _saved[self._saved_vec1_idx]
        beta = self._beta
        alpha = self._alpha
        with _grad_context(keyset):
            grad_self = redispatch("mul", keyset, grad, beta)
            grad_vec1 = redispatch("mul", keyset, redispatch("mv", keyset, grad, vec2), alpha)
            grad_vec2 = redispatch("mul", keyset, redispatch("mv", keyset, redispatch("transpose", keyset, grad, 0, 1), vec1), alpha)
        return (grad_self, grad_vec1, grad_vec2,)

class AffineGridGeneratorBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='AffineGridGeneratorBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._size = None
        self._align_corners = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        size = self._size
        align_corners = self._align_corners
        with _grad_context(keyset):
            grad_theta = redispatch("affine_grid_generator_backward_symint", keyset, grad, size, align_corners)
        return (grad_theta,)

class AliasBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='AliasBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        with _grad_context(keyset):
            grad_self = grad
        return (grad_self,)

class AngleBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='AngleBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("angle_backward", keyset, grad, self_)
        return (grad_self,)

class AnyBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='AnyBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        return (grad,)

class AnyDimBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='AnyDimBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._dim = None
        self._keepdim = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        dim = self._dim
        keepdim = self._keepdim
        return (grad,)

class AnyDimsBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='AnyDimsBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._dim = None
        self._keepdim = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        dim = self._dim
        keepdim = self._keepdim
        return (grad,)

class IsAllTrueBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='IsAllTrueBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        return (grad,)

class IsAnyTrueBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='IsAnyTrueBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        return (grad,)

class AllBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='AllBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        return (grad,)

class AllDimBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='AllDimBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._dim = None
        self._keepdim = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        dim = self._dim
        keepdim = self._keepdim
        return (grad,)

class AllDimsBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='AllDimsBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._dim = None
        self._keepdim = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        dim = self._dim
        keepdim = self._keepdim
        return (grad,)

class AcoshBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='AcoshBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = (redispatch("mul", keyset, grad, redispatch("mul", keyset, redispatch("rsqrt", keyset, redispatch("add", keyset, self_, 1)), redispatch("rsqrt", keyset, redispatch("sub", keyset, self_, 1)))) if False else redispatch("mul", keyset, grad, redispatch("rsqrt", keyset, redispatch("sub", keyset, redispatch("mul", keyset, self_, self_), 1))))
        return (grad_self,)

class AsinhBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='AsinhBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("mul", keyset, grad, redispatch("rsqrt", keyset, redispatch("add", keyset, redispatch("pow", keyset, self_, 2), 1)))
        return (grad_self,)

class AtanhBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='AtanhBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("mul", keyset, grad, redispatch("div", keyset, 1, redispatch("sub", keyset, 1, redispatch("pow", keyset, self_, 2))))
        return (grad_self,)

class AsStridedBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='AsStridedBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._size = None
        self._stride = None
        self._storage_offset = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        size = self._size
        stride = self._stride
        storage_offset = self._storage_offset
        with _grad_context(keyset):
            grad_self = _as_strided_backward_helper(grad, self_, size, stride, storage_offset, keyset)
        return (grad_self,)

class AsinBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='AsinBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("mul", keyset, grad, redispatch("rsqrt", keyset, redispatch("add", keyset, redispatch("mul", keyset, redispatch("neg", keyset, self_), self_), 1)))
        return (grad_self,)

class AtanBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='AtanBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("div", keyset, grad, redispatch("add", keyset, redispatch("mul", keyset, self_, self_), 1))
        return (grad_self,)

class Atan2Backward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Atan2Backward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_other_idx = None
        self._saved_self_idx = None

    def _save(self, *, other=None, self_=None):
        tensors = []
        if other is not None:
            self._saved_other_idx = len(tensors)
            tensors.append(other)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_other_idx is not None:
            self._saved_fields['other'] = self._saved_tensors_list[self._saved_other_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        other = _saved[self._saved_other_idx]
        self_ = _saved[self._saved_self_idx]
        grad_input_mask = [True, True]
        with _grad_context(keyset):
            _denom = redispatch("add", keyset, redispatch("mul", keyset, self_, self_), redispatch("mul", keyset, other, other))
            grad_self = redispatch("div", keyset, redispatch("mul", keyset, grad, other), _denom) if grad_input_mask[0] else None
            grad_other = redispatch("neg", keyset, redispatch("div", keyset, redispatch("mul", keyset, grad, self_), _denom)) if grad_input_mask[1] else None
        return (grad_self, grad_other,)

class BaddbmmBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='BaddbmmBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_batch2_idx = None
        self._saved_batch1_idx = None
        self._beta = None
        self._alpha = None

    def _save(self, *, batch2=None, batch1=None):
        tensors = []
        if batch2 is not None:
            self._saved_batch2_idx = len(tensors)
            tensors.append(batch2)
        if batch1 is not None:
            self._saved_batch1_idx = len(tensors)
            tensors.append(batch1)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_batch2_idx is not None:
            self._saved_fields['batch2'] = self._saved_tensors_list[self._saved_batch2_idx]
        if self._saved_batch1_idx is not None:
            self._saved_fields['batch1'] = self._saved_tensors_list[self._saved_batch1_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        batch2 = _saved[self._saved_batch2_idx]
        batch1 = _saved[self._saved_batch1_idx]
        beta = self._beta
        alpha = self._alpha
        with _grad_context(keyset):
            grad_self = redispatch("mul", keyset, grad, beta)
            grad_batch1 = redispatch("mul", keyset, redispatch("bmm", keyset, grad, redispatch("transpose", keyset, batch2, 1, 2)), alpha)
            grad_batch2 = redispatch("mul", keyset, redispatch("bmm", keyset, redispatch("transpose", keyset, batch1, 1, 2), grad), alpha)
        return (grad_self, grad_batch1, grad_batch2,)

class BernoulliBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='BernoulliBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._generator = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        generator = self._generator
        with _grad_context(keyset):
            grad_self = grad._zeros_like()
        return (grad_self,)

class BernoulliTensorBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='BernoulliTensorBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_p_idx = None
        self._generator = None

    def _save(self, *, p=None):
        tensors = []
        if p is not None:
            self._saved_p_idx = len(tensors)
            tensors.append(p)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_p_idx is not None:
            self._saved_fields['p'] = self._saved_tensors_list[self._saved_p_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        p = _saved[self._saved_p_idx]
        generator = self._generator
        with _grad_context(keyset):
            grad_self = grad._zeros_like()
            grad_p = p._zeros_like()
        return (grad_self, grad_p,)

class BernoulliFloatBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='BernoulliFloatBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._p = None
        self._generator = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        p = self._p
        generator = self._generator
        with _grad_context(keyset):
            grad_self = grad._zeros_like()
        return (grad_self,)

class BmmBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='BmmBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_mat2_idx = None
        self._saved_self_idx = None

    def _save(self, *, mat2=None, self_=None):
        tensors = []
        if mat2 is not None:
            self._saved_mat2_idx = len(tensors)
            tensors.append(mat2)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_mat2_idx is not None:
            self._saved_fields['mat2'] = self._saved_tensors_list[self._saved_mat2_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        mat2 = _saved[self._saved_mat2_idx]
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("bmm", keyset, grad, redispatch("transpose", keyset, mat2, 1, 2))
            grad_mat2 = redispatch("bmm", keyset, redispatch("transpose", keyset, self_, 1, 2), grad)
        return (grad_self, grad_mat2,)

class MatmulBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MatmulBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_other_idx = None
        self._saved_self_idx = None

    def _save(self, *, other=None, self_=None):
        tensors = []
        if other is not None:
            self._saved_other_idx = len(tensors)
            tensors.append(other)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_other_idx is not None:
            self._saved_fields['other'] = self._saved_tensors_list[self._saved_other_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        other = _saved[self._saved_other_idx]
        self_ = _saved[self._saved_self_idx]
        grad_input_mask = [True, True]
        with _grad_context(keyset):
            grad_self, grad_other = _matmul_backward_helper(grad, self_, other, grad_input_mask, keyset)
        return (grad_self, grad_other,)

class CatBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='CatBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._dim = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        tensors = list(self.inputs)
        dim = self._dim
        with _grad_context(keyset):
            grad_tensors = _cat_backward_helper(grad, tensors, dim, keyset)
        return grad_tensors

class CauchyBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='CauchyBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._median = None
        self._sigma = None
        self._generator = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        median = self._median
        sigma = self._sigma
        generator = self._generator
        with _grad_context(keyset):
            grad_self = grad._zeros_like()
        return (grad_self,)

class CeilBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='CeilBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        with _grad_context(keyset):
            grad_self = grad._zeros_like()
        return (grad_self,)

class CholeskyBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='CholeskyBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_result_idx = None
        self._upper = None

    def _save(self, *, result=None):
        tensors = []
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        result = _saved[self._saved_result_idx]
        upper = self._upper
        with _grad_context(keyset):
            grad_self = redispatch("cholesky_backward", keyset, grad, upper, result)
        return (grad_self,)

class ChunkBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ChunkBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._chunks = None
        self._dim = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        chunks = self._chunks
        dim = self._dim
        with _grad_context(keyset):
            grad_self = _raise_not_implemented("chunk")
        return (grad_self,)

class LinalgCholeskyExBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='LinalgCholeskyExBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._upper = None
        self._check_errors = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        upper = self._upper
        check_errors = self._check_errors
        with _grad_context(keyset):
            grad_self = redispatch("cholesky_backward", keyset, grad, upper, L)
        return (grad_self,)

class CholeskySolveBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='CholeskySolveBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input2_idx = None
        self._saved_self_idx = None
        self._saved_result_idx = None
        self._upper = None

    def _save(self, *, input2=None, self_=None, result=None):
        tensors = []
        if input2 is not None:
            self._saved_input2_idx = len(tensors)
            tensors.append(input2)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input2_idx is not None:
            self._saved_fields['input2'] = self._saved_tensors_list[self._saved_input2_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input2 = _saved[self._saved_input2_idx]
        self_ = _saved[self._saved_self_idx]
        result = _saved[self._saved_result_idx]
        upper = self._upper
        grad_input_mask = [True, True]
        with _grad_context(keyset):
            grad_self, grad_input2 = redispatch("cholesky_solve_backward", keyset, grad, self_, input2, result, upper, grad_input_mask)
        return (grad_self, grad_input2,)

class CholeskyInverseBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='CholeskyInverseBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_result_idx = None
        self._upper = None

    def _save(self, *, self_=None, result=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        result = _saved[self._saved_result_idx]
        upper = self._upper
        with _grad_context(keyset):
            grad_self = redispatch("cholesky_inverse_backward", keyset, grad, self_, upper, result)
        return (grad_self,)

class ClampTensorBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ClampTensorBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_max_idx = None
        self._saved_min_idx = None
        self._saved_self_idx = None

    def _save(self, *, max=None, min=None, self_=None):
        tensors = []
        if max is not None:
            self._saved_max_idx = len(tensors)
            tensors.append(max)
        if min is not None:
            self._saved_min_idx = len(tensors)
            tensors.append(min)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_max_idx is not None:
            self._saved_fields['max'] = self._saved_tensors_list[self._saved_max_idx]
        if self._saved_min_idx is not None:
            self._saved_fields['min'] = self._saved_tensors_list[self._saved_min_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        max = _saved[self._saved_max_idx] if self._saved_max_idx is not None else None
        min = _saved[self._saved_min_idx] if self._saved_min_idx is not None else None
        self_ = _saved[self._saved_self_idx]
        grad_input_mask = [True, (min is not None), (max is not None)]
        with _grad_context(keyset):
            grad_self = _clamp_backward_helper(grad, self_, min, max, keyset)
            # clamp_backward_min_max: simplified — no gradients for tensor min/max args
            grad_min, grad_max = None, None
        return (grad_self, grad_min, grad_max,)

class ClampBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ClampBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._min = None
        self._max = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        min = self._min
        max = self._max
        with _grad_context(keyset):
            grad_self = _clamp_backward_helper(grad, self_, min, max, keyset)
        return (grad_self,)

class ClampMinBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ClampMinBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._min = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        min = self._min
        with _grad_context(keyset):
            grad_self = redispatch("where", keyset, redispatch("ge", keyset, self_, min), grad, _scalar_tensor_like(grad, 0.))
        return (grad_self,)

class ClampMinTensorBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ClampMinTensorBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_min_idx = None
        self._saved_self_idx = None

    def _save(self, *, min=None, self_=None):
        tensors = []
        if min is not None:
            self._saved_min_idx = len(tensors)
            tensors.append(min)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_min_idx is not None:
            self._saved_fields['min'] = self._saved_tensors_list[self._saved_min_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        min = _saved[self._saved_min_idx]
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("where", keyset, redispatch("ge", keyset, self_, min), grad, _scalar_tensor_like(grad, 0.))
            grad_min = redispatch("where", keyset, redispatch("lt", keyset, self_, min), grad, _scalar_tensor_like(grad, 0.))
        return (grad_self, grad_min,)

class ClampMaxBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ClampMaxBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._max = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        max = self._max
        with _grad_context(keyset):
            grad_self = redispatch("where", keyset, redispatch("le", keyset, self_, max), grad, _scalar_tensor_like(grad, 0.))
        return (grad_self,)

class ClampMaxTensorBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ClampMaxTensorBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_max_idx = None
        self._saved_self_idx = None

    def _save(self, *, max=None, self_=None):
        tensors = []
        if max is not None:
            self._saved_max_idx = len(tensors)
            tensors.append(max)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_max_idx is not None:
            self._saved_fields['max'] = self._saved_tensors_list[self._saved_max_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        max = _saved[self._saved_max_idx]
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("where", keyset, redispatch("le", keyset, self_, max), grad, _scalar_tensor_like(grad, 0.))
            grad_max = redispatch("where", keyset, redispatch("gt", keyset, self_, max), grad, _scalar_tensor_like(grad, 0.))
        return (grad_self, grad_max,)

class CloneBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='CloneBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._memory_format = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        memory_format = self._memory_format
        with _grad_context(keyset):
            grad_self = grad
        return (grad_self,)

class LazyCloneBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='LazyCloneBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        with _grad_context(keyset):
            grad_self = grad
        return (grad_self,)

class ToCopyBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ToCopyBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._dtype = None
        self._layout = None
        self._device = None
        self._pin_memory = None
        self._non_blocking = None
        self._memory_format = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        dtype = self._dtype
        layout = self._layout
        device = self._device
        pin_memory = self._pin_memory
        non_blocking = self._non_blocking
        memory_format = self._memory_format
        with _grad_context(keyset):
            grad_self = redispatch("contiguous", keyset, grad)
        return (grad_self,)

class CoalesceBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='CoalesceBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        with _grad_context(keyset):
            grad_self = grad
        return (grad_self,)

class ComplexBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ComplexBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_real_idx = None
        self._saved_imag_idx = None

    def _save(self, *, real=None, imag=None):
        tensors = []
        if real is not None:
            self._saved_real_idx = len(tensors)
            tensors.append(real)
        if imag is not None:
            self._saved_imag_idx = len(tensors)
            tensors.append(imag)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_real_idx is not None:
            self._saved_fields['real'] = self._saved_tensors_list[self._saved_real_idx]
        if self._saved_imag_idx is not None:
            self._saved_fields['imag'] = self._saved_tensors_list[self._saved_imag_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        real = _saved[self._saved_real_idx]
        imag = _saved[self._saved_imag_idx]
        with _grad_context(keyset):
            grad_real = redispatch("real", keyset, grad)
            grad_imag = redispatch("imag", keyset, grad)
        return (grad_real, grad_imag,)

class PolarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='PolarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_result_idx = None

    def _save(self, *, result=None):
        tensors = []
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        result = _saved[self._saved_result_idx]
        with _grad_context(keyset):
            grad_abs, grad_angle = redispatch("polar_backward", keyset, grad, result)
        return (grad_abs, grad_angle,)

class ConjBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ConjBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        with _grad_context(keyset):
            grad_self = grad
        return (grad_self,)

class NegViewBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='NegViewBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        with _grad_context(keyset):
            grad_self = redispatch("neg", keyset, grad)
        return (grad_self,)

class ConjPhysicalBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ConjPhysicalBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        with _grad_context(keyset):
            grad_self = redispatch("conj_physical", keyset, grad)
        return (grad_self,)

class CopysignTensorBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='CopysignTensorBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_other_idx = None
        self._saved_result_idx = None

    def _save(self, *, self_=None, other=None, result=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if other is not None:
            self._saved_other_idx = len(tensors)
            tensors.append(other)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_other_idx is not None:
            self._saved_fields['other'] = self._saved_tensors_list[self._saved_other_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        other = _saved[self._saved_other_idx]
        result = _saved[self._saved_result_idx]
        with _grad_context(keyset):
            grad_self = redispatch("copysign_tensor_self_backward", keyset, grad, self_, result)
            grad_other = other._zeros_like()
        return (grad_self, grad_other,)

class CopysignScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='CopysignScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_result_idx = None
        self._other = None

    def _save(self, *, self_=None, result=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        result = _saved[self._saved_result_idx]
        other = self._other
        with _grad_context(keyset):
            grad_self = redispatch("copysign_tensor_self_backward", keyset, grad, self_, result)
        return (grad_self,)

class CosBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='CosBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("mul", keyset, grad, redispatch("neg", keyset, redispatch("sin", keyset, self_)))
        return (grad_self,)

class CoshBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='CoshBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("mul", keyset, grad, redispatch("sinh", keyset, self_))
        return (grad_self,)

class CountNonzeroDimIntListBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='CountNonzeroDimIntListBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._dim = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        dim = self._dim
        return (grad,)

class CountNonzeroBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='CountNonzeroBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._dim = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        dim = self._dim
        return (grad,)

class LinalgCrossBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='LinalgCrossBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_other_idx = None
        self._saved_self_idx = None
        self._dim = None

    def _save(self, *, other=None, self_=None):
        tensors = []
        if other is not None:
            self._saved_other_idx = len(tensors)
            tensors.append(other)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_other_idx is not None:
            self._saved_fields['other'] = self._saved_tensors_list[self._saved_other_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        other = _saved[self._saved_other_idx]
        self_ = _saved[self._saved_self_idx]
        dim = self._dim
        with _grad_context(keyset):
            grad_self = redispatch("linalg_cross", keyset, other, grad, dim)
            grad_other = redispatch("linalg_cross", keyset, grad, self_, dim)
        return (grad_self, grad_other,)

class LogcumsumexpBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='LogcumsumexpBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_result_idx = None
        self._dim = None

    def _save(self, *, self_=None, result=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        result = _saved[self._saved_result_idx]
        dim = self._dim
        with _grad_context(keyset):
            grad_self = redispatch("logcumsumexp_backward", keyset, grad, self_, result, dim)
        return (grad_self,)

class CumprodBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='CumprodBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_result_idx = None
        self._dim = None
        self._dtype = None

    def _save(self, *, self_=None, result=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        result = _saved[self._saved_result_idx]
        dim = self._dim
        dtype = self._dtype
        with _grad_context(keyset):
            grad_self = _cumprod_backward_helper(grad, self_, dim, result, keyset)
        return (grad_self,)

class CumsumBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='CumsumBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._dim = None

    def _save(self, *, input_=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        dim = self._dim
        with _grad_context(keyset):
            grad_input = _cumsum_backward_helper(grad, input_, dim, keyset)
        return (grad_input,)

class CummaxBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='CummaxBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_indices_idx = None
        self._dim = None

    def _save(self, *, self_=None, indices=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if indices is not None:
            self._saved_indices_idx = len(tensors)
            tensors.append(indices)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_indices_idx is not None:
            self._saved_fields['indices'] = self._saved_tensors_list[self._saved_indices_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        indices = _saved[self._saved_indices_idx]
        dim = self._dim
        with _grad_context(keyset):
            # cummaxmin_backward: use index-based accumulation
            # cummax/cummin backward: approximate with index-based accumulation
            _grad_zeros = redispatch("zeros", keyset, self_.shape, dtype=grad.dtype, device=grad.device)
            grad_self = _grad_zeros
        return (grad_self,)

class CumminBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='CumminBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_indices_idx = None
        self._dim = None

    def _save(self, *, self_=None, indices=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if indices is not None:
            self._saved_indices_idx = len(tensors)
            tensors.append(indices)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_indices_idx is not None:
            self._saved_fields['indices'] = self._saved_tensors_list[self._saved_indices_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        indices = _saved[self._saved_indices_idx]
        dim = self._dim
        with _grad_context(keyset):
            # cummaxmin_backward: use index-based accumulation
            # cummax/cummin backward: approximate with index-based accumulation
            _grad_zeros = redispatch("zeros", keyset, self_.shape, dtype=grad.dtype, device=grad.device)
            grad_self = _grad_zeros
        return (grad_self,)

class ConvTbcBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ConvTbcBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_bias_idx = None
        self._saved_self_idx = None
        self._saved_weight_idx = None
        self._pad = None

    def _save(self, *, bias=None, self_=None, weight=None):
        tensors = []
        if bias is not None:
            self._saved_bias_idx = len(tensors)
            tensors.append(bias)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_bias_idx is not None:
            self._saved_fields['bias'] = self._saved_tensors_list[self._saved_bias_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        bias = _saved[self._saved_bias_idx]
        self_ = _saved[self._saved_self_idx]
        weight = _saved[self._saved_weight_idx]
        pad = self._pad
        with _grad_context(keyset):
            grad_self, grad_weight, grad_bias = (redispatch("conv_tbc_backward", keyset, grad, self_, weight, bias, pad) if (grad is not None) else (None, None, None))
        return (grad_self, grad_weight, grad_bias,)

class CtcLossBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='CtcLossBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_log_probs_idx = None
        self._saved_targets_idx = None
        self._saved_result0_idx = None
        self._saved_result1_idx = None
        self._input_lengths = None
        self._target_lengths = None
        self._blank = None
        self._zero_infinity = None

    def _save(self, *, log_probs=None, targets=None, result0=None, result1=None):
        tensors = []
        if log_probs is not None:
            self._saved_log_probs_idx = len(tensors)
            tensors.append(log_probs)
        if targets is not None:
            self._saved_targets_idx = len(tensors)
            tensors.append(targets)
        if result0 is not None:
            self._saved_result0_idx = len(tensors)
            tensors.append(result0)
        if result1 is not None:
            self._saved_result1_idx = len(tensors)
            tensors.append(result1)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_log_probs_idx is not None:
            self._saved_fields['log_probs'] = self._saved_tensors_list[self._saved_log_probs_idx]
        if self._saved_targets_idx is not None:
            self._saved_fields['targets'] = self._saved_tensors_list[self._saved_targets_idx]
        if self._saved_result0_idx is not None:
            self._saved_fields['result0'] = self._saved_tensors_list[self._saved_result0_idx]
        if self._saved_result1_idx is not None:
            self._saved_fields['result1'] = self._saved_tensors_list[self._saved_result1_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        log_probs = _saved[self._saved_log_probs_idx]
        targets = _saved[self._saved_targets_idx]
        result0 = _saved[self._saved_result0_idx]
        result1 = _saved[self._saved_result1_idx]
        input_lengths = self._input_lengths
        target_lengths = self._target_lengths
        blank = self._blank
        zero_infinity = self._zero_infinity
        with _grad_context(keyset):
            grad_log_probs = redispatch("_ctc_loss_backward", keyset, grad, log_probs, targets, input_lengths, target_lengths, result0, result1, blank, zero_infinity)
        return (grad_log_probs,)

class CtcLossTensorBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='CtcLossTensorBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_lengths_idx = None
        self._saved_log_probs_idx = None
        self._saved_target_lengths_idx = None
        self._saved_targets_idx = None
        self._saved_result0_idx = None
        self._saved_result1_idx = None
        self._blank = None
        self._zero_infinity = None

    def _save(self, *, input_lengths=None, log_probs=None, target_lengths=None, targets=None, result0=None, result1=None):
        tensors = []
        if input_lengths is not None:
            self._saved_input_lengths_idx = len(tensors)
            tensors.append(input_lengths)
        if log_probs is not None:
            self._saved_log_probs_idx = len(tensors)
            tensors.append(log_probs)
        if target_lengths is not None:
            self._saved_target_lengths_idx = len(tensors)
            tensors.append(target_lengths)
        if targets is not None:
            self._saved_targets_idx = len(tensors)
            tensors.append(targets)
        if result0 is not None:
            self._saved_result0_idx = len(tensors)
            tensors.append(result0)
        if result1 is not None:
            self._saved_result1_idx = len(tensors)
            tensors.append(result1)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_lengths_idx is not None:
            self._saved_fields['input_lengths'] = self._saved_tensors_list[self._saved_input_lengths_idx]
        if self._saved_log_probs_idx is not None:
            self._saved_fields['log_probs'] = self._saved_tensors_list[self._saved_log_probs_idx]
        if self._saved_target_lengths_idx is not None:
            self._saved_fields['target_lengths'] = self._saved_tensors_list[self._saved_target_lengths_idx]
        if self._saved_targets_idx is not None:
            self._saved_fields['targets'] = self._saved_tensors_list[self._saved_targets_idx]
        if self._saved_result0_idx is not None:
            self._saved_fields['result0'] = self._saved_tensors_list[self._saved_result0_idx]
        if self._saved_result1_idx is not None:
            self._saved_fields['result1'] = self._saved_tensors_list[self._saved_result1_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_lengths = _saved[self._saved_input_lengths_idx]
        log_probs = _saved[self._saved_log_probs_idx]
        target_lengths = _saved[self._saved_target_lengths_idx]
        targets = _saved[self._saved_targets_idx]
        result0 = _saved[self._saved_result0_idx]
        result1 = _saved[self._saved_result1_idx]
        blank = self._blank
        zero_infinity = self._zero_infinity
        with _grad_context(keyset):
            grad_log_probs = redispatch("_ctc_loss_backward", keyset, grad, log_probs, targets, input_lengths, target_lengths, result0, result1, blank, zero_infinity)
        return (grad_log_probs,)

class Deg2radBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Deg2radBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        with _grad_context(keyset):
            grad_self = redispatch("deg2rad_backward", keyset, grad)
        return (grad_self,)

class LinalgDetBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='LinalgDetBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_A_idx = None
        self._saved_LU_idx = None
        self._saved_pivots_idx = None
        self._saved_result_idx = None

    def _save(self, *, A=None, LU=None, pivots=None, result=None):
        tensors = []
        if A is not None:
            self._saved_A_idx = len(tensors)
            tensors.append(A)
        if LU is not None:
            self._saved_LU_idx = len(tensors)
            tensors.append(LU)
        if pivots is not None:
            self._saved_pivots_idx = len(tensors)
            tensors.append(pivots)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_A_idx is not None:
            self._saved_fields['A'] = self._saved_tensors_list[self._saved_A_idx]
        if self._saved_LU_idx is not None:
            self._saved_fields['LU'] = self._saved_tensors_list[self._saved_LU_idx]
        if self._saved_pivots_idx is not None:
            self._saved_fields['pivots'] = self._saved_tensors_list[self._saved_pivots_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        A = _saved[self._saved_A_idx]
        LU = _saved[self._saved_LU_idx]
        pivots = _saved[self._saved_pivots_idx]
        result = _saved[self._saved_result_idx]
        with _grad_context(keyset):
            grad_A = redispatch("linalg_det_backward", keyset, grad, result, A, LU, pivots)
        return (grad_A,)

class LinalgSlogdetBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='LinalgSlogdetBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_A_idx = None
        self._saved_LU_idx = None
        self._saved_pivots_idx = None

    def _save(self, *, A=None, LU=None, pivots=None):
        tensors = []
        if A is not None:
            self._saved_A_idx = len(tensors)
            tensors.append(A)
        if LU is not None:
            self._saved_LU_idx = len(tensors)
            tensors.append(LU)
        if pivots is not None:
            self._saved_pivots_idx = len(tensors)
            tensors.append(pivots)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_A_idx is not None:
            self._saved_fields['A'] = self._saved_tensors_list[self._saved_A_idx]
        if self._saved_LU_idx is not None:
            self._saved_fields['LU'] = self._saved_tensors_list[self._saved_LU_idx]
        if self._saved_pivots_idx is not None:
            self._saved_fields['pivots'] = self._saved_tensors_list[self._saved_pivots_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        A = _saved[self._saved_A_idx]
        LU = _saved[self._saved_LU_idx]
        pivots = _saved[self._saved_pivots_idx]
        with _grad_context(keyset):
            grad_A = redispatch("slogdet_backward", keyset, grad_sign, grad_logabsdet, A, sign, LU, pivots)
        return (grad_A,)

class BlockDiagBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='BlockDiagBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        tensors = list(self.inputs)
        with _grad_context(keyset):
            grad_tensors = redispatch("block_diag_backward", keyset, grad, redispatch("to_args_sizes", keyset, tensors), redispatch("to_args_scalartypes", keyset, tensors))
        return grad_tensors

class DiagEmbedBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='DiagEmbedBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._offset = None
        self._dim1 = None
        self._dim2 = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        offset = self._offset
        dim1 = self._dim1
        dim2 = self._dim2
        with _grad_context(keyset):
            grad_self = redispatch("diagonal", keyset, grad, offset, dim1, dim2)
        return (grad_self,)

class DiagonalBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='DiagonalBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._offset = None
        self._dim1 = None
        self._dim2 = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        offset = self._offset
        dim1 = self._dim1
        dim2 = self._dim2
        with _grad_context(keyset):
            grad_self = redispatch("diagonal_backward_symint", keyset, grad, self_.shape, offset, dim1, dim2)
        return (grad_self,)

class DiagonalBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='DiagonalBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._input_sizes = None
        self._offset = None
        self._dim1 = None
        self._dim2 = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        input_sizes = self._input_sizes
        offset = self._offset
        dim1 = self._dim1
        dim2 = self._dim2
        with _grad_context(keyset):
            grad_grad_output = redispatch("diagonal", keyset, grad, offset, dim1, dim2)
        return (grad_grad_output,)

class DistBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='DistBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_other_idx = None
        self._saved_self_idx = None
        self._saved_result_idx = None
        self._p = None

    def _save(self, *, other=None, self_=None, result=None):
        tensors = []
        if other is not None:
            self._saved_other_idx = len(tensors)
            tensors.append(other)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_other_idx is not None:
            self._saved_fields['other'] = self._saved_tensors_list[self._saved_other_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        other = _saved[self._saved_other_idx]
        self_ = _saved[self._saved_self_idx]
        result = _saved[self._saved_result_idx]
        p = self._p
        with _grad_context(keyset):
            grad_self = redispatch("norm_backward", keyset, grad, redispatch("sub", keyset, self_, other), p, result)
            grad_other = redispatch("neg", keyset, redispatch("norm_backward", keyset, grad, redispatch("sub", keyset, self_, other), p, result))
        return (grad_self, grad_other,)

class DivTensorBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='DivTensorBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_other_idx = None
        self._saved_self_idx = None

    def _save(self, *, other=None, self_=None):
        tensors = []
        if other is not None:
            self._saved_other_idx = len(tensors)
            tensors.append(other)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_other_idx is not None:
            self._saved_fields['other'] = self._saved_tensors_list[self._saved_other_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        other = _saved[self._saved_other_idx]
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = _div_tensor_self_backward_helper(grad, other, self_.dtype, keyset)
            grad_other = _div_tensor_other_backward_helper(grad, self_, other, keyset)
        return (grad_self, grad_other,)

class DivScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='DivScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._other = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        other = self._other
        with _grad_context(keyset):
            grad_self = _div_tensor_self_backward_helper(grad, other, self_.dtype, keyset)
        return (grad_self,)

class DivTensorModeBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='DivTensorModeBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_other_idx = None
        self._saved_self_idx = None
        self._rounding_mode = None

    def _save(self, *, other=None, self_=None):
        tensors = []
        if other is not None:
            self._saved_other_idx = len(tensors)
            tensors.append(other)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_other_idx is not None:
            self._saved_fields['other'] = self._saved_tensors_list[self._saved_other_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        other = _saved[self._saved_other_idx]
        self_ = _saved[self._saved_self_idx]
        rounding_mode = self._rounding_mode
        with _grad_context(keyset):
            grad_self = _div_tensor_self_backward_helper(grad, other, self_.dtype, rounding_mode, keyset)
            grad_other = _div_tensor_other_backward_helper(grad, self_, other, rounding_mode, keyset)
        return (grad_self, grad_other,)

class DivScalarModeBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='DivScalarModeBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._other = None
        self._rounding_mode = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        other = self._other
        rounding_mode = self._rounding_mode
        with _grad_context(keyset):
            grad_self = _div_tensor_self_backward_helper(grad, other, self_.dtype, rounding_mode, keyset)
        return (grad_self,)

class DotBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='DotBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_tensor_idx = None
        self._saved_self_idx = None

    def _save(self, *, tensor=None, self_=None):
        tensors = []
        if tensor is not None:
            self._saved_tensor_idx = len(tensors)
            tensors.append(tensor)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_tensor_idx is not None:
            self._saved_fields['tensor'] = self._saved_tensors_list[self._saved_tensor_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        tensor = _saved[self._saved_tensor_idx]
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("mul", keyset, grad, tensor)
            grad_tensor = redispatch("mul", keyset, grad, self_)
        return (grad_self, grad_tensor,)

class VdotBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='VdotBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_other_idx = None
        self._saved_self_idx = None

    def _save(self, *, other=None, self_=None):
        tensors = []
        if other is not None:
            self._saved_other_idx = len(tensors)
            tensors.append(other)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_other_idx is not None:
            self._saved_fields['other'] = self._saved_tensors_list[self._saved_other_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        other = _saved[self._saved_other_idx]
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("mul", keyset, grad, other)
            grad_other = redispatch("mul", keyset, grad, self_)
        return (grad_self, grad_other,)

class FusedDropoutBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='FusedDropoutBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_result1_idx = None
        self._p = None
        self._generator = None

    def _save(self, *, result1=None):
        tensors = []
        if result1 is not None:
            self._saved_result1_idx = len(tensors)
            tensors.append(result1)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_result1_idx is not None:
            self._saved_fields['result1'] = self._saved_tensors_list[self._saved_result1_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        result1 = _saved[self._saved_result1_idx]
        p = self._p
        generator = self._generator
        with _grad_context(keyset):
            grad_self = redispatch("_fused_dropout_backward", keyset, grad, result1, p)
        return (grad_self,)

class NativeDropoutBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='NativeDropoutBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_result1_idx = None
        self._p = None
        self._train = None

    def _save(self, *, result1=None):
        tensors = []
        if result1 is not None:
            self._saved_result1_idx = len(tensors)
            tensors.append(result1)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_result1_idx is not None:
            self._saved_fields['result1'] = self._saved_tensors_list[self._saved_result1_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        result1 = _saved[self._saved_result1_idx]
        p = self._p
        train = self._train
        with _grad_context(keyset):
            grad_input = (redispatch("infinitely_differentiable_native_dropout_backward", keyset, grad, result1, (1 if ((not redispatch("has_value", keyset, train)) or (not redispatch("value", keyset, train))) else (0.0 if redispatch("eq", keyset, p, 1) else redispatch("div", keyset, 1.0, redispatch("sub", keyset, 1.0, p))))) if True else redispatch("native_dropout_backward", keyset, grad, result1, (1 if ((not redispatch("has_value", keyset, train)) or (not redispatch("value", keyset, train))) else (0.0 if redispatch("eq", keyset, p, 1) else redispatch("div", keyset, 1.0, redispatch("sub", keyset, 1.0, p))))))
        return (grad_input,)

class NativeDropoutBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='NativeDropoutBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_mask_idx = None
        self._scale = None

    def _save(self, *, mask=None):
        tensors = []
        if mask is not None:
            self._saved_mask_idx = len(tensors)
            tensors.append(mask)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_mask_idx is not None:
            self._saved_fields['mask'] = self._saved_tensors_list[self._saved_mask_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        mask = _saved[self._saved_mask_idx]
        grad_output = self.inputs[0]
        scale = self._scale
        with _grad_context(keyset):
            grad_grad_output = redispatch("native_dropout_double_backward", keyset, grad, grad_output, mask, scale)
            grad_mask = _raise_not_implemented("native_dropout_backward: mask")
        return (grad_grad_output, grad_mask,)

class EqScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='EqScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._other = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        other = self._other
        with _grad_context(keyset):
            grad_self = self_._zeros_like()
        return (grad_self,)

class EqTensorBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='EqTensorBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_other_idx = None

    def _save(self, *, self_=None, other=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if other is not None:
            self._saved_other_idx = len(tensors)
            tensors.append(other)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_other_idx is not None:
            self._saved_fields['other'] = self._saved_tensors_list[self._saved_other_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        other = _saved[self._saved_other_idx]
        with _grad_context(keyset):
            grad_self = self_._zeros_like()
            grad_other = other._zeros_like()
        return (grad_self, grad_other,)

class ErfBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ErfBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("mul", keyset, grad, redispatch("mul", keyset, _scalar_tensor_like(self_, 2.0 / __import__('math').sqrt(__import__('math').pi)), redispatch("exp", keyset, redispatch("neg", keyset, redispatch("mul", keyset, self_, self_)))))
        return (grad_self,)

class ErfcBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ErfcBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("mul", keyset, grad, redispatch("mul", keyset, _scalar_tensor_like(self_, -2.0 / __import__('math').sqrt(__import__('math').pi)), redispatch("exp", keyset, redispatch("neg", keyset, redispatch("mul", keyset, self_, self_)))))
        return (grad_self,)

class SpecialErfcxBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialErfcxBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_result_idx = None

    def _save(self, *, self_=None, result=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        result = _saved[self._saved_result_idx]
        with _grad_context(keyset):
            grad_self = redispatch("mul", keyset, redispatch("sub", keyset, redispatch("mul", keyset, redispatch("mul", keyset, 2.0, self_), result), redispatch("div", keyset, 2.0, redispatch("sqrt", keyset, 3.141592653589793))), grad)
        return (grad_self,)

class ErfinvBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ErfinvBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("mul", keyset, redispatch("mul", keyset, redispatch("mul", keyset, 0.5, redispatch("sqrt", keyset, 3.141592653589793)), redispatch("exp", keyset, redispatch("pow", keyset, redispatch("erfinv", keyset, self_), 2))), grad)
        return (grad_self,)

class ExpBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ExpBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_result_idx = None

    def _save(self, *, result=None):
        tensors = []
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        result = _saved[self._saved_result_idx]
        with _grad_context(keyset):
            grad_self = redispatch("mul", keyset, grad, result)
        return (grad_self,)

class Exp2Backward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Exp2Backward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_result_idx = None

    def _save(self, *, result=None):
        tensors = []
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        result = _saved[self._saved_result_idx]
        with _grad_context(keyset):
            grad_self = _exp2_backward_helper(grad, result, keyset)
        return (grad_self,)

class Expm1Backward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Expm1Backward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_result_idx = None

    def _save(self, *, result=None):
        tensors = []
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        result = _saved[self._saved_result_idx]
        with _grad_context(keyset):
            grad_self = redispatch("mul", keyset, grad, redispatch("add", keyset, result, 1))
        return (grad_self,)

class ExpandBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ExpandBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._size = None
        self._implicit = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        size = self._size
        implicit = self._implicit
        with _grad_context(keyset):
            grad_self = _sum_to_backward_helper(grad, self_.shape, keyset)
        return (grad_self,)

class SumToSizeBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SumToSizeBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._self_shape = None

    def _save(self, *, self_=None):
        if self_ is not None:
            self._self_shape = self_.shape

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        with _grad_context(keyset):
            grad_self = redispatch("expand", keyset, grad, self._self_shape)
        return (grad_self,)


class ExponentialBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ExponentialBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._lambd = None
        self._generator = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        lambd = self._lambd
        generator = self._generator
        with _grad_context(keyset):
            grad_self = grad._zeros_like()
        return (grad_self,)

class FakeQuantizePerTensorAffineCachemaskBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='FakeQuantizePerTensorAffineCachemaskBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._scale = None
        self._zero_point = None
        self._quant_min = None
        self._quant_max = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        scale = self._scale
        zero_point = self._zero_point
        quant_min = self._quant_min
        quant_max = self._quant_max
        with _grad_context(keyset):
            grad_self = redispatch("fake_quantize_per_tensor_affine_cachemask_backward", keyset, grad, mask)
        return (grad_self,)

class FakeQuantizePerTensorAffineCachemaskTensorQparamsBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='FakeQuantizePerTensorAffineCachemaskTensorQparamsBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._quant_min = None
        self._quant_max = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        quant_min = self._quant_min
        quant_max = self._quant_max
        with _grad_context(keyset):
            grad_self = redispatch("fake_quantize_per_tensor_affine_cachemask_backward", keyset, grad, mask)
        return (grad_self,)

class FakeQuantizeLearnablePerTensorAffineBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='FakeQuantizeLearnablePerTensorAffineBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_scale_idx = None
        self._saved_self_idx = None
        self._saved_zero_point_idx = None
        self._quant_min = None
        self._quant_max = None
        self._grad_factor = None

    def _save(self, *, scale=None, self_=None, zero_point=None):
        tensors = []
        if scale is not None:
            self._saved_scale_idx = len(tensors)
            tensors.append(scale)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if zero_point is not None:
            self._saved_zero_point_idx = len(tensors)
            tensors.append(zero_point)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_scale_idx is not None:
            self._saved_fields['scale'] = self._saved_tensors_list[self._saved_scale_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_zero_point_idx is not None:
            self._saved_fields['zero_point'] = self._saved_tensors_list[self._saved_zero_point_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        scale = _saved[self._saved_scale_idx]
        self_ = _saved[self._saved_self_idx]
        zero_point = _saved[self._saved_zero_point_idx]
        quant_min = self._quant_min
        quant_max = self._quant_max
        grad_factor = self._grad_factor
        with _grad_context(keyset):
            grad_self, grad_scale, grad_zero_point = (redispatch("_fake_quantize_learnable_per_tensor_affine_backward", keyset, grad, self_, scale, zero_point, quant_min, quant_max, grad_factor) if (grad is not None) else (None, None, None))
        return (grad_self, grad_scale, grad_zero_point,)

class FakeQuantizePerChannelAffineCachemaskBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='FakeQuantizePerChannelAffineCachemaskBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._axis = None
        self._quant_min = None
        self._quant_max = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        axis = self._axis
        quant_min = self._quant_min
        quant_max = self._quant_max
        with _grad_context(keyset):
            grad_self = redispatch("fake_quantize_per_channel_affine_cachemask_backward", keyset, grad, mask)
        return (grad_self,)

class FakeQuantizeLearnablePerChannelAffineBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='FakeQuantizeLearnablePerChannelAffineBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_scale_idx = None
        self._saved_self_idx = None
        self._saved_zero_point_idx = None
        self._axis = None
        self._quant_min = None
        self._quant_max = None
        self._grad_factor = None

    def _save(self, *, scale=None, self_=None, zero_point=None):
        tensors = []
        if scale is not None:
            self._saved_scale_idx = len(tensors)
            tensors.append(scale)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if zero_point is not None:
            self._saved_zero_point_idx = len(tensors)
            tensors.append(zero_point)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_scale_idx is not None:
            self._saved_fields['scale'] = self._saved_tensors_list[self._saved_scale_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_zero_point_idx is not None:
            self._saved_fields['zero_point'] = self._saved_tensors_list[self._saved_zero_point_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        scale = _saved[self._saved_scale_idx]
        self_ = _saved[self._saved_self_idx]
        zero_point = _saved[self._saved_zero_point_idx]
        axis = self._axis
        quant_min = self._quant_min
        quant_max = self._quant_max
        grad_factor = self._grad_factor
        with _grad_context(keyset):
            grad_self, grad_scale, grad_zero_point = (redispatch("_fake_quantize_learnable_per_channel_affine_backward", keyset, grad, self_, scale, zero_point, axis, quant_min, quant_max, grad_factor) if (grad is not None) else (None, None, None))
        return (grad_self, grad_scale, grad_zero_point,)

class FusedMovingAvgObsFqHelperBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='FusedMovingAvgObsFqHelperBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._averaging_const = None
        self._quant_min = None
        self._quant_max = None
        self._ch_axis = None
        self._per_row_fake_quant = None
        self._symmetric_quant = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        averaging_const = self._averaging_const
        quant_min = self._quant_min
        quant_max = self._quant_max
        ch_axis = self._ch_axis
        per_row_fake_quant = self._per_row_fake_quant
        symmetric_quant = self._symmetric_quant
        with _grad_context(keyset):
            grad_self = redispatch("fake_quantize_per_tensor_affine_cachemask_backward", keyset, grad, mask)
        return (grad_self,)

class FillScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='FillScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._value = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        value = self._value
        with _grad_context(keyset):
            grad_self = grad._zeros_like()
        return (grad_self,)

class FillTensorBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='FillTensorBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        with _grad_context(keyset):
            grad_self = grad._zeros_like()
            grad_value = redispatch("sum", keyset, grad)
        return (grad_self, grad_value,)

class FloorBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='FloorBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        with _grad_context(keyset):
            grad_self = grad._zeros_like()
        return (grad_self,)

class FmodScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='FmodScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._other = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        other = self._other
        with _grad_context(keyset):
            grad_self = grad
        return (grad_self,)

class FmodTensorBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='FmodTensorBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_other_idx = None
        self._saved_self_idx = None

    def _save(self, *, other=None, self_=None):
        tensors = []
        if other is not None:
            self._saved_other_idx = len(tensors)
            tensors.append(other)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_other_idx is not None:
            self._saved_fields['other'] = self._saved_tensors_list[self._saved_other_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        other = _saved[self._saved_other_idx]
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = grad
            grad_other = redispatch("mul", keyset, redispatch("neg", keyset, grad), redispatch("div", keyset, self_, other, "trunc"))
        return (grad_self, grad_other,)

class FracBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='FracBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        with _grad_context(keyset):
            grad_self = grad
        return (grad_self,)

class FrexpTensorBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='FrexpTensorBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        with _grad_context(keyset):
            grad_self = redispatch("div", keyset, grad, redispatch("exp2", keyset, exponent))
        return (grad_self,)

class GatherBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='GatherBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_index_idx = None
        self._saved_input_idx = None
        self._dim = None

    def _save(self, *, index=None, input_=None):
        tensors = []
        if index is not None:
            self._saved_index_idx = len(tensors)
            tensors.append(index)
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_index_idx is not None:
            self._saved_fields['index'] = self._saved_tensors_list[self._saved_index_idx]
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        index = _saved[self._saved_index_idx]
        input_ = _saved[self._saved_input_idx]
        dim = self._dim
        with _grad_context(keyset):
            grad_input = _gather_backward_helper(grad, input_, dim, index, keyset)
        return (grad_input,)

class GeScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='GeScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._other = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        other = self._other
        with _grad_context(keyset):
            grad_self = self_._zeros_like()
        return (grad_self,)

class GeTensorBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='GeTensorBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_other_idx = None

    def _save(self, *, self_=None, other=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if other is not None:
            self._saved_other_idx = len(tensors)
            tensors.append(other)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_other_idx is not None:
            self._saved_fields['other'] = self._saved_tensors_list[self._saved_other_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        other = _saved[self._saved_other_idx]
        with _grad_context(keyset):
            grad_self = self_._zeros_like()
            grad_other = other._zeros_like()
        return (grad_self, grad_other,)

class GeometricBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='GeometricBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._p = None
        self._generator = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        p = self._p
        generator = self._generator
        with _grad_context(keyset):
            grad_self = grad._zeros_like()
        return (grad_self,)

class GeqrfBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='GeqrfBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        with _grad_context(keyset):
            grad_self = _raise_not_implemented("geqrf")
        return (grad_self,)

class IndicesBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='IndicesBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        return (grad,)

class CrowIndicesBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='CrowIndicesBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        return (grad,)

class ColIndicesBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ColIndicesBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        return (grad,)

class CcolIndicesBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='CcolIndicesBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        return (grad,)

class RowIndicesBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='RowIndicesBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        return (grad,)

class GridSampler2dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='GridSampler2dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_grid_idx = None
        self._saved_input_idx = None
        self._interpolation_mode = None
        self._padding_mode = None
        self._align_corners = None

    def _save(self, *, grid=None, input_=None):
        tensors = []
        if grid is not None:
            self._saved_grid_idx = len(tensors)
            tensors.append(grid)
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_grid_idx is not None:
            self._saved_fields['grid'] = self._saved_tensors_list[self._saved_grid_idx]
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        grid = _saved[self._saved_grid_idx]
        input_ = _saved[self._saved_input_idx]
        interpolation_mode = self._interpolation_mode
        padding_mode = self._padding_mode
        align_corners = self._align_corners
        grad_input_mask = [True, True]
        with _grad_context(keyset):
            grad_input, grad_grid = (redispatch("grid_sampler_2d_backward", keyset, grad, input_, grid, interpolation_mode, padding_mode, align_corners, grad_input_mask) if (grad is not None) else (None, None))
        return (grad_input, grad_grid,)

class GridSampler3dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='GridSampler3dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_grid_idx = None
        self._saved_input_idx = None
        self._interpolation_mode = None
        self._padding_mode = None
        self._align_corners = None

    def _save(self, *, grid=None, input_=None):
        tensors = []
        if grid is not None:
            self._saved_grid_idx = len(tensors)
            tensors.append(grid)
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_grid_idx is not None:
            self._saved_fields['grid'] = self._saved_tensors_list[self._saved_grid_idx]
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        grid = _saved[self._saved_grid_idx]
        input_ = _saved[self._saved_input_idx]
        interpolation_mode = self._interpolation_mode
        padding_mode = self._padding_mode
        align_corners = self._align_corners
        grad_input_mask = [True, True]
        with _grad_context(keyset):
            grad_input, grad_grid = (redispatch("grid_sampler_3d_backward", keyset, grad, input_, grid, interpolation_mode, padding_mode, align_corners, grad_input_mask) if (grad is not None) else (None, None))
        return (grad_input, grad_grid,)

class GridSampler2dCpuFallbackBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='GridSampler2dCpuFallbackBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_grid_idx = None
        self._saved_input_idx = None
        self._interpolation_mode = None
        self._padding_mode = None
        self._align_corners = None

    def _save(self, *, grid=None, input_=None):
        tensors = []
        if grid is not None:
            self._saved_grid_idx = len(tensors)
            tensors.append(grid)
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_grid_idx is not None:
            self._saved_fields['grid'] = self._saved_tensors_list[self._saved_grid_idx]
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        grid = _saved[self._saved_grid_idx]
        input_ = _saved[self._saved_input_idx]
        interpolation_mode = self._interpolation_mode
        padding_mode = self._padding_mode
        align_corners = self._align_corners
        with _grad_context(keyset):
            grad_input, grad_grid = (redispatch("_grid_sampler_2d_cpu_fallback_backward", keyset, grad, input_, grid, interpolation_mode, padding_mode, align_corners) if (grad is not None) else (None, None))
        return (grad_input, grad_grid,)

class GtScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='GtScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._other = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        other = self._other
        with _grad_context(keyset):
            grad_self = self_._zeros_like()
        return (grad_self,)

class GtTensorBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='GtTensorBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_other_idx = None

    def _save(self, *, self_=None, other=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if other is not None:
            self._saved_other_idx = len(tensors)
            tensors.append(other)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_other_idx is not None:
            self._saved_fields['other'] = self._saved_tensors_list[self._saved_other_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        other = _saved[self._saved_other_idx]
        with _grad_context(keyset):
            grad_self = self_._zeros_like()
            grad_other = other._zeros_like()
        return (grad_self, grad_other,)

class HardsigmoidBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='HardsigmoidBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = _hardsigmoid_backward_helper(grad, self_, keyset)
        return (grad_self,)

class HistcBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='HistcBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._bins = None
        self._min = None
        self._max = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        bins = self._bins
        min = self._min
        max = self._max
        return (grad,)

class HardswishBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='HardswishBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = _hardswish_backward_helper(grad, self_, keyset)
        return (grad_self,)

class HardswishBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='HardswishBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        grad_output = self.inputs[0]
        with _grad_context(keyset):
            grad_grad_output = _hardswish_backward_helper(grad, self_, keyset)
            grad_self = redispatch("where", keyset, redispatch("logical_and", keyset, redispatch("lt", keyset, redispatch("neg", keyset, 3.0), self_), redispatch("lt", keyset, self_, 3.0)), redispatch("mul", keyset, grad, redispatch("div", keyset, grad_output, 3.0)), redispatch("zeros", keyset, [], dtype=self_.dtype, device=self_.device))
        return (grad_grad_output, grad_self,)

class HypotBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='HypotBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_other_idx = None
        self._saved_result_idx = None

    def _save(self, *, self_=None, other=None, result=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if other is not None:
            self._saved_other_idx = len(tensors)
            tensors.append(other)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_other_idx is not None:
            self._saved_fields['other'] = self._saved_tensors_list[self._saved_other_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        other = _saved[self._saved_other_idx]
        result = _saved[self._saved_result_idx]
        with _grad_context(keyset):
            grad_self = redispatch("mul", keyset, grad, redispatch("div", keyset, self_, result))
            grad_other = redispatch("mul", keyset, grad, redispatch("div", keyset, other, result))
        return (grad_self, grad_other,)

class I0Backward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='I0Backward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("mul", keyset, grad, redispatch("special_i1", keyset, self_))
        return (grad_self,)

class SpecialI0eBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialI0eBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_result_idx = None

    def _save(self, *, self_=None, result=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        result = _saved[self._saved_result_idx]
        with _grad_context(keyset):
            grad_self = redispatch("mul", keyset, grad, redispatch("sub", keyset, redispatch("special_i1e", keyset, self_), redispatch("mul", keyset, redispatch("sign", keyset, self_), result)))
        return (grad_self,)

class SpecialI1Backward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialI1Backward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_result_idx = None

    def _save(self, *, self_=None, result=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        result = _saved[self._saved_result_idx]
        with _grad_context(keyset):
            grad_self = redispatch("i1_backward", keyset, grad, self_, result)
        return (grad_self,)

class SpecialI1eBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialI1eBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_result_idx = None

    def _save(self, *, self_=None, result=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        result = _saved[self._saved_result_idx]
        with _grad_context(keyset):
            grad_self = redispatch("i1e_backward", keyset, grad, self_, result)
        return (grad_self,)

class IgammaBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='IgammaBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_other_idx = None
        self._saved_self_idx = None

    def _save(self, *, other=None, self_=None):
        tensors = []
        if other is not None:
            self._saved_other_idx = len(tensors)
            tensors.append(other)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_other_idx is not None:
            self._saved_fields['other'] = self._saved_tensors_list[self._saved_other_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        other = _saved[self._saved_other_idx]
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = _raise_not_implemented("igamma: input")
            grad_other = redispatch("mul", keyset, grad, redispatch("exp", keyset, redispatch("sub", keyset, redispatch("sub", keyset, redispatch("mul", keyset, redispatch("sub", keyset, self_, 1), redispatch("log", keyset, other)), other), redispatch("lgamma", keyset, self_))))
        return (grad_self, grad_other,)

class IgammacBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='IgammacBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_other_idx = None
        self._saved_self_idx = None

    def _save(self, *, other=None, self_=None):
        tensors = []
        if other is not None:
            self._saved_other_idx = len(tensors)
            tensors.append(other)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_other_idx is not None:
            self._saved_fields['other'] = self._saved_tensors_list[self._saved_other_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        other = _saved[self._saved_other_idx]
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = _raise_not_implemented("igammac: input")
            grad_other = redispatch("mul", keyset, redispatch("neg", keyset, grad), redispatch("exp", keyset, redispatch("sub", keyset, redispatch("sub", keyset, redispatch("mul", keyset, redispatch("sub", keyset, self_, 1), redispatch("log", keyset, other)), other), redispatch("lgamma", keyset, self_))))
        return (grad_self, grad_other,)

class IndexTensorBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='IndexTensorBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        indices = list(self.inputs)
        with _grad_context(keyset):
            grad_self = redispatch("index_backward", keyset, redispatch("zeros", keyset, self_.shape, dtype=self_.dtype, device=self_.device), indices, grad)
        return (grad_self,)

class UnsafeIndexTensorBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='UnsafeIndexTensorBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        indices = list(self.inputs)
        with _grad_context(keyset):
            grad_self = redispatch("_unsafe_index_put", keyset, redispatch("zeros", keyset, self_.shape, dtype=self_.dtype, device=self_.device), indices, grad, True)
        return (grad_self,)

class UnsafeMaskedIndexBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='UnsafeMaskedIndexBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_mask_idx = None
        self._saved_self_idx = None
        self._fill = None

    def _save(self, *, mask=None, self_=None):
        tensors = []
        if mask is not None:
            self._saved_mask_idx = len(tensors)
            tensors.append(mask)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_mask_idx is not None:
            self._saved_fields['mask'] = self._saved_tensors_list[self._saved_mask_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        mask = _saved[self._saved_mask_idx]
        self_ = _saved[self._saved_self_idx]
        indices = list(self.inputs)
        fill = self._fill
        with _grad_context(keyset):
            grad_self = redispatch("_unsafe_masked_index_put_accumulate", keyset, redispatch("zeros", keyset, self_.shape, dtype=self_.dtype, device=self_.device), mask, indices, grad)
        return (grad_self,)

class UnsafeMaskedIndexPutAccumulateBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='UnsafeMaskedIndexPutAccumulateBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_mask_idx = None

    def _save(self, *, mask=None):
        tensors = []
        if mask is not None:
            self._saved_mask_idx = len(tensors)
            tensors.append(mask)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_mask_idx is not None:
            self._saved_fields['mask'] = self._saved_tensors_list[self._saved_mask_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        mask = _saved[self._saved_mask_idx]
        indices = list(self.inputs)
        with _grad_context(keyset):
            grad_self = grad
            grad_values = redispatch("_unsafe_masked_index", keyset, grad, mask, indices, 0)
        return (grad_self, grad_values,)

class IndexAddBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='IndexAddBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_index_idx = None
        self._saved_source_idx = None
        self._dim = None
        self._alpha = None

    def _save(self, *, index=None, source=None):
        tensors = []
        if index is not None:
            self._saved_index_idx = len(tensors)
            tensors.append(index)
        if source is not None:
            self._saved_source_idx = len(tensors)
            tensors.append(source)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_index_idx is not None:
            self._saved_fields['index'] = self._saved_tensors_list[self._saved_index_idx]
        if self._saved_source_idx is not None:
            self._saved_fields['source'] = self._saved_tensors_list[self._saved_source_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        index = _saved[self._saved_index_idx]
        source = _saved[self._saved_source_idx]
        dim = self._dim
        alpha = self._alpha
        with _grad_context(keyset):
            grad_self = grad
            grad_source = redispatch("mul", keyset, (redispatch("expand_as", keyset, redispatch("index_select", keyset, grad, dim, index), source) if redispatch("gt", keyset, redispatch("dim", keyset, source), 0) else redispatch("index_select", keyset, grad, dim, redispatch("squeeze", keyset, index, 0))), alpha)
        return (grad_self, grad_source,)

class IndexReduceBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='IndexReduceBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_index_idx = None
        self._saved_self_idx = None
        self._saved_source_idx = None
        self._saved_result_idx = None
        self._dim = None
        self._reduce = None
        self._include_self = None

    def _save(self, *, index=None, self_=None, source=None, result=None):
        tensors = []
        if index is not None:
            self._saved_index_idx = len(tensors)
            tensors.append(index)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if source is not None:
            self._saved_source_idx = len(tensors)
            tensors.append(source)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_index_idx is not None:
            self._saved_fields['index'] = self._saved_tensors_list[self._saved_index_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_source_idx is not None:
            self._saved_fields['source'] = self._saved_tensors_list[self._saved_source_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        index = _saved[self._saved_index_idx]
        self_ = _saved[self._saved_self_idx]
        source = _saved[self._saved_source_idx]
        result = _saved[self._saved_result_idx]
        dim = self._dim
        reduce = self._reduce
        include_self = self._include_self
        with _grad_context(keyset):
            grad_self, grad_source = redispatch("index_reduce_backward", keyset, grad, self_, dim, index, source, reduce, include_self, result)
        return (grad_self, grad_source,)

class IndexCopyBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='IndexCopyBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_index_idx = None
        self._saved_source_idx = None
        self._dim = None

    def _save(self, *, index=None, source=None):
        tensors = []
        if index is not None:
            self._saved_index_idx = len(tensors)
            tensors.append(index)
        if source is not None:
            self._saved_source_idx = len(tensors)
            tensors.append(source)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_index_idx is not None:
            self._saved_fields['index'] = self._saved_tensors_list[self._saved_index_idx]
        if self._saved_source_idx is not None:
            self._saved_fields['source'] = self._saved_tensors_list[self._saved_source_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        index = _saved[self._saved_index_idx]
        source = _saved[self._saved_source_idx]
        dim = self._dim
        with _grad_context(keyset):
            grad_self = redispatch("index_fill", keyset, grad, dim, index, 0)
            grad_source = (redispatch("expand_as", keyset, redispatch("index_select", keyset, grad, dim, index), source) if redispatch("gt", keyset, redispatch("dim", keyset, source), 0) else redispatch("index_select", keyset, grad, dim, redispatch("squeeze", keyset, index, 0)))
        return (grad_self, grad_source,)

class IndexFillIntScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='IndexFillIntScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_index_idx = None
        self._dim = None
        self._value = None

    def _save(self, *, index=None):
        tensors = []
        if index is not None:
            self._saved_index_idx = len(tensors)
            tensors.append(index)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_index_idx is not None:
            self._saved_fields['index'] = self._saved_tensors_list[self._saved_index_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        index = _saved[self._saved_index_idx]
        dim = self._dim
        value = self._value
        with _grad_context(keyset):
            grad_self = redispatch("index_fill", keyset, grad, dim, index, 0)
        return (grad_self,)

class IndexFillIntTensorBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='IndexFillIntTensorBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_index_idx = None
        self._dim = None

    def _save(self, *, index=None):
        tensors = []
        if index is not None:
            self._saved_index_idx = len(tensors)
            tensors.append(index)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_index_idx is not None:
            self._saved_fields['index'] = self._saved_tensors_list[self._saved_index_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        index = _saved[self._saved_index_idx]
        dim = self._dim
        with _grad_context(keyset):
            grad_self = redispatch("index_fill", keyset, grad, dim, index, 0)
            grad_value = redispatch("sum", keyset, redispatch("index_select", keyset, grad, dim, redispatch("_unique", keyset, index, False)[0]))
        return (grad_self, grad_value,)

class IndexPutBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='IndexPutBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_values_idx = None
        self._accumulate = None

    def _save(self, *, values=None):
        tensors = []
        if values is not None:
            self._saved_values_idx = len(tensors)
            tensors.append(values)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_values_idx is not None:
            self._saved_fields['values'] = self._saved_tensors_list[self._saved_values_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        values = _saved[self._saved_values_idx]
        indices = list(self.inputs)
        accumulate = self._accumulate
        with _grad_context(keyset):
            grad_self = (grad if accumulate else redispatch("index_put", keyset, grad, indices, values._zeros_like(), False))
            grad_values = redispatch("index", keyset, grad, indices)
        return (grad_self, grad_values,)

class UnsafeIndexPutBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='UnsafeIndexPutBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_values_idx = None
        self._accumulate = None

    def _save(self, *, values=None):
        tensors = []
        if values is not None:
            self._saved_values_idx = len(tensors)
            tensors.append(values)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_values_idx is not None:
            self._saved_fields['values'] = self._saved_tensors_list[self._saved_values_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        values = _saved[self._saved_values_idx]
        indices = list(self.inputs)
        accumulate = self._accumulate
        with _grad_context(keyset):
            grad_self = (grad if accumulate else redispatch("_unsafe_index_put", keyset, grad, indices, values._zeros_like(), False))
            grad_values = redispatch("_unsafe_index", keyset, grad, indices)
        return (grad_self, grad_values,)

class IndexPutImplBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='IndexPutImplBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_values_idx = None
        self._accumulate = None
        self._unsafe = None

    def _save(self, *, values=None):
        tensors = []
        if values is not None:
            self._saved_values_idx = len(tensors)
            tensors.append(values)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_values_idx is not None:
            self._saved_fields['values'] = self._saved_tensors_list[self._saved_values_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        values = _saved[self._saved_values_idx]
        indices = list(self.inputs)
        accumulate = self._accumulate
        unsafe = self._unsafe
        with _grad_context(keyset):
            grad_self = (grad if accumulate else redispatch("index_put", keyset, grad, indices, values._zeros_like(), False))
            grad_values = redispatch("index", keyset, grad, indices)
        return (grad_self, grad_values,)

class IndexSelectBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='IndexSelectBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_index_idx = None
        self._saved_self_idx = None
        self._dim = None

    def _save(self, *, index=None, self_=None, input_=None):
        if input_ is not None and self_ is None:
            self_ = input_
        tensors = []
        if index is not None:
            self._saved_index_idx = len(tensors)
            tensors.append(index)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_index_idx is not None:
            self._saved_fields['index'] = self._saved_tensors_list[self._saved_index_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        index = _saved[self._saved_index_idx]
        self_ = _saved[self._saved_self_idx]
        dim = self._dim
        with _grad_context(keyset):
            grad_self = _index_select_backward_helper(grad, self_, dim, index, keyset)
        return (grad_self,)

class LinalgInvExBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='LinalgInvExBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._check_errors = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        check_errors = self._check_errors
        with _grad_context(keyset):
            grad_A = redispatch("neg", keyset, redispatch("matmul", keyset, redispatch("mH", keyset, inverse), redispatch("matmul", keyset, grad, redispatch("mH", keyset, inverse))))
        return (grad_A,)

class LinalgPinvAtolRtolTensorBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='LinalgPinvAtolRtolTensorBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_result_idx = None
        self._hermitian = None

    def _save(self, *, self_=None, result=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        result = _saved[self._saved_result_idx]
        hermitian = self._hermitian
        with _grad_context(keyset):
            grad_self = redispatch("pinv_backward", keyset, grad, result, self_)
        return (grad_self,)

class IsnanBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='IsnanBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        return (grad,)

class KthvalueBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='KthvalueBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_indices_idx = None
        self._k = None
        self._dim = None
        self._keepdim = None

    def _save(self, *, self_=None, indices=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if indices is not None:
            self._saved_indices_idx = len(tensors)
            tensors.append(indices)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_indices_idx is not None:
            self._saved_fields['indices'] = self._saved_tensors_list[self._saved_indices_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        indices = _saved[self._saved_indices_idx]
        k = self._k
        dim = self._dim
        keepdim = self._keepdim
        with _grad_context(keyset):
            grad_self = redispatch("value_selecting_reduction_backward_symint", keyset, grad, dim, indices, self_.shape, keepdim)
        return (grad_self,)

class LdexpTensorBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='LdexpTensorBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_other_idx = None
        self._saved_result_idx = None

    def _save(self, *, other=None, result=None):
        tensors = []
        if other is not None:
            self._saved_other_idx = len(tensors)
            tensors.append(other)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_other_idx is not None:
            self._saved_fields['other'] = self._saved_tensors_list[self._saved_other_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        other = _saved[self._saved_other_idx]
        result = _saved[self._saved_result_idx]
        with _grad_context(keyset):
            grad_self = redispatch("mul", keyset, grad, redispatch("pow", keyset, 2, other))
            grad_other = _exp2_backward_helper(grad, result, keyset)
        return (grad_self, grad_other,)

class LeScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='LeScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._other = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        other = self._other
        with _grad_context(keyset):
            grad_self = self_._zeros_like()
        return (grad_self,)

class LeTensorBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='LeTensorBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_other_idx = None

    def _save(self, *, self_=None, other=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if other is not None:
            self._saved_other_idx = len(tensors)
            tensors.append(other)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_other_idx is not None:
            self._saved_fields['other'] = self._saved_tensors_list[self._saved_other_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        other = _saved[self._saved_other_idx]
        with _grad_context(keyset):
            grad_self = self_._zeros_like()
            grad_other = other._zeros_like()
        return (grad_self, grad_other,)

class LerpScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='LerpScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._weight = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        weight = self._weight
        with _grad_context(keyset):
            grad_self = (redispatch("mul", keyset, grad, redispatch("sub", keyset, 1, redispatch("toComplexDouble", keyset, weight))) if redispatch("isComplex", keyset, weight) else redispatch("mul", keyset, grad, redispatch("sub", keyset, 1, float(weight))))
            grad_end = redispatch("mul", keyset, grad, weight)
        return (grad_self, grad_end,)

class LerpTensorBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='LerpTensorBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_weight_idx = None
        self._saved_end_idx = None
        self._saved_self_idx = None

    def _save(self, *, weight=None, end=None, self_=None):
        tensors = []
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if end is not None:
            self._saved_end_idx = len(tensors)
            tensors.append(end)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]
        if self._saved_end_idx is not None:
            self._saved_fields['end'] = self._saved_tensors_list[self._saved_end_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        weight = _saved[self._saved_weight_idx]
        end = _saved[self._saved_end_idx]
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("mul", keyset, grad, redispatch("sub", keyset, 1, weight))
            grad_end = redispatch("mul", keyset, grad, weight)
            grad_weight = redispatch("mul", keyset, grad, redispatch("sub", keyset, end, self_))
        return (grad_self, grad_end, grad_weight,)

class LgammaBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='LgammaBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("mul", keyset, grad, redispatch("digamma", keyset, self_))
        return (grad_self,)

class DigammaBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='DigammaBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("mul", keyset, grad, redispatch("polygamma", keyset, 1, self_))
        return (grad_self,)

class PolygammaBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='PolygammaBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._n = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        n = self._n
        with _grad_context(keyset):
            grad_self = redispatch("mul", keyset, grad, redispatch("polygamma", keyset, redispatch("add", keyset, n, 1), self_))
        return (grad_self,)

class LogBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='LogBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("div", keyset, grad, self_)
        return (grad_self,)

class Log10Backward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Log10Backward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("div", keyset, grad, redispatch("mul", keyset, self_, 2.3025850929940456))
        return (grad_self,)

class Log1pBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Log1pBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("log1p_backward", keyset, grad, self_)
        return (grad_self,)

class Log2Backward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Log2Backward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("div", keyset, grad, redispatch("mul", keyset, self_, 0.6931471805599453))
        return (grad_self,)

class LogaddexpBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='LogaddexpBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_other_idx = None
        self._saved_self_idx = None

    def _save(self, *, other=None, self_=None):
        tensors = []
        if other is not None:
            self._saved_other_idx = len(tensors)
            tensors.append(other)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_other_idx is not None:
            self._saved_fields['other'] = self._saved_tensors_list[self._saved_other_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        other = _saved[self._saved_other_idx]
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("div", keyset, grad, redispatch("add", keyset, 1, redispatch("exp", keyset, redispatch("sub", keyset, other, self_))))
            grad_other = redispatch("div", keyset, grad, redispatch("add", keyset, 1, redispatch("exp", keyset, redispatch("sub", keyset, self_, other))))
        return (grad_self, grad_other,)

class Logaddexp2Backward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Logaddexp2Backward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_other_idx = None
        self._saved_self_idx = None

    def _save(self, *, other=None, self_=None):
        tensors = []
        if other is not None:
            self._saved_other_idx = len(tensors)
            tensors.append(other)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_other_idx is not None:
            self._saved_fields['other'] = self._saved_tensors_list[self._saved_other_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        other = _saved[self._saved_other_idx]
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("div", keyset, grad, redispatch("add", keyset, 1, redispatch("pow", keyset, 2, redispatch("sub", keyset, other, self_))))
            grad_other = redispatch("div", keyset, grad, redispatch("add", keyset, 1, redispatch("pow", keyset, 2, redispatch("sub", keyset, self_, other))))
        return (grad_self, grad_other,)

class XlogyTensorBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='XlogyTensorBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_other_idx = None
        self._saved_self_idx = None

    def _save(self, *, other=None, self_=None):
        tensors = []
        if other is not None:
            self._saved_other_idx = len(tensors)
            tensors.append(other)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_other_idx is not None:
            self._saved_fields['other'] = self._saved_tensors_list[self._saved_other_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        other = _saved[self._saved_other_idx]
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("masked_fill", keyset, redispatch("xlogy", keyset, grad, other), (self_ == 0.) & (other <= 0.), 0.)
            grad_other = redispatch("mul", keyset, grad, redispatch("div", keyset, self_, other))
        return (grad_self, grad_other,)

class XlogyScalarSelfBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='XlogyScalarSelfBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_other_idx = None
        self._self = None

    def _save(self, *, other=None):
        tensors = []
        if other is not None:
            self._saved_other_idx = len(tensors)
            tensors.append(other)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_other_idx is not None:
            self._saved_fields['other'] = self._saved_tensors_list[self._saved_other_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        other = _saved[self._saved_other_idx]
        self_ = self._self
        with _grad_context(keyset):
            grad_other = redispatch("mul", keyset, grad, redispatch("div", keyset, self_, other))
        return (grad_other,)

class XlogyScalarOtherBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='XlogyScalarOtherBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._other = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        other = self._other
        with _grad_context(keyset):
            grad_self = (redispatch("xlogy", keyset, grad, other) if redispatch("gt", keyset, float(other), 0.) else redispatch("masked_fill", keyset, redispatch("xlogy", keyset, grad, other), redispatch("eq", keyset, self_, 0.), 0.))
        return (grad_self,)

class SpecialXlog1pyBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialXlog1pyBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_other_idx = None
        self._saved_self_idx = None

    def _save(self, *, other=None, self_=None):
        tensors = []
        if other is not None:
            self._saved_other_idx = len(tensors)
            tensors.append(other)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_other_idx is not None:
            self._saved_fields['other'] = self._saved_tensors_list[self._saved_other_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        other = _saved[self._saved_other_idx]
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("masked_fill", keyset, redispatch("special_xlog1py", keyset, grad, other), (self_ == 0.) & (other <= -1.), 0.)
            grad_other = redispatch("mul", keyset, grad, redispatch("div", keyset, self_, redispatch("add", keyset, other, 1)))
        return (grad_self, grad_other,)

class SpecialXlog1pySelfScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialXlog1pySelfScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_other_idx = None
        self._self = None

    def _save(self, *, other=None):
        tensors = []
        if other is not None:
            self._saved_other_idx = len(tensors)
            tensors.append(other)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_other_idx is not None:
            self._saved_fields['other'] = self._saved_tensors_list[self._saved_other_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        other = _saved[self._saved_other_idx]
        self_ = self._self
        with _grad_context(keyset):
            grad_other = redispatch("mul", keyset, grad, redispatch("div", keyset, self_, redispatch("add", keyset, other, 1)))
        return (grad_other,)

class SpecialXlog1pyOtherScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialXlog1pyOtherScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._other = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        other = self._other
        with _grad_context(keyset):
            grad_self = (redispatch("special_xlog1py", keyset, grad, other) if redispatch("gt", keyset, float(other), redispatch("neg", keyset, 1.)) else redispatch("masked_fill", keyset, redispatch("special_xlog1py", keyset, grad, other), redispatch("eq", keyset, self_, 0.), 0.))
        return (grad_self,)

class SpecialZetaBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialZetaBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_other_idx = None
        self._saved_self_idx = None

    def _save(self, *, other=None, self_=None):
        tensors = []
        if other is not None:
            self._saved_other_idx = len(tensors)
            tensors.append(other)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_other_idx is not None:
            self._saved_fields['other'] = self._saved_tensors_list[self._saved_other_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        other = _saved[self._saved_other_idx]
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = _raise_not_implemented("zeta")
            grad_other = redispatch("mul", keyset, redispatch("mul", keyset, grad, redispatch("neg", keyset, self_)), redispatch("special_zeta", keyset, redispatch("add", keyset, self_, 1.), other))
        return (grad_self, grad_other,)

class SpecialZetaSelfScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialZetaSelfScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_other_idx = None
        self._self = None

    def _save(self, *, other=None):
        tensors = []
        if other is not None:
            self._saved_other_idx = len(tensors)
            tensors.append(other)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_other_idx is not None:
            self._saved_fields['other'] = self._saved_tensors_list[self._saved_other_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        other = _saved[self._saved_other_idx]
        self_ = self._self
        with _grad_context(keyset):
            grad_other = redispatch("mul", keyset, redispatch("mul", keyset, grad, redispatch("neg", keyset, self_)), redispatch("special_zeta", keyset, redispatch("add", keyset, float(self_), 1.), other))
        return (grad_other,)

class SpecialZetaOtherScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialZetaOtherScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._other = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        other = self._other
        with _grad_context(keyset):
            grad_self = _raise_not_implemented("zeta")
        return (grad_self,)

class LogNormalBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='LogNormalBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._mean = None
        self._std = None
        self._generator = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        mean = self._mean
        std = self._std
        generator = self._generator
        with _grad_context(keyset):
            grad_self = grad._zeros_like()
        return (grad_self,)

class LogsumexpBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='LogsumexpBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_result_idx = None
        self._dim = None
        self._keepdim = None

    def _save(self, *, self_=None, result=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        result = _saved[self._saved_result_idx]
        dim = self._dim
        keepdim = self._keepdim
        with _grad_context(keyset):
            grad_self = redispatch("logsumexp_backward", keyset, grad, self_, result, dim, keepdim)
        return (grad_self,)

class LinalgLstsqBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='LinalgLstsqBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_b_idx = None
        self._saved_self_idx = None
        self._rcond = None
        self._driver = None

    def _save(self, *, b=None, self_=None):
        tensors = []
        if b is not None:
            self._saved_b_idx = len(tensors)
            tensors.append(b)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_b_idx is not None:
            self._saved_fields['b'] = self._saved_tensors_list[self._saved_b_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        b = _saved[self._saved_b_idx]
        self_ = _saved[self._saved_self_idx]
        rcond = self._rcond
        driver = self._driver
        grad_input_mask = [True, True]
        with _grad_context(keyset):
            grad_self, grad_b = redispatch("linalg_lstsq_backward", keyset, grads[0], grads[1], self_, b, solution, grad_input_mask)
        return (grad_self, grad_b,)

class LtScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='LtScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._other = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        other = self._other
        with _grad_context(keyset):
            grad_self = self_._zeros_like()
        return (grad_self,)

class LtTensorBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='LtTensorBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_other_idx = None

    def _save(self, *, self_=None, other=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if other is not None:
            self._saved_other_idx = len(tensors)
            tensors.append(other)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_other_idx is not None:
            self._saved_fields['other'] = self._saved_tensors_list[self._saved_other_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        other = _saved[self._saved_other_idx]
        with _grad_context(keyset):
            grad_self = self_._zeros_like()
            grad_other = other._zeros_like()
        return (grad_self, grad_other,)

class LinalgLuFactorExBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='LinalgLuFactorExBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_LU_idx = None
        self._saved_pivots_idx = None
        self._pivot = None
        self._check_errors = None

    def _save(self, *, LU=None, pivots=None):
        tensors = []
        if LU is not None:
            self._saved_LU_idx = len(tensors)
            tensors.append(LU)
        if pivots is not None:
            self._saved_pivots_idx = len(tensors)
            tensors.append(pivots)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_LU_idx is not None:
            self._saved_fields['LU'] = self._saved_tensors_list[self._saved_LU_idx]
        if self._saved_pivots_idx is not None:
            self._saved_fields['pivots'] = self._saved_tensors_list[self._saved_pivots_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        LU = _saved[self._saved_LU_idx]
        pivots = _saved[self._saved_pivots_idx]
        pivot = self._pivot
        check_errors = self._check_errors
        with _grad_context(keyset):
            grad_A = redispatch("lu_factor_ex_backward", keyset, grad, LU, pivots, pivot)
        return (grad_A,)

class LinalgLuBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='LinalgLuBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._pivot = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        pivot = self._pivot
        with _grad_context(keyset):
            grad_A = redispatch("linalg_lu_backward", keyset, grad_L, grad_U, P, L, U, pivot)
        return (grad_A,)

class LinalgLuSolveBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='LinalgLuSolveBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_LU_idx = None
        self._saved_pivots_idx = None
        self._saved_result_idx = None
        self._left = None
        self._adjoint = None

    def _save(self, *, LU=None, pivots=None, result=None):
        tensors = []
        if LU is not None:
            self._saved_LU_idx = len(tensors)
            tensors.append(LU)
        if pivots is not None:
            self._saved_pivots_idx = len(tensors)
            tensors.append(pivots)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_LU_idx is not None:
            self._saved_fields['LU'] = self._saved_tensors_list[self._saved_LU_idx]
        if self._saved_pivots_idx is not None:
            self._saved_fields['pivots'] = self._saved_tensors_list[self._saved_pivots_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        LU = _saved[self._saved_LU_idx]
        pivots = _saved[self._saved_pivots_idx]
        result = _saved[self._saved_result_idx]
        left = self._left
        adjoint = self._adjoint
        with _grad_context(keyset):
            grad_LU = redispatch("linalg_lu_solve_LU", keyset, grad, LU, pivots, result, left, adjoint)
            grad_B = redispatch("linalg_lu_solve", keyset, LU, pivots, grad, left, (not adjoint))
        return (grad_LU, grad_B,)

class LuUnpackBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='LuUnpackBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_LU_data_idx = None
        self._unpack_data = None
        self._unpack_pivots = None

    def _save(self, *, LU_data=None):
        tensors = []
        if LU_data is not None:
            self._saved_LU_data_idx = len(tensors)
            tensors.append(LU_data)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_LU_data_idx is not None:
            self._saved_fields['LU_data'] = self._saved_tensors_list[self._saved_LU_data_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        LU_data = _saved[self._saved_LU_data_idx]
        unpack_data = self._unpack_data
        unpack_pivots = self._unpack_pivots
        with _grad_context(keyset):
            grad_LU_data = redispatch("lu_unpack_backward", keyset, grad_L, grad_U, LU_data.shape[redispatch("neg", keyset, 2)], LU_data.shape[redispatch("neg", keyset, 1)])
        return (grad_LU_data,)

class MaskedFillScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MaskedFillScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_mask_idx = None
        self._value = None

    def _save(self, *, mask=None):
        tensors = []
        if mask is not None:
            self._saved_mask_idx = len(tensors)
            tensors.append(mask)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_mask_idx is not None:
            self._saved_fields['mask'] = self._saved_tensors_list[self._saved_mask_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        mask = _saved[self._saved_mask_idx]
        value = self._value
        with _grad_context(keyset):
            grad_self = redispatch("masked_fill", keyset, grad, mask, 0)
        return (grad_self,)

class MaskedFillTensorBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MaskedFillTensorBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_mask_idx = None

    def _save(self, *, mask=None):
        tensors = []
        if mask is not None:
            self._saved_mask_idx = len(tensors)
            tensors.append(mask)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_mask_idx is not None:
            self._saved_fields['mask'] = self._saved_tensors_list[self._saved_mask_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        mask = _saved[self._saved_mask_idx]
        with _grad_context(keyset):
            grad_self = redispatch("masked_fill", keyset, grad, mask, 0)
            grad_value = redispatch("masked_fill_backward", keyset, grad, mask)
        return (grad_self, grad_value,)

class MaskedScatterBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MaskedScatterBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_mask_idx = None
        self._saved_source_idx = None

    def _save(self, *, mask=None, source=None):
        tensors = []
        if mask is not None:
            self._saved_mask_idx = len(tensors)
            tensors.append(mask)
        if source is not None:
            self._saved_source_idx = len(tensors)
            tensors.append(source)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_mask_idx is not None:
            self._saved_fields['mask'] = self._saved_tensors_list[self._saved_mask_idx]
        if self._saved_source_idx is not None:
            self._saved_fields['source'] = self._saved_tensors_list[self._saved_source_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        mask = _saved[self._saved_mask_idx]
        source = _saved[self._saved_source_idx]
        with _grad_context(keyset):
            grad_self = redispatch("masked_fill", keyset, grad, mask, 0)
            grad_source = redispatch("masked_scatter_backward_symint", keyset, grad, mask, source.shape)
        return (grad_self, grad_source,)

class MaskedScatterBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MaskedScatterBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_mask_idx = None
        self._sizes = None

    def _save(self, *, mask=None):
        tensors = []
        if mask is not None:
            self._saved_mask_idx = len(tensors)
            tensors.append(mask)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_mask_idx is not None:
            self._saved_fields['mask'] = self._saved_tensors_list[self._saved_mask_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        mask = _saved[self._saved_mask_idx]
        grad_output = self.inputs[0]
        sizes = self._sizes
        with _grad_context(keyset):
            grad_grad_output = redispatch("masked_scatter", keyset, grad_output._zeros_like(), mask, grad)
        return (grad_grad_output,)

class MaskedSelectBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MaskedSelectBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_mask_idx = None
        self._saved_self_idx = None

    def _save(self, *, mask=None, self_=None):
        tensors = []
        if mask is not None:
            self._saved_mask_idx = len(tensors)
            tensors.append(mask)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_mask_idx is not None:
            self._saved_fields['mask'] = self._saved_tensors_list[self._saved_mask_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        mask = _saved[self._saved_mask_idx]
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("masked_select_backward", keyset, grad, self_, mask)
        return (grad_self,)

class LinalgMatrixExpBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='LinalgMatrixExpBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("linalg_matrix_exp_differential", keyset, self_, grad, True)
        return (grad_self,)

class MaxDimBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MaxDimBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_indices_idx = None
        self._dim = None
        self._keepdim = None

    def _save(self, *, self_=None, indices=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if indices is not None:
            self._saved_indices_idx = len(tensors)
            tensors.append(indices)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_indices_idx is not None:
            self._saved_fields['indices'] = self._saved_tensors_list[self._saved_indices_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        indices = _saved[self._saved_indices_idx]
        dim = self._dim
        keepdim = self._keepdim
        with _grad_context(keyset):
            grad_self = redispatch("value_selecting_reduction_backward_symint", keyset, grad, dim, indices, self_.shape, keepdim)
        return (grad_self,)

class MaxBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MaxBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_result_idx = None

    def _save(self, *, self_=None, result=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        result = _saved[self._saved_result_idx]
        with _grad_context(keyset):
            grad_self = redispatch("evenly_distribute_backward", keyset, grad, self_, result)
        return (grad_self,)

class MaximumBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MaximumBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_other_idx = None
        self._saved_self_idx = None

    def _save(self, *, other=None, self_=None):
        tensors = []
        if other is not None:
            self._saved_other_idx = len(tensors)
            tensors.append(other)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_other_idx is not None:
            self._saved_fields['other'] = self._saved_tensors_list[self._saved_other_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        other = _saved[self._saved_other_idx]
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("masked_fill_", keyset, redispatch("where", keyset, redispatch("eq", keyset, self_, other), redispatch("div", keyset, grad, 2), grad), redispatch("lt", keyset, self_, other), 0)
            grad_other = redispatch("masked_fill_", keyset, redispatch("where", keyset, redispatch("eq", keyset, self_, other), redispatch("div", keyset, grad, 2), grad), redispatch("gt", keyset, self_, other), 0)
        return (grad_self, grad_other,)

class FmaxBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='FmaxBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_other_idx = None
        self._saved_self_idx = None

    def _save(self, *, other=None, self_=None):
        tensors = []
        if other is not None:
            self._saved_other_idx = len(tensors)
            tensors.append(other)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_other_idx is not None:
            self._saved_fields['other'] = self._saved_tensors_list[self._saved_other_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        other = _saved[self._saved_other_idx]
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("masked_fill", keyset, grad, redispatch("logical_not_", keyset, redispatch("logical_or", keyset, redispatch("ge", keyset, self_, other), redispatch("isnan", keyset, other))), 0)
            grad_other = redispatch("masked_fill", keyset, grad, redispatch("logical_or", keyset, redispatch("ge", keyset, self_, other), redispatch("isnan", keyset, other)), 0)
        return (grad_self, grad_other,)

class MeanBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MeanBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._dtype = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        dtype = self._dtype
        with _grad_context(keyset):
            grad_self = redispatch("div", keyset, redispatch("expand", keyset, grad, self_.shape), self_.numel())
        return (grad_self,)

class MeanDimBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MeanDimBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._dim = None
        self._keepdim = None
        self._dtype = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        dim = self._dim
        keepdim = self._keepdim
        dtype = self._dtype
        with _grad_context(keyset):
            grad_self = _mean_backward_helper(grad, self_.shape, dim, self_.numel(), keepdim, keyset)
        return (grad_self,)

class MedianBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MedianBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_result_idx = None

    def _save(self, *, self_=None, result=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        result = _saved[self._saved_result_idx]
        with _grad_context(keyset):
            grad_self = redispatch("evenly_distribute_backward", keyset, grad, self_, result)
        return (grad_self,)

class NanmedianBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='NanmedianBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_result_idx = None

    def _save(self, *, self_=None, result=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        result = _saved[self._saved_result_idx]
        with _grad_context(keyset):
            grad_self = redispatch("evenly_distribute_backward", keyset, grad, self_, result)
        return (grad_self,)

class MedianDimBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MedianDimBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_indices_idx = None
        self._dim = None
        self._keepdim = None

    def _save(self, *, self_=None, indices=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if indices is not None:
            self._saved_indices_idx = len(tensors)
            tensors.append(indices)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_indices_idx is not None:
            self._saved_fields['indices'] = self._saved_tensors_list[self._saved_indices_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        indices = _saved[self._saved_indices_idx]
        dim = self._dim
        keepdim = self._keepdim
        with _grad_context(keyset):
            grad_self = redispatch("value_selecting_reduction_backward_symint", keyset, grad, dim, indices, self_.shape, keepdim)
        return (grad_self,)

class NanmedianDimBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='NanmedianDimBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_indices_idx = None
        self._dim = None
        self._keepdim = None

    def _save(self, *, self_=None, indices=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if indices is not None:
            self._saved_indices_idx = len(tensors)
            tensors.append(indices)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_indices_idx is not None:
            self._saved_fields['indices'] = self._saved_tensors_list[self._saved_indices_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        indices = _saved[self._saved_indices_idx]
        dim = self._dim
        keepdim = self._keepdim
        with _grad_context(keyset):
            grad_self = redispatch("value_selecting_reduction_backward_symint", keyset, grad, dim, indices, self_.shape, keepdim)
        return (grad_self,)

class MinDimBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MinDimBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_indices_idx = None
        self._dim = None
        self._keepdim = None

    def _save(self, *, self_=None, indices=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if indices is not None:
            self._saved_indices_idx = len(tensors)
            tensors.append(indices)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_indices_idx is not None:
            self._saved_fields['indices'] = self._saved_tensors_list[self._saved_indices_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        indices = _saved[self._saved_indices_idx]
        dim = self._dim
        keepdim = self._keepdim
        with _grad_context(keyset):
            grad_self = redispatch("value_selecting_reduction_backward_symint", keyset, grad, dim, indices, self_.shape, keepdim)
        return (grad_self,)

class MinBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MinBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_result_idx = None

    def _save(self, *, self_=None, result=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        result = _saved[self._saved_result_idx]
        with _grad_context(keyset):
            grad_self = redispatch("evenly_distribute_backward", keyset, grad, self_, result)
        return (grad_self,)

class MinimumBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MinimumBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_other_idx = None
        self._saved_self_idx = None

    def _save(self, *, other=None, self_=None):
        tensors = []
        if other is not None:
            self._saved_other_idx = len(tensors)
            tensors.append(other)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_other_idx is not None:
            self._saved_fields['other'] = self._saved_tensors_list[self._saved_other_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        other = _saved[self._saved_other_idx]
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("masked_fill_", keyset, redispatch("where", keyset, redispatch("eq", keyset, self_, other), redispatch("div", keyset, grad, 2), grad), redispatch("gt", keyset, self_, other), 0)
            grad_other = redispatch("masked_fill_", keyset, redispatch("where", keyset, redispatch("eq", keyset, self_, other), redispatch("div", keyset, grad, 2), grad), redispatch("lt", keyset, self_, other), 0)
        return (grad_self, grad_other,)

class FminBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='FminBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_other_idx = None
        self._saved_self_idx = None

    def _save(self, *, other=None, self_=None):
        tensors = []
        if other is not None:
            self._saved_other_idx = len(tensors)
            tensors.append(other)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_other_idx is not None:
            self._saved_fields['other'] = self._saved_tensors_list[self._saved_other_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        other = _saved[self._saved_other_idx]
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("masked_fill", keyset, grad, redispatch("logical_not_", keyset, redispatch("logical_or", keyset, redispatch("le", keyset, self_, other), redispatch("isnan", keyset, other))), 0)
            grad_other = redispatch("masked_fill", keyset, grad, redispatch("logical_or", keyset, redispatch("le", keyset, self_, other), redispatch("isnan", keyset, other)), 0)
        return (grad_self, grad_other,)

class AmaxBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='AmaxBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_result_idx = None
        self._dim = None
        self._keepdim = None

    def _save(self, *, self_=None, result=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        result = _saved[self._saved_result_idx]
        dim = self._dim
        keepdim = self._keepdim
        with _grad_context(keyset):
            grad_self = redispatch("scale_grad_by_count", keyset, redispatch("restore_reduced_dims", keyset, grad, dim, keepdim), redispatch("eq", keyset, redispatch("restore_reduced_dims", keyset, result, dim, keepdim), self_), dim)
        return (grad_self,)

class AminBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='AminBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_result_idx = None
        self._dim = None
        self._keepdim = None

    def _save(self, *, self_=None, result=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        result = _saved[self._saved_result_idx]
        dim = self._dim
        keepdim = self._keepdim
        with _grad_context(keyset):
            grad_self = redispatch("scale_grad_by_count", keyset, redispatch("restore_reduced_dims", keyset, grad, dim, keepdim), redispatch("eq", keyset, redispatch("restore_reduced_dims", keyset, result, dim, keepdim), self_), dim)
        return (grad_self,)

class MmBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MmBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_mat2_idx = None
        self._saved_self_idx = None

    def _save(self, *, mat2=None, self_=None):
        tensors = []
        if mat2 is not None:
            self._saved_mat2_idx = len(tensors)
            tensors.append(mat2)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_mat2_idx is not None:
            self._saved_fields['mat2'] = self._saved_tensors_list[self._saved_mat2_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        mat2 = _saved[self._saved_mat2_idx]
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("mm_mat1_backward", keyset, grad, mat2, self_.shape, redispatch("sym_strides", keyset, self_), redispatch("layout", keyset, self_), 1)
            grad_mat2 = redispatch("mm_mat2_backward", keyset, grad, self_, mat2.shape, redispatch("sym_strides", keyset, mat2), redispatch("layout", keyset, mat2), 1)
        return (grad_self, grad_mat2,)

class GroupedMmBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='GroupedMmBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_mat2_idx = None
        self._saved_offs_idx = None
        self._saved_self_idx = None
        self._out_dtype = None

    def _save(self, *, mat2=None, offs=None, self_=None):
        tensors = []
        if mat2 is not None:
            self._saved_mat2_idx = len(tensors)
            tensors.append(mat2)
        if offs is not None:
            self._saved_offs_idx = len(tensors)
            tensors.append(offs)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_mat2_idx is not None:
            self._saved_fields['mat2'] = self._saved_tensors_list[self._saved_mat2_idx]
        if self._saved_offs_idx is not None:
            self._saved_fields['offs'] = self._saved_tensors_list[self._saved_offs_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        mat2 = _saved[self._saved_mat2_idx]
        offs = _saved[self._saved_offs_idx] if self._saved_offs_idx is not None else None
        self_ = _saved[self._saved_self_idx]
        out_dtype = self._out_dtype
        with _grad_context(keyset):
            grad_self = redispatch("_grouped_mm_mat1_backward", keyset, grad, mat2, self_.shape, redispatch("sym_strides", keyset, self_), redispatch("layout", keyset, self_), offs, 1)
            grad_mat2 = redispatch("_grouped_mm_mat2_backward", keyset, grad, self_, mat2.shape, redispatch("sym_strides", keyset, mat2), redispatch("layout", keyset, mat2), offs, 1)
        return (grad_self, grad_mat2,)

class ModeBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ModeBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_indices_idx = None
        self._dim = None
        self._keepdim = None

    def _save(self, *, self_=None, indices=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if indices is not None:
            self._saved_indices_idx = len(tensors)
            tensors.append(indices)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_indices_idx is not None:
            self._saved_fields['indices'] = self._saved_tensors_list[self._saved_indices_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        indices = _saved[self._saved_indices_idx]
        dim = self._dim
        keepdim = self._keepdim
        with _grad_context(keyset):
            grad_self = redispatch("value_selecting_reduction_backward_symint", keyset, grad, dim, indices, self_.shape, keepdim)
        return (grad_self,)

class MulTensorBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MulTensorBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_other_idx = None
        self._saved_self_idx = None

    def _save(self, *, other=None, self_=None):
        tensors = []
        if other is not None:
            self._saved_other_idx = len(tensors)
            tensors.append(other)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_other_idx is not None:
            self._saved_fields['other'] = self._saved_tensors_list[self._saved_other_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        other = _saved[self._saved_other_idx]
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            _self_dtype = self_.dtype if hasattr(self_, 'dtype') else grad.dtype
            _other_dtype = other.dtype if hasattr(other, 'dtype') else grad.dtype
            grad_self = _sum_to_backward_helper(_mul_tensor_backward_helper(grad, other, _self_dtype, keyset), self_.shape if hasattr(self_, 'shape') else (), keyset)
            grad_other = _sum_to_backward_helper(_mul_tensor_backward_helper(grad, self_, _other_dtype, keyset), other.shape if hasattr(other, 'shape') else (), keyset)
        return (grad_self, grad_other,)

class MulScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MulScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._other = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        other = self._other
        with _grad_context(keyset):
            grad_self = _mul_tensor_backward_helper(grad, other, self_.dtype, keyset)
        return (grad_self,)

class MvBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MvBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_vec_idx = None
        self._saved_self_idx = None

    def _save(self, *, vec=None, self_=None):
        tensors = []
        if vec is not None:
            self._saved_vec_idx = len(tensors)
            tensors.append(vec)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_vec_idx is not None:
            self._saved_fields['vec'] = self._saved_tensors_list[self._saved_vec_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        vec = _saved[self._saved_vec_idx]
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("ger", keyset, grad, vec)
            grad_vec = redispatch("mv", keyset, redispatch("transpose", keyset, self_, 0, 1), grad)
        return (grad_self, grad_vec,)

class MvlgammaBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MvlgammaBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._p = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        p = self._p
        with _grad_context(keyset):
            grad_self = redispatch("mvlgamma_backward", keyset, grad, self_, p)
        return (grad_self,)

class NanToNumBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='NanToNumBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._nan = None
        self._posinf = None
        self._neginf = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        nan = self._nan
        posinf = self._posinf
        neginf = self._neginf
        with _grad_context(keyset):
            grad_self = redispatch("mul", keyset, grad, redispatch("isfinite", keyset, self_))
        return (grad_self,)

class NativeBatchNormBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='NativeBatchNormBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._saved_running_mean_idx = None
        self._saved_running_var_idx = None
        self._saved_weight_idx = None
        self._saved_result1_idx = None
        self._saved_result2_idx = None
        self._training = None
        self._momentum = None
        self._eps = None

    def _save(self, *, input_=None, running_mean=None, running_var=None, weight=None, result1=None, result2=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if running_mean is not None:
            self._saved_running_mean_idx = len(tensors)
            tensors.append(running_mean)
        if running_var is not None:
            self._saved_running_var_idx = len(tensors)
            tensors.append(running_var)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if result1 is not None:
            self._saved_result1_idx = len(tensors)
            tensors.append(result1)
        if result2 is not None:
            self._saved_result2_idx = len(tensors)
            tensors.append(result2)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]
        if self._saved_running_mean_idx is not None:
            self._saved_fields['running_mean'] = self._saved_tensors_list[self._saved_running_mean_idx]
        if self._saved_running_var_idx is not None:
            self._saved_fields['running_var'] = self._saved_tensors_list[self._saved_running_var_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]
        if self._saved_result1_idx is not None:
            self._saved_fields['result1'] = self._saved_tensors_list[self._saved_result1_idx]
        if self._saved_result2_idx is not None:
            self._saved_fields['result2'] = self._saved_tensors_list[self._saved_result2_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        running_mean = _saved[self._saved_running_mean_idx] if self._saved_running_mean_idx is not None else None
        running_var = _saved[self._saved_running_var_idx] if self._saved_running_var_idx is not None else None
        weight = _saved[self._saved_weight_idx] if self._saved_weight_idx is not None else None
        result1 = _saved[self._saved_result1_idx]
        result2 = _saved[self._saved_result2_idx]
        training = self._training
        momentum = self._momentum
        eps = self._eps
        grad_input_mask = [True, (weight is not None), (bias is not None)]
        with _grad_context(keyset):
            grad_input, grad_weight, grad_bias = (redispatch("native_batch_norm_backward", keyset, grad, input_, weight, running_mean, running_var, result1, result2, training, eps, grad_input_mask) if (grad is not None) else (None, None, None))
        return (grad_input, grad_weight, grad_bias,)

class NativeBatchNormLegitBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='NativeBatchNormLegitBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._saved_running_mean_idx = None
        self._saved_running_var_idx = None
        self._saved_weight_idx = None
        self._saved_result1_idx = None
        self._saved_result2_idx = None
        self._training = None
        self._momentum = None
        self._eps = None

    def _save(self, *, input_=None, running_mean=None, running_var=None, weight=None, result1=None, result2=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if running_mean is not None:
            self._saved_running_mean_idx = len(tensors)
            tensors.append(running_mean)
        if running_var is not None:
            self._saved_running_var_idx = len(tensors)
            tensors.append(running_var)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if result1 is not None:
            self._saved_result1_idx = len(tensors)
            tensors.append(result1)
        if result2 is not None:
            self._saved_result2_idx = len(tensors)
            tensors.append(result2)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]
        if self._saved_running_mean_idx is not None:
            self._saved_fields['running_mean'] = self._saved_tensors_list[self._saved_running_mean_idx]
        if self._saved_running_var_idx is not None:
            self._saved_fields['running_var'] = self._saved_tensors_list[self._saved_running_var_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]
        if self._saved_result1_idx is not None:
            self._saved_fields['result1'] = self._saved_tensors_list[self._saved_result1_idx]
        if self._saved_result2_idx is not None:
            self._saved_fields['result2'] = self._saved_tensors_list[self._saved_result2_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        running_mean = _saved[self._saved_running_mean_idx]
        running_var = _saved[self._saved_running_var_idx]
        weight = _saved[self._saved_weight_idx] if self._saved_weight_idx is not None else None
        result1 = _saved[self._saved_result1_idx]
        result2 = _saved[self._saved_result2_idx]
        training = self._training
        momentum = self._momentum
        eps = self._eps
        grad_input_mask = [True, (weight is not None), (bias is not None)]
        with _grad_context(keyset):
            grad_input, grad_weight, grad_bias = (redispatch("native_batch_norm_backward", keyset, grad, input_, weight, running_mean, running_var, result1, result2, training, eps, grad_input_mask) if (grad is not None) else (None, None, None))
        return (grad_input, grad_weight, grad_bias,)

class NativeBatchNormLegitNoTrainingBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='NativeBatchNormLegitNoTrainingBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._saved_running_mean_idx = None
        self._saved_running_var_idx = None
        self._saved_weight_idx = None
        self._saved_result1_idx = None
        self._saved_result2_idx = None
        self._momentum = None
        self._eps = None

    def _save(self, *, input_=None, running_mean=None, running_var=None, weight=None, result1=None, result2=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if running_mean is not None:
            self._saved_running_mean_idx = len(tensors)
            tensors.append(running_mean)
        if running_var is not None:
            self._saved_running_var_idx = len(tensors)
            tensors.append(running_var)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if result1 is not None:
            self._saved_result1_idx = len(tensors)
            tensors.append(result1)
        if result2 is not None:
            self._saved_result2_idx = len(tensors)
            tensors.append(result2)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]
        if self._saved_running_mean_idx is not None:
            self._saved_fields['running_mean'] = self._saved_tensors_list[self._saved_running_mean_idx]
        if self._saved_running_var_idx is not None:
            self._saved_fields['running_var'] = self._saved_tensors_list[self._saved_running_var_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]
        if self._saved_result1_idx is not None:
            self._saved_fields['result1'] = self._saved_tensors_list[self._saved_result1_idx]
        if self._saved_result2_idx is not None:
            self._saved_fields['result2'] = self._saved_tensors_list[self._saved_result2_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        running_mean = _saved[self._saved_running_mean_idx]
        running_var = _saved[self._saved_running_var_idx]
        weight = _saved[self._saved_weight_idx] if self._saved_weight_idx is not None else None
        result1 = _saved[self._saved_result1_idx]
        result2 = _saved[self._saved_result2_idx]
        momentum = self._momentum
        eps = self._eps
        grad_input_mask = [True, (weight is not None), (bias is not None)]
        with _grad_context(keyset):
            grad_input, grad_weight, grad_bias = (redispatch("native_batch_norm_backward", keyset, grad, input_, weight, running_mean, running_var, result1, result2, False, eps, grad_input_mask) if (grad is not None) else (None, None, None))
        return (grad_input, grad_weight, grad_bias,)

class NativeBatchNormLegitNoStatsBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='NativeBatchNormLegitNoStatsBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._saved_weight_idx = None
        self._saved_result1_idx = None
        self._saved_result2_idx = None
        self._training = None
        self._momentum = None
        self._eps = None

    def _save(self, *, input_=None, weight=None, result1=None, result2=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if result1 is not None:
            self._saved_result1_idx = len(tensors)
            tensors.append(result1)
        if result2 is not None:
            self._saved_result2_idx = len(tensors)
            tensors.append(result2)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]
        if self._saved_result1_idx is not None:
            self._saved_fields['result1'] = self._saved_tensors_list[self._saved_result1_idx]
        if self._saved_result2_idx is not None:
            self._saved_fields['result2'] = self._saved_tensors_list[self._saved_result2_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        weight = _saved[self._saved_weight_idx] if self._saved_weight_idx is not None else None
        result1 = _saved[self._saved_result1_idx]
        result2 = _saved[self._saved_result2_idx]
        training = self._training
        momentum = self._momentum
        eps = self._eps
        grad_input_mask = [True, (weight is not None), (bias is not None)]
        with _grad_context(keyset):
            grad_input, grad_weight, grad_bias = (redispatch("native_batch_norm_backward", keyset, grad, input_, weight, None, None, result1, result2, training, eps, grad_input_mask) if (grad is not None) else (None, None, None))
        return (grad_input, grad_weight, grad_bias,)

class NativeBatchNormBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='NativeBatchNormBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._saved_running_mean_idx = None
        self._saved_running_var_idx = None
        self._saved_save_invstd_idx = None
        self._saved_save_mean_idx = None
        self._saved_weight_idx = None
        self._train = None
        self._eps = None
        self._output_mask = None

    def _save(self, *, input_=None, running_mean=None, running_var=None, save_invstd=None, save_mean=None, weight=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if running_mean is not None:
            self._saved_running_mean_idx = len(tensors)
            tensors.append(running_mean)
        if running_var is not None:
            self._saved_running_var_idx = len(tensors)
            tensors.append(running_var)
        if save_invstd is not None:
            self._saved_save_invstd_idx = len(tensors)
            tensors.append(save_invstd)
        if save_mean is not None:
            self._saved_save_mean_idx = len(tensors)
            tensors.append(save_mean)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]
        if self._saved_running_mean_idx is not None:
            self._saved_fields['running_mean'] = self._saved_tensors_list[self._saved_running_mean_idx]
        if self._saved_running_var_idx is not None:
            self._saved_fields['running_var'] = self._saved_tensors_list[self._saved_running_var_idx]
        if self._saved_save_invstd_idx is not None:
            self._saved_fields['save_invstd'] = self._saved_tensors_list[self._saved_save_invstd_idx]
        if self._saved_save_mean_idx is not None:
            self._saved_fields['save_mean'] = self._saved_tensors_list[self._saved_save_mean_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        running_mean = _saved[self._saved_running_mean_idx] if self._saved_running_mean_idx is not None else None
        running_var = _saved[self._saved_running_var_idx] if self._saved_running_var_idx is not None else None
        save_invstd = _saved[self._saved_save_invstd_idx] if self._saved_save_invstd_idx is not None else None
        save_mean = _saved[self._saved_save_mean_idx] if self._saved_save_mean_idx is not None else None
        weight = _saved[self._saved_weight_idx] if self._saved_weight_idx is not None else None
        grad_out = self.inputs[0]
        train = self._train
        eps = self._eps
        output_mask = self._output_mask
        grad_input_mask = [True, True, (weight is not None), (save_mean is not None), (save_invstd is not None)]
        with _grad_context(keyset):
            grad_input, grad_weight, grad_grad_out = redispatch("batchnorm_double_backward", keyset, input_, weight, grads[0], grads[1], grads[2], grad_out, running_mean, running_var, train, eps, save_mean, save_invstd, grad_input_mask)
            grad_save_mean = _raise_not_implemented("native_batch_norm_backward save_mean")
            grad_save_invstd = _raise_not_implemented("native_batch_norm_backward save_invstd")
        return (grad_grad_out, grad_input, grad_weight, grad_save_mean, grad_save_invstd,)

class NativeLayerNormBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='NativeLayerNormBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_bias_idx = None
        self._saved_input_idx = None
        self._saved_weight_idx = None
        self._saved_result1_idx = None
        self._saved_result2_idx = None
        self._normalized_shape = None
        self._eps = None

    def _save(self, *, bias=None, input_=None, weight=None, result1=None, result2=None):
        tensors = []
        if bias is not None:
            self._saved_bias_idx = len(tensors)
            tensors.append(bias)
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if result1 is not None:
            self._saved_result1_idx = len(tensors)
            tensors.append(result1)
        if result2 is not None:
            self._saved_result2_idx = len(tensors)
            tensors.append(result2)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_bias_idx is not None:
            self._saved_fields['bias'] = self._saved_tensors_list[self._saved_bias_idx]
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]
        if self._saved_result1_idx is not None:
            self._saved_fields['result1'] = self._saved_tensors_list[self._saved_result1_idx]
        if self._saved_result2_idx is not None:
            self._saved_fields['result2'] = self._saved_tensors_list[self._saved_result2_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        bias = _saved[self._saved_bias_idx] if self._saved_bias_idx is not None else None
        input_ = _saved[self._saved_input_idx]
        weight = _saved[self._saved_weight_idx] if self._saved_weight_idx is not None else None
        result1 = _saved[self._saved_result1_idx]
        result2 = _saved[self._saved_result2_idx]
        normalized_shape = self._normalized_shape
        eps = self._eps
        grad_input_mask = [True, (weight is not None), (bias is not None)]
        with _grad_context(keyset):
            grad_input, grad_weight, grad_bias = (redispatch("native_layer_norm_backward_symint", keyset, grad, input_, normalized_shape, result1, result2, weight, bias, grad_input_mask) if (grad is not None) else (None, None, None))
        return (grad_input, grad_weight, grad_bias,)

class NativeLayerNormBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='NativeLayerNormBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._saved_mean_idx = None
        self._saved_rstd_idx = None
        self._saved_weight_idx = None
        self._normalized_shape = None
        self._output_mask = None

    def _save(self, *, input_=None, mean=None, rstd=None, weight=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if mean is not None:
            self._saved_mean_idx = len(tensors)
            tensors.append(mean)
        if rstd is not None:
            self._saved_rstd_idx = len(tensors)
            tensors.append(rstd)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]
        if self._saved_mean_idx is not None:
            self._saved_fields['mean'] = self._saved_tensors_list[self._saved_mean_idx]
        if self._saved_rstd_idx is not None:
            self._saved_fields['rstd'] = self._saved_tensors_list[self._saved_rstd_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        mean = _saved[self._saved_mean_idx]
        rstd = _saved[self._saved_rstd_idx]
        weight = _saved[self._saved_weight_idx] if self._saved_weight_idx is not None else None
        grad_out = self.inputs[0]
        normalized_shape = self._normalized_shape
        output_mask = self._output_mask
        grad_input_mask = [True, True, True, True, (weight is not None), (bias is not None)]
        with _grad_context(keyset):
            grad_input, grad_weight, grad_grad_out = redispatch("layer_norm_double_backward", keyset, input_, weight, grads[0], grads[1], grads[2], grad_out, mean, rstd, normalized_shape, grad_input_mask)
            grad_bias = None
            grad_mean = _raise_not_implemented("native_layer_norm_backward mean")
            grad_rstd = _raise_not_implemented("native_layer_norm_backward rstd")
        return (grad_grad_out, grad_input, grad_mean, grad_rstd, grad_weight, grad_bias,)

class FusedRmsNormBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='FusedRmsNormBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._saved_weight_idx = None
        self._saved_result1_idx = None
        self._normalized_shape = None
        self._eps = None

    def _save(self, *, input_=None, weight=None, result1=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if result1 is not None:
            self._saved_result1_idx = len(tensors)
            tensors.append(result1)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]
        if self._saved_result1_idx is not None:
            self._saved_fields['result1'] = self._saved_tensors_list[self._saved_result1_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        weight = _saved[self._saved_weight_idx] if self._saved_weight_idx is not None else None
        result1 = _saved[self._saved_result1_idx]
        normalized_shape = self._normalized_shape
        eps = self._eps
        grad_input_mask = [True, (weight is not None)]
        with _grad_context(keyset):
            grad_input, grad_weight = (redispatch("infinitely_differentiable_native_rms_norm_backward", keyset, grads[0], grads[1], input_, normalized_shape, result1, weight, grad_input_mask) if True else (redispatch("_fused_rms_norm_backward", keyset, grads[0], input_, normalized_shape, result1, weight, grad_input_mask) if (grads[0] is not None) else (None, None)))
        return (grad_input, grad_weight,)

class NativeGroupNormBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='NativeGroupNormBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._saved_weight_idx = None
        self._saved_result1_idx = None
        self._saved_result2_idx = None
        self._N = None
        self._C = None
        self._HxW = None
        self._group = None
        self._eps = None

    def _save(self, *, input_=None, weight=None, result1=None, result2=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if result1 is not None:
            self._saved_result1_idx = len(tensors)
            tensors.append(result1)
        if result2 is not None:
            self._saved_result2_idx = len(tensors)
            tensors.append(result2)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]
        if self._saved_result1_idx is not None:
            self._saved_fields['result1'] = self._saved_tensors_list[self._saved_result1_idx]
        if self._saved_result2_idx is not None:
            self._saved_fields['result2'] = self._saved_tensors_list[self._saved_result2_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        weight = _saved[self._saved_weight_idx] if self._saved_weight_idx is not None else None
        result1 = _saved[self._saved_result1_idx]
        result2 = _saved[self._saved_result2_idx]
        N = self._N
        C = self._C
        HxW = self._HxW
        group = self._group
        eps = self._eps
        grad_input_mask = [True, (weight is not None), (bias is not None)]
        with _grad_context(keyset):
            grad_input, grad_weight, grad_bias = _native_group_norm_helper(grads, input_, result1, result2, weight, N, C, HxW, group, eps, grad_input_mask)
        return (grad_input, grad_weight, grad_bias,)

class NeScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='NeScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._other = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        other = self._other
        with _grad_context(keyset):
            grad_self = self_._zeros_like()
        return (grad_self,)

class NeTensorBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='NeTensorBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_other_idx = None

    def _save(self, *, self_=None, other=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if other is not None:
            self._saved_other_idx = len(tensors)
            tensors.append(other)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_other_idx is not None:
            self._saved_fields['other'] = self._saved_tensors_list[self._saved_other_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        other = _saved[self._saved_other_idx]
        with _grad_context(keyset):
            grad_self = self_._zeros_like()
            grad_other = other._zeros_like()
        return (grad_self, grad_other,)

class NegBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='NegBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        with _grad_context(keyset):
            grad_self = redispatch("neg", keyset, grad)
        return (grad_self,)

class BatchNormWithUpdateBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='BatchNormWithUpdateBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._saved_running_mean_idx = None
        self._saved_running_var_idx = None
        self._saved_weight_idx = None
        self._saved_result1_idx = None
        self._saved_result2_idx = None
        self._saved_result3_idx = None
        self._momentum = None
        self._eps = None

    def _save(self, *, input_=None, running_mean=None, running_var=None, weight=None, result1=None, result2=None, result3=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if running_mean is not None:
            self._saved_running_mean_idx = len(tensors)
            tensors.append(running_mean)
        if running_var is not None:
            self._saved_running_var_idx = len(tensors)
            tensors.append(running_var)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if result1 is not None:
            self._saved_result1_idx = len(tensors)
            tensors.append(result1)
        if result2 is not None:
            self._saved_result2_idx = len(tensors)
            tensors.append(result2)
        if result3 is not None:
            self._saved_result3_idx = len(tensors)
            tensors.append(result3)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]
        if self._saved_running_mean_idx is not None:
            self._saved_fields['running_mean'] = self._saved_tensors_list[self._saved_running_mean_idx]
        if self._saved_running_var_idx is not None:
            self._saved_fields['running_var'] = self._saved_tensors_list[self._saved_running_var_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]
        if self._saved_result1_idx is not None:
            self._saved_fields['result1'] = self._saved_tensors_list[self._saved_result1_idx]
        if self._saved_result2_idx is not None:
            self._saved_fields['result2'] = self._saved_tensors_list[self._saved_result2_idx]
        if self._saved_result3_idx is not None:
            self._saved_fields['result3'] = self._saved_tensors_list[self._saved_result3_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        running_mean = _saved[self._saved_running_mean_idx]
        running_var = _saved[self._saved_running_var_idx]
        weight = _saved[self._saved_weight_idx] if self._saved_weight_idx is not None else None
        result1 = _saved[self._saved_result1_idx]
        result2 = _saved[self._saved_result2_idx]
        result3 = _saved[self._saved_result3_idx]
        momentum = self._momentum
        eps = self._eps
        grad_input_mask = [True, (weight is not None), (bias is not None)]
        with _grad_context(keyset):
            grad_input, grad_weight, grad_bias = (redispatch("batch_norm_backward", keyset, grad, input_, weight, running_mean, running_var, result1, result2, True, eps, grad_input_mask, (redispatch("clone", keyset, result3) if retain_variables else result3)) if (grad is not None) else (None, None, None))
        return (grad_input, grad_weight, grad_bias,)

class BatchNormNoUpdateBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='BatchNormNoUpdateBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._saved_running_mean_idx = None
        self._saved_running_var_idx = None
        self._saved_weight_idx = None
        self._saved_result1_idx = None
        self._saved_result2_idx = None
        self._saved_result3_idx = None
        self._momentum = None
        self._eps = None

    def _save(self, *, input_=None, running_mean=None, running_var=None, weight=None, result1=None, result2=None, result3=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if running_mean is not None:
            self._saved_running_mean_idx = len(tensors)
            tensors.append(running_mean)
        if running_var is not None:
            self._saved_running_var_idx = len(tensors)
            tensors.append(running_var)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if result1 is not None:
            self._saved_result1_idx = len(tensors)
            tensors.append(result1)
        if result2 is not None:
            self._saved_result2_idx = len(tensors)
            tensors.append(result2)
        if result3 is not None:
            self._saved_result3_idx = len(tensors)
            tensors.append(result3)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]
        if self._saved_running_mean_idx is not None:
            self._saved_fields['running_mean'] = self._saved_tensors_list[self._saved_running_mean_idx]
        if self._saved_running_var_idx is not None:
            self._saved_fields['running_var'] = self._saved_tensors_list[self._saved_running_var_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]
        if self._saved_result1_idx is not None:
            self._saved_fields['result1'] = self._saved_tensors_list[self._saved_result1_idx]
        if self._saved_result2_idx is not None:
            self._saved_fields['result2'] = self._saved_tensors_list[self._saved_result2_idx]
        if self._saved_result3_idx is not None:
            self._saved_fields['result3'] = self._saved_tensors_list[self._saved_result3_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        running_mean = _saved[self._saved_running_mean_idx] if self._saved_running_mean_idx is not None else None
        running_var = _saved[self._saved_running_var_idx] if self._saved_running_var_idx is not None else None
        weight = _saved[self._saved_weight_idx] if self._saved_weight_idx is not None else None
        result1 = _saved[self._saved_result1_idx]
        result2 = _saved[self._saved_result2_idx]
        result3 = _saved[self._saved_result3_idx]
        momentum = self._momentum
        eps = self._eps
        grad_input_mask = [True, (weight is not None), (bias is not None)]
        with _grad_context(keyset):
            grad_input, grad_weight, grad_bias = (redispatch("batch_norm_backward", keyset, grad, input_, weight, running_mean, running_var, result1, result2, False, eps, grad_input_mask, (redispatch("clone", keyset, result3) if retain_variables else result3)) if (grad is not None) else (None, None, None))
        return (grad_input, grad_weight, grad_bias,)

class BatchNormBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='BatchNormBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._saved_running_mean_idx = None
        self._saved_running_var_idx = None
        self._saved_save_mean_idx = None
        self._saved_save_var_idx = None
        self._saved_weight_idx = None
        self._saved_reserve_idx = None
        self._update = None
        self._eps = None
        self._output_mask = None

    def _save(self, *, input_=None, running_mean=None, running_var=None, save_mean=None, save_var=None, weight=None, reserve=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if running_mean is not None:
            self._saved_running_mean_idx = len(tensors)
            tensors.append(running_mean)
        if running_var is not None:
            self._saved_running_var_idx = len(tensors)
            tensors.append(running_var)
        if save_mean is not None:
            self._saved_save_mean_idx = len(tensors)
            tensors.append(save_mean)
        if save_var is not None:
            self._saved_save_var_idx = len(tensors)
            tensors.append(save_var)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if reserve is not None:
            self._saved_reserve_idx = len(tensors)
            tensors.append(reserve)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]
        if self._saved_running_mean_idx is not None:
            self._saved_fields['running_mean'] = self._saved_tensors_list[self._saved_running_mean_idx]
        if self._saved_running_var_idx is not None:
            self._saved_fields['running_var'] = self._saved_tensors_list[self._saved_running_var_idx]
        if self._saved_save_mean_idx is not None:
            self._saved_fields['save_mean'] = self._saved_tensors_list[self._saved_save_mean_idx]
        if self._saved_save_var_idx is not None:
            self._saved_fields['save_var'] = self._saved_tensors_list[self._saved_save_var_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]
        if self._saved_reserve_idx is not None:
            self._saved_fields['reserve'] = self._saved_tensors_list[self._saved_reserve_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        running_mean = _saved[self._saved_running_mean_idx] if self._saved_running_mean_idx is not None else None
        running_var = _saved[self._saved_running_var_idx] if self._saved_running_var_idx is not None else None
        save_mean = _saved[self._saved_save_mean_idx] if self._saved_save_mean_idx is not None else None
        save_var = _saved[self._saved_save_var_idx] if self._saved_save_var_idx is not None else None
        weight = _saved[self._saved_weight_idx]
        reserve = _saved[self._saved_reserve_idx]
        grad_out = self.inputs[0]
        update = self._update
        eps = self._eps
        output_mask = self._output_mask
        grad_input_mask = [True, True, True, (save_mean is not None), (save_var is not None), True]
        with _grad_context(keyset):
            grad_input, grad_weight, grad_grad_out = redispatch("batchnorm_double_backward", keyset, input_, weight, grads[0], grads[1], grads[2], grad_out, running_mean, running_var, update, eps, save_mean, save_var, grad_input_mask)
            grad_save_mean = _raise_not_implemented("batch_norm_backward save_mean")
            grad_save_var = _raise_not_implemented("batch_norm_backward save_var")
            grad_reserve = _raise_not_implemented("batch_norm_backward reserve")
        return (grad_grad_out, grad_input, grad_weight, grad_save_mean, grad_save_var, grad_reserve,)

class NextafterBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='NextafterBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        with _grad_context(keyset):
            grad_self = _raise_not_implemented("nextafter")
            grad_other = _raise_not_implemented("nextafter")
        return (grad_self, grad_other,)

class NormScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='NormScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_result_idx = None
        self._p = None

    def _save(self, *, self_=None, result=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        result = _saved[self._saved_result_idx]
        p = self._p
        with _grad_context(keyset):
            grad_self = _norm_backward_helper(grad, self_, p, None, False, keyset)
        return (grad_self,)

class NormScalarOptDimBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='NormScalarOptDimBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_result_idx = None
        self._p = None
        self._dim = None
        self._keepdim = None

    def _save(self, *, self_=None, result=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        result = _saved[self._saved_result_idx]
        p = self._p
        dim = self._dim
        keepdim = self._keepdim
        with _grad_context(keyset):
            grad_self = _norm_backward_helper(grad, self_, p, dim, keepdim, keyset)
        return (grad_self,)

class NormScalarOptDtypeBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='NormScalarOptDtypeBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_result_idx = None
        self._p = None
        self._dtype = None

    def _save(self, *, self_=None, result=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        result = _saved[self._saved_result_idx]
        p = self._p
        dtype = self._dtype
        with _grad_context(keyset):
            grad_self = _norm_backward_helper(grad, redispatch("to", keyset, self_, grad.dtype), p, None, False, keyset)
        return (grad_self,)

class NormScalarOptDimDtypeBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='NormScalarOptDimDtypeBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_result_idx = None
        self._p = None
        self._dim = None
        self._keepdim = None
        self._dtype = None

    def _save(self, *, self_=None, result=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        result = _saved[self._saved_result_idx]
        p = self._p
        dim = self._dim
        keepdim = self._keepdim
        dtype = self._dtype
        with _grad_context(keyset):
            grad_self = _norm_backward_helper(grad, redispatch("to", keyset, self_, grad.dtype), p, dim, keepdim, keyset)
        return (grad_self,)

class LinalgVectorNormBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='LinalgVectorNormBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_result_idx = None
        self._ord = None
        self._dim = None
        self._keepdim = None
        self._dtype = None

    def _save(self, *, self_=None, result=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        result = _saved[self._saved_result_idx]
        ord = self._ord
        dim = self._dim
        keepdim = self._keepdim
        dtype = self._dtype
        with _grad_context(keyset):
            grad_self = redispatch("linalg_vector_norm_backward", keyset, grad, self_, ord, result, dim, keepdim)
        return (grad_self,)

class PdistForwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='PdistForwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_result_idx = None
        self._p = None

    def _save(self, *, self_=None, result=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        result = _saved[self._saved_result_idx]
        p = self._p
        with _grad_context(keyset):
            grad_self = redispatch("_pdist_backward", keyset, grad, self_, p, result)
        return (grad_self,)

class PdistBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='PdistBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._p = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        p = self._p
        with _grad_context(keyset):
            grad_grad = _raise_not_implemented("_pdist_backward")
            grad_self = _raise_not_implemented("_pdist_backward")
            grad_pdist = _raise_not_implemented("_pdist_backward")
        return (grad_grad, grad_self, grad_pdist,)

class EuclideanDistBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='EuclideanDistBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_x1_idx = None
        self._saved_x2_idx = None
        self._saved_result_idx = None

    def _save(self, *, x1=None, x2=None, result=None):
        tensors = []
        if x1 is not None:
            self._saved_x1_idx = len(tensors)
            tensors.append(x1)
        if x2 is not None:
            self._saved_x2_idx = len(tensors)
            tensors.append(x2)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_x1_idx is not None:
            self._saved_fields['x1'] = self._saved_tensors_list[self._saved_x1_idx]
        if self._saved_x2_idx is not None:
            self._saved_fields['x2'] = self._saved_tensors_list[self._saved_x2_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        x1 = _saved[self._saved_x1_idx]
        x2 = _saved[self._saved_x2_idx]
        result = _saved[self._saved_result_idx]
        with _grad_context(keyset):
            grad_x1, grad_x2 = redispatch("_euclidean_dist_backward", keyset, grad, x1, x2, result)
        return (grad_x1, grad_x2,)

class CdistForwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='CdistForwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_x1_idx = None
        self._saved_x2_idx = None
        self._saved_result_idx = None
        self._p = None
        self._compute_mode = None

    def _save(self, *, x1=None, x2=None, result=None):
        tensors = []
        if x1 is not None:
            self._saved_x1_idx = len(tensors)
            tensors.append(x1)
        if x2 is not None:
            self._saved_x2_idx = len(tensors)
            tensors.append(x2)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_x1_idx is not None:
            self._saved_fields['x1'] = self._saved_tensors_list[self._saved_x1_idx]
        if self._saved_x2_idx is not None:
            self._saved_fields['x2'] = self._saved_tensors_list[self._saved_x2_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        x1 = _saved[self._saved_x1_idx]
        x2 = _saved[self._saved_x2_idx]
        result = _saved[self._saved_result_idx]
        p = self._p
        compute_mode = self._compute_mode
        with _grad_context(keyset):
            grad_x1 = redispatch("_cdist_backward", keyset, redispatch("contiguous", keyset, grad), x1, x2, p, result)
            grad_x2 = redispatch("_cdist_backward", keyset, redispatch("contiguous", keyset, redispatch("mT", keyset, grad)), x2, x1, p, redispatch("contiguous", keyset, redispatch("mT", keyset, result)))
        return (grad_x1, grad_x2,)

class CdistBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='CdistBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._p = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        p = self._p
        with _grad_context(keyset):
            grad_grad = _raise_not_implemented("_cdist_backward")
            grad_x1 = _raise_not_implemented("_cdist_backward")
            grad_x2 = _raise_not_implemented("_cdist_backward")
            grad_cdist = _raise_not_implemented("_cdist_backward")
        return (grad_grad, grad_x1, grad_x2, grad_cdist,)

class NormalBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='NormalBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._mean = None
        self._std = None
        self._generator = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        mean = self._mean
        std = self._std
        generator = self._generator
        with _grad_context(keyset):
            grad_self = grad._zeros_like()
        return (grad_self,)

class NormalTensorFloatBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='NormalTensorFloatBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_mean_idx = None
        self._std = None
        self._generator = None

    def _save(self, *, mean=None):
        tensors = []
        if mean is not None:
            self._saved_mean_idx = len(tensors)
            tensors.append(mean)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_mean_idx is not None:
            self._saved_fields['mean'] = self._saved_tensors_list[self._saved_mean_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        mean = _saved[self._saved_mean_idx]
        std = self._std
        generator = self._generator
        with _grad_context(keyset):
            grad_mean = redispatch("zeros_symint", keyset, mean.shape, grad.options)
        return (grad_mean,)

class NormalFloatTensorBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='NormalFloatTensorBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._mean = None
        self._generator = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        std = self.inputs[0]
        mean = self._mean
        generator = self._generator
        with _grad_context(keyset):
            grad_std = redispatch("zeros_symint", keyset, std.shape, grad.options)
        return (grad_std,)

class NormalTensorTensorBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='NormalTensorTensorBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_mean_idx = None
        self._generator = None

    def _save(self, *, mean=None):
        tensors = []
        if mean is not None:
            self._saved_mean_idx = len(tensors)
            tensors.append(mean)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_mean_idx is not None:
            self._saved_fields['mean'] = self._saved_tensors_list[self._saved_mean_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        mean = _saved[self._saved_mean_idx]
        std = self.inputs[1]
        generator = self._generator
        with _grad_context(keyset):
            grad_mean = redispatch("zeros_symint", keyset, mean.shape, grad.options)
            grad_std = redispatch("zeros_symint", keyset, std.shape, grad.options)
        return (grad_mean, grad_std,)

class LinalgHouseholderProductBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='LinalgHouseholderProductBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._saved_tau_idx = None
        self._saved_result_idx = None

    def _save(self, *, input_=None, tau=None, result=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tau is not None:
            self._saved_tau_idx = len(tensors)
            tensors.append(tau)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]
        if self._saved_tau_idx is not None:
            self._saved_fields['tau'] = self._saved_tensors_list[self._saved_tau_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        tau = _saved[self._saved_tau_idx]
        result = _saved[self._saved_result_idx]
        with _grad_context(keyset):
            grad_input, grad_tau = redispatch("householder_product_backward", keyset, grad, result, input_, tau)
        return (grad_input, grad_tau,)

class OrmqrBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='OrmqrBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input2_idx = None
        self._saved_input3_idx = None
        self._saved_self_idx = None
        self._saved_result_idx = None
        self._left = None
        self._transpose = None

    def _save(self, *, input2=None, input3=None, self_=None, result=None):
        tensors = []
        if input2 is not None:
            self._saved_input2_idx = len(tensors)
            tensors.append(input2)
        if input3 is not None:
            self._saved_input3_idx = len(tensors)
            tensors.append(input3)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input2_idx is not None:
            self._saved_fields['input2'] = self._saved_tensors_list[self._saved_input2_idx]
        if self._saved_input3_idx is not None:
            self._saved_fields['input3'] = self._saved_tensors_list[self._saved_input3_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input2 = _saved[self._saved_input2_idx]
        input3 = _saved[self._saved_input3_idx]
        self_ = _saved[self._saved_self_idx]
        result = _saved[self._saved_result_idx]
        left = self._left
        transpose = self._transpose
        grad_input_mask = [True, True, True]
        with _grad_context(keyset):
            grad_self, grad_input2, grad_input3 = redispatch("ormqr_backward", keyset, grad, result, self_, input2, input3, left, transpose, grad_input_mask)
        return (grad_self, grad_input2, grad_input3,)

class PermuteBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='PermuteBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._dims = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        dims = self._dims
        with _grad_context(keyset):
            grad_self = _permute_backward_helper(grad, dims, keyset)
        return (grad_self,)

class PoissonBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='PoissonBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._generator = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        generator = self._generator
        with _grad_context(keyset):
            grad_self = self_._zeros_like()
        return (grad_self,)

class PowTensorScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='PowTensorScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._exponent = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        exponent = self._exponent
        with _grad_context(keyset):
            grad_self = _pow_backward_helper(grad, self_, exponent, keyset)
        return (grad_self,)

class PowTensorTensorBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='PowTensorTensorBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_exponent_idx = None
        self._saved_self_idx = None
        self._saved_result_idx = None

    def _save(self, *, exponent=None, self_=None, result=None):
        tensors = []
        if exponent is not None:
            self._saved_exponent_idx = len(tensors)
            tensors.append(exponent)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_exponent_idx is not None:
            self._saved_fields['exponent'] = self._saved_tensors_list[self._saved_exponent_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        exponent = _saved[self._saved_exponent_idx]
        self_ = _saved[self._saved_self_idx]
        result = _saved[self._saved_result_idx]
        with _grad_context(keyset):
            grad_self = _pow_backward_self_helper(grad, self_, exponent, keyset)
            grad_exponent = _pow_backward_exponent_helper(grad, self_, exponent, result, keyset)
        return (grad_self, grad_exponent,)

class PowScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='PowScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_exponent_idx = None
        self._saved_result_idx = None
        self._self = None

    def _save(self, *, exponent=None, result=None):
        tensors = []
        if exponent is not None:
            self._saved_exponent_idx = len(tensors)
            tensors.append(exponent)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_exponent_idx is not None:
            self._saved_fields['exponent'] = self._saved_tensors_list[self._saved_exponent_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        exponent = _saved[self._saved_exponent_idx]
        result = _saved[self._saved_result_idx]
        self_ = self._self
        with _grad_context(keyset):
            grad_exponent = redispatch("pow_backward_exponent", keyset, grad, self_, exponent, result)
        return (grad_exponent,)

class ProdBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ProdBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._dim = None
        self._keepdim = None

    def _save(self, *, input_=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        dim = self._dim
        keepdim = self._keepdim
        with _grad_context(keyset):
            grad_input = _prod_backward_helper(grad, input_, dim, keepdim, keyset)
        return (grad_input,)

class ProdDimIntBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ProdDimIntBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_result_idx = None
        self._dim = None
        self._keepdim = None
        self._dtype = None

    def _save(self, *, self_=None, result=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        result = _saved[self._saved_result_idx]
        dim = self._dim
        keepdim = self._keepdim
        dtype = self._dtype
        with _grad_context(keyset):
            grad_self = redispatch("prod_backward", keyset, grad, redispatch("to", keyset, self_, grad.dtype), result, dim, keepdim)
        return (grad_self,)

class PutBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='PutBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_index_idx = None
        self._saved_source_idx = None
        self._accumulate = None

    def _save(self, *, index=None, source=None):
        tensors = []
        if index is not None:
            self._saved_index_idx = len(tensors)
            tensors.append(index)
        if source is not None:
            self._saved_source_idx = len(tensors)
            tensors.append(source)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_index_idx is not None:
            self._saved_fields['index'] = self._saved_tensors_list[self._saved_index_idx]
        if self._saved_source_idx is not None:
            self._saved_fields['source'] = self._saved_tensors_list[self._saved_source_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        index = _saved[self._saved_index_idx]
        source = _saved[self._saved_source_idx]
        accumulate = self._accumulate
        with _grad_context(keyset):
            grad_self = (grad if accumulate else redispatch("put", keyset, grad, index, source._zeros_like(), False))
            grad_source = redispatch("reshape_as", keyset, redispatch("take", keyset, grad, index), source)
        return (grad_self, grad_source,)

class LinalgQrBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='LinalgQrBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_Q_idx = None
        self._saved_R_idx = None
        self._mode = None

    def _save(self, *, Q=None, R=None):
        tensors = []
        if Q is not None:
            self._saved_Q_idx = len(tensors)
            tensors.append(Q)
        if R is not None:
            self._saved_R_idx = len(tensors)
            tensors.append(R)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_Q_idx is not None:
            self._saved_fields['Q'] = self._saved_tensors_list[self._saved_Q_idx]
        if self._saved_R_idx is not None:
            self._saved_fields['R'] = self._saved_tensors_list[self._saved_R_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        Q = _saved[self._saved_Q_idx]
        R = _saved[self._saved_R_idx]
        mode = self._mode
        with _grad_context(keyset):
            grad_A = redispatch("linalg_qr_backward", keyset, grad_Q, grad_R, Q, R, mode)
        return (grad_A,)

class Rad2degBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Rad2degBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        with _grad_context(keyset):
            grad_self = redispatch("rad2deg_backward", keyset, grad)
        return (grad_self,)

class RandomFromBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='RandomFromBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._from = None
        self._to = None
        self._generator = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        from_ = self._from
        to = self._to
        generator = self._generator
        with _grad_context(keyset):
            grad_self = grad._zeros_like()
        return (grad_self,)

class RandomToBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='RandomToBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._to = None
        self._generator = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        to = self._to
        generator = self._generator
        with _grad_context(keyset):
            grad_self = grad._zeros_like()
        return (grad_self,)

class RandomBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='RandomBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._generator = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        generator = self._generator
        with _grad_context(keyset):
            grad_self = grad._zeros_like()
        return (grad_self,)

class ReciprocalBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ReciprocalBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_result_idx = None

    def _save(self, *, result=None):
        tensors = []
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        result = _saved[self._saved_result_idx]
        with _grad_context(keyset):
            grad_self = redispatch("mul", keyset, redispatch("neg", keyset, grad), redispatch("mul", keyset, result, result))
        return (grad_self,)

class RemainderScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='RemainderScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._other = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        other = self._other
        with _grad_context(keyset):
            grad_self = grad
        return (grad_self,)

class RemainderTensorBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='RemainderTensorBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_other_idx = None
        self._saved_self_idx = None

    def _save(self, *, other=None, self_=None):
        tensors = []
        if other is not None:
            self._saved_other_idx = len(tensors)
            tensors.append(other)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_other_idx is not None:
            self._saved_fields['other'] = self._saved_tensors_list[self._saved_other_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        other = _saved[self._saved_other_idx]
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = grad
            grad_other = redispatch("mul", keyset, redispatch("neg", keyset, grad), redispatch("div", keyset, self_, other, "floor"))
        return (grad_self, grad_other,)

class RenormBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='RenormBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._p = None
        self._dim = None
        self._maxnorm = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        p = self._p
        dim = self._dim
        maxnorm = self._maxnorm
        with _grad_context(keyset):
            grad_self = redispatch("renorm_backward", keyset, grad, self_, p, dim, maxnorm)
        return (grad_self,)

class RepeatBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='RepeatBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._repeats = None

    def _save(self, *, input_=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        repeats = self._repeats
        with _grad_context(keyset):
            grad_input = _repeat_backward_helper(grad, input_, repeats, keyset)
        return (grad_input,)

class SpecialEntrBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialEntrBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("mul", keyset, grad, redispatch("neg", keyset, redispatch("add", keyset, 1, redispatch("log", keyset, self_))))
        return (grad_self,)

class SpecialNdtriBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialNdtriBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_result_idx = None

    def _save(self, *, result=None):
        tensors = []
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        result = _saved[self._saved_result_idx]
        with _grad_context(keyset):
            grad_self = redispatch("mul", keyset, redispatch("mul", keyset, grad, redispatch("sqrt", keyset, redispatch("mul", keyset, 2, 3.141592653589793))), redispatch("exp", keyset, redispatch("div", keyset, redispatch("square", keyset, result), 2)))
        return (grad_self,)

class SpecialLogNdtrBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialLogNdtrBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_result_idx = None

    def _save(self, *, self_=None, result=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        result = _saved[self._saved_result_idx]
        with _grad_context(keyset):
            grad_self = redispatch("mul", keyset, redispatch("div", keyset, grad, redispatch("sqrt", keyset, redispatch("mul", keyset, 2, 3.141592653589793))), redispatch("exp", keyset, redispatch("neg", keyset, redispatch("add", keyset, result, redispatch("div", keyset, redispatch("pow", keyset, self_, 2), 2)))))
        return (grad_self,)

class ReshapeAliasBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ReshapeAliasBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._size = None
        self._stride = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        size = self._size
        stride = self._stride
        with _grad_context(keyset):
            grad_self = redispatch("reshape_symint", keyset, grad, self_.shape)
        return (grad_self,)

class RoundBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='RoundBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        with _grad_context(keyset):
            grad_self = grad._zeros_like()
        return (grad_self,)

class RoundDecimalsBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='RoundDecimalsBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._decimals = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        decimals = self._decimals
        with _grad_context(keyset):
            grad_self = grad._zeros_like()
        return (grad_self,)

class RsqrtBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='RsqrtBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_result_idx = None

    def _save(self, *, result=None):
        tensors = []
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        result = _saved[self._saved_result_idx]
        with _grad_context(keyset):
            grad_self = _rsqrt_backward_helper(grad, result, keyset)
        return (grad_self,)

class ScatterSrcBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ScatterSrcBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_index_idx = None
        self._dim = None

    def _save(self, *, index=None):
        tensors = []
        if index is not None:
            self._saved_index_idx = len(tensors)
            tensors.append(index)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_index_idx is not None:
            self._saved_fields['index'] = self._saved_tensors_list[self._saved_index_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        index = _saved[self._saved_index_idx]
        dim = self._dim
        with _grad_context(keyset):
            grad_self = redispatch("scatter", keyset, grad, dim, index, 0)
            grad_src = redispatch("gather", keyset, grad, dim, index)
        return (grad_self, grad_src,)

class ScatterValueBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ScatterValueBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_index_idx = None
        self._dim = None
        self._value = None

    def _save(self, *, index=None):
        tensors = []
        if index is not None:
            self._saved_index_idx = len(tensors)
            tensors.append(index)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_index_idx is not None:
            self._saved_fields['index'] = self._saved_tensors_list[self._saved_index_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        index = _saved[self._saved_index_idx]
        dim = self._dim
        value = self._value
        with _grad_context(keyset):
            grad_self = redispatch("scatter", keyset, grad, dim, index, 0)
        return (grad_self,)

class ScatterAddBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ScatterAddBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_index_idx = None
        self._dim = None

    def _save(self, *, index=None):
        tensors = []
        if index is not None:
            self._saved_index_idx = len(tensors)
            tensors.append(index)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_index_idx is not None:
            self._saved_fields['index'] = self._saved_tensors_list[self._saved_index_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        index = _saved[self._saved_index_idx]
        dim = self._dim
        with _grad_context(keyset):
            grad_self = grad
            grad_src = redispatch("gather", keyset, grad, dim, index)
        return (grad_self, grad_src,)

class SelectIntBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SelectIntBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._dim = None
        self._index = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        dim = self._dim
        index = self._index
        with _grad_context(keyset):
            grad_self = _select_backward_symint_helper(grad, self_.shape, dim, index, keyset)
        return (grad_self,)

class SelectBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SelectBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._input_sizes = None
        self._dim = None
        self._index = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        input_sizes = self._input_sizes
        dim = self._dim
        index = self._index
        with _grad_context(keyset):
            grad_grad_output = redispatch("select_symint", keyset, grad, dim, index)
        return (grad_grad_output,)

class SigmoidBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SigmoidBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_result_idx = None

    def _save(self, *, result=None):
        tensors = []
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        result = _saved[self._saved_result_idx]
        with _grad_context(keyset):
            grad_self = _sigmoid_backward_helper(grad, result, keyset)
        return (grad_self,)

class LogitBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='LogitBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._eps = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        eps = self._eps
        with _grad_context(keyset):
            grad_self = (redispatch("infinitely_differentiable_logit_backward", keyset, grad, self_, eps) if True else redispatch("logit_backward", keyset, grad, self_, eps))
        return (grad_self,)

class SignBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SignBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        with _grad_context(keyset):
            grad_self = grad._zeros_like()
        return (grad_self,)

class SgnBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SgnBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_result_idx = None

    def _save(self, *, self_=None, result=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        result = _saved[self._saved_result_idx]
        with _grad_context(keyset):
            grad_self = redispatch("sgn_backward", keyset, self_, grad, result)
        return (grad_self,)

class SinBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SinBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("mul", keyset, grad, redispatch("cos", keyset, self_))
        return (grad_self,)

class SincBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SincBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("sinc_backward", keyset, grad, self_)
        return (grad_self,)

class SinhBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SinhBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("mul", keyset, grad, redispatch("cosh", keyset, self_))
        return (grad_self,)

class SliceTensorBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SliceTensorBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._dim = None
        self._start = None
        self._end = None
        self._step = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        dim = self._dim
        start = self._start
        end = self._end
        step = self._step
        with _grad_context(keyset):
            grad_self = _slice_backward_wrapper_helper(grad, self_.shape, dim, start, end, step, keyset)
        return (grad_self,)

class SliceBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SliceBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._input_sizes = None
        self._dim = None
        self._start = None
        self._end = None
        self._step = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        input_sizes = self._input_sizes
        dim = self._dim
        start = self._start
        end = self._end
        step = self._step
        with _grad_context(keyset):
            grad_grad_output = redispatch("slice_symint", keyset, grad, dim, start, end, step)
        return (grad_grad_output,)

class SliceInverseBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SliceInverseBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._dim = None
        self._start = None
        self._end = None
        self._step = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        dim = self._dim
        start = self._start
        end = self._end
        step = self._step
        with _grad_context(keyset):
            grad_self = redispatch("slice_symint", keyset, grad, dim, start, end, step)
            grad_src = redispatch("slice_scatter_symint", keyset, grad, self_._zeros_like(), dim, start, end, step)
        return (grad_self, grad_src,)

class SliceScatterBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SliceScatterBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_src_idx = None
        self._dim = None
        self._start = None
        self._end = None
        self._step = None

    def _save(self, *, src=None):
        tensors = []
        if src is not None:
            self._saved_src_idx = len(tensors)
            tensors.append(src)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_src_idx is not None:
            self._saved_fields['src'] = self._saved_tensors_list[self._saved_src_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        src = _saved[self._saved_src_idx]
        dim = self._dim
        start = self._start
        end = self._end
        step = self._step
        with _grad_context(keyset):
            grad_self = redispatch("slice_scatter_symint", keyset, grad, src._zeros_like(), dim, start, end, step)
            grad_src = redispatch("slice_symint", keyset, grad, dim, start, end, step)
        return (grad_self, grad_src,)

class SelectScatterBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SelectScatterBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_src_idx = None
        self._dim = None
        self._index = None

    def _save(self, *, src=None):
        tensors = []
        if src is not None:
            self._saved_src_idx = len(tensors)
            tensors.append(src)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_src_idx is not None:
            self._saved_fields['src'] = self._saved_tensors_list[self._saved_src_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        src = _saved[self._saved_src_idx]
        dim = self._dim
        index = self._index
        with _grad_context(keyset):
            grad_self = redispatch("select_scatter_symint", keyset, grad, src._zeros_like(), dim, index)
            grad_src = redispatch("select_symint", keyset, grad, dim, index)
        return (grad_self, grad_src,)

class DiagonalScatterBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='DiagonalScatterBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_src_idx = None
        self._offset = None
        self._dim1 = None
        self._dim2 = None

    def _save(self, *, src=None):
        tensors = []
        if src is not None:
            self._saved_src_idx = len(tensors)
            tensors.append(src)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_src_idx is not None:
            self._saved_fields['src'] = self._saved_tensors_list[self._saved_src_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        src = _saved[self._saved_src_idx]
        offset = self._offset
        dim1 = self._dim1
        dim2 = self._dim2
        with _grad_context(keyset):
            grad_self = redispatch("diagonal_scatter", keyset, grad, src._zeros_like(), offset, dim1, dim2)
            grad_src = redispatch("diagonal", keyset, grad, offset, dim1, dim2)
        return (grad_self, grad_src,)

class AsStridedScatterBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='AsStridedScatterBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_src_idx = None
        self._size = None
        self._stride = None
        self._storage_offset = None

    def _save(self, *, self_=None, src=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if src is not None:
            self._saved_src_idx = len(tensors)
            tensors.append(src)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_src_idx is not None:
            self._saved_fields['src'] = self._saved_tensors_list[self._saved_src_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        src = _saved[self._saved_src_idx]
        size = self._size
        stride = self._stride
        storage_offset = self._storage_offset
        with _grad_context(keyset):
            grad_self, grad_src = _as_strided_scatter_backward_helper(grad, self_, src, size, stride, storage_offset, keyset)
        return (grad_self, grad_src,)

class LinalgSolveExBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='LinalgSolveExBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_A_idx = None
        self._saved_LU_idx = None
        self._saved_pivots_idx = None
        self._saved_result_idx = None
        self._left = None
        self._check_errors = None

    def _save(self, *, A=None, LU=None, pivots=None, result=None):
        tensors = []
        if A is not None:
            self._saved_A_idx = len(tensors)
            tensors.append(A)
        if LU is not None:
            self._saved_LU_idx = len(tensors)
            tensors.append(LU)
        if pivots is not None:
            self._saved_pivots_idx = len(tensors)
            tensors.append(pivots)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_A_idx is not None:
            self._saved_fields['A'] = self._saved_tensors_list[self._saved_A_idx]
        if self._saved_LU_idx is not None:
            self._saved_fields['LU'] = self._saved_tensors_list[self._saved_LU_idx]
        if self._saved_pivots_idx is not None:
            self._saved_fields['pivots'] = self._saved_tensors_list[self._saved_pivots_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        A = _saved[self._saved_A_idx]
        LU = _saved[self._saved_LU_idx]
        pivots = _saved[self._saved_pivots_idx]
        result = _saved[self._saved_result_idx]
        left = self._left
        check_errors = self._check_errors
        grad_input_mask = [True, True]
        with _grad_context(keyset):
            grad_A, grad_B = redispatch("linalg_solve_backward", keyset, grad, result, A, LU, pivots, left, True)
        return (grad_A, grad_B,)

class SortBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SortBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_result1_idx = None
        self._dim = None
        self._descending = None
        self._stable = None

    def _save(self, *, self_=None, result1=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result1 is not None:
            self._saved_result1_idx = len(tensors)
            tensors.append(result1)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result1_idx is not None:
            self._saved_fields['result1'] = self._saved_tensors_list[self._saved_result1_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        result1 = _saved[self._saved_result1_idx]
        dim = self._dim
        descending = self._descending
        stable = self._stable
        with _grad_context(keyset):
            grad_self = _sort_backward_helper(grad, self_, result1, dim, keyset)
        return (grad_self,)

class SortStableBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SortStableBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_indices_idx = None
        self._stable = None
        self._dim = None
        self._descending = None

    def _save(self, *, self_=None, indices=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if indices is not None:
            self._saved_indices_idx = len(tensors)
            tensors.append(indices)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_indices_idx is not None:
            self._saved_fields['indices'] = self._saved_tensors_list[self._saved_indices_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        indices = _saved[self._saved_indices_idx]
        stable = self._stable
        dim = self._dim
        descending = self._descending
        with _grad_context(keyset):
            grad_self = redispatch("value_selecting_reduction_backward_symint", keyset, grad, dim, indices, self_.shape, True)
        return (grad_self,)

class SplitTensorBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SplitTensorBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._split_size = None
        self._dim = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        split_size = self._split_size
        dim = self._dim
        with _grad_context(keyset):
            grad_self = redispatch("split_backward", keyset, grads, split_size, dim, self_.shape, self_.options)
        return (grad_self,)

class UnsafeSplitTensorBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='UnsafeSplitTensorBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._split_size = None
        self._dim = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        split_size = self._split_size
        dim = self._dim
        with _grad_context(keyset):
            grad_self = redispatch("split_backward", keyset, grads, split_size, dim, self_.shape, self_.options)
        return (grad_self,)

class SplitWithSizesBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SplitWithSizesBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._split_sizes = None
        self._dim = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        split_sizes = self._split_sizes
        dim = self._dim
        with _grad_context(keyset):
            grad_self = redispatch("split_with_sizes_backward", keyset, grads, split_sizes, dim, self_.shape, self_.options)
        return (grad_self,)

class UnsafeSplitWithSizesBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='UnsafeSplitWithSizesBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._split_sizes = None
        self._dim = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        split_sizes = self._split_sizes
        dim = self._dim
        with _grad_context(keyset):
            grad_self = redispatch("split_with_sizes_backward", keyset, grads, split_sizes, dim, self_.shape, self_.options)
        return (grad_self,)

class SqrtBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SqrtBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_result_idx = None

    def _save(self, *, result=None):
        tensors = []
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        result = _saved[self._saved_result_idx]
        with _grad_context(keyset):
            grad_self = _sqrt_backward_helper(grad, result, keyset)
        return (grad_self,)

class SqueezeBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SqueezeBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._dim = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        dim = self._dim
        with _grad_context(keyset):
            grad_self = redispatch("reshape", keyset, grad, self_.shape)
        return (grad_self,)

class SqueezeDimBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SqueezeDimBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._dim = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        dim = self._dim
        with _grad_context(keyset):
            grad_self = _unsqueeze_to_backward_helper(grad, dim, self_.shape, keyset)
        return (grad_self,)

class SqueezeDimsBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SqueezeDimsBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._dim = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        dim = self._dim
        with _grad_context(keyset):
            grad_self = _unsqueeze_to_backward_helper(grad, dim, self_.shape, keyset)
        return (grad_self,)

class StdCorrectionBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='StdCorrectionBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_result_idx = None
        self._dim = None
        self._correction = None
        self._keepdim = None

    def _save(self, *, self_=None, result=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        result = _saved[self._saved_result_idx]
        dim = self._dim
        correction = self._correction
        keepdim = self._keepdim
        with _grad_context(keyset):
            grad_self = _std_backward_helper(grad, self_, dim, keepdim, correction == 1 if correction is not None else True, keyset)
        return (grad_self,)

class StdMeanCorrectionBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='StdMeanCorrectionBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_result0_idx = None
        self._dim = None
        self._correction = None
        self._keepdim = None

    def _save(self, *, self_=None, result0=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result0 is not None:
            self._saved_result0_idx = len(tensors)
            tensors.append(result0)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result0_idx is not None:
            self._saved_fields['result0'] = self._saved_tensors_list[self._saved_result0_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        result0 = _saved[self._saved_result0_idx]
        dim = self._dim
        correction = self._correction
        keepdim = self._keepdim
        with _grad_context(keyset):
            grad_self = redispatch("std_mean_backward", keyset, grads[0], grads[1], self_, result0, dim, correction, keepdim)
        return (grad_self,)

class SubTensorBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SubTensorBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_other_idx = None
        self._alpha = None

    def _save(self, *, self_=None, other=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if other is not None:
            self._saved_other_idx = len(tensors)
            tensors.append(other)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_other_idx is not None:
            self._saved_fields['other'] = self._saved_tensors_list[self._saved_other_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        other = _saved[self._saved_other_idx]
        alpha = self._alpha
        with _grad_context(keyset):
            grad_self = grad
            grad_other = redispatch("mul", keyset, redispatch("neg", keyset, grad), alpha)
        return (grad_self, grad_other,)

class SubScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SubScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._other = None
        self._alpha = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        other = self._other
        alpha = self._alpha
        with _grad_context(keyset):
            grad_self = grad
        return (grad_self,)

class RsubTensorBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='RsubTensorBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_other_idx = None
        self._alpha = None

    def _save(self, *, self_=None, other=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if other is not None:
            self._saved_other_idx = len(tensors)
            tensors.append(other)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_other_idx is not None:
            self._saved_fields['other'] = self._saved_tensors_list[self._saved_other_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        other = _saved[self._saved_other_idx]
        alpha = self._alpha
        with _grad_context(keyset):
            grad_self = redispatch("mul", keyset, redispatch("neg", keyset, grad), alpha)
            grad_other = grad
        return (grad_self, grad_other,)

class RsubScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='RsubScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._other = None
        self._alpha = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        other = self._other
        alpha = self._alpha
        with _grad_context(keyset):
            grad_self = redispatch("mul", keyset, redispatch("neg", keyset, grad), alpha)
        return (grad_self,)

class SumBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SumBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._dtype = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        dtype = self._dtype
        with _grad_context(keyset):
            grad_self = redispatch("expand", keyset, grad, self_.shape)
        return (grad_self,)

class SumDimIntListBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SumDimIntListBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._dim = None
        self._keepdim = None
        self._dtype = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        dim = self._dim
        keepdim = self._keepdim
        dtype = self._dtype
        with _grad_context(keyset):
            grad_self = _sum_backward_helper(grad, self_.shape, dim, keepdim, keyset)
        return (grad_self,)

class NansumBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='NansumBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._dim = None
        self._keepdim = None
        self._dtype = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        dim = self._dim
        keepdim = self._keepdim
        dtype = self._dtype
        with _grad_context(keyset):
            grad_self = _sum_to_backward_helper(grad, self_.shape, keyset)
        return (grad_self,)

class LinalgSvdBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='LinalgSvdBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._full_matrices = None
        self._compute_uv = None
        self._driver = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        full_matrices = self._full_matrices
        compute_uv = self._compute_uv
        driver = self._driver
        with _grad_context(keyset):
            grad_A = redispatch("svd_backward", keyset, (redispatch("narrow_symint", keyset, grad_U, redispatch("neg", keyset, 1), 0, S.shape[redispatch("neg", keyset, 1)]) if (full_matrices and (grad_U is not None)) else grad_U), grad_S, (redispatch("narrow_symint", keyset, grad_Vh, redispatch("neg", keyset, 2), 0, S.shape[redispatch("neg", keyset, 1)]) if (full_matrices and (grad_Vh is not None)) else grad_Vh), (redispatch("narrow_symint", keyset, U, redispatch("neg", keyset, 1), 0, S.shape[redispatch("neg", keyset, 1)]) if full_matrices else U), S, (redispatch("narrow_symint", keyset, Vh, redispatch("neg", keyset, 2), 0, S.shape[redispatch("neg", keyset, 1)]) if full_matrices else Vh))
        return (grad_A,)

class LinalgEighBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='LinalgEighBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._UPLO = None
        self._compute_v = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        UPLO = self._UPLO
        compute_v = self._compute_v
        with _grad_context(keyset):
            grad_A = redispatch("linalg_eig_backward", keyset, grads[0], grads[1], eigenvalues, eigenvectors, True)
        return (grad_A,)

class LinalgEigBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='LinalgEigBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("linalg_eig_backward", keyset, grads[0], grads[1], eigenvalues, eigenvectors, False)
        return (grad_self,)

class TBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='TBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        with _grad_context(keyset):
            grad_self = redispatch("transpose", keyset, grad, 0, 1)
        return (grad_self,)

class OneHotBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='OneHotBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._num_classes = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        num_classes = self._num_classes
        return (grad,)

class FlipBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='FlipBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._dims = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        dims = self._dims
        with _grad_context(keyset):
            grad_self = redispatch("flip", keyset, grad, dims)
        return (grad_self,)

class RollBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='RollBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._shifts = None
        self._dims = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        shifts = self._shifts
        dims = self._dims
        with _grad_context(keyset):
            grad_self = _roll_backward_helper(grad, grad, shifts, dims, keyset)
        return (grad_self,)

class Rot90Backward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Rot90Backward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._k = None
        self._dims = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        k = self._k
        dims = self._dims
        with _grad_context(keyset):
            grad_self = redispatch("rot90", keyset, grad, redispatch("neg", keyset, k), dims)
        return (grad_self,)

class TakeBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='TakeBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_index_idx = None
        self._saved_self_idx = None

    def _save(self, *, index=None, self_=None):
        tensors = []
        if index is not None:
            self._saved_index_idx = len(tensors)
            tensors.append(index)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_index_idx is not None:
            self._saved_fields['index'] = self._saved_tensors_list[self._saved_index_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        index = _saved[self._saved_index_idx]
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            _grad_out = redispatch("zeros", keyset, self_.shape, dtype=grad.dtype, device=grad.device)
            _flat = redispatch("reshape", keyset, _grad_out, (-1,))
            redispatch("index_put_", keyset, _flat, (index,), grad)
            grad_self = redispatch("reshape", keyset, _flat, self_.shape)
        return (grad_self,)

class TanBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='TanBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_result_idx = None

    def _save(self, *, result=None):
        tensors = []
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        result = _saved[self._saved_result_idx]
        with _grad_context(keyset):
            grad_self = redispatch("mul", keyset, grad, redispatch("add", keyset, 1, redispatch("pow", keyset, result, 2)))
        return (grad_self,)

class TanhBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='TanhBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_result_idx = None

    def _save(self, *, result=None):
        tensors = []
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        result = _saved[self._saved_result_idx]
        with _grad_context(keyset):
            grad_self = _tanh_backward_helper(grad, result, keyset)
        return (grad_self,)

class TopkBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='TopkBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_result1_idx = None
        self._k = None
        self._dim = None
        self._largest = None
        self._sorted = None

    def _save(self, *, self_=None, result1=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result1 is not None:
            self._saved_result1_idx = len(tensors)
            tensors.append(result1)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result1_idx is not None:
            self._saved_fields['result1'] = self._saved_tensors_list[self._saved_result1_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        result1 = _saved[self._saved_result1_idx]
        k = self._k
        dim = self._dim
        largest = self._largest
        sorted = self._sorted
        with _grad_context(keyset):
            grad_self = _topk_backward_helper(grad, self_, result1, k, dim, keyset)
        return (grad_self,)

class TraceBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='TraceBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("trace_backward_symint", keyset, grad, self_.shape)
        return (grad_self,)

class TransposeIntBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='TransposeIntBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._dim0 = None
        self._dim1 = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        dim0 = self._dim0
        dim1 = self._dim1
        with _grad_context(keyset):
            grad_self = redispatch("transpose", keyset, grad, dim0, dim1)
        return (grad_self,)

class TransposeBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='TransposeBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._dim0 = None
        self._dim1 = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        dim0 = self._dim0
        dim1 = self._dim1
        with _grad_context(keyset):
            grad_self = redispatch("transpose", keyset, grad, dim0, dim1)
        return (grad_self,)

class TriangularSolveBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='TriangularSolveBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_A_idx = None
        self._saved_self_idx = None
        self._upper = None
        self._transpose = None
        self._unitriangular = None

    def _save(self, *, A=None, self_=None):
        tensors = []
        if A is not None:
            self._saved_A_idx = len(tensors)
            tensors.append(A)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_A_idx is not None:
            self._saved_fields['A'] = self._saved_tensors_list[self._saved_A_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        A = _saved[self._saved_A_idx]
        self_ = _saved[self._saved_self_idx]
        upper = self._upper
        transpose = self._transpose
        unitriangular = self._unitriangular
        grad_input_mask = [True, True]
        with _grad_context(keyset):
            grad_self, grad_A = redispatch("triangular_solve_backward", keyset, grad_solution, grad_cloned_coefficient, self_, A, solution, upper, transpose, unitriangular, grad_input_mask)
        return (grad_self, grad_A,)

class LinalgSolveTriangularBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='LinalgSolveTriangularBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_result_idx = None
        self._upper = None
        self._left = None
        self._unitriangular = None

    def _save(self, *, self_=None, result=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        result = _saved[self._saved_result_idx]
        upper = self._upper
        left = self._left
        unitriangular = self._unitriangular
        grad_input_mask = [True, True]
        with _grad_context(keyset):
            grad_self, grad_B = redispatch("linalg_solve_triangular_backward", keyset, grad, self_, result, upper, left, unitriangular, grad_input_mask)
        return (grad_self, grad_B,)

class TrilBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='TrilBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._diagonal = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        diagonal = self._diagonal
        with _grad_context(keyset):
            grad_input = redispatch("tril", keyset, grad)
        return (grad_input,)

class TriuBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='TriuBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._diagonal = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        diagonal = self._diagonal
        with _grad_context(keyset):
            grad_input = redispatch("triu", keyset, grad)
        return (grad_input,)

class TruncBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='TruncBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        with _grad_context(keyset):
            grad_self = grad._zeros_like()
        return (grad_self,)

class HashTensorBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='HashTensorBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._dim = None
        self._keepdim = None
        self._mode = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        dim = self._dim
        keepdim = self._keepdim
        mode = self._mode
        return (grad,)

class ToDenseBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ToDenseBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._dtype = None
        self._masked_grad = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        dtype = self._dtype
        masked_grad = self._masked_grad
        with _grad_context(keyset):
            grad_self = redispatch("to_dense_backward", keyset, grad, self_, masked_grad)
        return (grad_self,)

class ToSparseSparseDimBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ToSparseSparseDimBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._sparse_dim = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        sparse_dim = self._sparse_dim
        with _grad_context(keyset):
            grad_self = redispatch("to_sparse_backward", keyset, grad, redispatch("layout", keyset, self_), redispatch("sym_blocksize", keyset, self_))
        return (grad_self,)

class ToSparseBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ToSparseBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._layout = None
        self._blocksize = None
        self._dense_dim = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        layout = self._layout
        blocksize = self._blocksize
        dense_dim = self._dense_dim
        with _grad_context(keyset):
            grad_self = redispatch("to_sparse_backward", keyset, grad, redispatch("layout", keyset, self_), redispatch("sym_blocksize", keyset, self_))
        return (grad_self,)

class ToSparseCsrBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ToSparseCsrBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._dense_dim = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        dense_dim = self._dense_dim
        with _grad_context(keyset):
            grad_self = redispatch("to_sparse_backward", keyset, grad, redispatch("layout", keyset, self_), redispatch("sym_blocksize", keyset, self_))
        return (grad_self,)

class ToSparseCscBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ToSparseCscBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._dense_dim = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        dense_dim = self._dense_dim
        with _grad_context(keyset):
            grad_self = redispatch("to_sparse_backward", keyset, grad, redispatch("layout", keyset, self_), redispatch("sym_blocksize", keyset, self_))
        return (grad_self,)

class ToSparseBsrBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ToSparseBsrBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._blocksize = None
        self._dense_dim = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        blocksize = self._blocksize
        dense_dim = self._dense_dim
        with _grad_context(keyset):
            grad_self = redispatch("to_sparse_backward", keyset, grad, redispatch("layout", keyset, self_), redispatch("sym_blocksize", keyset, self_))
        return (grad_self,)

class ToSparseBscBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ToSparseBscBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._blocksize = None
        self._dense_dim = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        blocksize = self._blocksize
        dense_dim = self._dense_dim
        with _grad_context(keyset):
            grad_self = redispatch("to_sparse_backward", keyset, grad, redispatch("layout", keyset, self_), redispatch("sym_blocksize", keyset, self_))
        return (grad_self,)

class ToMkldnnBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ToMkldnnBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._dtype = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        dtype = self._dtype
        with _grad_context(keyset):
            grad_self = redispatch("to_mkldnn_backward", keyset, grad, self_)
        return (grad_self,)

class UnfoldBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='UnfoldBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._dimension = None
        self._size = None
        self._step = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        dimension = self._dimension
        size = self._size
        step = self._step
        with _grad_context(keyset):
            grad_self = redispatch("unfold_backward_symint", keyset, grad, self_.shape, dimension, size, step)
        return (grad_self,)

class UnfoldBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='UnfoldBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._input_sizes = None
        self._dim = None
        self._size = None
        self._step = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        input_sizes = self._input_sizes
        dim = self._dim
        size = self._size
        step = self._step
        with _grad_context(keyset):
            grad_grad_in = redispatch("unfold", keyset, grad, dim, size, step)
        return (grad_grad_in,)

class UniformBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='UniformBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._from = None
        self._to = None
        self._generator = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        from_ = self._from
        to = self._to
        generator = self._generator
        with _grad_context(keyset):
            grad_self = grad._zeros_like()
        return (grad_self,)

class UniqueBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='UniqueBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._sorted = None
        self._return_inverse = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        sorted = self._sorted
        return_inverse = self._return_inverse
        with _grad_context(keyset):
            grad_self = _raise_not_implemented("_unique")
        return (grad_self,)

class UniqueDimBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='UniqueDimBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._dim = None
        self._sorted = None
        self._return_inverse = None
        self._return_counts = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        dim = self._dim
        sorted = self._sorted
        return_inverse = self._return_inverse
        return_counts = self._return_counts
        with _grad_context(keyset):
            grad_self = _raise_not_implemented("unique_dim")
        return (grad_self,)

class UniqueConsecutiveBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='UniqueConsecutiveBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._return_inverse = None
        self._return_counts = None
        self._dim = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        return_inverse = self._return_inverse
        return_counts = self._return_counts
        dim = self._dim
        with _grad_context(keyset):
            grad_self = _raise_not_implemented("unique_consecutive")
        return (grad_self,)

class UniqueDimConsecutiveBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='UniqueDimConsecutiveBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._dim = None
        self._return_inverse = None
        self._return_counts = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        dim = self._dim
        return_inverse = self._return_inverse
        return_counts = self._return_counts
        with _grad_context(keyset):
            grad_self = _raise_not_implemented("unique_dim_consecutive")
        return (grad_self,)

class Unique2Backward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Unique2Backward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._sorted = None
        self._return_inverse = None
        self._return_counts = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        sorted = self._sorted
        return_inverse = self._return_inverse
        return_counts = self._return_counts
        with _grad_context(keyset):
            grad_self = _raise_not_implemented("_unique2")
        return (grad_self,)

class UnsafeViewBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='UnsafeViewBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._size = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        size = self._size
        with _grad_context(keyset):
            grad_self = redispatch("reshape_symint", keyset, grad, self_.shape)
        return (grad_self,)

class LiftBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='LiftBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        with _grad_context(keyset):
            grad_self = grad
        return (grad_self,)

class LiftFreshBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='LiftFreshBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        with _grad_context(keyset):
            grad_self = grad
        return (grad_self,)

class UnsqueezeBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='UnsqueezeBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._dim = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        dim = self._dim
        with _grad_context(keyset):
            grad_self = redispatch("squeeze", keyset, grad, dim)
        return (grad_self,)

class VarCorrectionBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='VarCorrectionBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._dim = None
        self._correction = None
        self._keepdim = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        dim = self._dim
        correction = self._correction
        keepdim = self._keepdim
        with _grad_context(keyset):
            grad_self = _var_backward_helper(grad, self_, dim, correction == 1 if correction is not None else True, keepdim, keyset)
        return (grad_self,)

class VarMeanCorrectionBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='VarMeanCorrectionBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._dim = None
        self._correction = None
        self._keepdim = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        dim = self._dim
        correction = self._correction
        keepdim = self._keepdim
        with _grad_context(keyset):
            grad_self = redispatch("var_mean_backward", keyset, grads[0], grads[1], self_, dim, correction, keepdim)
        return (grad_self,)

class ViewBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ViewBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._size = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        size = self._size
        with _grad_context(keyset):
            grad_self = redispatch("reshape_symint", keyset, grad, self_.shape)
        return (grad_self,)

class ReshapeBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ReshapeBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._self_shape = None

    def _save(self, *, self_=None):
        if self_ is not None:
            self._self_shape = self_.shape

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        with _grad_context(keyset):
            grad_self = redispatch("reshape", keyset, grad, self._self_shape)
        return (grad_self,)


class ViewDtypeBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ViewDtypeBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._dtype = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        dtype = self._dtype
        return (grad,)

class ViewAsRealBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ViewAsRealBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        with _grad_context(keyset):
            grad_self = redispatch("view_as_complex", keyset, redispatch("contiguous", keyset, grad))
        return (grad_self,)

class ViewAsComplexBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ViewAsComplexBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        with _grad_context(keyset):
            grad_self = redispatch("view_as_real", keyset, redispatch("resolve_conj", keyset, redispatch("contiguous", keyset, grad)))
        return (grad_self,)

class WhereSelfBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='WhereSelfBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_condition_idx = None

    def _save(self, *, condition=None):
        tensors = []
        if condition is not None:
            self._saved_condition_idx = len(tensors)
            tensors.append(condition)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_condition_idx is not None:
            self._saved_fields['condition'] = self._saved_tensors_list[self._saved_condition_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        condition = _saved[self._saved_condition_idx]
        with _grad_context(keyset):
            grad_self = redispatch("where", keyset, condition, grad, 0)
            grad_other = redispatch("where", keyset, condition, 0, grad)
        return (grad_self, grad_other,)

class WeightNormInterfaceBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='WeightNormInterfaceBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_g_idx = None
        self._saved_v_idx = None
        self._saved_result1_idx = None
        self._dim = None

    def _save(self, *, g=None, v=None, result1=None):
        tensors = []
        if g is not None:
            self._saved_g_idx = len(tensors)
            tensors.append(g)
        if v is not None:
            self._saved_v_idx = len(tensors)
            tensors.append(v)
        if result1 is not None:
            self._saved_result1_idx = len(tensors)
            tensors.append(result1)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_g_idx is not None:
            self._saved_fields['g'] = self._saved_tensors_list[self._saved_g_idx]
        if self._saved_v_idx is not None:
            self._saved_fields['v'] = self._saved_tensors_list[self._saved_v_idx]
        if self._saved_result1_idx is not None:
            self._saved_fields['result1'] = self._saved_tensors_list[self._saved_result1_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        g = _saved[self._saved_g_idx]
        v = _saved[self._saved_v_idx]
        result1 = _saved[self._saved_result1_idx]
        dim = self._dim
        with _grad_context(keyset):
            grad_v, grad_g = ((redispatch("_weight_norm_differentiable_backward", keyset, redispatch("contiguous", keyset, grad), v, g, result1, dim) if True else redispatch("_weight_norm_interface_backward", keyset, redispatch("contiguous", keyset, grad), v, g, result1, dim)) if (grad is not None) else (None, None))
        return (grad_v, grad_g,)

class ZeroBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ZeroBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        with _grad_context(keyset):
            grad_self = grad._zeros_like()
        return (grad_self,)

class SparseMaskBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SparseMaskBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_mask_idx = None
        self._saved_self_idx = None

    def _save(self, *, mask=None, self_=None):
        tensors = []
        if mask is not None:
            self._saved_mask_idx = len(tensors)
            tensors.append(mask)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_mask_idx is not None:
            self._saved_fields['mask'] = self._saved_tensors_list[self._saved_mask_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        mask = _saved[self._saved_mask_idx]
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("sparse_mask_backward", keyset, grad, mask, redispatch("layout", keyset, self_))
        return (grad_self,)

class SparseSumDimBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SparseSumDimBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._dim = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        dim = self._dim
        with _grad_context(keyset):
            grad_self = redispatch("_sparse_sum_backward", keyset, grad, self_, dim)
        return (grad_self,)

class StandardGammaBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='StandardGammaBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_result_idx = None
        self._generator = None

    def _save(self, *, self_=None, result=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        result = _saved[self._saved_result_idx]
        generator = self._generator
        with _grad_context(keyset):
            grad_self = redispatch("mul", keyset, grad, redispatch("_standard_gamma_grad", keyset, self_, result))
        return (grad_self,)

class StandardGammaGradBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='StandardGammaGradBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        with _grad_context(keyset):
            grad_self = _raise_not_implemented("_standard_gamma_grad")
        return (grad_self,)

class ValuesBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ValuesBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("values_backward", keyset, grad, self_)
        return (grad_self,)

class TrilinearBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='TrilinearBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_i1_idx = None
        self._saved_i2_idx = None
        self._saved_i3_idx = None
        self._expand1 = None
        self._expand2 = None
        self._expand3 = None
        self._sumdim = None
        self._unroll_dim = None

    def _save(self, *, i1=None, i2=None, i3=None):
        tensors = []
        if i1 is not None:
            self._saved_i1_idx = len(tensors)
            tensors.append(i1)
        if i2 is not None:
            self._saved_i2_idx = len(tensors)
            tensors.append(i2)
        if i3 is not None:
            self._saved_i3_idx = len(tensors)
            tensors.append(i3)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_i1_idx is not None:
            self._saved_fields['i1'] = self._saved_tensors_list[self._saved_i1_idx]
        if self._saved_i2_idx is not None:
            self._saved_fields['i2'] = self._saved_tensors_list[self._saved_i2_idx]
        if self._saved_i3_idx is not None:
            self._saved_fields['i3'] = self._saved_tensors_list[self._saved_i3_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        i1 = _saved[self._saved_i1_idx]
        i2 = _saved[self._saved_i2_idx]
        i3 = _saved[self._saved_i3_idx]
        expand1 = self._expand1
        expand2 = self._expand2
        expand3 = self._expand3
        sumdim = self._sumdim
        unroll_dim = self._unroll_dim
        grad_input_mask = [True, True, True]
        with _grad_context(keyset):
            grad_i1, grad_i2, grad_i3 = redispatch("_trilinear_backward", keyset, grad, redispatch("wrap_opt_if", keyset, i1, True), redispatch("wrap_opt_if", keyset, i2, True), redispatch("wrap_opt_if", keyset, i3, True), expand1, expand2, expand3, sumdim, grad_input_mask)
        return (grad_i1, grad_i2, grad_i3,)

class ConstantPadNdBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ConstantPadNdBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._pad = None
        self._value = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        pad = self._pad
        value = self._value
        with _grad_context(keyset):
            grad_self = redispatch("constant_pad_nd_backward", keyset, grad, pad)
        return (grad_self,)

class BinaryCrossEntropyBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='BinaryCrossEntropyBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_target_idx = None
        self._saved_weight_idx = None
        self._reduction = None

    def _save(self, *, self_=None, target=None, weight=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if target is not None:
            self._saved_target_idx = len(tensors)
            tensors.append(target)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_target_idx is not None:
            self._saved_fields['target'] = self._saved_tensors_list[self._saved_target_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        target = _saved[self._saved_target_idx]
        weight = _saved[self._saved_weight_idx] if self._saved_weight_idx is not None else None
        reduction = self._reduction
        with _grad_context(keyset):
            grad_self = redispatch("binary_cross_entropy_backward", keyset, grad, self_, target, weight, reduction)
            grad_target = redispatch("binary_cross_entropy_target_backward", keyset, grad, self_, target, weight, reduction)
        return (grad_self, grad_target,)

class BinaryCrossEntropyBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='BinaryCrossEntropyBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_target_idx = None
        self._saved_weight_idx = None
        self._reduction = None

    def _save(self, *, self_=None, target=None, weight=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if target is not None:
            self._saved_target_idx = len(tensors)
            tensors.append(target)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_target_idx is not None:
            self._saved_fields['target'] = self._saved_tensors_list[self._saved_target_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        target = _saved[self._saved_target_idx]
        weight = _saved[self._saved_weight_idx] if self._saved_weight_idx is not None else None
        grad_output = self.inputs[0]
        reduction = self._reduction
        with _grad_context(keyset):
            grad_self = redispatch("binary_cross_entropy_double_backward", keyset, grad_output, grad, self_, target, weight, reduction)
            grad_target = redispatch("binary_cross_entropy_double_backward_target", keyset, grad, grad_output, self_, target, weight, reduction)
            grad_grad_output = redispatch("binary_cross_entropy_double_backward_grad_output", keyset, grad, self_, target, weight, reduction)
        return (grad_grad_output, grad_self, grad_target,)

class BinaryCrossEntropyWithLogitsBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='BinaryCrossEntropyWithLogitsBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_pos_weight_idx = None
        self._saved_self_idx = None
        self._saved_target_idx = None
        self._saved_weight_idx = None
        self._reduction = None

    def _save(self, *, pos_weight=None, self_=None, target=None, weight=None):
        tensors = []
        if pos_weight is not None:
            self._saved_pos_weight_idx = len(tensors)
            tensors.append(pos_weight)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if target is not None:
            self._saved_target_idx = len(tensors)
            tensors.append(target)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_pos_weight_idx is not None:
            self._saved_fields['pos_weight'] = self._saved_tensors_list[self._saved_pos_weight_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_target_idx is not None:
            self._saved_fields['target'] = self._saved_tensors_list[self._saved_target_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        pos_weight = _saved[self._saved_pos_weight_idx] if self._saved_pos_weight_idx is not None else None
        self_ = _saved[self._saved_self_idx]
        target = _saved[self._saved_target_idx]
        weight = _saved[self._saved_weight_idx] if self._saved_weight_idx is not None else None
        reduction = self._reduction
        with _grad_context(keyset):
            grad_self = redispatch("binary_cross_entropy_with_logits_backward", keyset, grad, self_, target, weight, pos_weight, reduction)
            grad_target = redispatch("binary_cross_entropy_with_logits_target_backward", keyset, grad, self_, target, weight, pos_weight, reduction)
        return (grad_self, grad_target,)

class EmbeddingBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='EmbeddingBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_indices_idx = None
        self._saved_weight_idx = None
        self._padding_idx = None
        self._scale_grad_by_freq = None
        self._sparse = None

    def _save(self, *, indices=None, weight=None):
        tensors = []
        if indices is not None:
            self._saved_indices_idx = len(tensors)
            tensors.append(indices)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_indices_idx is not None:
            self._saved_fields['indices'] = self._saved_tensors_list[self._saved_indices_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        indices = _saved[self._saved_indices_idx]
        weight = _saved[self._saved_weight_idx]
        padding_idx = self._padding_idx
        scale_grad_by_freq = self._scale_grad_by_freq
        sparse = self._sparse
        with _grad_context(keyset):
            grad_weight = _embedding_backward_helper(grad, weight, indices, padding_idx, scale_grad_by_freq, keyset)
        return (grad_weight,)

class EmbeddingDenseBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='EmbeddingDenseBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_indices_idx = None
        self._num_weights = None
        self._padding_idx = None
        self._scale_grad_by_freq = None

    def _save(self, *, indices=None):
        tensors = []
        if indices is not None:
            self._saved_indices_idx = len(tensors)
            tensors.append(indices)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_indices_idx is not None:
            self._saved_fields['indices'] = self._saved_tensors_list[self._saved_indices_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        indices = _saved[self._saved_indices_idx]
        num_weights = self._num_weights
        padding_idx = self._padding_idx
        scale_grad_by_freq = self._scale_grad_by_freq
        with _grad_context(keyset):
            grad_grad_output = redispatch("embedding_dense_double_backward_symint", keyset, grad, indices, padding_idx)
        return (grad_grad_output,)

class EmbeddingBagBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='EmbeddingBagBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_indices_idx = None
        self._saved_offsets_idx = None
        self._saved_per_sample_weights_idx = None
        self._saved_weight_idx = None
        self._saved_result1_idx = None
        self._saved_result2_idx = None
        self._saved_result3_idx = None
        self._scale_grad_by_freq = None
        self._mode = None
        self._sparse = None
        self._include_last_offset = None
        self._padding_idx = None

    def _save(self, *, indices=None, offsets=None, per_sample_weights=None, weight=None, result1=None, result2=None, result3=None):
        tensors = []
        if indices is not None:
            self._saved_indices_idx = len(tensors)
            tensors.append(indices)
        if offsets is not None:
            self._saved_offsets_idx = len(tensors)
            tensors.append(offsets)
        if per_sample_weights is not None:
            self._saved_per_sample_weights_idx = len(tensors)
            tensors.append(per_sample_weights)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if result1 is not None:
            self._saved_result1_idx = len(tensors)
            tensors.append(result1)
        if result2 is not None:
            self._saved_result2_idx = len(tensors)
            tensors.append(result2)
        if result3 is not None:
            self._saved_result3_idx = len(tensors)
            tensors.append(result3)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_indices_idx is not None:
            self._saved_fields['indices'] = self._saved_tensors_list[self._saved_indices_idx]
        if self._saved_offsets_idx is not None:
            self._saved_fields['offsets'] = self._saved_tensors_list[self._saved_offsets_idx]
        if self._saved_per_sample_weights_idx is not None:
            self._saved_fields['per_sample_weights'] = self._saved_tensors_list[self._saved_per_sample_weights_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]
        if self._saved_result1_idx is not None:
            self._saved_fields['result1'] = self._saved_tensors_list[self._saved_result1_idx]
        if self._saved_result2_idx is not None:
            self._saved_fields['result2'] = self._saved_tensors_list[self._saved_result2_idx]
        if self._saved_result3_idx is not None:
            self._saved_fields['result3'] = self._saved_tensors_list[self._saved_result3_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        indices = _saved[self._saved_indices_idx]
        offsets = _saved[self._saved_offsets_idx]
        per_sample_weights = _saved[self._saved_per_sample_weights_idx] if self._saved_per_sample_weights_idx is not None else None
        weight = _saved[self._saved_weight_idx]
        result1 = _saved[self._saved_result1_idx]
        result2 = _saved[self._saved_result2_idx]
        result3 = _saved[self._saved_result3_idx]
        scale_grad_by_freq = self._scale_grad_by_freq
        mode = self._mode
        sparse = self._sparse
        include_last_offset = self._include_last_offset
        padding_idx = self._padding_idx
        with _grad_context(keyset):
            grad_weight = redispatch("_embedding_bag_backward_symint", keyset, grad, indices, offsets, result1, result2, result3, weight.shape[0], scale_grad_by_freq, mode, sparse, per_sample_weights, padding_idx)
            grad_per_sample_weights = redispatch("_embedding_bag_per_sample_weights_backward", keyset, grad, weight, indices, offsets, result1, mode, padding_idx)
        return (grad_weight, grad_per_sample_weights,)

class EmbeddingBagBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='EmbeddingBagBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._num_weights = None
        self._scale_grad_by_freq = None
        self._mode = None
        self._sparse = None
        self._padding_idx = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        num_weights = self._num_weights
        scale_grad_by_freq = self._scale_grad_by_freq
        mode = self._mode
        sparse = self._sparse
        padding_idx = self._padding_idx
        with _grad_context(keyset):
            grad_grad = _raise_not_implemented("_embedding_bag_backward")
            grad_per_sample_weights = _raise_not_implemented("_embedding_bag_backward")
        return (grad_grad, grad_per_sample_weights,)

class EmbeddingBagDenseBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='EmbeddingBagDenseBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._num_weights = None
        self._scale_grad_by_freq = None
        self._mode = None
        self._padding_idx = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        num_weights = self._num_weights
        scale_grad_by_freq = self._scale_grad_by_freq
        mode = self._mode
        padding_idx = self._padding_idx
        with _grad_context(keyset):
            grad_grad = _raise_not_implemented("_embedding_bag_dense_backward")
            grad_per_sample_weights = _raise_not_implemented("_embedding_bag_dense_backward")
        return (grad_grad, grad_per_sample_weights,)

class EmbeddingRenormBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='EmbeddingRenormBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._max_norm = None
        self._norm_type = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        max_norm = self._max_norm
        norm_type = self._norm_type
        with _grad_context(keyset):
            grad_self = _raise_not_implemented("embedding_renorm")
        return (grad_self,)

class MseLossBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MseLossBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_target_idx = None
        self._reduction = None

    def _save(self, *, self_=None, target=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if target is not None:
            self._saved_target_idx = len(tensors)
            tensors.append(target)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_target_idx is not None:
            self._saved_fields['target'] = self._saved_tensors_list[self._saved_target_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        target = _saved[self._saved_target_idx]
        reduction = self._reduction
        with _grad_context(keyset):
            grad_self = redispatch("mse_loss_backward", keyset, grad, self_, target, reduction)
            grad_target = redispatch("mse_loss_backward", keyset, grad, target, self_, reduction)
        return (grad_self, grad_target,)

class MultiMarginLossBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MultiMarginLossBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_target_idx = None
        self._saved_weight_idx = None
        self._p = None
        self._margin = None
        self._reduction = None

    def _save(self, *, self_=None, target=None, weight=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if target is not None:
            self._saved_target_idx = len(tensors)
            tensors.append(target)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_target_idx is not None:
            self._saved_fields['target'] = self._saved_tensors_list[self._saved_target_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        target = _saved[self._saved_target_idx]
        weight = _saved[self._saved_weight_idx] if self._saved_weight_idx is not None else None
        p = self._p
        margin = self._margin
        reduction = self._reduction
        with _grad_context(keyset):
            grad_self = redispatch("multi_margin_loss_backward", keyset, grad, self_, target, p, margin, weight, reduction)
        return (grad_self,)

class MultilabelMarginLossForwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MultilabelMarginLossForwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_target_idx = None
        self._reduction = None

    def _save(self, *, self_=None, target=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if target is not None:
            self._saved_target_idx = len(tensors)
            tensors.append(target)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_target_idx is not None:
            self._saved_fields['target'] = self._saved_tensors_list[self._saved_target_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        target = _saved[self._saved_target_idx]
        reduction = self._reduction
        with _grad_context(keyset):
            grad_self = redispatch("multilabel_margin_loss_backward", keyset, grad, self_, target, reduction, is_target)
        return (grad_self,)

class NllLossForwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='NllLossForwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_target_idx = None
        self._saved_weight_idx = None
        self._reduction = None
        self._ignore_index = None

    def _save(self, *, self_=None, target=None, weight=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if target is not None:
            self._saved_target_idx = len(tensors)
            tensors.append(target)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_target_idx is not None:
            self._saved_fields['target'] = self._saved_tensors_list[self._saved_target_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        target = _saved[self._saved_target_idx]
        weight = _saved[self._saved_weight_idx] if self._saved_weight_idx is not None else None
        reduction = self._reduction
        ignore_index = self._ignore_index
        with _grad_context(keyset):
            grad_self = redispatch("nll_loss_backward_symint", keyset, grad, self_, target, weight, reduction, ignore_index, total_weight)
        return (grad_self,)

class NllLoss2dForwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='NllLoss2dForwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_target_idx = None
        self._saved_weight_idx = None
        self._reduction = None
        self._ignore_index = None

    def _save(self, *, self_=None, target=None, weight=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if target is not None:
            self._saved_target_idx = len(tensors)
            tensors.append(target)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_target_idx is not None:
            self._saved_fields['target'] = self._saved_tensors_list[self._saved_target_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        target = _saved[self._saved_target_idx]
        weight = _saved[self._saved_weight_idx] if self._saved_weight_idx is not None else None
        reduction = self._reduction
        ignore_index = self._ignore_index
        with _grad_context(keyset):
            grad_self = redispatch("nll_loss2d_backward_symint", keyset, grad, self_, target, weight, reduction, ignore_index, total_weight)
        return (grad_self,)

class SmoothL1LossBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SmoothL1LossBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_target_idx = None
        self._reduction = None
        self._beta = None

    def _save(self, *, self_=None, target=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if target is not None:
            self._saved_target_idx = len(tensors)
            tensors.append(target)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_target_idx is not None:
            self._saved_fields['target'] = self._saved_tensors_list[self._saved_target_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        target = _saved[self._saved_target_idx]
        reduction = self._reduction
        beta = self._beta
        with _grad_context(keyset):
            grad_self = redispatch("smooth_l1_loss_backward", keyset, grad, self_, target, reduction, beta)
            grad_target = redispatch("smooth_l1_loss_backward", keyset, grad, target, self_, reduction, beta)
        return (grad_self, grad_target,)

class HuberLossBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='HuberLossBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_target_idx = None
        self._reduction = None
        self._delta = None

    def _save(self, *, self_=None, target=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if target is not None:
            self._saved_target_idx = len(tensors)
            tensors.append(target)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_target_idx is not None:
            self._saved_fields['target'] = self._saved_tensors_list[self._saved_target_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        target = _saved[self._saved_target_idx]
        reduction = self._reduction
        delta = self._delta
        with _grad_context(keyset):
            grad_self = redispatch("huber_loss_backward", keyset, grad, self_, target, reduction, delta)
            grad_target = redispatch("huber_loss_backward", keyset, grad, target, self_, reduction, delta)
        return (grad_self, grad_target,)

class SoftMarginLossBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SoftMarginLossBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_target_idx = None
        self._reduction = None

    def _save(self, *, self_=None, target=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if target is not None:
            self._saved_target_idx = len(tensors)
            tensors.append(target)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_target_idx is not None:
            self._saved_fields['target'] = self._saved_tensors_list[self._saved_target_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        target = _saved[self._saved_target_idx]
        reduction = self._reduction
        with _grad_context(keyset):
            grad_self = redispatch("soft_margin_loss_backward", keyset, grad, self_, target, reduction)
        return (grad_self,)

class ReluBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ReluBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_result_idx = None

    def _save(self, *, result=None):
        tensors = []
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        result = _saved[self._saved_result_idx]
        with _grad_context(keyset):
            grad_self = _threshold_backward_helper(grad, result, 0, keyset)
        return (grad_self,)

class SiluBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SiluBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = _silu_grad(grad, self_, keyset)
        return (grad_self,)

class MishBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MishBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = (redispatch("infinitely_differentiable_mish_backward", keyset, grad, self_) if True else redispatch("mish_backward", keyset, grad, self_))
        return (grad_self,)

class EluBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='EluBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._alpha = None
        self._scale = None
        self._input_scale = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        alpha = self._alpha
        scale = self._scale
        input_scale = self._input_scale
        with _grad_context(keyset):
            grad_self = redispatch("elu_backward", keyset, grad, alpha, scale, input_scale, False, self_)
        return (grad_self,)

class CeluBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='CeluBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._alpha = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        alpha = self._alpha
        with _grad_context(keyset):
            grad_self = redispatch("elu_backward", keyset, grad, alpha, 1, redispatch("div", keyset, 1.0, float(alpha)), False, self_)
        return (grad_self,)

class GeluBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='GeluBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._approximate = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        approximate = self._approximate
        with _grad_context(keyset):
            grad_self = _gelu_backward_helper(grad, self_, approximate, keyset)
        return (grad_self,)

class GeluBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='GeluBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._approximate = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        grad_output = self.inputs[0]
        approximate = self._approximate
        with _grad_context(keyset):
            grad_grad_output = _gelu_backward_helper(grad, self_, approximate, keyset)
            grad_self = redispatch("gelu_double_backward", keyset, grad, grad_output, self_, approximate)
        return (grad_grad_output, grad_self,)

class GluBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='GluBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._dim = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        dim = self._dim
        with _grad_context(keyset):
            grad_self = redispatch("glu_backward", keyset, grad, self_, dim)
        return (grad_self,)

class HardshrinkBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='HardshrinkBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._lambd = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        lambd = self._lambd
        with _grad_context(keyset):
            grad_self = redispatch("hardshrink_backward", keyset, grad, self_, lambd)
        return (grad_self,)

class HardshrinkBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='HardshrinkBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._lambd = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        lambd = self._lambd
        with _grad_context(keyset):
            grad_grad_out = redispatch("hardshrink_backward", keyset, grad, self_, lambd)
            grad_self = grad._zeros_like()
        return (grad_grad_out, grad_self,)

class HardtanhBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='HardtanhBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._min_val = None
        self._max_val = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        min_val = self._min_val
        max_val = self._max_val
        with _grad_context(keyset):
            grad_self = _hardtanh_backward_helper(grad, self_, min_val, max_val, keyset)
        return (grad_self,)

class LeakyReluBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='LeakyReluBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._negative_slope = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        negative_slope = self._negative_slope
        with _grad_context(keyset):
            grad_self = redispatch("leaky_relu_backward", keyset, grad, self_, negative_slope, False)
        return (grad_self,)

class LogSigmoidForwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='LogSigmoidForwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("log_sigmoid_backward", keyset, grad, self_, buffer)
        return (grad_self,)

class LogSoftmaxBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='LogSoftmaxBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_result_idx = None
        self._dim = None
        self._half_to_float = None

    def _save(self, *, self_=None, result=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        result = _saved[self._saved_result_idx]
        dim = self._dim
        half_to_float = self._half_to_float
        with _grad_context(keyset):
            grad_self = redispatch("_log_softmax_backward_data", keyset, grad, result, dim, self_.dtype)
        return (grad_self,)

class SparseLogSoftmaxBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SparseLogSoftmaxBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_result_idx = None
        self._dim = None
        self._half_to_float = None

    def _save(self, *, self_=None, result=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        result = _saved[self._saved_result_idx]
        dim = self._dim
        half_to_float = self._half_to_float
        with _grad_context(keyset):
            grad_self = redispatch("_sparse_log_softmax_backward_data", keyset, grad, result, dim, self_)
        return (grad_self,)

class MaskedSoftmaxBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MaskedSoftmaxBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_mask_idx = None
        self._saved_result_idx = None
        self._dim = None
        self._mask_type = None

    def _save(self, *, mask=None, result=None):
        tensors = []
        if mask is not None:
            self._saved_mask_idx = len(tensors)
            tensors.append(mask)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_mask_idx is not None:
            self._saved_fields['mask'] = self._saved_tensors_list[self._saved_mask_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        mask = _saved[self._saved_mask_idx]
        result = _saved[self._saved_result_idx]
        dim = self._dim
        mask_type = self._mask_type
        with _grad_context(keyset):
            grad_self = redispatch("_masked_softmax_backward", keyset, grad, result, mask, dim)
        return (grad_self,)

class PreluKernelBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='PreluKernelBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_weight_idx = None

    def _save(self, *, self_=None, weight=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        weight = _saved[self._saved_weight_idx]
        with _grad_context(keyset):
            grad_self, grad_weight = (redispatch("_prelu_kernel_backward", keyset, grad, self_, weight) if (grad is not None) else (None, None))
        return (grad_self, grad_weight,)

class PreluKernelBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='PreluKernelBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_weight_idx = None

    def _save(self, *, self_=None, weight=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        weight = _saved[self._saved_weight_idx]
        grad_output = self.inputs[0]
        with _grad_context(keyset):
            grad_grad_output = _prelu_kernel_backward_grad_output_helper(grads, self_, weight, grad_output)
            grad_self = (redispatch("where", keyset, redispatch("ge", keyset, self_, 0), redispatch("zeros", keyset, [], dtype=self_.dtype, device=self_.device), redispatch("mul", keyset, grad_output, grads[1])) if (grads[1] is not None) else self_._zeros_like())
            grad_weight = (redispatch("where", keyset, redispatch("ge", keyset, self_, 0), redispatch("zeros", keyset, [], dtype=weight.dtype, device=weight.device), redispatch("mul", keyset, grad_output, grads[0])) if (grads[0] is not None) else self_._zeros_like())
        return (grad_grad_output, grad_self, grad_weight,)

class RreluWithNoiseBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='RreluWithNoiseBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_noise_idx = None
        self._saved_self_idx = None
        self._lower = None
        self._upper = None
        self._training = None
        self._generator = None

    def _save(self, *, noise=None, self_=None):
        tensors = []
        if noise is not None:
            self._saved_noise_idx = len(tensors)
            tensors.append(noise)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_noise_idx is not None:
            self._saved_fields['noise'] = self._saved_tensors_list[self._saved_noise_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        noise = _saved[self._saved_noise_idx]
        self_ = _saved[self._saved_self_idx]
        lower = self._lower
        upper = self._upper
        training = self._training
        generator = self._generator
        with _grad_context(keyset):
            grad_self = redispatch("rrelu_with_noise_backward", keyset, grad, self_, noise, lower, upper, training, False)
        return (grad_self,)

class RreluWithNoiseFunctionalBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='RreluWithNoiseFunctionalBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_noise_idx = None
        self._saved_self_idx = None
        self._lower = None
        self._upper = None
        self._training = None
        self._generator = None

    def _save(self, *, noise=None, self_=None):
        tensors = []
        if noise is not None:
            self._saved_noise_idx = len(tensors)
            tensors.append(noise)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_noise_idx is not None:
            self._saved_fields['noise'] = self._saved_tensors_list[self._saved_noise_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        noise = _saved[self._saved_noise_idx]
        self_ = _saved[self._saved_self_idx]
        lower = self._lower
        upper = self._upper
        training = self._training
        generator = self._generator
        with _grad_context(keyset):
            grad_self = redispatch("rrelu_with_noise_backward", keyset, grad, self_, noise, lower, upper, training, False)
        return (grad_self,)

class SoftmaxBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SoftmaxBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._dim = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        dim = self._dim
        with _grad_context(keyset):
            grad_self = _softmax_grad(grad, self_, dim, keyset)
        return (grad_self,)

class SparseSoftmaxBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SparseSoftmaxBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_result_idx = None
        self._dim = None
        self._half_to_float = None

    def _save(self, *, self_=None, result=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        result = _saved[self._saved_result_idx]
        dim = self._dim
        half_to_float = self._half_to_float
        with _grad_context(keyset):
            grad_self = redispatch("_sparse_softmax_backward_data", keyset, grad, result, dim, self_)
        return (grad_self,)

class SparseSparseMatmulBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SparseSparseMatmulBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_other_idx = None
        self._saved_self_idx = None

    def _save(self, *, other=None, self_=None):
        tensors = []
        if other is not None:
            self._saved_other_idx = len(tensors)
            tensors.append(other)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_other_idx is not None:
            self._saved_fields['other'] = self._saved_tensors_list[self._saved_other_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        other = _saved[self._saved_other_idx]
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("sparse_sparse_matmul_backward", keyset, grad, self_, other, 0)
            grad_other = redispatch("sparse_sparse_matmul_backward", keyset, grad, self_, other, 1)
        return (grad_self, grad_other,)

class SoftplusBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SoftplusBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._beta = None
        self._threshold = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        beta = self._beta
        threshold = self._threshold
        with _grad_context(keyset):
            grad_self = _softplus_backward_helper(grad, self_, beta, threshold, keyset)
        return (grad_self,)

class SoftshrinkBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SoftshrinkBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._lambd = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        lambd = self._lambd
        with _grad_context(keyset):
            grad_self = redispatch("softshrink_backward", keyset, grad, self_, lambd)
        return (grad_self,)

class ThresholdBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ThresholdBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._threshold = None
        self._value = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        threshold = self._threshold
        value = self._value
        with _grad_context(keyset):
            grad_self = _threshold_backward_helper(grad, self_, threshold, keyset)
        return (grad_self,)

class ReflectionPad1dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ReflectionPad1dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._padding = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        padding = self._padding
        with _grad_context(keyset):
            grad_self = redispatch("reflection_pad1d_backward_symint", keyset, grad, self_, padding)
        return (grad_self,)

class ReflectionPad2dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ReflectionPad2dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._padding = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        padding = self._padding
        with _grad_context(keyset):
            grad_self = redispatch("reflection_pad2d_backward_symint", keyset, grad, self_, padding)
        return (grad_self,)

class ReflectionPad3dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ReflectionPad3dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._padding = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        padding = self._padding
        with _grad_context(keyset):
            grad_self = redispatch("reflection_pad3d_backward_symint", keyset, grad, self_, padding)
        return (grad_self,)

class ReplicationPad1dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ReplicationPad1dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._padding = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        padding = self._padding
        with _grad_context(keyset):
            grad_self = redispatch("replication_pad1d_backward_symint", keyset, grad, self_, padding)
        return (grad_self,)

class ReplicationPad2dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ReplicationPad2dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._padding = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        padding = self._padding
        with _grad_context(keyset):
            grad_self = redispatch("replication_pad2d_backward_symint", keyset, grad, self_, padding)
        return (grad_self,)

class ReplicationPad3dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ReplicationPad3dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._padding = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        padding = self._padding
        with _grad_context(keyset):
            grad_self = redispatch("replication_pad3d_backward_symint", keyset, grad, self_, padding)
        return (grad_self,)

class UpsampleLinear1dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='UpsampleLinear1dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._output_size = None
        self._align_corners = None
        self._scales = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        output_size = self._output_size
        align_corners = self._align_corners
        scales = self._scales
        with _grad_context(keyset):
            grad_self = redispatch("upsample_linear1d_backward_symint", keyset, grad, output_size, self_.shape, align_corners, scales)
        return (grad_self,)

class UpsampleBilinear2dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='UpsampleBilinear2dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._output_size = None
        self._align_corners = None
        self._scales_h = None
        self._scales_w = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        output_size = self._output_size
        align_corners = self._align_corners
        scales_h = self._scales_h
        scales_w = self._scales_w
        with _grad_context(keyset):
            grad_self = redispatch("upsample_bilinear2d_backward_symint", keyset, grad, output_size, self_.shape, align_corners, scales_h, scales_w)
        return (grad_self,)

class UpsampleBilinear2dAaBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='UpsampleBilinear2dAaBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._output_size = None
        self._align_corners = None
        self._scales_h = None
        self._scales_w = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        output_size = self._output_size
        align_corners = self._align_corners
        scales_h = self._scales_h
        scales_w = self._scales_w
        with _grad_context(keyset):
            grad_self = redispatch("_upsample_bilinear2d_aa_backward_symint", keyset, grad, output_size, self_.shape, align_corners, scales_h, scales_w)
        return (grad_self,)

class UpsampleBicubic2dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='UpsampleBicubic2dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._output_size = None
        self._align_corners = None
        self._scales_h = None
        self._scales_w = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        output_size = self._output_size
        align_corners = self._align_corners
        scales_h = self._scales_h
        scales_w = self._scales_w
        with _grad_context(keyset):
            grad_self = redispatch("upsample_bicubic2d_backward_symint", keyset, grad, output_size, self_.shape, align_corners, scales_h, scales_w)
        return (grad_self,)

class UpsampleBicubic2dAaBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='UpsampleBicubic2dAaBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._output_size = None
        self._align_corners = None
        self._scales_h = None
        self._scales_w = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        output_size = self._output_size
        align_corners = self._align_corners
        scales_h = self._scales_h
        scales_w = self._scales_w
        with _grad_context(keyset):
            grad_self = redispatch("_upsample_bicubic2d_aa_backward_symint", keyset, grad, output_size, self_.shape, align_corners, scales_h, scales_w)
        return (grad_self,)

class UpsampleTrilinear3dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='UpsampleTrilinear3dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._output_size = None
        self._align_corners = None
        self._scales_d = None
        self._scales_h = None
        self._scales_w = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        output_size = self._output_size
        align_corners = self._align_corners
        scales_d = self._scales_d
        scales_h = self._scales_h
        scales_w = self._scales_w
        with _grad_context(keyset):
            grad_self = redispatch("upsample_trilinear3d_backward_symint", keyset, grad, output_size, self_.shape, align_corners, scales_d, scales_h, scales_w)
        return (grad_self,)

class UpsampleNearest1dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='UpsampleNearest1dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._output_size = None
        self._scales = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        output_size = self._output_size
        scales = self._scales
        with _grad_context(keyset):
            grad_self = redispatch("upsample_nearest1d_backward_symint", keyset, grad, output_size, self_.shape, scales)
        return (grad_self,)

class UpsampleNearestExact1dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='UpsampleNearestExact1dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._output_size = None
        self._scales = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        output_size = self._output_size
        scales = self._scales
        with _grad_context(keyset):
            grad_self = redispatch("_upsample_nearest_exact1d_backward_symint", keyset, grad, output_size, self_.shape, scales)
        return (grad_self,)

class UpsampleNearest2dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='UpsampleNearest2dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._output_size = None
        self._scales_h = None
        self._scales_w = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        output_size = self._output_size
        scales_h = self._scales_h
        scales_w = self._scales_w
        with _grad_context(keyset):
            grad_self = redispatch("upsample_nearest2d_backward_symint", keyset, grad, output_size, self_.shape, scales_h, scales_w)
        return (grad_self,)

class UpsampleNearestExact2dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='UpsampleNearestExact2dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._output_size = None
        self._scales_h = None
        self._scales_w = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        output_size = self._output_size
        scales_h = self._scales_h
        scales_w = self._scales_w
        with _grad_context(keyset):
            grad_self = redispatch("_upsample_nearest_exact2d_backward_symint", keyset, grad, output_size, self_.shape, scales_h, scales_w)
        return (grad_self,)

class UpsampleNearest3dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='UpsampleNearest3dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._output_size = None
        self._scales_d = None
        self._scales_h = None
        self._scales_w = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        output_size = self._output_size
        scales_d = self._scales_d
        scales_h = self._scales_h
        scales_w = self._scales_w
        with _grad_context(keyset):
            grad_self = redispatch("upsample_nearest3d_backward_symint", keyset, grad, output_size, self_.shape, scales_d, scales_h, scales_w)
        return (grad_self,)

class UpsampleNearestExact3dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='UpsampleNearestExact3dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._output_size = None
        self._scales_d = None
        self._scales_h = None
        self._scales_w = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        output_size = self._output_size
        scales_d = self._scales_d
        scales_h = self._scales_h
        scales_w = self._scales_w
        with _grad_context(keyset):
            grad_self = redispatch("_upsample_nearest_exact3d_backward_symint", keyset, grad, output_size, self_.shape, scales_d, scales_h, scales_w)
        return (grad_self,)

class PixelShuffleBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='PixelShuffleBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._upscale_factor = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        upscale_factor = self._upscale_factor
        with _grad_context(keyset):
            grad_self = redispatch("pixel_unshuffle", keyset, grad, upscale_factor)
        return (grad_self,)

class PixelUnshuffleBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='PixelUnshuffleBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._downscale_factor = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        downscale_factor = self._downscale_factor
        with _grad_context(keyset):
            grad_self = redispatch("pixel_shuffle", keyset, grad, downscale_factor)
        return (grad_self,)

class ChannelShuffleBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ChannelShuffleBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._groups = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        groups = self._groups
        with _grad_context(keyset):
            grad_self = redispatch("channel_shuffle_symint", keyset, grad, redispatch("div", keyset, grad.shape[1], groups))
        return (grad_self,)

class AdaptiveAvgPool2dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='AdaptiveAvgPool2dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._output_size = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        output_size = self._output_size
        with _grad_context(keyset):
            grad_self = redispatch("_adaptive_avg_pool2d_backward", keyset, grad, self_)
        return (grad_self,)

class AdaptiveAvgPool3dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='AdaptiveAvgPool3dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._output_size = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        output_size = self._output_size
        with _grad_context(keyset):
            grad_self = redispatch("_adaptive_avg_pool3d_backward", keyset, grad, self_)
        return (grad_self,)

class AdaptiveMaxPool2dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='AdaptiveMaxPool2dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_result1_idx = None
        self._output_size = None

    def _save(self, *, self_=None, result1=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result1 is not None:
            self._saved_result1_idx = len(tensors)
            tensors.append(result1)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result1_idx is not None:
            self._saved_fields['result1'] = self._saved_tensors_list[self._saved_result1_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        result1 = _saved[self._saved_result1_idx]
        output_size = self._output_size
        with _grad_context(keyset):
            grad_self = redispatch("adaptive_max_pool2d_backward", keyset, grad, self_, result1)
        return (grad_self,)

class AdaptiveMaxPool3dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='AdaptiveMaxPool3dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_result1_idx = None
        self._output_size = None

    def _save(self, *, self_=None, result1=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result1 is not None:
            self._saved_result1_idx = len(tensors)
            tensors.append(result1)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result1_idx is not None:
            self._saved_fields['result1'] = self._saved_tensors_list[self._saved_result1_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        result1 = _saved[self._saved_result1_idx]
        output_size = self._output_size
        with _grad_context(keyset):
            grad_self = redispatch("adaptive_max_pool3d_backward", keyset, grad, self_, result1)
        return (grad_self,)

class AvgPool2dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='AvgPool2dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._kernel_size = None
        self._stride = None
        self._padding = None
        self._ceil_mode = None
        self._count_include_pad = None
        self._divisor_override = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        kernel_size = self._kernel_size
        stride = self._stride
        padding = self._padding
        ceil_mode = self._ceil_mode
        count_include_pad = self._count_include_pad
        divisor_override = self._divisor_override
        with _grad_context(keyset):
            grad_self = _avg_pool2d_backward_helper(grad, self_, kernel_size, stride, padding, ceil_mode, count_include_pad, keyset)
        return (grad_self,)

class AvgPool3dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='AvgPool3dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._kernel_size = None
        self._stride = None
        self._padding = None
        self._ceil_mode = None
        self._count_include_pad = None
        self._divisor_override = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        kernel_size = self._kernel_size
        stride = self._stride
        padding = self._padding
        ceil_mode = self._ceil_mode
        count_include_pad = self._count_include_pad
        divisor_override = self._divisor_override
        with _grad_context(keyset):
            grad_self = redispatch("avg_pool3d_backward", keyset, grad, self_, kernel_size, stride, padding, ceil_mode, count_include_pad, divisor_override)
        return (grad_self,)

class FractionalMaxPool2dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='FractionalMaxPool2dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_result1_idx = None
        self._kernel_size = None
        self._output_size = None

    def _save(self, *, self_=None, result1=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result1 is not None:
            self._saved_result1_idx = len(tensors)
            tensors.append(result1)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result1_idx is not None:
            self._saved_fields['result1'] = self._saved_tensors_list[self._saved_result1_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        result1 = _saved[self._saved_result1_idx]
        kernel_size = self._kernel_size
        output_size = self._output_size
        with _grad_context(keyset):
            grad_self = redispatch("fractional_max_pool2d_backward", keyset, grad, self_, kernel_size, output_size, result1)
        return (grad_self,)

class FractionalMaxPool3dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='FractionalMaxPool3dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_result1_idx = None
        self._kernel_size = None
        self._output_size = None

    def _save(self, *, self_=None, result1=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result1 is not None:
            self._saved_result1_idx = len(tensors)
            tensors.append(result1)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result1_idx is not None:
            self._saved_fields['result1'] = self._saved_tensors_list[self._saved_result1_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        result1 = _saved[self._saved_result1_idx]
        kernel_size = self._kernel_size
        output_size = self._output_size
        with _grad_context(keyset):
            grad_self = redispatch("fractional_max_pool3d_backward", keyset, grad, self_, kernel_size, output_size, result1)
        return (grad_self,)

class LinearBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='LinearBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._saved_weight_idx = None

    def _save(self, *, input_=None, weight=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        weight = _saved[self._saved_weight_idx]
        grad_input_mask = [True, True, (bias is not None)]
        with _grad_context(keyset):
            grad_input, grad_weight, grad_bias = (redispatch("linear_backward", keyset, input_, grad, weight, grad_input_mask) if (grad is not None) else (None, None, None))
        return (grad_input, grad_weight, grad_bias,)

class LinearBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='LinearBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_weight_idx = None
        self._output_mask = None

    def _save(self, *, self_=None, weight=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        weight = _saved[self._saved_weight_idx]
        grad_output = self.inputs[1]
        output_mask = self._output_mask
        with _grad_context(keyset):
            grad_self, grad_grad_output, grad_weight = redispatch("linear_double_backward", keyset, grads, self_, grad_output, weight)
        return (grad_self, grad_grad_output, grad_weight,)

class MaxPool2dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MaxPool2dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._kernel_size = None
        self._stride = None
        self._padding = None
        self._dilation = None
        self._ceil_mode = None

    def _save(self, *, self_=None, result=None):  # result is ignored
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        kernel_size = self._kernel_size
        stride = self._stride
        padding = self._padding
        dilation = self._dilation
        ceil_mode = self._ceil_mode
        with _grad_context(keyset):
            _, _pool_result = redispatch("max_pool2d", keyset, self_, kernel_size, stride, padding, dilation, ceil_mode, True)
            grad_self = _max_pool2d_backward_helper(grad, self_, _pool_result, kernel_size, stride, padding, dilation, ceil_mode, keyset)
        return (grad_self,)

class MpsConvolutionBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MpsConvolutionBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_weight_idx = None
        self._padding = None
        self._stride = None
        self._dilation = None
        self._groups = None

    def _save(self, *, self_=None, weight=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        weight = _saved[self._saved_weight_idx]
        padding = self._padding
        stride = self._stride
        dilation = self._dilation
        groups = self._groups
        grad_input_mask = [True, True, (bias is not None)]
        with _grad_context(keyset):
            grad_self, grad_weight, grad_bias = (redispatch("mps_convolution_backward_symint", keyset, self_, grad, weight, padding, stride, dilation, groups, grad_input_mask) if (grad is not None) else (None, None, None))
        return (grad_self, grad_weight, grad_bias,)

class MpsConvolutionBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MpsConvolutionBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_weight_idx = None
        self._padding = None
        self._stride = None
        self._dilation = None
        self._groups = None
        self._output_mask = None

    def _save(self, *, self_=None, weight=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        weight = _saved[self._saved_weight_idx]
        grad_output = self.inputs[1]
        padding = self._padding
        stride = self._stride
        dilation = self._dilation
        groups = self._groups
        output_mask = self._output_mask
        grad_input_mask = [True, True, True]
        with _grad_context(keyset):
            grad_grad_output, grad_self, grad_weight = redispatch("_convolution_double_backward_symint", keyset, grads[0], grads[1], grads[2], grad_output, weight, self_, stride, padding, dilation, False, [0] * redispatch("size", keyset, padding), groups, grad_input_mask)
        return (grad_self, grad_grad_output, grad_weight,)

class MaxPool2dWithIndicesBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MaxPool2dWithIndicesBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_result1_idx = None
        self._kernel_size = None
        self._stride = None
        self._padding = None
        self._dilation = None
        self._ceil_mode = None

    def _save(self, *, self_=None, result1=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result1 is not None:
            self._saved_result1_idx = len(tensors)
            tensors.append(result1)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result1_idx is not None:
            self._saved_fields['result1'] = self._saved_tensors_list[self._saved_result1_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        result1 = _saved[self._saved_result1_idx]
        kernel_size = self._kernel_size
        stride = self._stride
        padding = self._padding
        dilation = self._dilation
        ceil_mode = self._ceil_mode
        with _grad_context(keyset):
            grad_self = redispatch("max_pool2d_with_indices_backward", keyset, grad, self_, kernel_size, stride, padding, dilation, ceil_mode, result1)
        return (grad_self,)

class MaxPool3dWithIndicesBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MaxPool3dWithIndicesBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_result1_idx = None
        self._kernel_size = None
        self._stride = None
        self._padding = None
        self._dilation = None
        self._ceil_mode = None

    def _save(self, *, self_=None, result1=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result1 is not None:
            self._saved_result1_idx = len(tensors)
            tensors.append(result1)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result1_idx is not None:
            self._saved_fields['result1'] = self._saved_tensors_list[self._saved_result1_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        result1 = _saved[self._saved_result1_idx]
        kernel_size = self._kernel_size
        stride = self._stride
        padding = self._padding
        dilation = self._dilation
        ceil_mode = self._ceil_mode
        with _grad_context(keyset):
            grad_self = redispatch("max_pool3d_with_indices_backward", keyset, grad, self_, kernel_size, stride, padding, dilation, ceil_mode, result1)
        return (grad_self,)

class MaxUnpool2dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MaxUnpool2dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_indices_idx = None
        self._output_size = None

    def _save(self, *, indices=None):
        tensors = []
        if indices is not None:
            self._saved_indices_idx = len(tensors)
            tensors.append(indices)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_indices_idx is not None:
            self._saved_fields['indices'] = self._saved_tensors_list[self._saved_indices_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        indices = _saved[self._saved_indices_idx]
        output_size = self._output_size
        with _grad_context(keyset):
            grad_self = redispatch("max_pool_double_backward", keyset, grad, indices, 2)
        return (grad_self,)

class MaxUnpool3dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MaxUnpool3dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_indices_idx = None
        self._output_size = None
        self._stride = None
        self._padding = None

    def _save(self, *, indices=None):
        tensors = []
        if indices is not None:
            self._saved_indices_idx = len(tensors)
            tensors.append(indices)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_indices_idx is not None:
            self._saved_fields['indices'] = self._saved_tensors_list[self._saved_indices_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        indices = _saved[self._saved_indices_idx]
        output_size = self._output_size
        stride = self._stride
        padding = self._padding
        with _grad_context(keyset):
            grad_self = redispatch("max_pool_double_backward", keyset, grad, indices, 3)
        return (grad_self,)

class ConvolutionBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ConvolutionBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_bias_idx = None
        self._saved_input_idx = None
        self._saved_weight_idx = None
        self._stride = None
        self._padding = None
        self._dilation = None
        self._transposed = None
        self._output_padding = None
        self._groups = None

    def _save(self, *, bias=None, input_=None, weight=None):
        tensors = []
        if bias is not None:
            self._saved_bias_idx = len(tensors)
            tensors.append(bias)
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_bias_idx is not None:
            self._saved_fields['bias'] = self._saved_tensors_list[self._saved_bias_idx]
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        bias = _saved[self._saved_bias_idx] if self._saved_bias_idx is not None else None
        input_ = _saved[self._saved_input_idx]
        weight = _saved[self._saved_weight_idx]
        stride = self._stride
        padding = self._padding
        dilation = self._dilation
        transposed = self._transposed
        output_padding = self._output_padding
        groups = self._groups
        grad_input_mask = [True, True, (bias is not None)]
        with _grad_context(keyset):
            grad_input, grad_weight, grad_bias = (redispatch("convolution_backward_symint", keyset, grad, input_, weight, bias.shape, stride, padding, dilation, transposed, output_padding, groups, grad_input_mask) if (grad is not None) else (None, None, None))
        return (grad_input, grad_weight, grad_bias,)

class ConvolutionBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ConvolutionBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._saved_weight_idx = None
        self._bias_sizes = None
        self._stride = None
        self._padding = None
        self._dilation = None
        self._transposed = None
        self._output_padding = None
        self._groups = None
        self._output_mask = None

    def _save(self, *, input_=None, weight=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        weight = _saved[self._saved_weight_idx]
        grad_output = self.inputs[0]
        bias_sizes = self._bias_sizes
        stride = self._stride
        padding = self._padding
        dilation = self._dilation
        transposed = self._transposed
        output_padding = self._output_padding
        groups = self._groups
        output_mask = self._output_mask
        grad_input_mask = [True, True, True]
        with _grad_context(keyset):
            grad_grad_output, grad_input, grad_weight = redispatch("_convolution_double_backward_symint", keyset, grads[0], grads[1], grads[2], grad_output, weight, input_, stride, padding, dilation, transposed, output_padding, groups, grad_input_mask)
        return (grad_grad_output, grad_input, grad_weight,)

class ConvolutionOverrideableBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ConvolutionOverrideableBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._saved_weight_idx = None
        self._stride = None
        self._padding = None
        self._dilation = None
        self._transposed = None
        self._output_padding = None
        self._groups = None

    def _save(self, *, input_=None, weight=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        weight = _saved[self._saved_weight_idx]
        stride = self._stride
        padding = self._padding
        dilation = self._dilation
        transposed = self._transposed
        output_padding = self._output_padding
        groups = self._groups
        grad_input_mask = [True, True, (bias is not None)]
        with _grad_context(keyset):
            grad_input, grad_weight, grad_bias = (redispatch("convolution_backward_overrideable_symint", keyset, grad, input_, weight, stride, padding, dilation, transposed, output_padding, groups, grad_input_mask) if (grad is not None) else (None, None, None))
        return (grad_input, grad_weight, grad_bias,)

class ConvolutionBackwardOverrideableBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ConvolutionBackwardOverrideableBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._saved_weight_idx = None
        self._stride = None
        self._padding = None
        self._dilation = None
        self._transposed = None
        self._output_padding = None
        self._groups = None
        self._output_mask = None

    def _save(self, *, input_=None, weight=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        weight = _saved[self._saved_weight_idx]
        grad_output = self.inputs[0]
        stride = self._stride
        padding = self._padding
        dilation = self._dilation
        transposed = self._transposed
        output_padding = self._output_padding
        groups = self._groups
        output_mask = self._output_mask
        grad_input_mask = [True, True, True]
        with _grad_context(keyset):
            grad_grad_output, grad_input, grad_weight = redispatch("_convolution_double_backward_symint", keyset, grads[0], grads[1], grads[2], grad_output, weight, input_, stride, padding, dilation, transposed, output_padding, groups, grad_input_mask)
        return (grad_grad_output, grad_input, grad_weight,)

class SlowConvTranspose2dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SlowConvTranspose2dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_bias_idx = None
        self._saved_self_idx = None
        self._saved_weight_idx = None
        self._kernel_size = None
        self._stride = None
        self._padding = None
        self._output_padding = None
        self._dilation = None

    def _save(self, *, bias=None, self_=None, weight=None):
        tensors = []
        if bias is not None:
            self._saved_bias_idx = len(tensors)
            tensors.append(bias)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_bias_idx is not None:
            self._saved_fields['bias'] = self._saved_tensors_list[self._saved_bias_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        bias = _saved[self._saved_bias_idx] if self._saved_bias_idx is not None else None
        self_ = _saved[self._saved_self_idx]
        weight = _saved[self._saved_weight_idx]
        kernel_size = self._kernel_size
        stride = self._stride
        padding = self._padding
        output_padding = self._output_padding
        dilation = self._dilation
        grad_input_mask = [True, True, (bias is not None)]
        with _grad_context(keyset):
            grad_self, grad_weight, grad_bias = (redispatch("convolution_backward_symint", keyset, grad, self_, weight, bias.shape, stride, padding, dilation, True, output_padding, 1, grad_input_mask) if (grad is not None) else (None, None, None))
        return (grad_self, grad_weight, grad_bias,)

class SlowConvTranspose3dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SlowConvTranspose3dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_bias_idx = None
        self._saved_self_idx = None
        self._saved_weight_idx = None
        self._kernel_size = None
        self._stride = None
        self._padding = None
        self._output_padding = None
        self._dilation = None

    def _save(self, *, bias=None, self_=None, weight=None):
        tensors = []
        if bias is not None:
            self._saved_bias_idx = len(tensors)
            tensors.append(bias)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_bias_idx is not None:
            self._saved_fields['bias'] = self._saved_tensors_list[self._saved_bias_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        bias = _saved[self._saved_bias_idx] if self._saved_bias_idx is not None else None
        self_ = _saved[self._saved_self_idx]
        weight = _saved[self._saved_weight_idx]
        kernel_size = self._kernel_size
        stride = self._stride
        padding = self._padding
        output_padding = self._output_padding
        dilation = self._dilation
        grad_input_mask = [True, True, (bias is not None)]
        with _grad_context(keyset):
            grad_self, grad_weight, grad_bias = (redispatch("convolution_backward_symint", keyset, grad, self_, weight, bias.shape, stride, padding, dilation, True, output_padding, 1, grad_input_mask) if (grad is not None) else (None, None, None))
        return (grad_self, grad_weight, grad_bias,)

class SlowConv2dForwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SlowConv2dForwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_weight_idx = None
        self._kernel_size = None
        self._stride = None
        self._padding = None

    def _save(self, *, self_=None, weight=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        weight = _saved[self._saved_weight_idx]
        kernel_size = self._kernel_size
        stride = self._stride
        padding = self._padding
        grad_input_mask = [True, True, (bias is not None)]
        with _grad_context(keyset):
            grad_self, grad_weight, grad_bias = (redispatch("_slow_conv2d_backward_symint", keyset, grad, self_, weight, kernel_size, stride, padding, grad_input_mask) if (grad is not None) else (None, None, None))
        return (grad_self, grad_weight, grad_bias,)

class SlowConv2dBackwardOutputMaskBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SlowConv2dBackwardOutputMaskBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_weight_idx = None
        self._kernel_size = None
        self._stride = None
        self._padding = None
        self._output_mask = None

    def _save(self, *, self_=None, weight=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        weight = _saved[self._saved_weight_idx]
        grad_output = self.inputs[0]
        kernel_size = self._kernel_size
        stride = self._stride
        padding = self._padding
        output_mask = self._output_mask
        grad_input_mask = [True, True, True]
        with _grad_context(keyset):
            grad_grad_output, grad_self, grad_weight = redispatch("_convolution_double_backward_symint", keyset, grads[0], grads[1], grads[2], grad_output, weight, self_, stride, padding, [[1, 1]], False, [[0, 0]], 1, grad_input_mask)
        return (grad_grad_output, grad_self, grad_weight,)

class ConvDepthwise2dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ConvDepthwise2dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_bias_idx = None
        self._saved_self_idx = None
        self._saved_weight_idx = None
        self._kernel_size = None
        self._stride = None
        self._padding = None
        self._dilation = None

    def _save(self, *, bias=None, self_=None, weight=None):
        tensors = []
        if bias is not None:
            self._saved_bias_idx = len(tensors)
            tensors.append(bias)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_bias_idx is not None:
            self._saved_fields['bias'] = self._saved_tensors_list[self._saved_bias_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        bias = _saved[self._saved_bias_idx] if self._saved_bias_idx is not None else None
        self_ = _saved[self._saved_self_idx]
        weight = _saved[self._saved_weight_idx]
        kernel_size = self._kernel_size
        stride = self._stride
        padding = self._padding
        dilation = self._dilation
        grad_input_mask = [True, True, (bias is not None)]
        with _grad_context(keyset):
            grad_self, grad_weight, grad_bias = (redispatch("convolution_backward_symint", keyset, redispatch("contiguous", keyset, grad), self_, weight, bias.shape, stride, padding, dilation, False, [[0, 0]], 1, grad_input_mask) if (grad is not None) else (None, None, None))
        return (grad_self, grad_weight, grad_bias,)

class ConvDepthwise3dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ConvDepthwise3dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_bias_idx = None
        self._saved_self_idx = None
        self._saved_weight_idx = None
        self._kernel_size = None
        self._stride = None
        self._padding = None
        self._dilation = None

    def _save(self, *, bias=None, self_=None, weight=None):
        tensors = []
        if bias is not None:
            self._saved_bias_idx = len(tensors)
            tensors.append(bias)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_bias_idx is not None:
            self._saved_fields['bias'] = self._saved_tensors_list[self._saved_bias_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        bias = _saved[self._saved_bias_idx] if self._saved_bias_idx is not None else None
        self_ = _saved[self._saved_self_idx]
        weight = _saved[self._saved_weight_idx]
        kernel_size = self._kernel_size
        stride = self._stride
        padding = self._padding
        dilation = self._dilation
        grad_input_mask = [True, True, (bias is not None)]
        with _grad_context(keyset):
            grad_self, grad_weight, grad_bias = (redispatch("convolution_backward_symint", keyset, redispatch("contiguous", keyset, grad), self_, weight, bias.shape, stride, padding, dilation, False, [[0, 0, 0]], 1, grad_input_mask) if (grad is not None) else (None, None, None))
        return (grad_self, grad_weight, grad_bias,)

class SlowConv3dForwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SlowConv3dForwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_bias_idx = None
        self._saved_self_idx = None
        self._saved_weight_idx = None
        self._kernel_size = None
        self._stride = None
        self._padding = None

    def _save(self, *, bias=None, self_=None, weight=None):
        tensors = []
        if bias is not None:
            self._saved_bias_idx = len(tensors)
            tensors.append(bias)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_bias_idx is not None:
            self._saved_fields['bias'] = self._saved_tensors_list[self._saved_bias_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        bias = _saved[self._saved_bias_idx] if self._saved_bias_idx is not None else None
        self_ = _saved[self._saved_self_idx]
        weight = _saved[self._saved_weight_idx]
        kernel_size = self._kernel_size
        stride = self._stride
        padding = self._padding
        grad_input_mask = [True, True, (bias is not None)]
        with _grad_context(keyset):
            grad_self, grad_weight, grad_bias = (redispatch("convolution_backward_symint", keyset, grad, self_, weight, bias.shape, stride, padding, [[1, 1, 1]], False, [[0, 0, 0]], 1, grad_input_mask) if (grad is not None) else (None, None, None))
        return (grad_self, grad_weight, grad_bias,)

class SlowConvDilated2dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SlowConvDilated2dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_bias_idx = None
        self._saved_self_idx = None
        self._saved_weight_idx = None
        self._kernel_size = None
        self._stride = None
        self._padding = None
        self._dilation = None

    def _save(self, *, bias=None, self_=None, weight=None):
        tensors = []
        if bias is not None:
            self._saved_bias_idx = len(tensors)
            tensors.append(bias)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_bias_idx is not None:
            self._saved_fields['bias'] = self._saved_tensors_list[self._saved_bias_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        bias = _saved[self._saved_bias_idx] if self._saved_bias_idx is not None else None
        self_ = _saved[self._saved_self_idx]
        weight = _saved[self._saved_weight_idx]
        kernel_size = self._kernel_size
        stride = self._stride
        padding = self._padding
        dilation = self._dilation
        grad_input_mask = [True, True, (bias is not None)]
        with _grad_context(keyset):
            grad_self, grad_weight, grad_bias = (redispatch("convolution_backward_symint", keyset, grad, self_, weight, bias.shape, stride, padding, dilation, False, [0] * redispatch("size", keyset, padding), 1, grad_input_mask) if (grad is not None) else (None, None, None))
        return (grad_self, grad_weight, grad_bias,)

class SlowConvDilated3dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SlowConvDilated3dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_bias_idx = None
        self._saved_self_idx = None
        self._saved_weight_idx = None
        self._kernel_size = None
        self._stride = None
        self._padding = None
        self._dilation = None

    def _save(self, *, bias=None, self_=None, weight=None):
        tensors = []
        if bias is not None:
            self._saved_bias_idx = len(tensors)
            tensors.append(bias)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_bias_idx is not None:
            self._saved_fields['bias'] = self._saved_tensors_list[self._saved_bias_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        bias = _saved[self._saved_bias_idx] if self._saved_bias_idx is not None else None
        self_ = _saved[self._saved_self_idx]
        weight = _saved[self._saved_weight_idx]
        kernel_size = self._kernel_size
        stride = self._stride
        padding = self._padding
        dilation = self._dilation
        grad_input_mask = [True, True, (bias is not None)]
        with _grad_context(keyset):
            grad_self, grad_weight, grad_bias = (redispatch("convolution_backward_symint", keyset, grad, self_, weight, bias.shape, stride, padding, dilation, False, [0] * redispatch("size", keyset, padding), 1, grad_input_mask) if (grad is not None) else (None, None, None))
        return (grad_self, grad_weight, grad_bias,)

class Col2imBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Col2imBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._output_size = None
        self._kernel_size = None
        self._dilation = None
        self._padding = None
        self._stride = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        output_size = self._output_size
        kernel_size = self._kernel_size
        dilation = self._dilation
        padding = self._padding
        stride = self._stride
        with _grad_context(keyset):
            grad_self = redispatch("im2col", keyset, grad, kernel_size, dilation, padding, stride)
        return (grad_self,)

class Im2colBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Im2colBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._kernel_size = None
        self._dilation = None
        self._padding = None
        self._stride = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        kernel_size = self._kernel_size
        dilation = self._dilation
        padding = self._padding
        stride = self._stride
        with _grad_context(keyset):
            grad_self = redispatch("col2im_symint", keyset, grad, [self_.shape[redispatch("neg", keyset, 2)], self_.shape[redispatch("neg", keyset, 1)]], kernel_size, dilation, padding, stride)
        return (grad_self,)

class AdaptiveAvgPool2dBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='AdaptiveAvgPool2dBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        grad_output = self.inputs[0]
        with _grad_context(keyset):
            grad_grad_output = redispatch("_adaptive_avg_pool2d_symint", keyset, grad, [grad_output.shape[redispatch("neg", keyset, 2)], grad_output.shape[redispatch("neg", keyset, 1)]])
            grad_self = self_._zeros_like()
        return (grad_grad_output, grad_self,)

class AdaptiveAvgPool3dBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='AdaptiveAvgPool3dBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        grad_output = self.inputs[0]
        with _grad_context(keyset):
            grad_grad_output = redispatch("_adaptive_avg_pool3d_symint", keyset, grad, [grad_output.shape[redispatch("neg", keyset, 3)], grad_output.shape[redispatch("neg", keyset, 2)], grad_output.shape[redispatch("neg", keyset, 1)]])
            grad_self = self_._zeros_like()
        return (grad_grad_output, grad_self,)

class AdaptiveMaxPool2dBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='AdaptiveMaxPool2dBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_indices_idx = None
        self._saved_self_idx = None

    def _save(self, *, indices=None, self_=None):
        tensors = []
        if indices is not None:
            self._saved_indices_idx = len(tensors)
            tensors.append(indices)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_indices_idx is not None:
            self._saved_fields['indices'] = self._saved_tensors_list[self._saved_indices_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        indices = _saved[self._saved_indices_idx]
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_grad_output = redispatch("max_pool_double_backward", keyset, grad, indices, 2)
            grad_self = self_._zeros_like()
        return (grad_grad_output, grad_self,)

class AdaptiveMaxPool3dBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='AdaptiveMaxPool3dBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_indices_idx = None
        self._saved_self_idx = None

    def _save(self, *, indices=None, self_=None):
        tensors = []
        if indices is not None:
            self._saved_indices_idx = len(tensors)
            tensors.append(indices)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_indices_idx is not None:
            self._saved_fields['indices'] = self._saved_tensors_list[self._saved_indices_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        indices = _saved[self._saved_indices_idx]
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_grad_output = redispatch("max_pool_double_backward", keyset, grad, indices, 3)
            grad_self = self_._zeros_like()
        return (grad_grad_output, grad_self,)

class AvgPool2dBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='AvgPool2dBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._kernel_size = None
        self._stride = None
        self._padding = None
        self._ceil_mode = None
        self._count_include_pad = None
        self._divisor_override = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        kernel_size = self._kernel_size
        stride = self._stride
        padding = self._padding
        ceil_mode = self._ceil_mode
        count_include_pad = self._count_include_pad
        divisor_override = self._divisor_override
        with _grad_context(keyset):
            grad_grad_output = redispatch("avg_pool2d", keyset, grad, kernel_size, stride, padding, ceil_mode, count_include_pad, divisor_override)
            grad_self = self_._zeros_like()
        return (grad_grad_output, grad_self,)

class AvgPool3dBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='AvgPool3dBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._kernel_size = None
        self._stride = None
        self._padding = None
        self._ceil_mode = None
        self._count_include_pad = None
        self._divisor_override = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        kernel_size = self._kernel_size
        stride = self._stride
        padding = self._padding
        ceil_mode = self._ceil_mode
        count_include_pad = self._count_include_pad
        divisor_override = self._divisor_override
        with _grad_context(keyset):
            grad_grad_output = redispatch("avg_pool3d", keyset, grad, kernel_size, stride, padding, ceil_mode, count_include_pad, divisor_override)
            grad_self = self_._zeros_like()
        return (grad_grad_output, grad_self,)

class EluBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='EluBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_or_result_idx = None
        self._alpha = None
        self._scale = None
        self._input_scale = None
        self._is_result = None

    def _save(self, *, self_or_result=None):
        tensors = []
        if self_or_result is not None:
            self._saved_self_or_result_idx = len(tensors)
            tensors.append(self_or_result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_or_result_idx is not None:
            self._saved_fields['self_or_result'] = self._saved_tensors_list[self._saved_self_or_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_or_result = _saved[self._saved_self_or_result_idx]
        grad_output = self.inputs[0]
        alpha = self._alpha
        scale = self._scale
        input_scale = self._input_scale
        is_result = self._is_result
        with _grad_context(keyset):
            grad_grad_output = redispatch("elu_backward", keyset, grad, alpha, scale, input_scale, is_result, self_or_result)
            grad_self_or_result = redispatch("elu_double_backward", keyset, grad, grad_output, alpha, scale, input_scale, is_result, self_or_result)
        return (grad_grad_output, grad_self_or_result,)

class FractionalMaxPool2dBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='FractionalMaxPool2dBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_indices_idx = None
        self._saved_self_idx = None
        self._kernel_size = None
        self._output_size = None

    def _save(self, *, indices=None, self_=None):
        tensors = []
        if indices is not None:
            self._saved_indices_idx = len(tensors)
            tensors.append(indices)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_indices_idx is not None:
            self._saved_fields['indices'] = self._saved_tensors_list[self._saved_indices_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        indices = _saved[self._saved_indices_idx]
        self_ = _saved[self._saved_self_idx]
        kernel_size = self._kernel_size
        output_size = self._output_size
        with _grad_context(keyset):
            grad_grad_output = redispatch("max_pool_double_backward", keyset, grad, indices, 2)
            grad_self = self_._zeros_like()
        return (grad_grad_output, grad_self,)

class FractionalMaxPool3dBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='FractionalMaxPool3dBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_indices_idx = None
        self._saved_self_idx = None
        self._kernel_size = None
        self._output_size = None

    def _save(self, *, indices=None, self_=None):
        tensors = []
        if indices is not None:
            self._saved_indices_idx = len(tensors)
            tensors.append(indices)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_indices_idx is not None:
            self._saved_fields['indices'] = self._saved_tensors_list[self._saved_indices_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        indices = _saved[self._saved_indices_idx]
        self_ = _saved[self._saved_self_idx]
        kernel_size = self._kernel_size
        output_size = self._output_size
        with _grad_context(keyset):
            grad_grad_output = redispatch("max_pool_double_backward", keyset, grad, indices, 3)
            grad_self = self_._zeros_like()
        return (grad_grad_output, grad_self,)

class GluBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='GluBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._dim = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        grad_output = self.inputs[0]
        dim = self._dim
        with _grad_context(keyset):
            grad_grad_output = redispatch("glu_double_backward_grad_output", keyset, grad, self_, dim)
            grad_self = redispatch("glu_double_backward", keyset, grad, grad_output, self_, dim)
        return (grad_grad_output, grad_self,)

class HardtanhBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='HardtanhBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._min_val = None
        self._max_val = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        min_val = self._min_val
        max_val = self._max_val
        with _grad_context(keyset):
            grad_grad_output = _hardtanh_backward_helper(grad, self_, min_val, max_val, keyset)
            grad_self = grad._zeros_like()
        return (grad_grad_output, grad_self,)

class LogSigmoidBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='LogSigmoidBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_buffer_idx = None
        self._saved_self_idx = None

    def _save(self, *, buffer=None, self_=None):
        tensors = []
        if buffer is not None:
            self._saved_buffer_idx = len(tensors)
            tensors.append(buffer)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_buffer_idx is not None:
            self._saved_fields['buffer'] = self._saved_tensors_list[self._saved_buffer_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        buffer = _saved[self._saved_buffer_idx]
        self_ = _saved[self._saved_self_idx]
        grad_output = self.inputs[0]
        with _grad_context(keyset):
            grad_grad_output = redispatch("log_sigmoid_backward", keyset, grad, self_, buffer)
            grad_self = redispatch("log_sigmoid_double_backward", keyset, redispatch("mul", keyset, grad, grad_output), self_)
        return (grad_grad_output, grad_self,)

class LogSoftmaxBackwardDataBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='LogSoftmaxBackwardDataBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_output_idx = None
        self._dim = None
        self._input_dtype = None

    def _save(self, *, output=None):
        tensors = []
        if output is not None:
            self._saved_output_idx = len(tensors)
            tensors.append(output)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_output_idx is not None:
            self._saved_fields['output'] = self._saved_tensors_list[self._saved_output_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        output = _saved[self._saved_output_idx]
        grad_output = self.inputs[0]
        dim = self._dim
        input_dtype = self._input_dtype
        with _grad_context(keyset):
            grad_grad_output = redispatch("sub", keyset, redispatch("to", keyset, grad, redispatch("dtype", keyset, output)), redispatch("sum", keyset, redispatch("mul", keyset, redispatch("to", keyset, grad, redispatch("dtype", keyset, output)), redispatch("exp", keyset, output)), dim, True))
            grad_output = redispatch("to", keyset, redispatch("mul", keyset, redispatch("mul", keyset, redispatch("neg", keyset, redispatch("sum", keyset, grad_output, dim, True)), redispatch("exp", keyset, output)), redispatch("to", keyset, grad, redispatch("dtype", keyset, output))), redispatch("dtype", keyset, output))
        return (grad_grad_output, grad_output,)

class LeakyReluBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='LeakyReluBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._negative_slope = None
        self._self_is_result = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        negative_slope = self._negative_slope
        self_is_result = self._self_is_result
        with _grad_context(keyset):
            grad_grad_output = redispatch("leaky_relu_backward", keyset, grad, self_, negative_slope, False)
            grad_self = grad._zeros_like()
        return (grad_grad_output, grad_self,)

class MaxPool2dBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MaxPool2dBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._kernel_size = None
        self._stride = None
        self._padding = None
        self._dilation = None
        self._ceil_mode = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        kernel_size = self._kernel_size
        stride = self._stride
        padding = self._padding
        dilation = self._dilation
        ceil_mode = self._ceil_mode
        with _grad_context(keyset):
            grad_grad_output = redispatch("error_for_max_pool2d_double_backward", keyset, )
            grad_self = self_._zeros_like()
        return (grad_grad_output, grad_self,)

class MaxPool2dWithIndicesBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MaxPool2dWithIndicesBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_indices_idx = None
        self._saved_self_idx = None
        self._kernel_size = None
        self._stride = None
        self._padding = None
        self._dilation = None
        self._ceil_mode = None

    def _save(self, *, indices=None, self_=None):
        tensors = []
        if indices is not None:
            self._saved_indices_idx = len(tensors)
            tensors.append(indices)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_indices_idx is not None:
            self._saved_fields['indices'] = self._saved_tensors_list[self._saved_indices_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        indices = _saved[self._saved_indices_idx]
        self_ = _saved[self._saved_self_idx]
        kernel_size = self._kernel_size
        stride = self._stride
        padding = self._padding
        dilation = self._dilation
        ceil_mode = self._ceil_mode
        with _grad_context(keyset):
            grad_grad_output = redispatch("max_pool_double_backward", keyset, grad, indices, 2)
            grad_self = self_._zeros_like()
        return (grad_grad_output, grad_self,)

class MaxPool3dWithIndicesBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MaxPool3dWithIndicesBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_indices_idx = None
        self._saved_self_idx = None
        self._kernel_size = None
        self._stride = None
        self._padding = None
        self._dilation = None
        self._ceil_mode = None

    def _save(self, *, indices=None, self_=None):
        tensors = []
        if indices is not None:
            self._saved_indices_idx = len(tensors)
            tensors.append(indices)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_indices_idx is not None:
            self._saved_fields['indices'] = self._saved_tensors_list[self._saved_indices_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        indices = _saved[self._saved_indices_idx]
        self_ = _saved[self._saved_self_idx]
        kernel_size = self._kernel_size
        stride = self._stride
        padding = self._padding
        dilation = self._dilation
        ceil_mode = self._ceil_mode
        with _grad_context(keyset):
            grad_grad_output = redispatch("max_pool_double_backward", keyset, grad, indices, 3)
            grad_self = self_._zeros_like()
        return (grad_grad_output, grad_self,)

class MseLossBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MseLossBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_target_idx = None
        self._reduction = None

    def _save(self, *, self_=None, target=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if target is not None:
            self._saved_target_idx = len(tensors)
            tensors.append(target)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_target_idx is not None:
            self._saved_fields['target'] = self._saved_tensors_list[self._saved_target_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        target = _saved[self._saved_target_idx]
        grad_output = self.inputs[0]
        reduction = self._reduction
        with _grad_context(keyset):
            grad_grad_output = redispatch("mse_loss_backward", keyset, grad, self_, target, reduction)
            grad_self = redispatch("mse_loss_double_backward", keyset, redispatch("mul", keyset, grad, grad_output), self_, reduction)
            grad_target = redispatch("neg", keyset, redispatch("mse_loss_double_backward", keyset, redispatch("mul", keyset, grad, grad_output), target, reduction))
        return (grad_grad_output, grad_self, grad_target,)

class NllLossBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='NllLossBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_target_idx = None
        self._saved_weight_idx = None
        self._reduction = None
        self._ignore_index = None

    def _save(self, *, target=None, weight=None):
        tensors = []
        if target is not None:
            self._saved_target_idx = len(tensors)
            tensors.append(target)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_target_idx is not None:
            self._saved_fields['target'] = self._saved_tensors_list[self._saved_target_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        target = _saved[self._saved_target_idx]
        weight = _saved[self._saved_weight_idx] if self._saved_weight_idx is not None else None
        reduction = self._reduction
        ignore_index = self._ignore_index
        with _grad_context(keyset):
            grad_grad_output = redispatch("nll_loss_symint", keyset, grad, target, weight, reduction, ignore_index)
            grad_self = grad._zeros_like()
        return (grad_grad_output, grad_self,)

class NllLoss2dBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='NllLoss2dBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_target_idx = None
        self._saved_weight_idx = None
        self._reduction = None
        self._ignore_index = None

    def _save(self, *, target=None, weight=None):
        tensors = []
        if target is not None:
            self._saved_target_idx = len(tensors)
            tensors.append(target)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_target_idx is not None:
            self._saved_fields['target'] = self._saved_tensors_list[self._saved_target_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        target = _saved[self._saved_target_idx]
        weight = _saved[self._saved_weight_idx] if self._saved_weight_idx is not None else None
        reduction = self._reduction
        ignore_index = self._ignore_index
        with _grad_context(keyset):
            grad_grad_output = redispatch("nll_loss2d_symint", keyset, grad, target, weight, reduction, ignore_index)
            grad_self = grad._zeros_like()
        return (grad_grad_output, grad_self,)

class RreluWithNoiseBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='RreluWithNoiseBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_noise_idx = None
        self._saved_self_idx = None
        self._lower = None
        self._upper = None
        self._training = None
        self._self_is_result = None

    def _save(self, *, noise=None, self_=None):
        tensors = []
        if noise is not None:
            self._saved_noise_idx = len(tensors)
            tensors.append(noise)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_noise_idx is not None:
            self._saved_fields['noise'] = self._saved_tensors_list[self._saved_noise_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        noise = _saved[self._saved_noise_idx]
        self_ = _saved[self._saved_self_idx]
        lower = self._lower
        upper = self._upper
        training = self._training
        self_is_result = self._self_is_result
        with _grad_context(keyset):
            grad_grad_output = redispatch("rrelu_with_noise_backward", keyset, grad, self_, noise, lower, upper, training, False)
            grad_self = grad._zeros_like()
        return (grad_grad_output, grad_self,)

class ReflectionPad1dBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ReflectionPad1dBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._padding = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        padding = self._padding
        with _grad_context(keyset):
            grad_grad_output = redispatch("reflection_pad1d_symint", keyset, grad, padding)
            grad_self = self_._zeros_like()
        return (grad_grad_output, grad_self,)

class ReflectionPad2dBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ReflectionPad2dBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._padding = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        padding = self._padding
        with _grad_context(keyset):
            grad_grad_output = redispatch("reflection_pad2d_symint", keyset, grad, padding)
            grad_self = self_._zeros_like()
        return (grad_grad_output, grad_self,)

class ReflectionPad3dBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ReflectionPad3dBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._padding = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        padding = self._padding
        with _grad_context(keyset):
            grad_grad_output = redispatch("reflection_pad3d_symint", keyset, grad, padding)
            grad_self = self_._zeros_like()
        return (grad_grad_output, grad_self,)

class ReplicationPad1dBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ReplicationPad1dBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._padding = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        padding = self._padding
        with _grad_context(keyset):
            grad_grad_output = redispatch("replication_pad1d_symint", keyset, grad, padding)
            grad_self = self_._zeros_like()
        return (grad_grad_output, grad_self,)

class ReplicationPad2dBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ReplicationPad2dBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._padding = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        padding = self._padding
        with _grad_context(keyset):
            grad_grad_output = redispatch("replication_pad2d_symint", keyset, grad, padding)
            grad_self = self_._zeros_like()
        return (grad_grad_output, grad_self,)

class ReplicationPad3dBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ReplicationPad3dBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._padding = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        padding = self._padding
        with _grad_context(keyset):
            grad_grad_output = redispatch("replication_pad3d_symint", keyset, grad, padding)
            grad_self = self_._zeros_like()
        return (grad_grad_output, grad_self,)

class SparseSampledAddmmBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SparseSampledAddmmBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_mat1_idx = None
        self._saved_mat2_idx = None
        self._saved_self_idx = None
        self._beta = None
        self._alpha = None

    def _save(self, *, mat1=None, mat2=None, self_=None):
        tensors = []
        if mat1 is not None:
            self._saved_mat1_idx = len(tensors)
            tensors.append(mat1)
        if mat2 is not None:
            self._saved_mat2_idx = len(tensors)
            tensors.append(mat2)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_mat1_idx is not None:
            self._saved_fields['mat1'] = self._saved_tensors_list[self._saved_mat1_idx]
        if self._saved_mat2_idx is not None:
            self._saved_fields['mat2'] = self._saved_tensors_list[self._saved_mat2_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        mat1 = _saved[self._saved_mat1_idx]
        mat2 = _saved[self._saved_mat2_idx]
        self_ = _saved[self._saved_self_idx]
        beta = self._beta
        alpha = self._alpha
        grad_input_mask = [True, True, True]
        with _grad_context(keyset):
            grad_self, grad_mat1, grad_mat2 = redispatch("sparse_sampled_addmm_backward", keyset, grad, self_, redispatch("wrap_opt_if", keyset, mat1, True), redispatch("wrap_opt_if", keyset, mat2, True), alpha, beta, grad_input_mask)
        return (grad_self, grad_mat1, grad_mat2,)

class SparseMmReduceImplBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SparseMmReduceImplBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_other_idx = None
        self._saved_self_idx = None
        self._saved_result1_idx = None
        self._reduce = None

    def _save(self, *, other=None, self_=None, result1=None):
        tensors = []
        if other is not None:
            self._saved_other_idx = len(tensors)
            tensors.append(other)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result1 is not None:
            self._saved_result1_idx = len(tensors)
            tensors.append(result1)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_other_idx is not None:
            self._saved_fields['other'] = self._saved_tensors_list[self._saved_other_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result1_idx is not None:
            self._saved_fields['result1'] = self._saved_tensors_list[self._saved_result1_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        other = _saved[self._saved_other_idx]
        self_ = _saved[self._saved_self_idx]
        result1 = _saved[self._saved_result1_idx]
        reduce = self._reduce
        grad_input_mask = [True, True]
        with _grad_context(keyset):
            grad_self, grad_other = (redispatch("_sparse_mm_reduce_impl_backward", keyset, self_, grad, other, reduce, result1, grad_input_mask) if (grad is not None) else (None, None))
        return (grad_self, grad_other,)

class SmoothL1LossBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SmoothL1LossBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_target_idx = None
        self._reduction = None
        self._beta = None

    def _save(self, *, self_=None, target=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if target is not None:
            self._saved_target_idx = len(tensors)
            tensors.append(target)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_target_idx is not None:
            self._saved_fields['target'] = self._saved_tensors_list[self._saved_target_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        target = _saved[self._saved_target_idx]
        grad_output = self.inputs[0]
        reduction = self._reduction
        beta = self._beta
        with _grad_context(keyset):
            grad_grad_output = redispatch("smooth_l1_loss_backward", keyset, grad, self_, target, reduction, beta)
            grad_self = redispatch("smooth_l1_loss_double_backward", keyset, redispatch("mul", keyset, grad, grad_output), self_, target, reduction, beta)
            grad_target = redispatch("neg", keyset, redispatch("smooth_l1_loss_double_backward", keyset, redispatch("mul", keyset, grad, grad_output), self_, target, reduction, beta))
        return (grad_grad_output, grad_self, grad_target,)

class HuberLossBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='HuberLossBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_target_idx = None
        self._reduction = None
        self._delta = None

    def _save(self, *, self_=None, target=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if target is not None:
            self._saved_target_idx = len(tensors)
            tensors.append(target)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_target_idx is not None:
            self._saved_fields['target'] = self._saved_tensors_list[self._saved_target_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        target = _saved[self._saved_target_idx]
        grad_output = self.inputs[0]
        reduction = self._reduction
        delta = self._delta
        with _grad_context(keyset):
            grad_grad_output = redispatch("huber_loss_double_backward_grad_output", keyset, grad, grad_output, self_, target, reduction, delta)
            grad_self = redispatch("huber_loss_double_backward", keyset, redispatch("mul", keyset, grad, grad_output), self_, target, reduction, delta)
            grad_target = redispatch("neg", keyset, redispatch("huber_loss_double_backward", keyset, redispatch("mul", keyset, grad, grad_output), self_, target, reduction, delta))
        return (grad_grad_output, grad_self, grad_target,)

class SoftplusBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SoftplusBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._beta = None
        self._threshold = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        grad_output = self.inputs[0]
        beta = self._beta
        threshold = self._threshold
        with _grad_context(keyset):
            grad_grad_output = _softplus_backward_helper(grad, self_, beta, threshold, keyset)
            grad_self = redispatch("softplus_double_backward", keyset, redispatch("mul", keyset, grad, grad_output), self_, beta, threshold)
        return (grad_grad_output, grad_self,)

class SoftmaxBackwardDataBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SoftmaxBackwardDataBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_output_idx = None
        self._dim = None
        self._input_dtype = None

    def _save(self, *, output=None):
        tensors = []
        if output is not None:
            self._saved_output_idx = len(tensors)
            tensors.append(output)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_output_idx is not None:
            self._saved_fields['output'] = self._saved_tensors_list[self._saved_output_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        output = _saved[self._saved_output_idx]
        grad_output = self.inputs[0]
        dim = self._dim
        input_dtype = self._input_dtype
        with _grad_context(keyset):
            grad_grad_output = redispatch("_softmax_backward_data", keyset, redispatch("to", keyset, grad, redispatch("dtype", keyset, output)), output, dim, input_dtype)
            grad_output = redispatch("to", keyset, redispatch("softmax_double_backward", keyset, redispatch("to", keyset, grad, redispatch("dtype", keyset, output)), grad_output, dim, output), redispatch("dtype", keyset, output))
        return (grad_grad_output, grad_output,)

class SoftMarginLossBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SoftMarginLossBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_target_idx = None
        self._reduction = None

    def _save(self, *, self_=None, target=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if target is not None:
            self._saved_target_idx = len(tensors)
            tensors.append(target)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_target_idx is not None:
            self._saved_fields['target'] = self._saved_tensors_list[self._saved_target_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        target = _saved[self._saved_target_idx]
        grad_output = self.inputs[0]
        reduction = self._reduction
        with _grad_context(keyset):
            grad_grad_output = redispatch("soft_margin_loss_double_backward_grad_output", keyset, grad, grad_output, self_, target, reduction)
            grad_self = redispatch("soft_margin_loss_double_backward", keyset, redispatch("mul", keyset, grad, grad_output), self_, target, reduction)
        return (grad_grad_output, grad_self,)

class SoftshrinkBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SoftshrinkBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._lambd = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        lambd = self._lambd
        with _grad_context(keyset):
            grad_grad_output = redispatch("softshrink_backward", keyset, grad, self_, lambd)
            grad_self = grad._zeros_like()
        return (grad_grad_output, grad_self,)

class ThresholdBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ThresholdBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._threshold = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        threshold = self._threshold
        with _grad_context(keyset):
            grad_grad_output = _threshold_backward_helper(grad, self_, threshold, keyset)
            grad_self = grad._zeros_like()
        return (grad_grad_output, grad_self,)

class UpsampleLinear1dBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='UpsampleLinear1dBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._output_size = None
        self._input_size = None
        self._align_corners = None
        self._scales = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        output_size = self._output_size
        input_size = self._input_size
        align_corners = self._align_corners
        scales = self._scales
        with _grad_context(keyset):
            grad_grad_output = redispatch("upsample_linear1d_symint", keyset, grad, output_size, align_corners, scales)
        return (grad_grad_output,)

class UpsampleBilinear2dBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='UpsampleBilinear2dBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._output_size = None
        self._input_size = None
        self._align_corners = None
        self._scales_h = None
        self._scales_w = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        output_size = self._output_size
        input_size = self._input_size
        align_corners = self._align_corners
        scales_h = self._scales_h
        scales_w = self._scales_w
        with _grad_context(keyset):
            grad_grad_output = redispatch("upsample_bilinear2d_symint", keyset, grad, output_size, align_corners, scales_h, scales_w)
        return (grad_grad_output,)

class UpsampleBilinear2dAaBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='UpsampleBilinear2dAaBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._output_size = None
        self._input_size = None
        self._align_corners = None
        self._scales_h = None
        self._scales_w = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        output_size = self._output_size
        input_size = self._input_size
        align_corners = self._align_corners
        scales_h = self._scales_h
        scales_w = self._scales_w
        with _grad_context(keyset):
            grad_grad_output = redispatch("_upsample_bilinear2d_aa_symint", keyset, grad, output_size, align_corners, scales_h, scales_w)
        return (grad_grad_output,)

class UpsampleBicubic2dBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='UpsampleBicubic2dBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._output_size = None
        self._input_size = None
        self._align_corners = None
        self._scales_h = None
        self._scales_w = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        output_size = self._output_size
        input_size = self._input_size
        align_corners = self._align_corners
        scales_h = self._scales_h
        scales_w = self._scales_w
        with _grad_context(keyset):
            grad_grad_output = redispatch("upsample_bicubic2d_symint", keyset, grad, output_size, align_corners, scales_h, scales_w)
        return (grad_grad_output,)

class UpsampleBicubic2dAaBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='UpsampleBicubic2dAaBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._output_size = None
        self._input_size = None
        self._align_corners = None
        self._scales_h = None
        self._scales_w = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        output_size = self._output_size
        input_size = self._input_size
        align_corners = self._align_corners
        scales_h = self._scales_h
        scales_w = self._scales_w
        with _grad_context(keyset):
            grad_grad_output = redispatch("_upsample_bicubic2d_aa_symint", keyset, grad, output_size, align_corners, scales_h, scales_w)
        return (grad_grad_output,)

class UpsampleTrilinear3dBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='UpsampleTrilinear3dBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._output_size = None
        self._input_size = None
        self._align_corners = None
        self._scales_d = None
        self._scales_h = None
        self._scales_w = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        output_size = self._output_size
        input_size = self._input_size
        align_corners = self._align_corners
        scales_d = self._scales_d
        scales_h = self._scales_h
        scales_w = self._scales_w
        with _grad_context(keyset):
            grad_grad_output = redispatch("upsample_trilinear3d_symint", keyset, grad, output_size, align_corners, scales_d, scales_h, scales_w)
        return (grad_grad_output,)

class UpsampleNearest1dBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='UpsampleNearest1dBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._output_size = None
        self._input_size = None
        self._scales = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        output_size = self._output_size
        input_size = self._input_size
        scales = self._scales
        with _grad_context(keyset):
            grad_grad_output = redispatch("upsample_nearest1d_symint", keyset, grad, output_size, scales)
        return (grad_grad_output,)

class UpsampleNearestExact1dBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='UpsampleNearestExact1dBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._output_size = None
        self._input_size = None
        self._scales = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        output_size = self._output_size
        input_size = self._input_size
        scales = self._scales
        with _grad_context(keyset):
            grad_grad_output = redispatch("_upsample_nearest_exact1d_symint", keyset, grad, output_size, scales)
        return (grad_grad_output,)

class UpsampleNearest2dBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='UpsampleNearest2dBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._output_size = None
        self._input_size = None
        self._scales_h = None
        self._scales_w = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        output_size = self._output_size
        input_size = self._input_size
        scales_h = self._scales_h
        scales_w = self._scales_w
        with _grad_context(keyset):
            grad_grad_output = redispatch("upsample_nearest2d_symint", keyset, grad, output_size, scales_h, scales_w)
        return (grad_grad_output,)

class UpsampleNearestExact2dBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='UpsampleNearestExact2dBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._output_size = None
        self._input_size = None
        self._scales_h = None
        self._scales_w = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        output_size = self._output_size
        input_size = self._input_size
        scales_h = self._scales_h
        scales_w = self._scales_w
        with _grad_context(keyset):
            grad_grad_output = redispatch("_upsample_nearest_exact2d_symint", keyset, grad, output_size, scales_h, scales_w)
        return (grad_grad_output,)

class UpsampleNearest3dBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='UpsampleNearest3dBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._output_size = None
        self._input_size = None
        self._scales_d = None
        self._scales_h = None
        self._scales_w = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        output_size = self._output_size
        input_size = self._input_size
        scales_d = self._scales_d
        scales_h = self._scales_h
        scales_w = self._scales_w
        with _grad_context(keyset):
            grad_grad_output = redispatch("upsample_nearest3d_symint", keyset, grad, output_size, scales_d, scales_h, scales_w)
        return (grad_grad_output,)

class UpsampleNearestExact3dBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='UpsampleNearestExact3dBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._output_size = None
        self._input_size = None
        self._scales_d = None
        self._scales_h = None
        self._scales_w = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        output_size = self._output_size
        input_size = self._input_size
        scales_d = self._scales_d
        scales_h = self._scales_h
        scales_w = self._scales_w
        with _grad_context(keyset):
            grad_grad_output = redispatch("_upsample_nearest_exact3d_symint", keyset, grad, output_size, scales_d, scales_h, scales_w)
        return (grad_grad_output,)

class SigmoidBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SigmoidBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_output_idx = None

    def _save(self, *, output=None):
        tensors = []
        if output is not None:
            self._saved_output_idx = len(tensors)
            tensors.append(output)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_output_idx is not None:
            self._saved_fields['output'] = self._saved_tensors_list[self._saved_output_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        output = _saved[self._saved_output_idx]
        grad_output = self.inputs[0]
        with _grad_context(keyset):
            grad_grad_output = _sigmoid_backward_helper(grad, output, keyset)
            grad_output = redispatch("mul", keyset, redispatch("mul", keyset, grad, grad_output), redispatch("add", keyset, redispatch("mul", keyset, redispatch("neg", keyset, 2), output), 1))
        return (grad_grad_output, grad_output,)

class TanhBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='TanhBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_output_idx = None

    def _save(self, *, output=None):
        tensors = []
        if output is not None:
            self._saved_output_idx = len(tensors)
            tensors.append(output)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_output_idx is not None:
            self._saved_fields['output'] = self._saved_tensors_list[self._saved_output_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        output = _saved[self._saved_output_idx]
        grad_output = self.inputs[0]
        with _grad_context(keyset):
            grad_grad_output = _tanh_backward_helper(grad, output, keyset)
            grad_output = redispatch("mul", keyset, grad, redispatch("mul", keyset, redispatch("mul", keyset, redispatch("neg", keyset, 2), output), grad_output))
        return (grad_grad_output, grad_output,)

class CudnnConvolutionTransposeBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='CudnnConvolutionTransposeBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_weight_idx = None
        self._padding = None
        self._output_padding = None
        self._stride = None
        self._dilation = None
        self._groups = None
        self._benchmark = None
        self._deterministic = None
        self._allow_tf32 = None

    def _save(self, *, self_=None, weight=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        weight = _saved[self._saved_weight_idx]
        padding = self._padding
        output_padding = self._output_padding
        stride = self._stride
        dilation = self._dilation
        groups = self._groups
        benchmark = self._benchmark
        deterministic = self._deterministic
        allow_tf32 = self._allow_tf32
        grad_input_mask = [True, True]
        with _grad_context(keyset):
            grad_self, grad_weight = redispatch("_cudnn_convolution_backward", keyset, self_, grad, weight, padding, output_padding, stride, dilation, True, groups, [True, True])
        return (grad_self, grad_weight,)

class MpsConvolutionTransposeBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MpsConvolutionTransposeBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_weight_idx = None
        self._padding = None
        self._output_padding = None
        self._stride = None
        self._dilation = None
        self._groups = None

    def _save(self, *, self_=None, weight=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        weight = _saved[self._saved_weight_idx]
        padding = self._padding
        output_padding = self._output_padding
        stride = self._stride
        dilation = self._dilation
        groups = self._groups
        grad_input_mask = [True, True]
        with _grad_context(keyset):
            grad_self, grad_weight = (redispatch("mps_convolution_transpose_backward_symint", keyset, self_, grad, weight, padding, output_padding, stride, dilation, groups, grad_input_mask) if (grad is not None) else (None, None))
        return (grad_self, grad_weight,)

class CudnnConvolutionBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='CudnnConvolutionBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_weight_idx = None
        self._padding = None
        self._stride = None
        self._dilation = None
        self._groups = None
        self._benchmark = None
        self._deterministic = None
        self._allow_tf32 = None

    def _save(self, *, self_=None, weight=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        weight = _saved[self._saved_weight_idx]
        padding = self._padding
        stride = self._stride
        dilation = self._dilation
        groups = self._groups
        benchmark = self._benchmark
        deterministic = self._deterministic
        allow_tf32 = self._allow_tf32
        grad_input_mask = [True, True]
        with _grad_context(keyset):
            grad_self, grad_weight = redispatch("_cudnn_convolution_backward", keyset, self_, grad, weight, padding, [0] * redispatch("size", keyset, padding), stride, dilation, False, groups, [True, True])
        return (grad_self, grad_weight,)

class CudnnGridSamplerBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='CudnnGridSamplerBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_grid_idx = None
        self._saved_self_idx = None

    def _save(self, *, grid=None, self_=None):
        tensors = []
        if grid is not None:
            self._saved_grid_idx = len(tensors)
            tensors.append(grid)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_grid_idx is not None:
            self._saved_fields['grid'] = self._saved_tensors_list[self._saved_grid_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        grid = _saved[self._saved_grid_idx]
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self, grad_grid = (redispatch("cudnn_grid_sampler_backward", keyset, self_, grid, grad) if (grad is not None) else (None, None))
        return (grad_self, grad_grid,)

class CudnnAffineGridGeneratorBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='CudnnAffineGridGeneratorBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._N = None
        self._C = None
        self._H = None
        self._W = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        N = self._N
        C = self._C
        H = self._H
        W = self._W
        with _grad_context(keyset):
            grad_theta = redispatch("cudnn_affine_grid_generator_backward", keyset, grad, N, C, H, W)
        return (grad_theta,)

class CudnnBatchNormBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='CudnnBatchNormBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._saved_running_mean_idx = None
        self._saved_running_var_idx = None
        self._saved_weight_idx = None
        self._saved_result1_idx = None
        self._saved_result2_idx = None
        self._saved_result3_idx = None
        self._training = None
        self._exponential_average_factor = None
        self._epsilon = None

    def _save(self, *, input_=None, running_mean=None, running_var=None, weight=None, result1=None, result2=None, result3=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if running_mean is not None:
            self._saved_running_mean_idx = len(tensors)
            tensors.append(running_mean)
        if running_var is not None:
            self._saved_running_var_idx = len(tensors)
            tensors.append(running_var)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if result1 is not None:
            self._saved_result1_idx = len(tensors)
            tensors.append(result1)
        if result2 is not None:
            self._saved_result2_idx = len(tensors)
            tensors.append(result2)
        if result3 is not None:
            self._saved_result3_idx = len(tensors)
            tensors.append(result3)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]
        if self._saved_running_mean_idx is not None:
            self._saved_fields['running_mean'] = self._saved_tensors_list[self._saved_running_mean_idx]
        if self._saved_running_var_idx is not None:
            self._saved_fields['running_var'] = self._saved_tensors_list[self._saved_running_var_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]
        if self._saved_result1_idx is not None:
            self._saved_fields['result1'] = self._saved_tensors_list[self._saved_result1_idx]
        if self._saved_result2_idx is not None:
            self._saved_fields['result2'] = self._saved_tensors_list[self._saved_result2_idx]
        if self._saved_result3_idx is not None:
            self._saved_fields['result3'] = self._saved_tensors_list[self._saved_result3_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        running_mean = _saved[self._saved_running_mean_idx] if self._saved_running_mean_idx is not None else None
        running_var = _saved[self._saved_running_var_idx] if self._saved_running_var_idx is not None else None
        weight = _saved[self._saved_weight_idx]
        result1 = _saved[self._saved_result1_idx]
        result2 = _saved[self._saved_result2_idx]
        result3 = _saved[self._saved_result3_idx]
        training = self._training
        exponential_average_factor = self._exponential_average_factor
        epsilon = self._epsilon
        grad_input_mask = [True, True, (bias is not None)]
        with _grad_context(keyset):
            grad_input, grad_weight, grad_bias = ((redispatch("cudnn_batch_norm_backward", keyset, input_, redispatch("contiguous", keyset, grad, redispatch("suggest_memory_format", keyset, input_)), weight, running_mean, running_var, result1, result2, epsilon, (redispatch("clone", keyset, result3) if retain_variables else result3)) if training else redispatch("native_batch_norm_backward", keyset, grad, input_, weight, running_mean, running_var, result1, result2, training, epsilon, grad_input_mask)) if (grad is not None) else (None, None, None))
        return (grad_input, grad_weight, grad_bias,)

class CudnnBatchNormBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='CudnnBatchNormBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_save_mean_idx = None
        self._saved_save_var_idx = None
        self._saved_reserveSpace_idx = None
        self._saved_input_idx = None
        self._saved_running_mean_idx = None
        self._saved_running_var_idx = None
        self._saved_weight_idx = None
        self._epsilon = None

    def _save(self, *, save_mean=None, save_var=None, reserveSpace=None, input_=None, running_mean=None, running_var=None, weight=None):
        tensors = []
        if save_mean is not None:
            self._saved_save_mean_idx = len(tensors)
            tensors.append(save_mean)
        if save_var is not None:
            self._saved_save_var_idx = len(tensors)
            tensors.append(save_var)
        if reserveSpace is not None:
            self._saved_reserveSpace_idx = len(tensors)
            tensors.append(reserveSpace)
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if running_mean is not None:
            self._saved_running_mean_idx = len(tensors)
            tensors.append(running_mean)
        if running_var is not None:
            self._saved_running_var_idx = len(tensors)
            tensors.append(running_var)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_save_mean_idx is not None:
            self._saved_fields['save_mean'] = self._saved_tensors_list[self._saved_save_mean_idx]
        if self._saved_save_var_idx is not None:
            self._saved_fields['save_var'] = self._saved_tensors_list[self._saved_save_var_idx]
        if self._saved_reserveSpace_idx is not None:
            self._saved_fields['reserveSpace'] = self._saved_tensors_list[self._saved_reserveSpace_idx]
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]
        if self._saved_running_mean_idx is not None:
            self._saved_fields['running_mean'] = self._saved_tensors_list[self._saved_running_mean_idx]
        if self._saved_running_var_idx is not None:
            self._saved_fields['running_var'] = self._saved_tensors_list[self._saved_running_var_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        save_mean = _saved[self._saved_save_mean_idx] if self._saved_save_mean_idx is not None else None
        save_var = _saved[self._saved_save_var_idx] if self._saved_save_var_idx is not None else None
        reserveSpace = _saved[self._saved_reserveSpace_idx]
        input_ = _saved[self._saved_input_idx]
        running_mean = _saved[self._saved_running_mean_idx] if self._saved_running_mean_idx is not None else None
        running_var = _saved[self._saved_running_var_idx] if self._saved_running_var_idx is not None else None
        weight = _saved[self._saved_weight_idx]
        grad_output = self.inputs[1]
        epsilon = self._epsilon
        grad_input_mask = [True, True, True, (save_mean is not None), (save_var is not None), True]
        with _grad_context(keyset):
            grad_save_mean = _raise_not_implemented("cudnn_batch_norm_backward save_mean")
            grad_save_var = _raise_not_implemented("cudnn_batch_norm_backward save_var")
            grad_reserveSpace = _raise_not_implemented("cudnn_batch_norm_backward reserveSpace")
            grad_input, grad_weight, grad_grad_output = redispatch("batchnorm_double_backward", keyset, input_, weight, grads[0], grads[1], grads[2], grad_output, running_mean, running_var, True, epsilon, save_mean, save_var, grad_input_mask)
        return (grad_input, grad_grad_output, grad_weight, grad_save_mean, grad_save_var, grad_reserveSpace,)

class NnpackSpatialConvolutionBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='NnpackSpatialConvolutionBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_bias_idx = None
        self._saved_input_idx = None
        self._saved_weight_idx = None
        self._padding = None
        self._stride = None

    def _save(self, *, bias=None, input_=None, weight=None):
        tensors = []
        if bias is not None:
            self._saved_bias_idx = len(tensors)
            tensors.append(bias)
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_bias_idx is not None:
            self._saved_fields['bias'] = self._saved_tensors_list[self._saved_bias_idx]
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        bias = _saved[self._saved_bias_idx] if self._saved_bias_idx is not None else None
        input_ = _saved[self._saved_input_idx]
        weight = _saved[self._saved_weight_idx]
        padding = self._padding
        stride = self._stride
        grad_input_mask = [True, True, (bias is not None)]
        with _grad_context(keyset):
            grad_input, grad_weight, grad_bias = (redispatch("convolution_backward_symint", keyset, grad, input_, weight, bias.shape, stride, padding, [1] * redispatch("size", keyset, padding), False, [0] * redispatch("size", keyset, padding), 1, grad_input_mask) if (grad is not None) else (None, None, None))
        return (grad_input, grad_weight, grad_bias,)

class LstmMpsBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='LstmMpsBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._saved_result3_idx = None
        self._saved_result4_idx = None
        self._saved_result5_idx = None
        self._has_biases = None
        self._num_layers = None
        self._dropout = None
        self._train = None
        self._bidirectional = None
        self._batch_first = None

    def _save(self, *, input_=None, result3=None, result4=None, result5=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if result3 is not None:
            self._saved_result3_idx = len(tensors)
            tensors.append(result3)
        if result4 is not None:
            self._saved_result4_idx = len(tensors)
            tensors.append(result4)
        if result5 is not None:
            self._saved_result5_idx = len(tensors)
            tensors.append(result5)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]
        if self._saved_result3_idx is not None:
            self._saved_fields['result3'] = self._saved_tensors_list[self._saved_result3_idx]
        if self._saved_result4_idx is not None:
            self._saved_fields['result4'] = self._saved_tensors_list[self._saved_result4_idx]
        if self._saved_result5_idx is not None:
            self._saved_fields['result5'] = self._saved_tensors_list[self._saved_result5_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        result3 = _saved[self._saved_result3_idx]
        result4 = _saved[self._saved_result4_idx]
        result5 = _saved[self._saved_result5_idx]
        hx = list(self.inputs)
        params = list(self.inputs)
        has_biases = self._has_biases
        num_layers = self._num_layers
        dropout = self._dropout
        train = self._train
        bidirectional = self._bidirectional
        batch_first = self._batch_first
        with _grad_context(keyset):
            grad_input, grad_hx, grad_params = redispatch("lstm_mps_backward", keyset, grads[0], grads[1], grads[2], result3, result4, input_, result5, hx, params, has_biases, num_layers, dropout, train, bidirectional, batch_first)
        return (grad_input, grad_hx, grad_params,)

class LstmMpsBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='LstmMpsBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._has_biases = None
        self._num_layers = None
        self._dropout = None
        self._train = None
        self._bidirectional = None
        self._batch_first = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        has_biases = self._has_biases
        num_layers = self._num_layers
        dropout = self._dropout
        train = self._train
        bidirectional = self._bidirectional
        batch_first = self._batch_first
        return (grad,)

class MiopenConvolutionTransposeBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MiopenConvolutionTransposeBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_bias_idx = None
        self._saved_self_idx = None
        self._saved_weight_idx = None
        self._padding = None
        self._output_padding = None
        self._stride = None
        self._dilation = None
        self._groups = None
        self._benchmark = None
        self._deterministic = None

    def _save(self, *, bias=None, self_=None, weight=None):
        tensors = []
        if bias is not None:
            self._saved_bias_idx = len(tensors)
            tensors.append(bias)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_bias_idx is not None:
            self._saved_fields['bias'] = self._saved_tensors_list[self._saved_bias_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        bias = _saved[self._saved_bias_idx] if self._saved_bias_idx is not None else None
        self_ = _saved[self._saved_self_idx]
        weight = _saved[self._saved_weight_idx]
        padding = self._padding
        output_padding = self._output_padding
        stride = self._stride
        dilation = self._dilation
        groups = self._groups
        benchmark = self._benchmark
        deterministic = self._deterministic
        grad_input_mask = [True, True, (bias is not None)]
        with _grad_context(keyset):
            grad_self, grad_weight, grad_bias = (redispatch("convolution_backward_symint", keyset, grad, self_, weight, bias.shape, stride, padding, dilation, True, output_padding, groups, grad_input_mask) if (grad is not None) else (None, None, None))
        return (grad_self, grad_weight, grad_bias,)

class MiopenConvolutionBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MiopenConvolutionBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_bias_idx = None
        self._saved_self_idx = None
        self._saved_weight_idx = None
        self._padding = None
        self._stride = None
        self._dilation = None
        self._groups = None
        self._benchmark = None
        self._deterministic = None

    def _save(self, *, bias=None, self_=None, weight=None):
        tensors = []
        if bias is not None:
            self._saved_bias_idx = len(tensors)
            tensors.append(bias)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_bias_idx is not None:
            self._saved_fields['bias'] = self._saved_tensors_list[self._saved_bias_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        bias = _saved[self._saved_bias_idx] if self._saved_bias_idx is not None else None
        self_ = _saved[self._saved_self_idx]
        weight = _saved[self._saved_weight_idx]
        padding = self._padding
        stride = self._stride
        dilation = self._dilation
        groups = self._groups
        benchmark = self._benchmark
        deterministic = self._deterministic
        grad_input_mask = [True, True, (bias is not None)]
        with _grad_context(keyset):
            grad_self, grad_weight, grad_bias = (redispatch("convolution_backward_symint", keyset, grad, self_, weight, bias.shape, stride, padding, dilation, False, [0] * redispatch("size", keyset, padding), groups, grad_input_mask) if (grad is not None) else (None, None, None))
        return (grad_self, grad_weight, grad_bias,)

class MiopenDepthwiseConvolutionBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MiopenDepthwiseConvolutionBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_bias_idx = None
        self._saved_self_idx = None
        self._saved_weight_idx = None
        self._padding = None
        self._stride = None
        self._dilation = None
        self._groups = None
        self._benchmark = None
        self._deterministic = None

    def _save(self, *, bias=None, self_=None, weight=None):
        tensors = []
        if bias is not None:
            self._saved_bias_idx = len(tensors)
            tensors.append(bias)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_bias_idx is not None:
            self._saved_fields['bias'] = self._saved_tensors_list[self._saved_bias_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        bias = _saved[self._saved_bias_idx] if self._saved_bias_idx is not None else None
        self_ = _saved[self._saved_self_idx]
        weight = _saved[self._saved_weight_idx]
        padding = self._padding
        stride = self._stride
        dilation = self._dilation
        groups = self._groups
        benchmark = self._benchmark
        deterministic = self._deterministic
        grad_input_mask = [True, True, (bias is not None)]
        with _grad_context(keyset):
            grad_self, grad_weight, grad_bias = (redispatch("convolution_backward_symint", keyset, grad, self_, weight, bias.shape, stride, padding, dilation, False, [0] * redispatch("size", keyset, padding), groups, grad_input_mask) if (grad is not None) else (None, None, None))
        return (grad_self, grad_weight, grad_bias,)

class MiopenBatchNormBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MiopenBatchNormBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._saved_running_mean_idx = None
        self._saved_running_var_idx = None
        self._saved_weight_idx = None
        self._saved_result1_idx = None
        self._saved_result2_idx = None
        self._training = None
        self._exponential_average_factor = None
        self._epsilon = None

    def _save(self, *, input_=None, running_mean=None, running_var=None, weight=None, result1=None, result2=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if running_mean is not None:
            self._saved_running_mean_idx = len(tensors)
            tensors.append(running_mean)
        if running_var is not None:
            self._saved_running_var_idx = len(tensors)
            tensors.append(running_var)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if result1 is not None:
            self._saved_result1_idx = len(tensors)
            tensors.append(result1)
        if result2 is not None:
            self._saved_result2_idx = len(tensors)
            tensors.append(result2)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]
        if self._saved_running_mean_idx is not None:
            self._saved_fields['running_mean'] = self._saved_tensors_list[self._saved_running_mean_idx]
        if self._saved_running_var_idx is not None:
            self._saved_fields['running_var'] = self._saved_tensors_list[self._saved_running_var_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]
        if self._saved_result1_idx is not None:
            self._saved_fields['result1'] = self._saved_tensors_list[self._saved_result1_idx]
        if self._saved_result2_idx is not None:
            self._saved_fields['result2'] = self._saved_tensors_list[self._saved_result2_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        running_mean = _saved[self._saved_running_mean_idx] if self._saved_running_mean_idx is not None else None
        running_var = _saved[self._saved_running_var_idx] if self._saved_running_var_idx is not None else None
        weight = _saved[self._saved_weight_idx]
        result1 = _saved[self._saved_result1_idx]
        result2 = _saved[self._saved_result2_idx]
        training = self._training
        exponential_average_factor = self._exponential_average_factor
        epsilon = self._epsilon
        grad_input_mask = [True, True, (bias is not None)]
        with _grad_context(keyset):
            grad_input, grad_weight, grad_bias = ((redispatch("miopen_batch_norm_backward", keyset, input_, redispatch("contiguous", keyset, grad, redispatch("suggest_memory_format", keyset, input_)), weight, running_mean, running_var, result1, result2, epsilon) if training else redispatch("native_batch_norm_backward", keyset, grad, input_, weight, running_mean, running_var, result1, result2, training, epsilon, grad_input_mask)) if (grad is not None) else (None, None, None))
        return (grad_input, grad_weight, grad_bias,)

class MiopenBatchNormBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MiopenBatchNormBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_save_mean_idx = None
        self._saved_save_var_idx = None
        self._saved_input_idx = None
        self._saved_running_mean_idx = None
        self._saved_running_var_idx = None
        self._saved_weight_idx = None
        self._epsilon = None

    def _save(self, *, save_mean=None, save_var=None, input_=None, running_mean=None, running_var=None, weight=None):
        tensors = []
        if save_mean is not None:
            self._saved_save_mean_idx = len(tensors)
            tensors.append(save_mean)
        if save_var is not None:
            self._saved_save_var_idx = len(tensors)
            tensors.append(save_var)
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if running_mean is not None:
            self._saved_running_mean_idx = len(tensors)
            tensors.append(running_mean)
        if running_var is not None:
            self._saved_running_var_idx = len(tensors)
            tensors.append(running_var)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_save_mean_idx is not None:
            self._saved_fields['save_mean'] = self._saved_tensors_list[self._saved_save_mean_idx]
        if self._saved_save_var_idx is not None:
            self._saved_fields['save_var'] = self._saved_tensors_list[self._saved_save_var_idx]
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]
        if self._saved_running_mean_idx is not None:
            self._saved_fields['running_mean'] = self._saved_tensors_list[self._saved_running_mean_idx]
        if self._saved_running_var_idx is not None:
            self._saved_fields['running_var'] = self._saved_tensors_list[self._saved_running_var_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        save_mean = _saved[self._saved_save_mean_idx] if self._saved_save_mean_idx is not None else None
        save_var = _saved[self._saved_save_var_idx] if self._saved_save_var_idx is not None else None
        input_ = _saved[self._saved_input_idx]
        running_mean = _saved[self._saved_running_mean_idx] if self._saved_running_mean_idx is not None else None
        running_var = _saved[self._saved_running_var_idx] if self._saved_running_var_idx is not None else None
        weight = _saved[self._saved_weight_idx]
        grad_output = self.inputs[1]
        epsilon = self._epsilon
        grad_input_mask = [True, True, True, (save_mean is not None), (save_var is not None)]
        with _grad_context(keyset):
            grad_save_mean = _raise_not_implemented("miopen_batch_norm_backward save_mean")
            grad_save_var = _raise_not_implemented("miopen_batch_norm_backward save_var")
            grad_input, grad_weight, grad_grad_output = redispatch("batchnorm_double_backward", keyset, input_, weight, grads[0], grads[1], grads[2], grad_output, running_mean, running_var, True, epsilon, save_mean, save_var, grad_input_mask)
        return (grad_input, grad_grad_output, grad_weight, grad_save_mean, grad_save_var,)

class MiopenRnnBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MiopenRnnBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_cx_idx = None
        self._saved_dropout_state_idx = None
        self._saved_hx_idx = None
        self._saved_input_idx = None
        self._saved_result0_idx = None
        self._saved_result3_idx = None
        self._saved_result4_idx = None
        self._weight_stride0 = None
        self._mode = None
        self._hidden_size = None
        self._num_layers = None
        self._batch_first = None
        self._dropout = None
        self._train = None
        self._bidirectional = None
        self._batch_sizes = None

    def _save(self, *, cx=None, dropout_state=None, hx=None, input_=None, result0=None, result3=None, result4=None):
        tensors = []
        if cx is not None:
            self._saved_cx_idx = len(tensors)
            tensors.append(cx)
        if dropout_state is not None:
            self._saved_dropout_state_idx = len(tensors)
            tensors.append(dropout_state)
        if hx is not None:
            self._saved_hx_idx = len(tensors)
            tensors.append(hx)
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if result0 is not None:
            self._saved_result0_idx = len(tensors)
            tensors.append(result0)
        if result3 is not None:
            self._saved_result3_idx = len(tensors)
            tensors.append(result3)
        if result4 is not None:
            self._saved_result4_idx = len(tensors)
            tensors.append(result4)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_cx_idx is not None:
            self._saved_fields['cx'] = self._saved_tensors_list[self._saved_cx_idx]
        if self._saved_dropout_state_idx is not None:
            self._saved_fields['dropout_state'] = self._saved_tensors_list[self._saved_dropout_state_idx]
        if self._saved_hx_idx is not None:
            self._saved_fields['hx'] = self._saved_tensors_list[self._saved_hx_idx]
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]
        if self._saved_result0_idx is not None:
            self._saved_fields['result0'] = self._saved_tensors_list[self._saved_result0_idx]
        if self._saved_result3_idx is not None:
            self._saved_fields['result3'] = self._saved_tensors_list[self._saved_result3_idx]
        if self._saved_result4_idx is not None:
            self._saved_fields['result4'] = self._saved_tensors_list[self._saved_result4_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        cx = _saved[self._saved_cx_idx] if self._saved_cx_idx is not None else None
        dropout_state = _saved[self._saved_dropout_state_idx] if self._saved_dropout_state_idx is not None else None
        hx = _saved[self._saved_hx_idx]
        input_ = _saved[self._saved_input_idx]
        result0 = _saved[self._saved_result0_idx]
        result3 = _saved[self._saved_result3_idx]
        result4 = _saved[self._saved_result4_idx]
        weight = list(self.inputs)
        weight_stride0 = self._weight_stride0
        mode = self._mode
        hidden_size = self._hidden_size
        num_layers = self._num_layers
        batch_first = self._batch_first
        dropout = self._dropout
        train = self._train
        bidirectional = self._bidirectional
        batch_sizes = self._batch_sizes
        grad_input_mask = [True, True, True, (cx is not None)]
        with _grad_context(keyset):
            grad_input, grad_hx, grad_cx, grad_weight = redispatch("miopen_rnn_backward", keyset, input_, weight, weight_stride0, result4, hx, cx, result0, grads[0], grads[1], grads[2], mode, hidden_size, num_layers, batch_first, dropout, train, bidirectional, batch_sizes, dropout_state, (redispatch("clone", keyset, result3) if retain_variables else result3), grad_input_mask)
        return (grad_input, grad_weight, grad_hx, grad_cx,)

class MiopenRnnBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MiopenRnnBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._weight_stride0 = None
        self._mode = None
        self._hidden_size = None
        self._num_layers = None
        self._batch_first = None
        self._dropout = None
        self._train = None
        self._bidirectional = None
        self._batch_sizes = None
        self._output_mask = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        weight_stride0 = self._weight_stride0
        mode = self._mode
        hidden_size = self._hidden_size
        num_layers = self._num_layers
        batch_first = self._batch_first
        dropout = self._dropout
        train = self._train
        bidirectional = self._bidirectional
        batch_sizes = self._batch_sizes
        output_mask = self._output_mask
        return (grad,)

class MiopenCtcLossBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MiopenCtcLossBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_result0_idx = None
        self._saved_result1_idx = None
        self._input_lengths = None
        self._target_lengths = None
        self._blank = None
        self._deterministic = None
        self._zero_infinity = None

    def _save(self, *, result0=None, result1=None):
        tensors = []
        if result0 is not None:
            self._saved_result0_idx = len(tensors)
            tensors.append(result0)
        if result1 is not None:
            self._saved_result1_idx = len(tensors)
            tensors.append(result1)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_result0_idx is not None:
            self._saved_fields['result0'] = self._saved_tensors_list[self._saved_result0_idx]
        if self._saved_result1_idx is not None:
            self._saved_fields['result1'] = self._saved_tensors_list[self._saved_result1_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        result0 = _saved[self._saved_result0_idx]
        result1 = _saved[self._saved_result1_idx]
        input_lengths = self._input_lengths
        target_lengths = self._target_lengths
        blank = self._blank
        deterministic = self._deterministic
        zero_infinity = self._zero_infinity
        with _grad_context(keyset):
            grad_log_probs = redispatch("_miopen_ctc_loss_backward", keyset, grad, result0, result1, zero_infinity)
        return (grad_log_probs,)

class MiopenCtcLossTensorBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MiopenCtcLossTensorBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_result0_idx = None
        self._saved_result1_idx = None
        self._blank = None
        self._deterministic = None
        self._zero_infinity = None

    def _save(self, *, result0=None, result1=None):
        tensors = []
        if result0 is not None:
            self._saved_result0_idx = len(tensors)
            tensors.append(result0)
        if result1 is not None:
            self._saved_result1_idx = len(tensors)
            tensors.append(result1)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_result0_idx is not None:
            self._saved_fields['result0'] = self._saved_tensors_list[self._saved_result0_idx]
        if self._saved_result1_idx is not None:
            self._saved_fields['result1'] = self._saved_tensors_list[self._saved_result1_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        result0 = _saved[self._saved_result0_idx]
        result1 = _saved[self._saved_result1_idx]
        blank = self._blank
        deterministic = self._deterministic
        zero_infinity = self._zero_infinity
        with _grad_context(keyset):
            grad_log_probs = redispatch("_miopen_ctc_loss_backward", keyset, grad, result0, result1, zero_infinity)
        return (grad_log_probs,)

class MkldnnRnnLayerBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MkldnnRnnLayerBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_cx__idx = None
        self._saved_hx__idx = None
        self._saved_input_idx = None
        self._saved_weight0_idx = None
        self._saved_weight1_idx = None
        self._saved_weight2_idx = None
        self._saved_weight3_idx = None
        self._saved_result0_idx = None
        self._saved_result1_idx = None
        self._saved_result2_idx = None
        self._saved_result3_idx = None
        self._reverse = None
        self._batch_sizes = None
        self._mode = None
        self._hidden_size = None
        self._num_layers = None
        self._has_biases = None
        self._bidirectional = None
        self._batch_first = None
        self._train = None

    def _save(self, *, cx_=None, hx_=None, input_=None, weight0=None, weight1=None, weight2=None, weight3=None, result0=None, result1=None, result2=None, result3=None):
        tensors = []
        if cx_ is not None:
            self._saved_cx__idx = len(tensors)
            tensors.append(cx_)
        if hx_ is not None:
            self._saved_hx__idx = len(tensors)
            tensors.append(hx_)
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if weight0 is not None:
            self._saved_weight0_idx = len(tensors)
            tensors.append(weight0)
        if weight1 is not None:
            self._saved_weight1_idx = len(tensors)
            tensors.append(weight1)
        if weight2 is not None:
            self._saved_weight2_idx = len(tensors)
            tensors.append(weight2)
        if weight3 is not None:
            self._saved_weight3_idx = len(tensors)
            tensors.append(weight3)
        if result0 is not None:
            self._saved_result0_idx = len(tensors)
            tensors.append(result0)
        if result1 is not None:
            self._saved_result1_idx = len(tensors)
            tensors.append(result1)
        if result2 is not None:
            self._saved_result2_idx = len(tensors)
            tensors.append(result2)
        if result3 is not None:
            self._saved_result3_idx = len(tensors)
            tensors.append(result3)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_cx__idx is not None:
            self._saved_fields['cx_'] = self._saved_tensors_list[self._saved_cx__idx]
        if self._saved_hx__idx is not None:
            self._saved_fields['hx_'] = self._saved_tensors_list[self._saved_hx__idx]
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]
        if self._saved_weight0_idx is not None:
            self._saved_fields['weight0'] = self._saved_tensors_list[self._saved_weight0_idx]
        if self._saved_weight1_idx is not None:
            self._saved_fields['weight1'] = self._saved_tensors_list[self._saved_weight1_idx]
        if self._saved_weight2_idx is not None:
            self._saved_fields['weight2'] = self._saved_tensors_list[self._saved_weight2_idx]
        if self._saved_weight3_idx is not None:
            self._saved_fields['weight3'] = self._saved_tensors_list[self._saved_weight3_idx]
        if self._saved_result0_idx is not None:
            self._saved_fields['result0'] = self._saved_tensors_list[self._saved_result0_idx]
        if self._saved_result1_idx is not None:
            self._saved_fields['result1'] = self._saved_tensors_list[self._saved_result1_idx]
        if self._saved_result2_idx is not None:
            self._saved_fields['result2'] = self._saved_tensors_list[self._saved_result2_idx]
        if self._saved_result3_idx is not None:
            self._saved_fields['result3'] = self._saved_tensors_list[self._saved_result3_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        cx_ = _saved[self._saved_cx__idx]
        hx_ = _saved[self._saved_hx__idx]
        input_ = _saved[self._saved_input_idx]
        weight0 = _saved[self._saved_weight0_idx]
        weight1 = _saved[self._saved_weight1_idx]
        weight2 = _saved[self._saved_weight2_idx]
        weight3 = _saved[self._saved_weight3_idx]
        result0 = _saved[self._saved_result0_idx]
        result1 = _saved[self._saved_result1_idx]
        result2 = _saved[self._saved_result2_idx]
        result3 = _saved[self._saved_result3_idx]
        reverse = self._reverse
        batch_sizes = self._batch_sizes
        mode = self._mode
        hidden_size = self._hidden_size
        num_layers = self._num_layers
        has_biases = self._has_biases
        bidirectional = self._bidirectional
        batch_first = self._batch_first
        train = self._train
        with _grad_context(keyset):
            grad_input, grad_weight0, grad_weight1, grad_weight2, grad_weight3, grad_hx_, grad_cx_ = (redispatch("mkldnn_rnn_layer_differentiable_backward", keyset, input_, weight0, weight1, weight2, weight3, hx_, cx_, result0, result1, result2, grads[0], grads[1], grads[2], reverse, mode, hidden_size, num_layers, has_biases, train, bidirectional, batch_sizes, batch_first, result3) if True else redispatch("mkldnn_rnn_layer_backward", keyset, input_, weight0, weight1, weight2, weight3, hx_, cx_, result0, result1, result2, grads[0], grads[1], grads[2], reverse, mode, hidden_size, num_layers, has_biases, train, bidirectional, batch_sizes, batch_first, result3))
        return (grad_input, grad_weight0, grad_weight1, grad_weight2, grad_weight3, grad_hx_, grad_cx_,)

class MkldnnRnnLayerBackwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MkldnnRnnLayerBackwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._reverse = None
        self._mode = None
        self._hidden_size = None
        self._num_layers = None
        self._has_biases = None
        self._train = None
        self._bidirectional = None
        self._batch_sizes = None
        self._batch_first = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        reverse = self._reverse
        mode = self._mode
        hidden_size = self._hidden_size
        num_layers = self._num_layers
        has_biases = self._has_biases
        train = self._train
        bidirectional = self._bidirectional
        batch_sizes = self._batch_sizes
        batch_first = self._batch_first
        return (grad,)

class MkldnnConvolutionBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MkldnnConvolutionBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_bias_idx = None
        self._saved_self_idx = None
        self._saved_weight_idx = None
        self._padding = None
        self._stride = None
        self._dilation = None
        self._groups = None

    def _save(self, *, bias=None, self_=None, weight=None):
        tensors = []
        if bias is not None:
            self._saved_bias_idx = len(tensors)
            tensors.append(bias)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_bias_idx is not None:
            self._saved_fields['bias'] = self._saved_tensors_list[self._saved_bias_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        bias = _saved[self._saved_bias_idx] if self._saved_bias_idx is not None else None
        self_ = _saved[self._saved_self_idx]
        weight = _saved[self._saved_weight_idx]
        padding = self._padding
        stride = self._stride
        dilation = self._dilation
        groups = self._groups
        grad_input_mask = [True, True, (bias is not None)]
        with _grad_context(keyset):
            grad_self, grad_weight, grad_bias = (redispatch("convolution_backward_symint", keyset, grad, self_, weight, bias.shape, stride, padding, dilation, False, [0] * redispatch("size", keyset, padding), groups, grad_input_mask) if (grad is not None) else (None, None, None))
        return (grad_self, grad_weight, grad_bias,)

class MkldnnLinearBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MkldnnLinearBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_weight_idx = None

    def _save(self, *, self_=None, weight=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        weight = _saved[self._saved_weight_idx]
        grad_input_mask = [True, True, (bias is not None)]
        with _grad_context(keyset):
            grad_self, grad_weight, grad_bias = redispatch("mkldnn_linear_backward", keyset, self_, grad, weight, grad_input_mask)
        return (grad_self, grad_weight, grad_bias,)

class MkldnnMaxPool2dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MkldnnMaxPool2dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_result_idx = None
        self._kernel_size = None
        self._stride = None
        self._padding = None
        self._dilation = None
        self._ceil_mode = None

    def _save(self, *, self_=None, result=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        result = _saved[self._saved_result_idx]
        kernel_size = self._kernel_size
        stride = self._stride
        padding = self._padding
        dilation = self._dilation
        ceil_mode = self._ceil_mode
        with _grad_context(keyset):
            grad_self = redispatch("mkldnn_max_pool2d_backward", keyset, grad, result, self_, kernel_size, stride, padding, dilation, ceil_mode)
        return (grad_self,)

class MkldnnMaxPool3dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MkldnnMaxPool3dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_result_idx = None
        self._kernel_size = None
        self._stride = None
        self._padding = None
        self._dilation = None
        self._ceil_mode = None

    def _save(self, *, self_=None, result=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        result = _saved[self._saved_result_idx]
        kernel_size = self._kernel_size
        stride = self._stride
        padding = self._padding
        dilation = self._dilation
        ceil_mode = self._ceil_mode
        with _grad_context(keyset):
            grad_self = redispatch("mkldnn_max_pool3d_backward", keyset, grad, result, self_, kernel_size, stride, padding, dilation, ceil_mode)
        return (grad_self,)

class MkldnnAdaptiveAvgPool2dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MkldnnAdaptiveAvgPool2dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._output_size = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        output_size = self._output_size
        with _grad_context(keyset):
            grad_self = redispatch("mkldnn_adaptive_avg_pool2d_backward", keyset, grad, self_)
        return (grad_self,)

class ToPaddedTensorBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ToPaddedTensorBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._padding = None
        self._output_size = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        padding = self._padding
        output_size = self._output_size
        with _grad_context(keyset):
            grad_self = _to_padded_tensor_backward_helper(grad, self_)
        return (grad_self,)

class SafeSoftmaxBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SafeSoftmaxBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_result_idx = None
        self._dim = None
        self._dtype = None

    def _save(self, *, self_=None, result=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        result = _saved[self._saved_result_idx]
        dim = self._dim
        dtype = self._dtype
        with _grad_context(keyset):
            grad_self = redispatch("_softmax_backward_data", keyset, grad, result, dim, self_.dtype)
        return (grad_self,)

class ScaledDotProductEfficientAttentionBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ScaledDotProductEfficientAttentionBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_attn_bias_idx = None
        self._saved_key_idx = None
        self._saved_query_idx = None
        self._saved_value_idx = None
        self._compute_log_sumexp = None
        self._dropout_p = None
        self._is_causal = None
        self._scale = None

    def _save(self, *, attn_bias=None, key=None, query=None, value=None):
        tensors = []
        if attn_bias is not None:
            self._saved_attn_bias_idx = len(tensors)
            tensors.append(attn_bias)
        if key is not None:
            self._saved_key_idx = len(tensors)
            tensors.append(key)
        if query is not None:
            self._saved_query_idx = len(tensors)
            tensors.append(query)
        if value is not None:
            self._saved_value_idx = len(tensors)
            tensors.append(value)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_attn_bias_idx is not None:
            self._saved_fields['attn_bias'] = self._saved_tensors_list[self._saved_attn_bias_idx]
        if self._saved_key_idx is not None:
            self._saved_fields['key'] = self._saved_tensors_list[self._saved_key_idx]
        if self._saved_query_idx is not None:
            self._saved_fields['query'] = self._saved_tensors_list[self._saved_query_idx]
        if self._saved_value_idx is not None:
            self._saved_fields['value'] = self._saved_tensors_list[self._saved_value_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        attn_bias = _saved[self._saved_attn_bias_idx] if self._saved_attn_bias_idx is not None else None
        key = _saved[self._saved_key_idx]
        query = _saved[self._saved_query_idx]
        value = _saved[self._saved_value_idx]
        compute_log_sumexp = self._compute_log_sumexp
        dropout_p = self._dropout_p
        is_causal = self._is_causal
        scale = self._scale
        grad_input_mask = [True, True, True, (attn_bias is not None)]
        with _grad_context(keyset):
            grad_query, grad_key, grad_value, grad_attn_bias = redispatch("_scaled_dot_product_efficient_attention_backward", keyset, grad, query, key, value, attn_bias, output, log_sumexp, philox_seed, philox_offset, dropout_p, grad_input_mask, is_causal, scale)
        return (grad_query, grad_key, grad_value, grad_attn_bias,)

class ScaledDotProductFlashAttentionBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ScaledDotProductFlashAttentionBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_key_idx = None
        self._saved_query_idx = None
        self._saved_value_idx = None
        self._dropout_p = None
        self._is_causal = None
        self._return_debug_mask = None
        self._scale = None

    def _save(self, *, key=None, query=None, value=None):
        tensors = []
        if key is not None:
            self._saved_key_idx = len(tensors)
            tensors.append(key)
        if query is not None:
            self._saved_query_idx = len(tensors)
            tensors.append(query)
        if value is not None:
            self._saved_value_idx = len(tensors)
            tensors.append(value)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_key_idx is not None:
            self._saved_fields['key'] = self._saved_tensors_list[self._saved_key_idx]
        if self._saved_query_idx is not None:
            self._saved_fields['query'] = self._saved_tensors_list[self._saved_query_idx]
        if self._saved_value_idx is not None:
            self._saved_fields['value'] = self._saved_tensors_list[self._saved_value_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        key = _saved[self._saved_key_idx]
        query = _saved[self._saved_query_idx]
        value = _saved[self._saved_value_idx]
        dropout_p = self._dropout_p
        is_causal = self._is_causal
        return_debug_mask = self._return_debug_mask
        scale = self._scale
        with _grad_context(keyset):
            grad_query, grad_key, grad_value = redispatch("_scaled_dot_product_flash_attention_backward_symint", keyset, grad, query, key, value, output, logsumexp, cum_seq_q, cum_seq_k, max_q, max_k, dropout_p, is_causal, rng_state, unused, scale)
        return (grad_query, grad_key, grad_value,)

class ScaledDotProductFlashAttentionForCpuBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ScaledDotProductFlashAttentionForCpuBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_attn_mask_idx = None
        self._saved_key_idx = None
        self._saved_query_idx = None
        self._saved_value_idx = None
        self._dropout_p = None
        self._is_causal = None
        self._scale = None

    def _save(self, *, attn_mask=None, key=None, query=None, value=None):
        tensors = []
        if attn_mask is not None:
            self._saved_attn_mask_idx = len(tensors)
            tensors.append(attn_mask)
        if key is not None:
            self._saved_key_idx = len(tensors)
            tensors.append(key)
        if query is not None:
            self._saved_query_idx = len(tensors)
            tensors.append(query)
        if value is not None:
            self._saved_value_idx = len(tensors)
            tensors.append(value)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_attn_mask_idx is not None:
            self._saved_fields['attn_mask'] = self._saved_tensors_list[self._saved_attn_mask_idx]
        if self._saved_key_idx is not None:
            self._saved_fields['key'] = self._saved_tensors_list[self._saved_key_idx]
        if self._saved_query_idx is not None:
            self._saved_fields['query'] = self._saved_tensors_list[self._saved_query_idx]
        if self._saved_value_idx is not None:
            self._saved_fields['value'] = self._saved_tensors_list[self._saved_value_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        attn_mask = _saved[self._saved_attn_mask_idx] if self._saved_attn_mask_idx is not None else None
        key = _saved[self._saved_key_idx]
        query = _saved[self._saved_query_idx]
        value = _saved[self._saved_value_idx]
        dropout_p = self._dropout_p
        is_causal = self._is_causal
        scale = self._scale
        with _grad_context(keyset):
            grad_query, grad_key, grad_value = redispatch("_scaled_dot_product_flash_attention_for_cpu_backward", keyset, grad, query, key, value, output, logsumexp, dropout_p, is_causal, attn_mask, scale)
        return (grad_query, grad_key, grad_value,)

class FlashAttentionForwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='FlashAttentionForwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_cum_seq_k_idx = None
        self._saved_cum_seq_q_idx = None
        self._saved_key_idx = None
        self._saved_query_idx = None
        self._saved_value_idx = None
        self._max_q = None
        self._max_k = None
        self._dropout_p = None
        self._is_causal = None
        self._return_debug_mask = None
        self._scale = None
        self._window_size_left = None
        self._window_size_right = None
        self._num_splits = None

    def _save(self, *, cum_seq_k=None, cum_seq_q=None, key=None, query=None, value=None):
        tensors = []
        if cum_seq_k is not None:
            self._saved_cum_seq_k_idx = len(tensors)
            tensors.append(cum_seq_k)
        if cum_seq_q is not None:
            self._saved_cum_seq_q_idx = len(tensors)
            tensors.append(cum_seq_q)
        if key is not None:
            self._saved_key_idx = len(tensors)
            tensors.append(key)
        if query is not None:
            self._saved_query_idx = len(tensors)
            tensors.append(query)
        if value is not None:
            self._saved_value_idx = len(tensors)
            tensors.append(value)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_cum_seq_k_idx is not None:
            self._saved_fields['cum_seq_k'] = self._saved_tensors_list[self._saved_cum_seq_k_idx]
        if self._saved_cum_seq_q_idx is not None:
            self._saved_fields['cum_seq_q'] = self._saved_tensors_list[self._saved_cum_seq_q_idx]
        if self._saved_key_idx is not None:
            self._saved_fields['key'] = self._saved_tensors_list[self._saved_key_idx]
        if self._saved_query_idx is not None:
            self._saved_fields['query'] = self._saved_tensors_list[self._saved_query_idx]
        if self._saved_value_idx is not None:
            self._saved_fields['value'] = self._saved_tensors_list[self._saved_value_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        cum_seq_k = _saved[self._saved_cum_seq_k_idx] if self._saved_cum_seq_k_idx is not None else None
        cum_seq_q = _saved[self._saved_cum_seq_q_idx] if self._saved_cum_seq_q_idx is not None else None
        key = _saved[self._saved_key_idx]
        query = _saved[self._saved_query_idx]
        value = _saved[self._saved_value_idx]
        max_q = self._max_q
        max_k = self._max_k
        dropout_p = self._dropout_p
        is_causal = self._is_causal
        return_debug_mask = self._return_debug_mask
        scale = self._scale
        window_size_left = self._window_size_left
        window_size_right = self._window_size_right
        num_splits = self._num_splits
        with _grad_context(keyset):
            grad_query, grad_key, grad_value = redispatch("_flash_attention_backward_symint", keyset, grad, query, key, value, output, softmax_logsumexp, cum_seq_q, cum_seq_k, max_q, max_k, dropout_p, is_causal, rng_state, unused, scale, window_size_left, window_size_right)
        return (grad_query, grad_key, grad_value,)

class EfficientAttentionForwardBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='EfficientAttentionForwardBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_bias_idx = None
        self._saved_cu_seqlens_k_idx = None
        self._saved_cu_seqlens_q_idx = None
        self._saved_key_idx = None
        self._saved_query_idx = None
        self._saved_value_idx = None
        self._max_seqlen_q = None
        self._max_seqlen_k = None
        self._dropout_p = None
        self._custom_mask_type = None
        self._compute_log_sumexp = None
        self._scale = None
        self._window_size = None

    def _save(self, *, bias=None, cu_seqlens_k=None, cu_seqlens_q=None, key=None, query=None, value=None):
        tensors = []
        if bias is not None:
            self._saved_bias_idx = len(tensors)
            tensors.append(bias)
        if cu_seqlens_k is not None:
            self._saved_cu_seqlens_k_idx = len(tensors)
            tensors.append(cu_seqlens_k)
        if cu_seqlens_q is not None:
            self._saved_cu_seqlens_q_idx = len(tensors)
            tensors.append(cu_seqlens_q)
        if key is not None:
            self._saved_key_idx = len(tensors)
            tensors.append(key)
        if query is not None:
            self._saved_query_idx = len(tensors)
            tensors.append(query)
        if value is not None:
            self._saved_value_idx = len(tensors)
            tensors.append(value)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_bias_idx is not None:
            self._saved_fields['bias'] = self._saved_tensors_list[self._saved_bias_idx]
        if self._saved_cu_seqlens_k_idx is not None:
            self._saved_fields['cu_seqlens_k'] = self._saved_tensors_list[self._saved_cu_seqlens_k_idx]
        if self._saved_cu_seqlens_q_idx is not None:
            self._saved_fields['cu_seqlens_q'] = self._saved_tensors_list[self._saved_cu_seqlens_q_idx]
        if self._saved_key_idx is not None:
            self._saved_fields['key'] = self._saved_tensors_list[self._saved_key_idx]
        if self._saved_query_idx is not None:
            self._saved_fields['query'] = self._saved_tensors_list[self._saved_query_idx]
        if self._saved_value_idx is not None:
            self._saved_fields['value'] = self._saved_tensors_list[self._saved_value_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        bias = _saved[self._saved_bias_idx] if self._saved_bias_idx is not None else None
        cu_seqlens_k = _saved[self._saved_cu_seqlens_k_idx] if self._saved_cu_seqlens_k_idx is not None else None
        cu_seqlens_q = _saved[self._saved_cu_seqlens_q_idx] if self._saved_cu_seqlens_q_idx is not None else None
        key = _saved[self._saved_key_idx]
        query = _saved[self._saved_query_idx]
        value = _saved[self._saved_value_idx]
        max_seqlen_q = self._max_seqlen_q
        max_seqlen_k = self._max_seqlen_k
        dropout_p = self._dropout_p
        custom_mask_type = self._custom_mask_type
        compute_log_sumexp = self._compute_log_sumexp
        scale = self._scale
        window_size = self._window_size
        with _grad_context(keyset):
            grad_query, grad_key, grad_value, grad_bias = redispatch("_efficient_attention_backward_symint", keyset, grad, query, key, value, bias, output, cu_seqlens_q, cu_seqlens_k, max_seqlen_batch_q, max_seqlen_batch_k, logsumexp, dropout_p, philox_seed, philox_offset, custom_mask_type, redispatch("requires_grad", keyset, bias), scale)
        return (grad_query, grad_key, grad_value, grad_bias,)

class ScaledDotProductCudnnAttentionBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ScaledDotProductCudnnAttentionBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_attn_bias_idx = None
        self._saved_key_idx = None
        self._saved_query_idx = None
        self._saved_value_idx = None
        self._compute_log_sumexp = None
        self._dropout_p = None
        self._is_causal = None
        self._return_debug_mask = None
        self._scale = None

    def _save(self, *, attn_bias=None, key=None, query=None, value=None):
        tensors = []
        if attn_bias is not None:
            self._saved_attn_bias_idx = len(tensors)
            tensors.append(attn_bias)
        if key is not None:
            self._saved_key_idx = len(tensors)
            tensors.append(key)
        if query is not None:
            self._saved_query_idx = len(tensors)
            tensors.append(query)
        if value is not None:
            self._saved_value_idx = len(tensors)
            tensors.append(value)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_attn_bias_idx is not None:
            self._saved_fields['attn_bias'] = self._saved_tensors_list[self._saved_attn_bias_idx]
        if self._saved_key_idx is not None:
            self._saved_fields['key'] = self._saved_tensors_list[self._saved_key_idx]
        if self._saved_query_idx is not None:
            self._saved_fields['query'] = self._saved_tensors_list[self._saved_query_idx]
        if self._saved_value_idx is not None:
            self._saved_fields['value'] = self._saved_tensors_list[self._saved_value_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        attn_bias = _saved[self._saved_attn_bias_idx] if self._saved_attn_bias_idx is not None else None
        key = _saved[self._saved_key_idx]
        query = _saved[self._saved_query_idx]
        value = _saved[self._saved_value_idx]
        compute_log_sumexp = self._compute_log_sumexp
        dropout_p = self._dropout_p
        is_causal = self._is_causal
        return_debug_mask = self._return_debug_mask
        scale = self._scale
        with _grad_context(keyset):
            grad_query, grad_key, grad_value = redispatch("_scaled_dot_product_cudnn_attention_backward_symint", keyset, grad, query, key, value, output, logsumexp, philox_seed, philox_offset, attn_bias, cum_seq_q, cum_seq_k, max_q, max_k, dropout_p, is_causal, scale)
        return (grad_query, grad_key, grad_value,)

class ScaledDotProductFusedAttentionOverrideableBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ScaledDotProductFusedAttentionOverrideableBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_attn_bias_idx = None
        self._saved_key_idx = None
        self._saved_query_idx = None
        self._saved_value_idx = None
        self._dropout_p = None
        self._is_causal = None
        self._return_debug_mask = None
        self._scale = None

    def _save(self, *, attn_bias=None, key=None, query=None, value=None):
        tensors = []
        if attn_bias is not None:
            self._saved_attn_bias_idx = len(tensors)
            tensors.append(attn_bias)
        if key is not None:
            self._saved_key_idx = len(tensors)
            tensors.append(key)
        if query is not None:
            self._saved_query_idx = len(tensors)
            tensors.append(query)
        if value is not None:
            self._saved_value_idx = len(tensors)
            tensors.append(value)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_attn_bias_idx is not None:
            self._saved_fields['attn_bias'] = self._saved_tensors_list[self._saved_attn_bias_idx]
        if self._saved_key_idx is not None:
            self._saved_fields['key'] = self._saved_tensors_list[self._saved_key_idx]
        if self._saved_query_idx is not None:
            self._saved_fields['query'] = self._saved_tensors_list[self._saved_query_idx]
        if self._saved_value_idx is not None:
            self._saved_fields['value'] = self._saved_tensors_list[self._saved_value_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        attn_bias = _saved[self._saved_attn_bias_idx] if self._saved_attn_bias_idx is not None else None
        key = _saved[self._saved_key_idx]
        query = _saved[self._saved_query_idx]
        value = _saved[self._saved_value_idx]
        dropout_p = self._dropout_p
        is_causal = self._is_causal
        return_debug_mask = self._return_debug_mask
        scale = self._scale
        grad_input_mask = [True, True, True, (attn_bias is not None)]
        with _grad_context(keyset):
            grad_query, grad_key, grad_value, grad_attn_bias = redispatch("_scaled_dot_product_fused_attention_overrideable_backward_symint", keyset, grad, query, key, value, attn_bias, grad_input_mask, output, logsumexp, cum_seq_q, cum_seq_k, max_q, max_k, dropout_p, is_causal, philox_seed, philox_offset, scale)
        return (grad_query, grad_key, grad_value, grad_attn_bias,)

class FftR2cBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='FftR2cBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._dim = None
        self._normalization = None
        self._onesided = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        dim = self._dim
        normalization = self._normalization
        onesided = self._onesided
        with _grad_context(keyset):
            grad_self = redispatch("fft_r2c_backward", keyset, grad, dim, normalization, onesided, self_.shape[redispatch("back", keyset, dim)])
        return (grad_self,)

class FftC2rBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='FftC2rBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._dim = None
        self._normalization = None
        self._last_dim_size = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        dim = self._dim
        normalization = self._normalization
        last_dim_size = self._last_dim_size
        with _grad_context(keyset):
            grad_self = redispatch("fft_c2r_backward", keyset, grad, dim, normalization)
        return (grad_self,)

class FftC2cBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='FftC2cBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._dim = None
        self._normalization = None
        self._forward = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        dim = self._dim
        normalization = self._normalization
        forward = self._forward
        with _grad_context(keyset):
            grad_self = redispatch("_fft_c2c_symint", keyset, grad, dim, normalization, (not forward))
        return (grad_self,)

class UnbindIntBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='UnbindIntBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._dim = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        dim = self._dim
        with _grad_context(keyset):
            grad_self = redispatch("unbind_backward", keyset, grads, dim)
        return (grad_self,)

class StackBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='StackBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._dim = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        tensors = list(self.inputs)
        dim = self._dim
        with _grad_context(keyset):
            grad_tensors = _stack_backward_helper(grad, tensors, dim, keyset)
        return grad_tensors

class ThnnFusedLstmCellBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ThnnFusedLstmCellBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_cx_idx = None
        self._saved_hidden_bias_idx = None
        self._saved_hidden_gates_idx = None
        self._saved_input_bias_idx = None
        self._saved_input_gates_idx = None
        self._saved_result1_idx = None
        self._saved_result2_idx = None

    def _save(self, *, cx=None, hidden_bias=None, hidden_gates=None, input_bias=None, input_gates=None, result1=None, result2=None):
        tensors = []
        if cx is not None:
            self._saved_cx_idx = len(tensors)
            tensors.append(cx)
        if hidden_bias is not None:
            self._saved_hidden_bias_idx = len(tensors)
            tensors.append(hidden_bias)
        if hidden_gates is not None:
            self._saved_hidden_gates_idx = len(tensors)
            tensors.append(hidden_gates)
        if input_bias is not None:
            self._saved_input_bias_idx = len(tensors)
            tensors.append(input_bias)
        if input_gates is not None:
            self._saved_input_gates_idx = len(tensors)
            tensors.append(input_gates)
        if result1 is not None:
            self._saved_result1_idx = len(tensors)
            tensors.append(result1)
        if result2 is not None:
            self._saved_result2_idx = len(tensors)
            tensors.append(result2)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_cx_idx is not None:
            self._saved_fields['cx'] = self._saved_tensors_list[self._saved_cx_idx]
        if self._saved_hidden_bias_idx is not None:
            self._saved_fields['hidden_bias'] = self._saved_tensors_list[self._saved_hidden_bias_idx]
        if self._saved_hidden_gates_idx is not None:
            self._saved_fields['hidden_gates'] = self._saved_tensors_list[self._saved_hidden_gates_idx]
        if self._saved_input_bias_idx is not None:
            self._saved_fields['input_bias'] = self._saved_tensors_list[self._saved_input_bias_idx]
        if self._saved_input_gates_idx is not None:
            self._saved_fields['input_gates'] = self._saved_tensors_list[self._saved_input_gates_idx]
        if self._saved_result1_idx is not None:
            self._saved_fields['result1'] = self._saved_tensors_list[self._saved_result1_idx]
        if self._saved_result2_idx is not None:
            self._saved_fields['result2'] = self._saved_tensors_list[self._saved_result2_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        cx = _saved[self._saved_cx_idx]
        hidden_bias = _saved[self._saved_hidden_bias_idx] if self._saved_hidden_bias_idx is not None else None
        hidden_gates = _saved[self._saved_hidden_gates_idx]
        input_bias = _saved[self._saved_input_bias_idx] if self._saved_input_bias_idx is not None else None
        input_gates = _saved[self._saved_input_gates_idx]
        result1 = _saved[self._saved_result1_idx]
        result2 = _saved[self._saved_result2_idx]
        with _grad_context(keyset):
            grad_input_gates, grad_hidden_gates, grad_cx, grad_input_bias, grad_hidden_bias = (redispatch("_thnn_differentiable_lstm_cell_backward", keyset, grads[0], grads[1], input_gates, hidden_gates, input_bias, hidden_bias, cx, result1) if True else redispatch("_thnn_fused_lstm_cell_backward", keyset, grads[0], grads[1], cx, result1, result2, (input_bias is not None)))
        return (grad_input_gates, grad_hidden_gates, grad_cx, grad_input_bias, grad_hidden_bias,)

class ThnnFusedGruCellBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ThnnFusedGruCellBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_hidden_bias_idx = None
        self._saved_hidden_gates_idx = None
        self._saved_hx_idx = None
        self._saved_input_bias_idx = None
        self._saved_input_gates_idx = None
        self._saved_result1_idx = None

    def _save(self, *, hidden_bias=None, hidden_gates=None, hx=None, input_bias=None, input_gates=None, result1=None):
        tensors = []
        if hidden_bias is not None:
            self._saved_hidden_bias_idx = len(tensors)
            tensors.append(hidden_bias)
        if hidden_gates is not None:
            self._saved_hidden_gates_idx = len(tensors)
            tensors.append(hidden_gates)
        if hx is not None:
            self._saved_hx_idx = len(tensors)
            tensors.append(hx)
        if input_bias is not None:
            self._saved_input_bias_idx = len(tensors)
            tensors.append(input_bias)
        if input_gates is not None:
            self._saved_input_gates_idx = len(tensors)
            tensors.append(input_gates)
        if result1 is not None:
            self._saved_result1_idx = len(tensors)
            tensors.append(result1)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_hidden_bias_idx is not None:
            self._saved_fields['hidden_bias'] = self._saved_tensors_list[self._saved_hidden_bias_idx]
        if self._saved_hidden_gates_idx is not None:
            self._saved_fields['hidden_gates'] = self._saved_tensors_list[self._saved_hidden_gates_idx]
        if self._saved_hx_idx is not None:
            self._saved_fields['hx'] = self._saved_tensors_list[self._saved_hx_idx]
        if self._saved_input_bias_idx is not None:
            self._saved_fields['input_bias'] = self._saved_tensors_list[self._saved_input_bias_idx]
        if self._saved_input_gates_idx is not None:
            self._saved_fields['input_gates'] = self._saved_tensors_list[self._saved_input_gates_idx]
        if self._saved_result1_idx is not None:
            self._saved_fields['result1'] = self._saved_tensors_list[self._saved_result1_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        hidden_bias = _saved[self._saved_hidden_bias_idx] if self._saved_hidden_bias_idx is not None else None
        hidden_gates = _saved[self._saved_hidden_gates_idx]
        hx = _saved[self._saved_hx_idx]
        input_bias = _saved[self._saved_input_bias_idx] if self._saved_input_bias_idx is not None else None
        input_gates = _saved[self._saved_input_gates_idx]
        result1 = _saved[self._saved_result1_idx]
        with _grad_context(keyset):
            grad_input_gates, grad_hidden_gates, grad_hx, grad_input_bias, grad_hidden_bias = ((redispatch("_thnn_differentiable_gru_cell_backward", keyset, grad, input_gates, hidden_gates, hx, input_bias, hidden_bias) if True else redispatch("_thnn_fused_gru_cell_backward", keyset, grad, result1, (input_bias is not None))) if (grad is not None) else (None, None, None, None, None))
        return (grad_input_gates, grad_hidden_gates, grad_hx, grad_input_bias, grad_hidden_bias,)

class PackPaddedSequenceBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='PackPaddedSequenceBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._saved_result1_idx = None
        self._batch_first = None

    def _save(self, *, input_=None, result1=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if result1 is not None:
            self._saved_result1_idx = len(tensors)
            tensors.append(result1)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]
        if self._saved_result1_idx is not None:
            self._saved_fields['result1'] = self._saved_tensors_list[self._saved_result1_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        result1 = _saved[self._saved_result1_idx]
        batch_first = self._batch_first
        with _grad_context(keyset):
            grad_input = redispatch("_pack_padded_sequence_backward_symint", keyset, grad, input_.shape, result1, batch_first)
        return (grad_input,)

class MultinomialBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MultinomialBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._num_samples = None
        self._replacement = None
        self._generator = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        num_samples = self._num_samples
        replacement = self._replacement
        generator = self._generator
        return (grad,)

class NonzeroBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='NonzeroBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        return (grad,)

class SegmentReduceBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SegmentReduceBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_data_idx = None
        self._saved_lengths_idx = None
        self._saved_offsets_idx = None
        self._saved_result_idx = None
        self._reduce = None
        self._axis = None
        self._unsafe = None
        self._initial = None

    def _save(self, *, data=None, lengths=None, offsets=None, result=None):
        tensors = []
        if data is not None:
            self._saved_data_idx = len(tensors)
            tensors.append(data)
        if lengths is not None:
            self._saved_lengths_idx = len(tensors)
            tensors.append(lengths)
        if offsets is not None:
            self._saved_offsets_idx = len(tensors)
            tensors.append(offsets)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_data_idx is not None:
            self._saved_fields['data'] = self._saved_tensors_list[self._saved_data_idx]
        if self._saved_lengths_idx is not None:
            self._saved_fields['lengths'] = self._saved_tensors_list[self._saved_lengths_idx]
        if self._saved_offsets_idx is not None:
            self._saved_fields['offsets'] = self._saved_tensors_list[self._saved_offsets_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        data = _saved[self._saved_data_idx]
        lengths = _saved[self._saved_lengths_idx] if self._saved_lengths_idx is not None else None
        offsets = _saved[self._saved_offsets_idx] if self._saved_offsets_idx is not None else None
        result = _saved[self._saved_result_idx]
        reduce = self._reduce
        axis = self._axis
        unsafe = self._unsafe
        initial = self._initial
        with _grad_context(keyset):
            grad_data = redispatch("_segment_reduce_backward", keyset, grad, result, data, reduce, lengths, offsets, axis, initial)
        return (grad_data,)

class PinMemoryBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='PinMemoryBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._device = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        device = self._device
        with _grad_context(keyset):
            grad_self = grad
        return (grad_self,)

class NewZerosWithSameFeatureMetaBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='NewZerosWithSameFeatureMetaBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._self_num_batch_dims = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        self_num_batch_dims = self._self_num_batch_dims
        return (grad,)

class EfficientzerotensorBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='EfficientzerotensorBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._size = None
        self._dtype = None
        self._layout = None
        self._device = None
        self._pin_memory = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        size = self._size
        dtype = self._dtype
        layout = self._layout
        device = self._device
        pin_memory = self._pin_memory
        return (grad,)

class ScatterReduceTwoBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ScatterReduceTwoBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_index_idx = None
        self._saved_self_idx = None
        self._saved_src_idx = None
        self._saved_result_idx = None
        self._dim = None
        self._reduce = None
        self._include_self = None

    def _save(self, *, index=None, self_=None, src=None, result=None):
        tensors = []
        if index is not None:
            self._saved_index_idx = len(tensors)
            tensors.append(index)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if src is not None:
            self._saved_src_idx = len(tensors)
            tensors.append(src)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_index_idx is not None:
            self._saved_fields['index'] = self._saved_tensors_list[self._saved_index_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_src_idx is not None:
            self._saved_fields['src'] = self._saved_tensors_list[self._saved_src_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        index = _saved[self._saved_index_idx]
        self_ = _saved[self._saved_self_idx]
        src = _saved[self._saved_src_idx]
        result = _saved[self._saved_result_idx]
        dim = self._dim
        reduce = self._reduce
        include_self = self._include_self
        with _grad_context(keyset):
            grad_self, grad_src = redispatch("scatter_reduce_backward", keyset, grad, self_, dim, index, src, reduce, include_self, result)
        return (grad_self, grad_src,)

class SpecialAiryAiBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialAiryAiBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        return (grad,)

class SpecialBesselJ0Backward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialBesselJ0Backward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        return (grad,)

class SpecialBesselJ1Backward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialBesselJ1Backward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        return (grad,)

class SpecialBesselY0Backward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialBesselY0Backward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        return (grad,)

class SpecialBesselY1Backward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialBesselY1Backward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        return (grad,)

class SpecialChebyshevPolynomialTBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialChebyshevPolynomialTBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        return (grad,)

class SpecialChebyshevPolynomialTXScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialChebyshevPolynomialTXScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._x = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        x = self._x
        return (grad,)

class SpecialChebyshevPolynomialTNScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialChebyshevPolynomialTNScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._n = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        n = self._n
        return (grad,)

class SpecialChebyshevPolynomialUBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialChebyshevPolynomialUBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        return (grad,)

class SpecialChebyshevPolynomialUXScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialChebyshevPolynomialUXScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._x = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        x = self._x
        return (grad,)

class SpecialChebyshevPolynomialUNScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialChebyshevPolynomialUNScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._n = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        n = self._n
        return (grad,)

class SpecialChebyshevPolynomialVBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialChebyshevPolynomialVBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        return (grad,)

class SpecialChebyshevPolynomialVXScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialChebyshevPolynomialVXScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._x = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        x = self._x
        return (grad,)

class SpecialChebyshevPolynomialVNScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialChebyshevPolynomialVNScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._n = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        n = self._n
        return (grad,)

class SpecialChebyshevPolynomialWBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialChebyshevPolynomialWBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        return (grad,)

class SpecialChebyshevPolynomialWXScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialChebyshevPolynomialWXScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._x = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        x = self._x
        return (grad,)

class SpecialChebyshevPolynomialWNScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialChebyshevPolynomialWNScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._n = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        n = self._n
        return (grad,)

class SpecialHermitePolynomialHBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialHermitePolynomialHBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        return (grad,)

class SpecialHermitePolynomialHXScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialHermitePolynomialHXScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._x = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        x = self._x
        return (grad,)

class SpecialHermitePolynomialHNScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialHermitePolynomialHNScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._n = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        n = self._n
        return (grad,)

class SpecialHermitePolynomialHeBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialHermitePolynomialHeBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        return (grad,)

class SpecialHermitePolynomialHeXScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialHermitePolynomialHeXScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._x = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        x = self._x
        return (grad,)

class SpecialHermitePolynomialHeNScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialHermitePolynomialHeNScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._n = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        n = self._n
        return (grad,)

class SpecialLaguerrePolynomialLBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialLaguerrePolynomialLBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        return (grad,)

class SpecialLaguerrePolynomialLXScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialLaguerrePolynomialLXScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._x = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        x = self._x
        return (grad,)

class SpecialLaguerrePolynomialLNScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialLaguerrePolynomialLNScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._n = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        n = self._n
        return (grad,)

class SpecialLegendrePolynomialPBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialLegendrePolynomialPBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        return (grad,)

class SpecialLegendrePolynomialPXScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialLegendrePolynomialPXScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._x = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        x = self._x
        return (grad,)

class SpecialLegendrePolynomialPNScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialLegendrePolynomialPNScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._n = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        n = self._n
        return (grad,)

class SpecialModifiedBesselI0Backward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialModifiedBesselI0Backward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        return (grad,)

class SpecialModifiedBesselI1Backward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialModifiedBesselI1Backward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        return (grad,)

class SpecialModifiedBesselK0Backward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialModifiedBesselK0Backward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        return (grad,)

class SpecialModifiedBesselK1Backward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialModifiedBesselK1Backward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        return (grad,)

class SpecialScaledModifiedBesselK0Backward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialScaledModifiedBesselK0Backward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        return (grad,)

class SpecialScaledModifiedBesselK1Backward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialScaledModifiedBesselK1Backward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        return (grad,)

class SpecialShiftedChebyshevPolynomialTBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialShiftedChebyshevPolynomialTBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        return (grad,)

class SpecialShiftedChebyshevPolynomialTXScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialShiftedChebyshevPolynomialTXScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._x = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        x = self._x
        return (grad,)

class SpecialShiftedChebyshevPolynomialTNScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialShiftedChebyshevPolynomialTNScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._n = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        n = self._n
        return (grad,)

class SpecialShiftedChebyshevPolynomialUBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialShiftedChebyshevPolynomialUBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        return (grad,)

class SpecialShiftedChebyshevPolynomialUXScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialShiftedChebyshevPolynomialUXScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._x = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        x = self._x
        return (grad,)

class SpecialShiftedChebyshevPolynomialUNScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialShiftedChebyshevPolynomialUNScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._n = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        n = self._n
        return (grad,)

class SpecialShiftedChebyshevPolynomialVBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialShiftedChebyshevPolynomialVBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        return (grad,)

class SpecialShiftedChebyshevPolynomialVXScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialShiftedChebyshevPolynomialVXScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._x = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        x = self._x
        return (grad,)

class SpecialShiftedChebyshevPolynomialVNScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialShiftedChebyshevPolynomialVNScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._n = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        n = self._n
        return (grad,)

class SpecialShiftedChebyshevPolynomialWBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialShiftedChebyshevPolynomialWBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        return (grad,)

class SpecialShiftedChebyshevPolynomialWXScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialShiftedChebyshevPolynomialWXScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._x = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        x = self._x
        return (grad,)

class SpecialShiftedChebyshevPolynomialWNScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialShiftedChebyshevPolynomialWNScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._n = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        n = self._n
        return (grad,)

class SpecialSphericalBesselJ0Backward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SpecialSphericalBesselJ0Backward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        return (grad,)

class ReshapeCopyBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ReshapeCopyBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._size = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        size = self._size
        with _grad_context(keyset):
            grad_self = redispatch("reshape_symint", keyset, grad, self_.shape)
        return (grad_self,)

class NarrowCopyBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='NarrowCopyBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._dim = None
        self._start = None
        self._length = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        dim = self._dim
        start = self._start
        length = self._length
        with _grad_context(keyset):
            grad_self = _slice_backward_wrapper_helper(grad, self_.shape, dim, start, start + length, 1, keyset)
        return (grad_self,)

class ForeachDivListBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ForeachDivListBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        other = list(self.inputs)
        self_ = list(self.inputs)
        with _grad_context(keyset):
            grad_self = _div_tensor_self_backward_helper(grads[i], other[i], self_[i].dtype, keyset)
            grad_other = _div_tensor_other_backward_helper(grads[i], self_[i], other[i], keyset)
        return (grad_self, grad_other,)

class ForeachPowListBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ForeachPowListBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_result_idx = None

    def _save(self, *, result=None):
        tensors = []
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        result = _saved[self._saved_result_idx]
        exponent = list(self.inputs)
        self_ = list(self.inputs)
        with _grad_context(keyset):
            grad_self = redispatch("pow_backward_self", keyset, grads[i], self_[i], exponent[i])
            grad_exponent = redispatch("pow_backward_exponent", keyset, grads[i], self_[i], exponent[i], result[i])
        return (grad_self, grad_exponent,)

class ForeachPowScalarListBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ForeachPowScalarListBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._exponent = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        self_ = list(self.inputs)
        exponent = self._exponent
        with _grad_context(keyset):
            grad_self = redispatch("pow_backward", keyset, grads[i], self_[i], exponent[i])
        return grad_self

class ForeachPowScalarAndTensorBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ForeachPowScalarAndTensorBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_result_idx = None
        self._self = None

    def _save(self, *, result=None):
        tensors = []
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        result = _saved[self._saved_result_idx]
        exponent = list(self.inputs)
        self_ = self._self
        with _grad_context(keyset):
            grad_exponent = redispatch("pow_backward_exponent", keyset, grads[i], self_, exponent[i], result[i])
        return grad_exponent

class ForeachMinimumScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ForeachMinimumScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._scalar = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        self_ = list(self.inputs)
        scalar = self._scalar
        with _grad_context(keyset):
            grad_self = redispatch("masked_fill_", keyset, redispatch("where", keyset, redispatch("eq", keyset, self_[i], scalar), redispatch("div", keyset, grads[i], 2), grads[i]), redispatch("gt", keyset, self_[i], scalar), 0)
        return grad_self

class ForeachMinimumScalarListBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ForeachMinimumScalarListBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._scalars = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        self_ = list(self.inputs)
        scalars = self._scalars
        with _grad_context(keyset):
            grad_self = redispatch("masked_fill_", keyset, redispatch("where", keyset, redispatch("eq", keyset, self_[i], scalars[i]), redispatch("div", keyset, grads[i], 2), grads[i]), redispatch("gt", keyset, self_[i], scalars[i]), 0)
        return grad_self

class ForeachMaximumScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ForeachMaximumScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._scalar = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        self_ = list(self.inputs)
        scalar = self._scalar
        with _grad_context(keyset):
            grad_self = redispatch("masked_fill_", keyset, redispatch("where", keyset, redispatch("eq", keyset, self_[i], scalar), redispatch("div", keyset, grads[i], 2), grads[i]), redispatch("lt", keyset, self_[i], scalar), 0)
        return grad_self

class ForeachMaximumScalarListBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ForeachMaximumScalarListBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._scalars = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        self_ = list(self.inputs)
        scalars = self._scalars
        with _grad_context(keyset):
            grad_self = redispatch("masked_fill_", keyset, redispatch("where", keyset, redispatch("eq", keyset, self_[i], scalars[i]), redispatch("div", keyset, grads[i], 2), grads[i]), redispatch("lt", keyset, self_[i], scalars[i]), 0)
        return grad_self

class ForeachNormScalarBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ForeachNormScalarBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_result_idx = None
        self._ord = None
        self._dtype = None

    def _save(self, *, result=None):
        tensors = []
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        result = _saved[self._saved_result_idx]
        self_ = list(self.inputs)
        ord = self._ord
        dtype = self._dtype
        with _grad_context(keyset):
            grad_self = redispatch("norm_backward", keyset, grads[i], self_[i], ord, result[i])
        return grad_self


# Canonical overload aliases
AddBackward0 = AddTensorBackward0
_sparse_addmmBackward0 = SparseAddmmBackward0
Affine_grid_generatorBackward0 = AffineGridGeneratorBackward0
_is_all_trueBackward0 = IsAllTrueBackward0
_is_any_trueBackward0 = IsAnyTrueBackward0
Acosh_Backward0 = AcoshBackward0
Asinh_Backward0 = AsinhBackward0
Atanh_Backward0 = AtanhBackward0
As_stridedBackward0 = AsStridedBackward0
As_strided_Backward0 = AsStridedBackward0
Bernoulli_Backward0 = BernoulliTensorBackward0
Cauchy_Backward0 = CauchyBackward0
Linalg_cholesky_exBackward0 = LinalgCholeskyExBackward0
Cholesky_solveBackward0 = CholeskySolveBackward0
Cholesky_inverseBackward0 = CholeskyInverseBackward0
ClampBackward0 = ClampTensorBackward0
Clamp_minBackward0 = ClampMinBackward0
Clamp_maxBackward0 = ClampMaxBackward0
_lazy_cloneBackward0 = LazyCloneBackward0
_to_copyBackward0 = ToCopyBackward0
_coalesceBackward0 = CoalesceBackward0
_conjBackward0 = ConjBackward0
_neg_viewBackward0 = NegViewBackward0
_conj_physicalBackward0 = ConjPhysicalBackward0
Conj_physical_Backward0 = ConjPhysicalBackward0
CopysignBackward0 = CopysignTensorBackward0
Count_nonzeroBackward0 = CountNonzeroDimIntListBackward0
Linalg_crossBackward0 = LinalgCrossBackward0
Conv_tbcBackward0 = ConvTbcBackward0
_ctc_lossBackward0 = CtcLossBackward0
_linalg_detBackward0 = LinalgDetBackward0
_linalg_slogdetBackward0 = LinalgSlogdetBackward0
Block_diagBackward0 = BlockDiagBackward0
Diag_embedBackward0 = DiagEmbedBackward0
Diagonal_backwardBackward0 = DiagonalBackwardBackward0
DivBackward0 = DivTensorBackward0
_fused_dropoutBackward0 = FusedDropoutBackward0
Native_dropoutBackward0 = NativeDropoutBackward0
Native_dropout_backwardBackward0 = NativeDropoutBackwardBackward0
Eq_Backward0 = EqScalarBackward0
Special_erfcxBackward0 = SpecialErfcxBackward0
Exponential_Backward0 = ExponentialBackward0
Fake_quantize_per_tensor_affine_cachemaskBackward0 = FakeQuantizePerTensorAffineCachemaskBackward0
_fake_quantize_per_tensor_affine_cachemask_tensor_qparamsBackward0 = FakeQuantizePerTensorAffineCachemaskTensorQparamsBackward0
_fake_quantize_learnable_per_tensor_affineBackward0 = FakeQuantizeLearnablePerTensorAffineBackward0
Fake_quantize_per_channel_affine_cachemaskBackward0 = FakeQuantizePerChannelAffineCachemaskBackward0
_fake_quantize_learnable_per_channel_affineBackward0 = FakeQuantizeLearnablePerChannelAffineBackward0
_fused_moving_avg_obs_fq_helperBackward0 = FusedMovingAvgObsFqHelperBackward0
FillBackward0 = FillScalarBackward0
Fill_Backward0 = FillScalarBackward0
FmodBackward0 = FmodScalarBackward0
FrexpBackward0 = FrexpTensorBackward0
Ge_Backward0 = GeScalarBackward0
Geometric_Backward0 = GeometricBackward0
_indicesBackward0 = IndicesBackward0
Crow_indicesBackward0 = CrowIndicesBackward0
Col_indicesBackward0 = ColIndicesBackward0
Ccol_indicesBackward0 = CcolIndicesBackward0
Row_indicesBackward0 = RowIndicesBackward0
Grid_sampler_2dBackward0 = GridSampler2dBackward0
Grid_sampler_3dBackward0 = GridSampler3dBackward0
_grid_sampler_2d_cpu_fallbackBackward0 = GridSampler2dCpuFallbackBackward0
Gt_Backward0 = GtScalarBackward0
Hardswish_backwardBackward0 = HardswishBackwardBackward0
Special_i0eBackward0 = SpecialI0eBackward0
Special_i1Backward0 = SpecialI1Backward0
Special_i1eBackward0 = SpecialI1eBackward0
IndexBackward0 = IndexTensorBackward0
_unsafe_indexBackward0 = UnsafeIndexTensorBackward0
_unsafe_masked_indexBackward0 = UnsafeMaskedIndexBackward0
_unsafe_masked_index_put_accumulateBackward0 = UnsafeMaskedIndexPutAccumulateBackward0
Index_addBackward0 = IndexAddBackward0
Index_reduceBackward0 = IndexReduceBackward0
Index_copyBackward0 = IndexCopyBackward0
Index_fillBackward0 = IndexFillIntScalarBackward0
Index_putBackward0 = IndexPutBackward0
_unsafe_index_putBackward0 = UnsafeIndexPutBackward0
_index_put_impl_Backward0 = IndexPutImplBackward0
Index_selectBackward0 = IndexSelectBackward0
Linalg_inv_exBackward0 = LinalgInvExBackward0
Linalg_pinvBackward0 = LinalgPinvAtolRtolTensorBackward0
LdexpBackward0 = LdexpTensorBackward0
Le_Backward0 = LeScalarBackward0
LerpBackward0 = LerpScalarBackward0
Polygamma_Backward0 = PolygammaBackward0
XlogyBackward0 = XlogyTensorBackward0
Special_xlog1pyBackward0 = SpecialXlog1pyBackward0
Special_zetaBackward0 = SpecialZetaBackward0
Log_normal_Backward0 = LogNormalBackward0
Linalg_lstsqBackward0 = LinalgLstsqBackward0
Lt_Backward0 = LtScalarBackward0
Linalg_lu_factor_exBackward0 = LinalgLuFactorExBackward0
Linalg_luBackward0 = LinalgLuBackward0
Linalg_lu_solveBackward0 = LinalgLuSolveBackward0
Lu_unpackBackward0 = LuUnpackBackward0
Masked_fillBackward0 = MaskedFillScalarBackward0
Masked_scatterBackward0 = MaskedScatterBackward0
Masked_scatter_backwardBackward0 = MaskedScatterBackwardBackward0
Masked_selectBackward0 = MaskedSelectBackward0
Linalg_matrix_expBackward0 = LinalgMatrixExpBackward0
MaxBackward0 = MaxDimBackward0
MinBackward0 = MinDimBackward0
_grouped_mmBackward0 = GroupedMmBackward0
MulBackward0 = MulTensorBackward0
Nan_to_numBackward0 = NanToNumBackward0
Native_batch_normBackward0 = NativeBatchNormBackward0
_native_batch_norm_legitBackward0 = NativeBatchNormLegitBackward0
_native_batch_norm_legit_no_trainingBackward0 = NativeBatchNormLegitNoTrainingBackward0
Native_batch_norm_backwardBackward0 = NativeBatchNormBackwardBackward0
Native_layer_normBackward0 = NativeLayerNormBackward0
Native_layer_norm_backwardBackward0 = NativeLayerNormBackwardBackward0
_fused_rms_normBackward0 = FusedRmsNormBackward0
Native_group_normBackward0 = NativeGroupNormBackward0
Ne_Backward0 = NeScalarBackward0
_batch_norm_with_updateBackward0 = BatchNormWithUpdateBackward0
_batch_norm_no_updateBackward0 = BatchNormNoUpdateBackward0
Batch_norm_backwardBackward0 = BatchNormBackwardBackward0
NormBackward0 = NormScalarBackward0
Linalg_vector_normBackward0 = LinalgVectorNormBackward0
_pdist_forwardBackward0 = PdistForwardBackward0
_pdist_backwardBackward0 = PdistBackwardBackward0
_euclidean_distBackward0 = EuclideanDistBackward0
_cdist_forwardBackward0 = CdistForwardBackward0
_cdist_backwardBackward0 = CdistBackwardBackward0
Normal_Backward0 = NormalBackward0
NormalBackward0 = NormalTensorFloatBackward0
Linalg_householder_productBackward0 = LinalgHouseholderProductBackward0
PowBackward0 = PowTensorScalarBackward0
Linalg_qrBackward0 = LinalgQrBackward0
Random_Backward0 = RandomFromBackward0
RemainderBackward0 = RemainderScalarBackward0
Special_entrBackward0 = SpecialEntrBackward0
Special_ndtriBackward0 = SpecialNdtriBackward0
Special_log_ndtrBackward0 = SpecialLogNdtrBackward0
_reshape_aliasBackward0 = ReshapeAliasBackward0
ScatterBackward0 = ScatterSrcBackward0
Scatter_addBackward0 = ScatterAddBackward0
SelectBackward0 = SelectIntBackward0
Select_backwardBackward0 = SelectBackwardBackward0
SliceBackward0 = SliceTensorBackward0
Slice_backwardBackward0 = SliceBackwardBackward0
Slice_inverseBackward0 = SliceInverseBackward0
Slice_scatterBackward0 = SliceScatterBackward0
Select_scatterBackward0 = SelectScatterBackward0
Diagonal_scatterBackward0 = DiagonalScatterBackward0
As_strided_scatterBackward0 = AsStridedScatterBackward0
_linalg_solve_exBackward0 = LinalgSolveExBackward0
SplitBackward0 = SplitTensorBackward0
Unsafe_splitBackward0 = UnsafeSplitTensorBackward0
Split_with_sizesBackward0 = SplitWithSizesBackward0
Unsafe_split_with_sizesBackward0 = UnsafeSplitWithSizesBackward0
Squeeze_Backward0 = SqueezeBackward0
StdBackward0 = StdCorrectionBackward0
Std_meanBackward0 = StdMeanCorrectionBackward0
SubBackward0 = SubTensorBackward0
RsubBackward0 = RsubTensorBackward0
_linalg_svdBackward0 = LinalgSvdBackward0
_linalg_eighBackward0 = LinalgEighBackward0
Linalg_eigBackward0 = LinalgEigBackward0
T_Backward0 = TBackward0
One_hotBackward0 = OneHotBackward0
TransposeBackward0 = TransposeIntBackward0
Transpose_Backward0 = TransposeBackward0
Triangular_solveBackward0 = TriangularSolveBackward0
Linalg_solve_triangularBackward0 = LinalgSolveTriangularBackward0
Hash_tensorBackward0 = HashTensorBackward0
_to_denseBackward0 = ToDenseBackward0
_to_sparseBackward0 = ToSparseSparseDimBackward0
_to_sparse_csrBackward0 = ToSparseCsrBackward0
_to_sparse_cscBackward0 = ToSparseCscBackward0
_to_sparse_bsrBackward0 = ToSparseBsrBackward0
_to_sparse_bscBackward0 = ToSparseBscBackward0
To_mkldnnBackward0 = ToMkldnnBackward0
Unfold_backwardBackward0 = UnfoldBackwardBackward0
Uniform_Backward0 = UniformBackward0
_uniqueBackward0 = UniqueBackward0
Unique_dimBackward0 = UniqueDimBackward0
Unique_consecutiveBackward0 = UniqueConsecutiveBackward0
Unique_dim_consecutiveBackward0 = UniqueDimConsecutiveBackward0
_unique2Backward0 = Unique2Backward0
_unsafe_viewBackward0 = UnsafeViewBackward0
Lift_freshBackward0 = LiftFreshBackward0
Unsqueeze_Backward0 = UnsqueezeBackward0
VarBackward0 = VarCorrectionBackward0
Var_meanBackward0 = VarMeanCorrectionBackward0
View_as_realBackward0 = ViewAsRealBackward0
View_as_complexBackward0 = ViewAsComplexBackward0
WhereBackward0 = WhereSelfBackward0
_weight_norm_interfaceBackward0 = WeightNormInterfaceBackward0
Zero_Backward0 = ZeroBackward0
Sparse_maskBackward0 = SparseMaskBackward0
_sparse_sumBackward0 = SparseSumDimBackward0
_standard_gammaBackward0 = StandardGammaBackward0
_standard_gamma_gradBackward0 = StandardGammaGradBackward0
_valuesBackward0 = ValuesBackward0
_trilinearBackward0 = TrilinearBackward0
Constant_pad_ndBackward0 = ConstantPadNdBackward0
Binary_cross_entropyBackward0 = BinaryCrossEntropyBackward0
Binary_cross_entropy_backwardBackward0 = BinaryCrossEntropyBackwardBackward0
Binary_cross_entropy_with_logitsBackward0 = BinaryCrossEntropyWithLogitsBackward0
Embedding_dense_backwardBackward0 = EmbeddingDenseBackwardBackward0
_embedding_bagBackward0 = EmbeddingBagBackward0
_embedding_bag_backwardBackward0 = EmbeddingBagBackwardBackward0
_embedding_bag_dense_backwardBackward0 = EmbeddingBagDenseBackwardBackward0
Embedding_renorm_Backward0 = EmbeddingRenormBackward0
Mse_lossBackward0 = MseLossBackward0
Multi_margin_lossBackward0 = MultiMarginLossBackward0
Multilabel_margin_loss_forwardBackward0 = MultilabelMarginLossForwardBackward0
Nll_loss_forwardBackward0 = NllLossForwardBackward0
Nll_loss2d_forwardBackward0 = NllLoss2dForwardBackward0
Smooth_l1_lossBackward0 = SmoothL1LossBackward0
Huber_lossBackward0 = HuberLossBackward0
Soft_margin_lossBackward0 = SoftMarginLossBackward0
Elu_Backward0 = EluBackward0
Celu_Backward0 = CeluBackward0
Gelu_backwardBackward0 = GeluBackwardBackward0
Hardshrink_backwardBackward0 = HardshrinkBackwardBackward0
Leaky_reluBackward0 = LeakyReluBackward0
Leaky_relu_Backward0 = LeakyReluBackward0
Log_sigmoid_forwardBackward0 = LogSigmoidForwardBackward0
_log_softmaxBackward0 = LogSoftmaxBackward0
_sparse_log_softmaxBackward0 = SparseLogSoftmaxBackward0
_masked_softmaxBackward0 = MaskedSoftmaxBackward0
_prelu_kernelBackward0 = PreluKernelBackward0
_prelu_kernel_backwardBackward0 = PreluKernelBackwardBackward0
Rrelu_with_noiseBackward0 = RreluWithNoiseBackward0
Rrelu_with_noise_Backward0 = RreluWithNoiseBackward0
Rrelu_with_noise_functionalBackward0 = RreluWithNoiseFunctionalBackward0
_softmaxBackward0 = SoftmaxBackward0
_sparse_softmaxBackward0 = SparseSoftmaxBackward0
_sparse_sparse_matmulBackward0 = SparseSparseMatmulBackward0
Threshold_Backward0 = ThresholdBackward0
Reflection_pad1dBackward0 = ReflectionPad1dBackward0
Reflection_pad2dBackward0 = ReflectionPad2dBackward0
Reflection_pad3dBackward0 = ReflectionPad3dBackward0
Replication_pad1dBackward0 = ReplicationPad1dBackward0
Replication_pad2dBackward0 = ReplicationPad2dBackward0
Replication_pad3dBackward0 = ReplicationPad3dBackward0
Upsample_linear1dBackward0 = UpsampleLinear1dBackward0
Upsample_bilinear2dBackward0 = UpsampleBilinear2dBackward0
_upsample_bilinear2d_aaBackward0 = UpsampleBilinear2dAaBackward0
Upsample_bicubic2dBackward0 = UpsampleBicubic2dBackward0
_upsample_bicubic2d_aaBackward0 = UpsampleBicubic2dAaBackward0
Upsample_trilinear3dBackward0 = UpsampleTrilinear3dBackward0
Upsample_nearest1dBackward0 = UpsampleNearest1dBackward0
_upsample_nearest_exact1dBackward0 = UpsampleNearestExact1dBackward0
Upsample_nearest2dBackward0 = UpsampleNearest2dBackward0
_upsample_nearest_exact2dBackward0 = UpsampleNearestExact2dBackward0
Upsample_nearest3dBackward0 = UpsampleNearest3dBackward0
_upsample_nearest_exact3dBackward0 = UpsampleNearestExact3dBackward0
Pixel_shuffleBackward0 = PixelShuffleBackward0
Pixel_unshuffleBackward0 = PixelUnshuffleBackward0
Channel_shuffleBackward0 = ChannelShuffleBackward0
_adaptive_avg_pool2dBackward0 = AdaptiveAvgPool2dBackward0
_adaptive_avg_pool3dBackward0 = AdaptiveAvgPool3dBackward0
Adaptive_max_pool2dBackward0 = AdaptiveMaxPool2dBackward0
Adaptive_max_pool3dBackward0 = AdaptiveMaxPool3dBackward0
Avg_pool2dBackward0 = AvgPool2dBackward0
Avg_pool3dBackward0 = AvgPool3dBackward0
Fractional_max_pool2dBackward0 = FractionalMaxPool2dBackward0
Fractional_max_pool3dBackward0 = FractionalMaxPool3dBackward0
Linear_backwardBackward0 = LinearBackwardBackward0
Max_pool2dBackward0 = MaxPool2dBackward0
_mps_convolutionBackward0 = MpsConvolutionBackward0
Mps_convolution_backwardBackward0 = MpsConvolutionBackwardBackward0
Max_pool2d_with_indicesBackward0 = MaxPool2dWithIndicesBackward0
Max_pool3d_with_indicesBackward0 = MaxPool3dWithIndicesBackward0
Max_unpool2dBackward0 = MaxUnpool2dBackward0
Max_unpool3dBackward0 = MaxUnpool3dBackward0
_convolutionBackward0 = ConvolutionBackward0
Convolution_backwardBackward0 = ConvolutionBackwardBackward0
Convolution_overrideableBackward0 = ConvolutionOverrideableBackward0
Convolution_backward_overrideableBackward0 = ConvolutionBackwardOverrideableBackward0
Slow_conv_transpose2dBackward0 = SlowConvTranspose2dBackward0
Slow_conv_transpose3dBackward0 = SlowConvTranspose3dBackward0
_slow_conv2d_forwardBackward0 = SlowConv2dForwardBackward0
_slow_conv2d_backwardBackward0 = SlowConv2dBackwardOutputMaskBackward0
_conv_depthwise2dBackward0 = ConvDepthwise2dBackward0
Conv_depthwise3dBackward0 = ConvDepthwise3dBackward0
Slow_conv3d_forwardBackward0 = SlowConv3dForwardBackward0
Slow_conv_dilated2dBackward0 = SlowConvDilated2dBackward0
Slow_conv_dilated3dBackward0 = SlowConvDilated3dBackward0
_adaptive_avg_pool2d_backwardBackward0 = AdaptiveAvgPool2dBackwardBackward0
_adaptive_avg_pool3d_backwardBackward0 = AdaptiveAvgPool3dBackwardBackward0
Adaptive_max_pool2d_backwardBackward0 = AdaptiveMaxPool2dBackwardBackward0
Adaptive_max_pool3d_backwardBackward0 = AdaptiveMaxPool3dBackwardBackward0
Avg_pool2d_backwardBackward0 = AvgPool2dBackwardBackward0
Avg_pool3d_backwardBackward0 = AvgPool3dBackwardBackward0
Elu_backwardBackward0 = EluBackwardBackward0
Fractional_max_pool2d_backwardBackward0 = FractionalMaxPool2dBackwardBackward0
Fractional_max_pool3d_backwardBackward0 = FractionalMaxPool3dBackwardBackward0
Glu_backwardBackward0 = GluBackwardBackward0
Hardtanh_backwardBackward0 = HardtanhBackwardBackward0
Log_sigmoid_backwardBackward0 = LogSigmoidBackwardBackward0
_log_softmax_backward_dataBackward0 = LogSoftmaxBackwardDataBackward0
Leaky_relu_backwardBackward0 = LeakyReluBackwardBackward0
Max_pool2d_backwardBackward0 = MaxPool2dBackwardBackward0
Max_pool2d_with_indices_backwardBackward0 = MaxPool2dWithIndicesBackwardBackward0
Max_pool3d_with_indices_backwardBackward0 = MaxPool3dWithIndicesBackwardBackward0
Mse_loss_backwardBackward0 = MseLossBackwardBackward0
Nll_loss_backwardBackward0 = NllLossBackwardBackward0
Nll_loss2d_backwardBackward0 = NllLoss2dBackwardBackward0
Rrelu_with_noise_backwardBackward0 = RreluWithNoiseBackwardBackward0
Reflection_pad1d_backwardBackward0 = ReflectionPad1dBackwardBackward0
Reflection_pad2d_backwardBackward0 = ReflectionPad2dBackwardBackward0
Reflection_pad3d_backwardBackward0 = ReflectionPad3dBackwardBackward0
Replication_pad1d_backwardBackward0 = ReplicationPad1dBackwardBackward0
Replication_pad2d_backwardBackward0 = ReplicationPad2dBackwardBackward0
Replication_pad3d_backwardBackward0 = ReplicationPad3dBackwardBackward0
Sparse_sampled_addmmBackward0 = SparseSampledAddmmBackward0
_sparse_mm_reduce_implBackward0 = SparseMmReduceImplBackward0
Smooth_l1_loss_backwardBackward0 = SmoothL1LossBackwardBackward0
Huber_loss_backwardBackward0 = HuberLossBackwardBackward0
Softplus_backwardBackward0 = SoftplusBackwardBackward0
_softmax_backward_dataBackward0 = SoftmaxBackwardDataBackward0
Soft_margin_loss_backwardBackward0 = SoftMarginLossBackwardBackward0
Softshrink_backwardBackward0 = SoftshrinkBackwardBackward0
Threshold_backwardBackward0 = ThresholdBackwardBackward0
Upsample_linear1d_backwardBackward0 = UpsampleLinear1dBackwardBackward0
Upsample_bilinear2d_backwardBackward0 = UpsampleBilinear2dBackwardBackward0
_upsample_bilinear2d_aa_backwardBackward0 = UpsampleBilinear2dAaBackwardBackward0
Upsample_bicubic2d_backwardBackward0 = UpsampleBicubic2dBackwardBackward0
_upsample_bicubic2d_aa_backwardBackward0 = UpsampleBicubic2dAaBackwardBackward0
Upsample_trilinear3d_backwardBackward0 = UpsampleTrilinear3dBackwardBackward0
Upsample_nearest1d_backwardBackward0 = UpsampleNearest1dBackwardBackward0
_upsample_nearest_exact1d_backwardBackward0 = UpsampleNearestExact1dBackwardBackward0
Upsample_nearest2d_backwardBackward0 = UpsampleNearest2dBackwardBackward0
_upsample_nearest_exact2d_backwardBackward0 = UpsampleNearestExact2dBackwardBackward0
Upsample_nearest3d_backwardBackward0 = UpsampleNearest3dBackwardBackward0
_upsample_nearest_exact3d_backwardBackward0 = UpsampleNearestExact3dBackwardBackward0
Sigmoid_backwardBackward0 = SigmoidBackwardBackward0
Tanh_backwardBackward0 = TanhBackwardBackward0
Cudnn_convolution_transposeBackward0 = CudnnConvolutionTransposeBackward0
_mps_convolution_transposeBackward0 = MpsConvolutionTransposeBackward0
Cudnn_convolutionBackward0 = CudnnConvolutionBackward0
Cudnn_grid_samplerBackward0 = CudnnGridSamplerBackward0
Cudnn_affine_grid_generatorBackward0 = CudnnAffineGridGeneratorBackward0
Cudnn_batch_normBackward0 = CudnnBatchNormBackward0
Cudnn_batch_norm_backwardBackward0 = CudnnBatchNormBackwardBackward0
_nnpack_spatial_convolutionBackward0 = NnpackSpatialConvolutionBackward0
_lstm_mpsBackward0 = LstmMpsBackward0
Lstm_mps_backwardBackward0 = LstmMpsBackwardBackward0
Miopen_convolution_transposeBackward0 = MiopenConvolutionTransposeBackward0
Miopen_convolutionBackward0 = MiopenConvolutionBackward0
Miopen_depthwise_convolutionBackward0 = MiopenDepthwiseConvolutionBackward0
Miopen_batch_normBackward0 = MiopenBatchNormBackward0
Miopen_batch_norm_backwardBackward0 = MiopenBatchNormBackwardBackward0
Miopen_rnnBackward0 = MiopenRnnBackward0
Miopen_rnn_backwardBackward0 = MiopenRnnBackwardBackward0
Miopen_ctc_lossBackward0 = MiopenCtcLossBackward0
Mkldnn_rnn_layerBackward0 = MkldnnRnnLayerBackward0
Mkldnn_rnn_layer_backwardBackward0 = MkldnnRnnLayerBackwardBackward0
Mkldnn_convolutionBackward0 = MkldnnConvolutionBackward0
Mkldnn_linearBackward0 = MkldnnLinearBackward0
Mkldnn_max_pool2dBackward0 = MkldnnMaxPool2dBackward0
Mkldnn_max_pool3dBackward0 = MkldnnMaxPool3dBackward0
Mkldnn_adaptive_avg_pool2dBackward0 = MkldnnAdaptiveAvgPool2dBackward0
To_padded_tensorBackward0 = ToPaddedTensorBackward0
_safe_softmaxBackward0 = SafeSoftmaxBackward0
_scaled_dot_product_efficient_attentionBackward0 = ScaledDotProductEfficientAttentionBackward0
_scaled_dot_product_flash_attentionBackward0 = ScaledDotProductFlashAttentionBackward0
_scaled_dot_product_flash_attention_for_cpuBackward0 = ScaledDotProductFlashAttentionForCpuBackward0
_flash_attention_forwardBackward0 = FlashAttentionForwardBackward0
_efficient_attention_forwardBackward0 = EfficientAttentionForwardBackward0
_scaled_dot_product_cudnn_attentionBackward0 = ScaledDotProductCudnnAttentionBackward0
_scaled_dot_product_fused_attention_overrideableBackward0 = ScaledDotProductFusedAttentionOverrideableBackward0
_fft_r2cBackward0 = FftR2cBackward0
_fft_c2rBackward0 = FftC2rBackward0
_fft_c2cBackward0 = FftC2cBackward0
UnbindBackward0 = UnbindIntBackward0
_thnn_fused_lstm_cellBackward0 = ThnnFusedLstmCellBackward0
_thnn_fused_gru_cellBackward0 = ThnnFusedGruCellBackward0
_pack_padded_sequenceBackward0 = PackPaddedSequenceBackward0
EqBackward0 = EqScalarBackward0
GeBackward0 = GeScalarBackward0
GtBackward0 = GtScalarBackward0
LeBackward0 = LeScalarBackward0
LtBackward0 = LtScalarBackward0
NeBackward0 = NeScalarBackward0
Segment_reduceBackward0 = SegmentReduceBackward0
_pin_memoryBackward0 = PinMemoryBackward0
_new_zeros_with_same_feature_metaBackward0 = NewZerosWithSameFeatureMetaBackward0
_efficientzerotensorBackward0 = EfficientzerotensorBackward0
Scatter_reduceBackward0 = ScatterReduceTwoBackward0
Special_airy_aiBackward0 = SpecialAiryAiBackward0
Special_bessel_j0Backward0 = SpecialBesselJ0Backward0
Special_bessel_j1Backward0 = SpecialBesselJ1Backward0
Special_bessel_y0Backward0 = SpecialBesselY0Backward0
Special_bessel_y1Backward0 = SpecialBesselY1Backward0
Special_chebyshev_polynomial_tBackward0 = SpecialChebyshevPolynomialTBackward0
Special_chebyshev_polynomial_uBackward0 = SpecialChebyshevPolynomialUBackward0
Special_chebyshev_polynomial_vBackward0 = SpecialChebyshevPolynomialVBackward0
Special_chebyshev_polynomial_wBackward0 = SpecialChebyshevPolynomialWBackward0
Special_hermite_polynomial_hBackward0 = SpecialHermitePolynomialHBackward0
Special_hermite_polynomial_heBackward0 = SpecialHermitePolynomialHeBackward0
Special_laguerre_polynomial_lBackward0 = SpecialLaguerrePolynomialLBackward0
Special_legendre_polynomial_pBackward0 = SpecialLegendrePolynomialPBackward0
Special_modified_bessel_i0Backward0 = SpecialModifiedBesselI0Backward0
Special_modified_bessel_i1Backward0 = SpecialModifiedBesselI1Backward0
Special_modified_bessel_k0Backward0 = SpecialModifiedBesselK0Backward0
Special_modified_bessel_k1Backward0 = SpecialModifiedBesselK1Backward0
Special_scaled_modified_bessel_k0Backward0 = SpecialScaledModifiedBesselK0Backward0
Special_scaled_modified_bessel_k1Backward0 = SpecialScaledModifiedBesselK1Backward0
Special_shifted_chebyshev_polynomial_tBackward0 = SpecialShiftedChebyshevPolynomialTBackward0
Special_shifted_chebyshev_polynomial_uBackward0 = SpecialShiftedChebyshevPolynomialUBackward0
Special_shifted_chebyshev_polynomial_vBackward0 = SpecialShiftedChebyshevPolynomialVBackward0
Special_shifted_chebyshev_polynomial_wBackward0 = SpecialShiftedChebyshevPolynomialWBackward0
Special_spherical_bessel_j0Backward0 = SpecialSphericalBesselJ0Backward0
_reshape_copyBackward0 = ReshapeCopyBackward0
Narrow_copyBackward0 = NarrowCopyBackward0
_foreach_divBackward0 = ForeachDivListBackward0
_foreach_powBackward0 = ForeachPowListBackward0
_foreach_minimumBackward0 = ForeachMinimumScalarBackward0
_foreach_maximumBackward0 = ForeachMaximumScalarBackward0
_foreach_normBackward0 = ForeachNormScalarBackward0


# === UPSTREAM LEGACY BACKWARD CLASSES (referenced by legacy wrappers) ===


class Adaptive_avg_pool1dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Adaptive_avg_pool1dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._output_size = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        output_size = self._output_size
        with _grad_context(keyset):
            grad_self = _adaptive_avg_pool1d_backward_helper(grad, self_, output_size, keyset)
        return (grad_self,)


class Adaptive_avg_pool2dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Adaptive_avg_pool2dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._output_size = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        output_size = self._output_size
        with _grad_context(keyset):
            grad_self = _adaptive_avg_pool2d_backward_helper(grad, self_, output_size, keyset)
        return (grad_self,)


class Adaptive_avg_pool3dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Adaptive_avg_pool3dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._output_size = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        output_size = self._output_size
        with _grad_context(keyset):
            grad_self = _adaptive_avg_pool3d_backward_helper(grad, self_, output_size, keyset)
        return (grad_self,)


class Adaptive_max_pool1dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Adaptive_max_pool1dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_result_idx = None
        self._output_size = None
        self._return_indices = None

    def _save(self, *, self_=None, result=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        result = _saved[self._saved_result_idx]
        output_size = self._output_size
        return_indices = self._return_indices
        with _grad_context(keyset):
            grad_self = _adaptive_max_pool1d_backward_helper(grad, self_, result, output_size, keyset)
        return (grad_self,)


class Affine_gridBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Affine_gridBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_theta_idx = None
        self._size = None
        self._align_corners = None

    def _save(self, *, theta=None):
        tensors = []
        if theta is not None:
            self._saved_theta_idx = len(tensors)
            tensors.append(theta)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_theta_idx is not None:
            self._saved_fields['theta'] = self._saved_tensors_list[self._saved_theta_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        theta = _saved[self._saved_theta_idx]
        size = self._size
        align_corners = self._align_corners
        with _grad_context(keyset):
            grad_theta = _affine_grid_backward_helper(grad, theta, size, align_corners, keyset)
        return (grad_theta,)


class Avg_pool1dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Avg_pool1dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._kernel_size = None
        self._stride = None
        self._padding = None
        self._ceil_mode = None
        self._count_include_pad = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        kernel_size = self._kernel_size
        stride = self._stride
        padding = self._padding
        ceil_mode = self._ceil_mode
        count_include_pad = self._count_include_pad
        with _grad_context(keyset):
            grad_self = _avg_pool1d_backward_helper(grad, self_, kernel_size, stride, padding, ceil_mode, count_include_pad, keyset)
        return (grad_self,)


class Batch_normBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Batch_normBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._saved_weight_idx = None
        self._training = None
        self._momentum = None
        self._eps = None

    def _save(self, *, input_=None, weight=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        weight = _saved[self._saved_weight_idx] if self._saved_weight_idx is not None else None
        training = self._training
        momentum = self._momentum
        eps = self._eps
        with _grad_context(keyset):
            grad_input = _batch_norm_grad_input(grad, input_, weight, eps, keyset)
        return (grad_input,)


class Broadcast_toBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Broadcast_toBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._shape = None

    def _save(self, *, input_=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        shape = self._shape
        with _grad_context(keyset):
            grad_input = reduce_grad(grad, input_.shape)
        return (grad_input,)


class CdistBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='CdistBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_x1_idx = None
        self._saved_x2_idx = None
        self._p = None

    def _save(self, *, x1=None, x2=None):
        tensors = []
        if x1 is not None:
            self._saved_x1_idx = len(tensors)
            tensors.append(x1)
        if x2 is not None:
            self._saved_x2_idx = len(tensors)
            tensors.append(x2)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_x1_idx is not None:
            self._saved_fields['x1'] = self._saved_tensors_list[self._saved_x1_idx]
        if self._saved_x2_idx is not None:
            self._saved_fields['x2'] = self._saved_tensors_list[self._saved_x2_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        x1 = _saved[self._saved_x1_idx]
        x2 = _saved[self._saved_x2_idx]
        p = self._p
        with _grad_context(keyset):
            grad_x1, grad_x2 = _cdist_backward_all(grad, x1, x2, p, keyset)
        return (grad_x1, grad_x2,)


class Column_stackBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Column_stackBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        tensors = list(self.inputs)
        with _grad_context(keyset):
            grad_tensors = _column_stack_backward_helper(grad, tensors, keyset)
        return grad_tensors


class ConcatBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ConcatBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._dim = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        tensors = list(self.inputs)
        dim = self._dim
        with _grad_context(keyset):
            grad_tensors = _concat_backward_helper(grad, tensors, dim, keyset)
        return grad_tensors


class ConcatenateBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ConcatenateBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._dim = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        tensors = list(self.inputs)
        dim = self._dim
        with _grad_context(keyset):
            grad_tensors = _concatenate_backward_helper(grad, tensors, dim, keyset)
        return grad_tensors


class ContiguousBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='ContiguousBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        with _grad_context(keyset):
            grad_self = grad
        return (grad_self,)


class Conv1dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Conv1dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_bias_idx = None
        self._saved_input_idx = None
        self._saved_weight_idx = None
        self._stride = None
        self._padding = None
        self._dilation = None
        self._groups = None

    def _save(self, *, bias=None, input_=None, weight=None):
        tensors = []
        if bias is not None:
            self._saved_bias_idx = len(tensors)
            tensors.append(bias)
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_bias_idx is not None:
            self._saved_fields['bias'] = self._saved_tensors_list[self._saved_bias_idx]
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        bias = _saved[self._saved_bias_idx] if self._saved_bias_idx is not None else None
        input_ = _saved[self._saved_input_idx]
        weight = _saved[self._saved_weight_idx]
        stride = self._stride
        padding = self._padding
        dilation = self._dilation
        groups = self._groups
        with _grad_context(keyset):
            grad_input, grad_weight, grad_bias = _conv_backward_all(grad, input_, weight, bias, stride, padding, dilation, groups, 'conv1d', keyset)
        return (grad_input, grad_weight, grad_bias,)


class Conv2dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Conv2dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_bias_idx = None
        self._saved_input_idx = None
        self._saved_weight_idx = None
        self._stride = None
        self._padding = None
        self._dilation = None
        self._groups = None

    def _save(self, *, bias=None, input_=None, weight=None):
        tensors = []
        if bias is not None:
            self._saved_bias_idx = len(tensors)
            tensors.append(bias)
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_bias_idx is not None:
            self._saved_fields['bias'] = self._saved_tensors_list[self._saved_bias_idx]
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        bias = _saved[self._saved_bias_idx] if self._saved_bias_idx is not None else None
        input_ = _saved[self._saved_input_idx]
        weight = _saved[self._saved_weight_idx]
        stride = self._stride
        padding = self._padding
        dilation = self._dilation
        groups = self._groups
        with _grad_context(keyset):
            grad_input, grad_weight, grad_bias = _conv_backward_all(grad, input_, weight, bias, stride, padding, dilation, groups, 'conv2d', keyset)
        return (grad_input, grad_weight, grad_bias,)


class Conv3dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Conv3dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_bias_idx = None
        self._saved_input_idx = None
        self._saved_weight_idx = None
        self._stride = None
        self._padding = None
        self._dilation = None
        self._groups = None

    def _save(self, *, bias=None, input_=None, weight=None):
        tensors = []
        if bias is not None:
            self._saved_bias_idx = len(tensors)
            tensors.append(bias)
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_bias_idx is not None:
            self._saved_fields['bias'] = self._saved_tensors_list[self._saved_bias_idx]
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        bias = _saved[self._saved_bias_idx] if self._saved_bias_idx is not None else None
        input_ = _saved[self._saved_input_idx]
        weight = _saved[self._saved_weight_idx]
        stride = self._stride
        padding = self._padding
        dilation = self._dilation
        groups = self._groups
        with _grad_context(keyset):
            grad_input, grad_weight, grad_bias = _conv_backward_all(grad, input_, weight, bias, stride, padding, dilation, groups, 'conv3d', keyset)
        return (grad_input, grad_weight, grad_bias,)


class Conv_transpose1dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Conv_transpose1dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_bias_idx = None
        self._saved_input_idx = None
        self._saved_weight_idx = None
        self._stride = None
        self._padding = None
        self._output_padding = None
        self._groups = None
        self._dilation = None

    def _save(self, *, bias=None, input_=None, weight=None):
        tensors = []
        if bias is not None:
            self._saved_bias_idx = len(tensors)
            tensors.append(bias)
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_bias_idx is not None:
            self._saved_fields['bias'] = self._saved_tensors_list[self._saved_bias_idx]
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        bias = _saved[self._saved_bias_idx] if self._saved_bias_idx is not None else None
        input_ = _saved[self._saved_input_idx]
        weight = _saved[self._saved_weight_idx]
        stride = self._stride
        padding = self._padding
        output_padding = self._output_padding
        groups = self._groups
        dilation = self._dilation
        with _grad_context(keyset):
            grad_input, grad_weight, grad_bias = _conv_backward_all(grad, input_, weight, bias, stride, padding, dilation, groups, 'conv_transpose1d', keyset)
        return (grad_input, grad_weight, grad_bias,)


class Conv_transpose2dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Conv_transpose2dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_bias_idx = None
        self._saved_input_idx = None
        self._saved_weight_idx = None
        self._stride = None
        self._padding = None
        self._output_padding = None
        self._groups = None
        self._dilation = None

    def _save(self, *, bias=None, input_=None, weight=None):
        tensors = []
        if bias is not None:
            self._saved_bias_idx = len(tensors)
            tensors.append(bias)
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_bias_idx is not None:
            self._saved_fields['bias'] = self._saved_tensors_list[self._saved_bias_idx]
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        bias = _saved[self._saved_bias_idx] if self._saved_bias_idx is not None else None
        input_ = _saved[self._saved_input_idx]
        weight = _saved[self._saved_weight_idx]
        stride = self._stride
        padding = self._padding
        output_padding = self._output_padding
        groups = self._groups
        dilation = self._dilation
        with _grad_context(keyset):
            grad_input, grad_weight, grad_bias = _conv_backward_all(grad, input_, weight, bias, stride, padding, dilation, groups, 'conv_transpose2d', keyset)
        return (grad_input, grad_weight, grad_bias,)


class Conv_transpose3dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Conv_transpose3dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_bias_idx = None
        self._saved_input_idx = None
        self._saved_weight_idx = None
        self._stride = None
        self._padding = None
        self._output_padding = None
        self._groups = None
        self._dilation = None

    def _save(self, *, bias=None, input_=None, weight=None):
        tensors = []
        if bias is not None:
            self._saved_bias_idx = len(tensors)
            tensors.append(bias)
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_bias_idx is not None:
            self._saved_fields['bias'] = self._saved_tensors_list[self._saved_bias_idx]
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        bias = _saved[self._saved_bias_idx] if self._saved_bias_idx is not None else None
        input_ = _saved[self._saved_input_idx]
        weight = _saved[self._saved_weight_idx]
        stride = self._stride
        padding = self._padding
        output_padding = self._output_padding
        groups = self._groups
        dilation = self._dilation
        with _grad_context(keyset):
            grad_input, grad_weight, grad_bias = _conv_backward_all(grad, input_, weight, bias, stride, padding, dilation, groups, 'conv_transpose3d', keyset)
        return (grad_input, grad_weight, grad_bias,)


class CrossBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='CrossBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_other_idx = None
        self._saved_self_idx = None
        self._dim = None

    def _save(self, *, other=None, self_=None):
        tensors = []
        if other is not None:
            self._saved_other_idx = len(tensors)
            tensors.append(other)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_other_idx is not None:
            self._saved_fields['other'] = self._saved_tensors_list[self._saved_other_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        other = _saved[self._saved_other_idx]
        self_ = _saved[self._saved_self_idx]
        dim = self._dim
        with _grad_context(keyset):
            grad_self, grad_other = _cross_backward_all(grad, self_, other, dim, keyset)
        return (grad_self, grad_other,)


class Ctc_lossBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Ctc_lossBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._targets = None
        self._input_lengths = None
        self._target_lengths = None
        self._blank = None
        self._reduction = None
        self._zero_infinity = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        targets = self._targets
        input_lengths = self._input_lengths
        target_lengths = self._target_lengths
        blank = self._blank
        reduction = self._reduction
        zero_infinity = self._zero_infinity
        with _grad_context(keyset):
            grad_self = _ctc_loss_backward_helper(grad, self_, targets, input_lengths, target_lengths, blank, reduction, zero_infinity, keyset)
        return (grad_self,)


class DetBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='DetBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None

    def _save(self, *, input_=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        with _grad_context(keyset):
            grad_input = _det_backward_helper(grad, input_, keyset)
        return (grad_input,)


class DiagBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='DiagBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._diagonal = None

    def _save(self, *, input_=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        diagonal = self._diagonal
        with _grad_context(keyset):
            grad_input = _diag_backward_helper(grad, input_, diagonal, keyset)
        return (grad_input,)


class DiffBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='DiffBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._n = None
        self._dim = None

    def _save(self, *, input_=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        n = self._n
        dim = self._dim
        with _grad_context(keyset):
            grad_input = _diff_backward_helper(grad, input_, n, dim, keyset)
        return (grad_input,)


class DstackBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='DstackBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        tensors = list(self.inputs)
        with _grad_context(keyset):
            grad_tensors = _dstack_backward_helper(grad, tensors, keyset)
        return grad_tensors


class Fft_fft2Backward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Fft_fft2Backward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._s = None
        self._dim = None
        self._norm = None

    def _save(self, *, input_=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        s = self._s
        dim = self._dim
        norm = self._norm
        with _grad_context(keyset):
            grad_input = _fft_c2c_backward_helper(grad, input_, s, dim, norm, 'fft_ifft2', keyset)
        return (grad_input,)


class Fft_fftBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Fft_fftBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._n = None
        self._dim = None
        self._norm = None

    def _save(self, *, input_=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        n = self._n
        dim = self._dim
        norm = self._norm
        with _grad_context(keyset):
            grad_input = _fft_c2c_backward_helper(grad, input_, n, dim, norm, 'fft_ifft', keyset)
        return (grad_input,)


class Fft_fftnBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Fft_fftnBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._s = None
        self._dim = None
        self._norm = None

    def _save(self, *, input_=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        s = self._s
        dim = self._dim
        norm = self._norm
        with _grad_context(keyset):
            grad_input = _fft_c2c_backward_helper(grad, input_, s, dim, norm, 'fft_ifftn', keyset)
        return (grad_input,)


class Fft_fftshiftBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Fft_fftshiftBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._dim = None

    def _save(self, *, input_=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        dim = self._dim
        with _grad_context(keyset):
            grad_input = _fft_shift_backward_helper(grad, input_, dim, 'fft_ifftshift', keyset)
        return (grad_input,)


class Fft_hfftBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Fft_hfftBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._n = None
        self._dim = None
        self._norm = None

    def _save(self, *, input_=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        n = self._n
        dim = self._dim
        norm = self._norm
        with _grad_context(keyset):
            grad_input = _fft_c2r_backward_helper(grad, input_, n, dim, norm, 'fft_ihfft', keyset)
        return (grad_input,)


class Fft_ifft2Backward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Fft_ifft2Backward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._s = None
        self._dim = None
        self._norm = None

    def _save(self, *, input_=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        s = self._s
        dim = self._dim
        norm = self._norm
        with _grad_context(keyset):
            grad_input = _fft_c2c_backward_helper(grad, input_, s, dim, norm, 'fft_fft2', keyset)
        return (grad_input,)


class Fft_ifftBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Fft_ifftBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._n = None
        self._dim = None
        self._norm = None

    def _save(self, *, input_=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        n = self._n
        dim = self._dim
        norm = self._norm
        with _grad_context(keyset):
            grad_input = _fft_c2c_backward_helper(grad, input_, n, dim, norm, 'fft_fft', keyset)
        return (grad_input,)


class Fft_ifftnBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Fft_ifftnBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._s = None
        self._dim = None
        self._norm = None

    def _save(self, *, input_=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        s = self._s
        dim = self._dim
        norm = self._norm
        with _grad_context(keyset):
            grad_input = _fft_c2c_backward_helper(grad, input_, s, dim, norm, 'fft_fftn', keyset)
        return (grad_input,)


class Fft_ifftshiftBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Fft_ifftshiftBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._dim = None

    def _save(self, *, input_=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        dim = self._dim
        with _grad_context(keyset):
            grad_input = _fft_shift_backward_helper(grad, input_, dim, 'fft_fftshift', keyset)
        return (grad_input,)


class Fft_ihfftBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Fft_ihfftBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._n = None
        self._dim = None
        self._norm = None

    def _save(self, *, input_=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        n = self._n
        dim = self._dim
        norm = self._norm
        with _grad_context(keyset):
            grad_input = _fft_r2c_backward_helper(grad, input_, n, dim, norm, 'fft_hfft', keyset)
        return (grad_input,)


class Fft_irfft2Backward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Fft_irfft2Backward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._s = None
        self._dim = None
        self._norm = None

    def _save(self, *, input_=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        s = self._s
        dim = self._dim
        norm = self._norm
        with _grad_context(keyset):
            grad_input = _fft_c2r_backward_helper(grad, input_, s, dim, norm, 'fft_rfft2', keyset)
        return (grad_input,)


class Fft_irfftBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Fft_irfftBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._n = None
        self._dim = None
        self._norm = None

    def _save(self, *, input_=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        n = self._n
        dim = self._dim
        norm = self._norm
        with _grad_context(keyset):
            grad_input = _fft_c2r_backward_helper(grad, input_, n, dim, norm, 'fft_rfft', keyset)
        return (grad_input,)


class Fft_irfftnBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Fft_irfftnBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._s = None
        self._dim = None
        self._norm = None

    def _save(self, *, input_=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        s = self._s
        dim = self._dim
        norm = self._norm
        with _grad_context(keyset):
            grad_input = _fft_c2r_backward_helper(grad, input_, s, dim, norm, 'fft_rfftn', keyset)
        return (grad_input,)


class Fft_rfft2Backward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Fft_rfft2Backward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._s = None
        self._dim = None
        self._norm = None

    def _save(self, *, input_=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        s = self._s
        dim = self._dim
        norm = self._norm
        with _grad_context(keyset):
            grad_input = _fft_r2c_backward_helper(grad, input_, s, dim, norm, 'fft_irfft2', keyset)
        return (grad_input,)


class Fft_rfftBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Fft_rfftBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._n = None
        self._dim = None
        self._norm = None

    def _save(self, *, input_=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        n = self._n
        dim = self._dim
        norm = self._norm
        with _grad_context(keyset):
            grad_input = _fft_r2c_backward_helper(grad, input_, n, dim, norm, 'fft_irfft', keyset)
        return (grad_input,)


class Fft_rfftnBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Fft_rfftnBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._s = None
        self._dim = None
        self._norm = None

    def _save(self, *, input_=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        s = self._s
        dim = self._dim
        norm = self._norm
        with _grad_context(keyset):
            grad_input = _fft_r2c_backward_helper(grad, input_, s, dim, norm, 'fft_irfftn', keyset)
        return (grad_input,)


class FlattenBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='FlattenBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._start_dim = None
        self._end_dim = None

    def _save(self, *, input_=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        start_dim = self._start_dim
        end_dim = self._end_dim
        with _grad_context(keyset):
            grad_input = redispatch("reshape", keyset, grad, input_.shape)
        return (grad_input,)


class Floor_divideBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Floor_divideBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_other_idx = None

    def _save(self, *, self_=None, other=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if other is not None:
            self._saved_other_idx = len(tensors)
            tensors.append(other)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_other_idx is not None:
            self._saved_fields['other'] = self._saved_tensors_list[self._saved_other_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        other = _saved[self._saved_other_idx]
        with _grad_context(keyset):
            grad_self = redispatch("mul", keyset, grad, _scalar_tensor_like(self_, 0.0))
            grad_other = redispatch("mul", keyset, grad, _scalar_tensor_like(other, 0.0))
        return (grad_self, grad_other,)


class GetitemBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='GetitemBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._key = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        key = self._key
        with _grad_context(keyset):
            grad_self = _getitem_backward_helper(grad, self_, key, keyset)
        return (grad_self,)


class Grid_sampleBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Grid_sampleBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_grid_idx = None
        self._saved_self_idx = None
        self._mode = None
        self._padding_mode = None
        self._align_corners = None

    def _save(self, *, grid=None, self_=None):
        tensors = []
        if grid is not None:
            self._saved_grid_idx = len(tensors)
            tensors.append(grid)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_grid_idx is not None:
            self._saved_fields['grid'] = self._saved_tensors_list[self._saved_grid_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        grid = _saved[self._saved_grid_idx]
        self_ = _saved[self._saved_self_idx]
        mode = self._mode
        padding_mode = self._padding_mode
        align_corners = self._align_corners
        with _grad_context(keyset):
            grad_self, grad_grid = _grid_sample_backward_all(grad, self_, grid, mode, padding_mode, align_corners, keyset)
        return (grad_self, grad_grid,)


class Group_normBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Group_normBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._saved_weight_idx = None
        self._num_groups = None
        self._eps = None

    def _save(self, *, input_=None, weight=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        weight = _saved[self._saved_weight_idx] if self._saved_weight_idx is not None else None
        num_groups = self._num_groups
        eps = self._eps
        with _grad_context(keyset):
            grad_input = _group_norm_grad_input(grad, input_, num_groups, weight, eps, keyset)
        return (grad_input,)


class HeavisideBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='HeavisideBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_other_idx = None

    def _save(self, *, self_=None, other=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if other is not None:
            self._saved_other_idx = len(tensors)
            tensors.append(other)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_other_idx is not None:
            self._saved_fields['other'] = self._saved_tensors_list[self._saved_other_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        other = _saved[self._saved_other_idx]
        with _grad_context(keyset):
            grad_self = redispatch("mul", keyset, grad, _scalar_tensor_like(self_, 0.0))
            grad_other = redispatch("mul", keyset, grad, _scalar_tensor_like(other, 0.0))
        return (grad_self, grad_other,)


class HstackBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='HstackBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        tensors = list(self.inputs)
        with _grad_context(keyset):
            grad_tensors = _hstack_backward_helper(grad, tensors, keyset)
        return grad_tensors


class InnerBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='InnerBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_other_idx = None
        self._saved_self_idx = None

    def _save(self, *, other=None, self_=None):
        tensors = []
        if other is not None:
            self._saved_other_idx = len(tensors)
            tensors.append(other)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_other_idx is not None:
            self._saved_fields['other'] = self._saved_tensors_list[self._saved_other_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        other = _saved[self._saved_other_idx]
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self, grad_other = _inner_backward_all(grad, self_, other, keyset)
        return (grad_self, grad_other,)


class Instance_normBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Instance_normBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._weight = None
        self._bias = None
        self._running_mean = None
        self._running_var = None
        self._use_input_stats = None
        self._momentum = None
        self._eps = None
        self._cudnn_enabled = None

    def _save(self, *, input_=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        weight = self._weight
        bias = self._bias
        running_mean = self._running_mean
        running_var = self._running_var
        use_input_stats = self._use_input_stats
        momentum = self._momentum
        eps = self._eps
        cudnn_enabled = self._cudnn_enabled
        with _grad_context(keyset):
            grad_input = _instance_norm_backward_helper(grad, input_, weight, bias, running_mean, running_var, use_input_stats, momentum, eps, cudnn_enabled, keyset)
        return (grad_input,)


class Layer_normBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Layer_normBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_bias_idx = None
        self._saved_input_idx = None
        self._saved_weight_idx = None
        self._normalized_shape = None
        self._eps = None

    def _save(self, *, bias=None, input_=None, weight=None):
        tensors = []
        if bias is not None:
            self._saved_bias_idx = len(tensors)
            tensors.append(bias)
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_bias_idx is not None:
            self._saved_fields['bias'] = self._saved_tensors_list[self._saved_bias_idx]
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        bias = _saved[self._saved_bias_idx] if self._saved_bias_idx is not None else None
        input_ = _saved[self._saved_input_idx]
        weight = _saved[self._saved_weight_idx] if self._saved_weight_idx is not None else None
        normalized_shape = self._normalized_shape
        eps = self._eps
        with _grad_context(keyset):
            grad_input, grad_weight, grad_bias = _layer_norm_backward_all(grad, input_, normalized_shape, weight, bias, eps, keyset)
        return (grad_input, grad_weight, grad_bias,)


class Linalg_choleskyBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Linalg_choleskyBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._upper = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        upper = self._upper
        with _grad_context(keyset):
            grad_self = _linalg_cholesky_backward_helper(grad, self_, upper, keyset)
        return (grad_self,)


class Linalg_condBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Linalg_condBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._p = None

    def _save(self, *, input_=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        p = self._p
        with _grad_context(keyset):
            grad_input = _linalg_cond_backward_helper(grad, input_, p, keyset)
        return (grad_input,)


class Linalg_detBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Linalg_detBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = _linalg_det_backward_helper(grad, self_, keyset)
        return (grad_self,)


class Linalg_eigvalsBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Linalg_eigvalsBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("mul", keyset, grad, _scalar_tensor_like(self_, 0.0))
        return (grad_self,)


class Linalg_eigvalshBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Linalg_eigvalshBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._UPLO = None

    def _save(self, *, input_=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        UPLO = self._UPLO
        with _grad_context(keyset):
            grad_input = _linalg_eigvalsh_backward_helper(grad, input_, UPLO, keyset)
        return (grad_input,)


class Linalg_invBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Linalg_invBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = _linalg_inv_grad(grad, self_, keyset)
        return (grad_self,)


class Linalg_matrix_normBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Linalg_matrix_normBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._ord = None
        self._dim = None
        self._keepdim = None

    def _save(self, *, input_=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        ord = self._ord
        dim = self._dim
        keepdim = self._keepdim
        with _grad_context(keyset):
            grad_input = _linalg_matrix_norm_backward_helper(grad, input_, ord, dim, keepdim, keyset)
        return (grad_input,)


class Linalg_matrix_powerBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Linalg_matrix_powerBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._n = None

    def _save(self, *, input_=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        n = self._n
        with _grad_context(keyset):
            grad_input = _matrix_power_backward_helper(grad, input_, n, keyset)
        return (grad_input,)


class Linalg_matrix_rankBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Linalg_matrix_rankBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._atol = None
        self._rtol = None
        self._hermitian = None

    def _save(self, *, input_=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        atol = self._atol
        rtol = self._rtol
        hermitian = self._hermitian
        with _grad_context(keyset):
            grad_input = redispatch("mul", keyset, grad, _scalar_tensor_like(input_, 0.0))
        return (grad_input,)


class Linalg_normBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Linalg_normBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._ord = None
        self._dim = None
        self._keepdim = None

    def _save(self, *, input_=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        ord = self._ord
        dim = self._dim
        keepdim = self._keepdim
        with _grad_context(keyset):
            grad_input = _linalg_norm_backward_helper(grad, input_, ord, dim, keepdim, keyset)
        return (grad_input,)


class Linalg_slogdetBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Linalg_slogdetBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = _linalg_slogdet_backward_helper(grad, self_, keyset)
        return (grad_self,)


class Linalg_solveBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Linalg_solveBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_other_idx = None
        self._saved_self_idx = None
        self._left = None

    def _save(self, *, other=None, self_=None):
        tensors = []
        if other is not None:
            self._saved_other_idx = len(tensors)
            tensors.append(other)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_other_idx is not None:
            self._saved_fields['other'] = self._saved_tensors_list[self._saved_other_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        other = _saved[self._saved_other_idx]
        self_ = _saved[self._saved_self_idx]
        left = self._left
        with _grad_context(keyset):
            grad_self, grad_other = _linalg_solve_backward_all(grad, self_, other, left, keyset)
        return (grad_self, grad_other,)


class Linalg_svdvalsBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Linalg_svdvalsBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = _linalg_svdvals_grad(grad, self_, keyset)
        return (grad_self,)


class Linalg_tensorinvBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Linalg_tensorinvBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._ind = None

    def _save(self, *, input_=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        ind = self._ind
        with _grad_context(keyset):
            grad_input = _linalg_tensorinv_backward_helper(grad, input_, ind, keyset)
        return (grad_input,)


class Linalg_tensorsolveBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Linalg_tensorsolveBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._dims = None

    def _save(self, *, input_=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        dims = self._dims
        with _grad_context(keyset):
            grad_input = redispatch("mul", keyset, grad, _scalar_tensor_like(input_, 0.0))
        return (grad_input,)


class Linalg_vanderBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Linalg_vanderBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_x_idx = None
        self._N = None

    def _save(self, *, x=None):
        tensors = []
        if x is not None:
            self._saved_x_idx = len(tensors)
            tensors.append(x)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_x_idx is not None:
            self._saved_fields['x'] = self._saved_tensors_list[self._saved_x_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        x = _saved[self._saved_x_idx]
        N = self._N
        with _grad_context(keyset):
            grad_x = _linalg_vander_backward_helper(grad, x, N, keyset)
        return (grad_x,)


class Log_softmaxBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Log_softmaxBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._dim = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        dim = self._dim
        with _grad_context(keyset):
            grad_self = _log_softmax_grad(grad, self_, dim, keyset)
        return (grad_self,)


class Matrix_powerBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Matrix_powerBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._n = None

    def _save(self, *, input_=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        n = self._n
        with _grad_context(keyset):
            grad_input = _matrix_power_backward_helper(grad, input_, n, keyset)
        return (grad_input,)


class Max_pool1dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Max_pool1dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_result_idx = None
        self._kernel_size = None
        self._stride = None
        self._padding = None
        self._dilation = None
        self._ceil_mode = None
        self._return_indices = None

    def _save(self, *, self_=None, result=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        result = _saved[self._saved_result_idx]
        kernel_size = self._kernel_size
        stride = self._stride
        padding = self._padding
        dilation = self._dilation
        ceil_mode = self._ceil_mode
        return_indices = self._return_indices
        with _grad_context(keyset):
            grad_self = _max_pool1d_backward_helper(grad, self_, result, kernel_size, stride, padding, dilation, ceil_mode, keyset)
        return (grad_self,)


class Max_pool3dBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Max_pool3dBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_result_idx = None
        self._kernel_size = None
        self._stride = None
        self._padding = None
        self._dilation = None
        self._ceil_mode = None
        self._return_indices = None

    def _save(self, *, self_=None, result=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if result is not None:
            self._saved_result_idx = len(tensors)
            tensors.append(result)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_result_idx is not None:
            self._saved_fields['result'] = self._saved_tensors_list[self._saved_result_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        result = _saved[self._saved_result_idx]
        kernel_size = self._kernel_size
        stride = self._stride
        padding = self._padding
        dilation = self._dilation
        ceil_mode = self._ceil_mode
        return_indices = self._return_indices
        with _grad_context(keyset):
            grad_self = _max_pool3d_backward_helper(grad, self_, result, kernel_size, stride, padding, dilation, ceil_mode, keyset)
        return (grad_self,)


class MoveaxisBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MoveaxisBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._source = None
        self._destination = None

    def _save(self, *, input_=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        source = self._source
        destination = self._destination
        with _grad_context(keyset):
            grad_input = _movedim_backward_helper(grad, input_, source, destination, keyset)
        return (grad_input,)


class MovedimBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='MovedimBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._source = None
        self._destination = None

    def _save(self, *, input_=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        source = self._source
        destination = self._destination
        with _grad_context(keyset):
            grad_input = _movedim_backward_helper(grad, input_, source, destination, keyset)
        return (grad_input,)


class NanmeanBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='NanmeanBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._dim = None
        self._keepdim = None

    def _save(self, *, input_=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        dim = self._dim
        keepdim = self._keepdim
        with _grad_context(keyset):
            grad_input = _nanmean_backward_helper(grad, input_, dim, keepdim, keyset)
        return (grad_input,)


class NanquantileBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='NanquantileBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._q = None
        self._dim = None
        self._keepdim = None

    def _save(self, *, input_=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        q = self._q
        dim = self._dim
        keepdim = self._keepdim
        with _grad_context(keyset):
            grad_input = _quantile_backward_helper(grad, input_, q, dim, keepdim, keyset)
        return (grad_input,)


class NarrowBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='NarrowBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._dim = None
        self._start = None
        self._length = None

    def _save(self, *, input_=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        dim = self._dim
        start = self._start
        length = self._length
        with _grad_context(keyset):
            grad_input = _narrow_backward_helper(grad, input_, dim, start, length, keyset)
        return (grad_input,)


class NormalizeBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='NormalizeBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._p = None
        self._dim = None
        self._eps = None

    def _save(self, *, input_=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        p = self._p
        dim = self._dim
        eps = self._eps
        with _grad_context(keyset):
            grad_input = _normalize_backward_helper(grad, input_, p, dim, eps, keyset)
        return (grad_input,)


class OuterBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='OuterBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_other_idx = None
        self._saved_self_idx = None

    def _save(self, *, other=None, self_=None):
        tensors = []
        if other is not None:
            self._saved_other_idx = len(tensors)
            tensors.append(other)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_other_idx is not None:
            self._saved_fields['other'] = self._saved_tensors_list[self._saved_other_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        other = _saved[self._saved_other_idx]
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("matmul", keyset, grad, other)
            grad_other = redispatch("matmul", keyset, redispatch("transpose", keyset, grad, 0, 1), self_)
        return (grad_self, grad_other,)


class PadBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='PadBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._pad = None
        self._mode = None
        self._value = None

    def _save(self, *, input_=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        pad = self._pad
        mode = self._mode
        value = self._value
        with _grad_context(keyset):
            grad_input = _pad_backward_helper(grad, input_, pad, keyset)
        return (grad_input,)


class Pad_sequenceBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Pad_sequenceBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._batch_first = None
        self._padding_value = None
        self._padding_side = None

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        sequences = list(self.inputs)
        batch_first = self._batch_first
        padding_value = self._padding_value
        padding_side = self._padding_side
        with _grad_context(keyset):
            grad_sequences = _pad_sequence_backward_helper(grad, sequences, batch_first, padding_value, padding_side, keyset)
        return grad_sequences


class PreluBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='PreluBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._saved_weight_idx = None

    def _save(self, *, self_=None, weight=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        weight = _saved[self._saved_weight_idx]
        with _grad_context(keyset):
            grad_self = _prelu_grad_input(grad, self_, weight, keyset)
            grad_weight = reduce_grad(_prelu_grad_weight(grad, self_, weight, keyset), weight.shape)
        return (grad_self, grad_weight,)


class QuantileBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='QuantileBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._q = None
        self._dim = None
        self._keepdim = None

    def _save(self, *, input_=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        q = self._q
        dim = self._dim
        keepdim = self._keepdim
        with _grad_context(keyset):
            grad_input = _quantile_backward_helper(grad, input_, q, dim, keepdim, keyset)
        return (grad_input,)


class Relu6Backward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Relu6Backward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = _hardtanh_grad(grad, self_, 0.0, 6.0, keyset)
        return (grad_self,)


class Repeat_interleaveBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Repeat_interleaveBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._repeats = None
        self._dim = None

    def _save(self, *, input_=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        repeats = self._repeats
        dim = self._dim
        with _grad_context(keyset):
            grad_input = _repeat_interleave_backward_helper(grad, input_, repeats, dim, keyset)
        return (grad_input,)


class Rms_normBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Rms_normBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._saved_weight_idx = None
        self._normalized_shape = None
        self._eps = None

    def _save(self, *, input_=None, weight=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if weight is not None:
            self._saved_weight_idx = len(tensors)
            tensors.append(weight)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]
        if self._saved_weight_idx is not None:
            self._saved_fields['weight'] = self._saved_tensors_list[self._saved_weight_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        weight = _saved[self._saved_weight_idx] if self._saved_weight_idx is not None else None
        normalized_shape = self._normalized_shape
        eps = self._eps
        with _grad_context(keyset):
            grad_input = _rms_norm_grad_input(grad, input_, normalized_shape, weight, eps, keyset)
        return (grad_input,)


class Row_stackBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Row_stackBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        tensors = list(self.inputs)
        with _grad_context(keyset):
            grad_tensors = _vstack_backward_helper(grad, tensors, keyset)
        return grad_tensors


class SeluBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SeluBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = _selu_grad(grad, self_, keyset)
        return (grad_self,)


class SignbitBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SignbitBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("mul", keyset, grad, _scalar_tensor_like(self_, 0.0))
        return (grad_self,)


class SoftsignBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SoftsignBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = _softsign_grad(grad, self_, keyset)
        return (grad_self,)


class Special_digammaBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Special_digammaBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("mul", keyset, grad, redispatch("special_polygamma", keyset, 1, self_))
        return (grad_self,)


class Special_erfinvBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Special_erfinvBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = _special_erfinv_grad(grad, self_, keyset)
        return (grad_self,)


class Special_gammaincBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Special_gammaincBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_other_idx = None
        self._saved_self_idx = None

    def _save(self, *, other=None, self_=None):
        tensors = []
        if other is not None:
            self._saved_other_idx = len(tensors)
            tensors.append(other)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_other_idx is not None:
            self._saved_fields['other'] = self._saved_tensors_list[self._saved_other_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        other = _saved[self._saved_other_idx]
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self, grad_other = _special_gammainc_backward_all(grad, self_, other, keyset)
        return (grad_self, grad_other,)


class Special_gammainccBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Special_gammainccBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_other_idx = None
        self._saved_self_idx = None

    def _save(self, *, other=None, self_=None):
        tensors = []
        if other is not None:
            self._saved_other_idx = len(tensors)
            tensors.append(other)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_other_idx is not None:
            self._saved_fields['other'] = self._saved_tensors_list[self._saved_other_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        other = _saved[self._saved_other_idx]
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self, grad_other = _special_gammaincc_backward_all(grad, self_, other, keyset)
        return (grad_self, grad_other,)


class Special_gammalnBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Special_gammalnBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("mul", keyset, grad, redispatch("special_digamma", keyset, self_))
        return (grad_self,)


class Special_i0Backward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Special_i0Backward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("mul", keyset, grad, redispatch("special_i1", keyset, self_))
        return (grad_self,)


class Special_logitBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Special_logitBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._eps = None

    def _save(self, *, input_=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        eps = self._eps
        with _grad_context(keyset):
            grad_input = _special_logit_backward_helper(grad, input_, eps, keyset)
        return (grad_input,)


class Special_multigammalnBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Special_multigammalnBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._p = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        p = self._p
        with _grad_context(keyset):
            grad_self = _special_multigammaln_backward_helper(grad, self_, p, keyset)
        return (grad_self,)


class Special_ndtrBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Special_ndtrBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = _special_ndtr_grad(grad, self_, keyset)
        return (grad_self,)


class Special_polygammaBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Special_polygammaBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None
        self._n = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        n = self._n
        with _grad_context(keyset):
            grad_self = _special_polygamma_backward_helper(grad, n, self_, keyset)
        return (grad_self,)


class Special_sincBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Special_sincBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = _special_sinc_grad(grad, self_, keyset)
        return (grad_self,)


class Special_xlogyBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Special_xlogyBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_other_idx = None
        self._saved_self_idx = None

    def _save(self, *, other=None, self_=None):
        tensors = []
        if other is not None:
            self._saved_other_idx = len(tensors)
            tensors.append(other)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_other_idx is not None:
            self._saved_fields['other'] = self._saved_tensors_list[self._saved_other_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        other = _saved[self._saved_other_idx]
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self, grad_other = _special_xlogy_backward_all(grad, self_, other, keyset)
        return (grad_self, grad_other,)


class SquareBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='SquareBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_self_idx = None

    def _save(self, *, self_=None):
        tensors = []
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self = redispatch("mul", keyset, grad, redispatch("mul", keyset, _scalar_tensor_like(self_, 2.0), self_))
        return (grad_self,)


class Take_along_dimBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='Take_along_dimBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_indices_idx = None
        self._saved_input_idx = None
        self._dim = None

    def _save(self, *, indices=None, input_=None):
        tensors = []
        if indices is not None:
            self._saved_indices_idx = len(tensors)
            tensors.append(indices)
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_indices_idx is not None:
            self._saved_fields['indices'] = self._saved_tensors_list[self._saved_indices_idx]
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        indices = _saved[self._saved_indices_idx]
        input_ = _saved[self._saved_input_idx]
        dim = self._dim
        with _grad_context(keyset):
            grad_input = _take_along_dim_backward_helper(grad, input_, indices, dim, keyset)
        return (grad_input,)


class TensordotBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='TensordotBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_other_idx = None
        self._saved_self_idx = None
        self._dims = None

    def _save(self, *, other=None, self_=None):
        tensors = []
        if other is not None:
            self._saved_other_idx = len(tensors)
            tensors.append(other)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_other_idx is not None:
            self._saved_fields['other'] = self._saved_tensors_list[self._saved_other_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        other = _saved[self._saved_other_idx]
        self_ = _saved[self._saved_self_idx]
        dims = self._dims
        with _grad_context(keyset):
            grad_self, grad_other = _tensordot_backward_all(grad, self_, other, dims, keyset)
        return (grad_self, grad_other,)


class TileBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='TileBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._dims = None

    def _save(self, *, input_=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        dims = self._dims
        with _grad_context(keyset):
            grad_input = _tile_backward_helper(grad, input_, dims, keyset)
        return (grad_input,)


class True_divideBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='True_divideBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_other_idx = None
        self._saved_self_idx = None

    def _save(self, *, other=None, self_=None):
        tensors = []
        if other is not None:
            self._saved_other_idx = len(tensors)
            tensors.append(other)
        if self_ is not None:
            self._saved_self_idx = len(tensors)
            tensors.append(self_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_other_idx is not None:
            self._saved_fields['other'] = self._saved_tensors_list[self._saved_other_idx]
        if self._saved_self_idx is not None:
            self._saved_fields['self'] = self._saved_tensors_list[self._saved_self_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        other = _saved[self._saved_other_idx]
        self_ = _saved[self._saved_self_idx]
        with _grad_context(keyset):
            grad_self, grad_other = _div_backward_all(grad, self_, other, keyset)
        return (grad_self, grad_other,)


class UnflattenBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='UnflattenBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset
        self._saved_input_idx = None
        self._dim = None
        self._sizes = None

    def _save(self, *, input_=None):
        tensors = []
        if input_ is not None:
            self._saved_input_idx = len(tensors)
            tensors.append(input_)
        if tensors:
            super().save_for_backward(*tensors)
        if self._saved_input_idx is not None:
            self._saved_fields['input'] = self._saved_tensors_list[self._saved_input_idx]

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        _saved = self.saved_tensors()
        input_ = _saved[self._saved_input_idx]
        dim = self._dim
        sizes = self._sizes
        with _grad_context(keyset):
            grad_input = redispatch("reshape", keyset, grad, input_.shape)
        return (grad_input,)


class VstackBackward0(Node):
    def __init__(self, inputs, *, raw_keyset=None, active_keyset=None):
        super().__init__(None, inputs, name='VstackBackward0')
        self._raw_keyset = raw_keyset
        self._active_keyset = active_keyset

    def backward(self, grad):
        from .._dispatch.dispatcher import current_dispatch_keyset
        keyset = _backward_dispatch_keyset(self._raw_keyset, self._active_keyset)
        tensors = list(self.inputs)
        with _grad_context(keyset):
            grad_tensors = _vstack_backward_helper(grad, tensors, keyset)
        return grad_tensors
