"""Generate forward autograd wrappers from DifferentiabilityInfo."""
from __future__ import annotations

from .model import DifferentiabilityInfo
from .gen_functions import _safe_param


def gen_variable_type(infos: list[DifferentiabilityInfo]) -> str:
    """Generate the full variable_type.py source."""
    parts = [_HEADER]
    for info in infos:
        parts.append(_gen_one_wrapper(info))
    for info in infos:
        parts.append(_gen_one_post_wrapper(info))
    seen_ops = set()
    alias_lines = []
    for info in infos:
        if info.op_name in seen_ops:
            continue
        seen_ops.add(info.op_name)
        canonical = f"{info.op_name}_autograd"
        canonical_post = f"{info.op_name}_autograd_post"
        specific = f"{info.generated_func_stem}_autograd"
        specific_post = f"{info.generated_func_stem}_autograd_post"
        if canonical != specific:
            alias_lines.append(f"{canonical} = {specific}")
        if canonical_post != specific_post:
            alias_lines.append(f"{canonical_post} = {specific_post}")
    if alias_lines:
        parts.append("\n\n# Canonical overload aliases")
        parts.extend(alias_lines)
    return "\n".join(parts) + "\n"


_HEADER = '''\
"""Auto-generated forward autograd wrappers — DO NOT EDIT.

Generated by tools/autograd/gen_variable_type.py from derivatives.yaml.
"""
from __future__ import annotations

from ..autograd.grad_mode import GradMode
from ..autograd.anomaly_mode import annotate_node_creation
from .._backends.autograd import _strip_autograd_keys
from .._dispatch.dispatcher import current_dispatch_keyset, redispatch
from . import functions as _F
'''


def _gen_one_wrapper(info: DifferentiabilityInfo) -> str:
    cls_name = info.backward_name
    op_name = info.op_name
    func_name = f"{info.generated_func_stem}_autograd"

    tensor_args = info.tensor_args
    non_tensor_args = info.non_tensor_args
    saved_inputs = info.all_saved_inputs
    saved_outputs = info.all_saved_outputs
    diff_inputs = info.differentiable_inputs

    lines: list[str] = []

    # Build function signature
    params = []
    for arg in info.args:
        param_name = _safe_param(arg.name)
        if arg.default is not None:
            params.append(f"{param_name}={arg.default}")
        else:
            params.append(param_name)
    sig = ", ".join(params)
    lines.append(f"\n\ndef {func_name}({sig}, **_kwargs):")

    # Dispatch setup
    lines.append("    active_keyset = current_dispatch_keyset()")
    lines.append("    raw_keyset = _strip_autograd_keys(active_keyset)")

    # Forward call
    fwd_args = ", ".join([f'"{op_name}"', "raw_keyset"] + [_safe_param(a.name) for a in info.args])
    lines.append(f"    result = redispatch({fwd_args}, **_kwargs)")

    # Check requires_grad — Tensor[] needs iteration
    if len(diff_inputs) == 1:
        a = diff_inputs[0]
        safe_name = _safe_param(a.name)
        if a.is_tensor_list:
            rg_check = f"any(getattr(t, 'requires_grad', False) for t in {safe_name})"
        elif a.is_optional_tensor:
            rg_check = f"({safe_name} is not None and {safe_name}.requires_grad)"
        else:
            rg_check = f"{safe_name}.requires_grad"
    elif len(diff_inputs) > 1:
        parts = []
        for a in diff_inputs:
            safe_name = _safe_param(a.name)
            if a.is_tensor_list:
                parts.append(f"any(getattr(t, 'requires_grad', False) for t in {safe_name})")
            elif a.is_optional_tensor:
                parts.append(f"({safe_name} is not None and getattr({safe_name}, 'requires_grad', False))")
            else:
                parts.append(f"getattr({safe_name}, 'requires_grad', False)")
        rg_check = " or ".join(parts)
    else:
        # No differentiable inputs — skip autograd entirely
        lines.append("    return result")
        return "\n".join(lines)

    lines.append(f"    if GradMode.enabled and ({rg_check}):")

    # Build inputs list for Node — Tensor[] needs unpacking
    has_tensor_list = any(a.is_tensor_list for a in diff_inputs)
    if has_tensor_list:
        # Unpack tensor lists into the inputs tuple
        unpack_parts = []
        for a in diff_inputs:
            safe_name = _safe_param(a.name)
            if a.is_tensor_list:
                unpack_parts.append(f"*{safe_name}")
            else:
                unpack_parts.append(safe_name)
        inputs_expr = f"({', '.join(unpack_parts)},)"
    elif any(a.is_optional_tensor for a in diff_inputs):
        input_names = [_safe_param(a.name) for a in diff_inputs]
        lines.append(f"        _inputs = [x for x in ({', '.join(input_names)},) if x is not None]")
        inputs_expr = "_inputs"
    else:
        input_names = [_safe_param(a.name) for a in diff_inputs]
        inputs_expr = f"({', '.join(input_names)},)"

    lines.append(f"        grad_fn = _F.{cls_name}({inputs_expr}, raw_keyset=raw_keyset, active_keyset=active_keyset)")
    lines.append(f"        annotate_node_creation(grad_fn)")

    # Save tensors — skip Tensor[] args (they're accessed via Node.inputs)
    if saved_inputs or saved_outputs:
        save_kw = []
        for name in saved_inputs:
            arg = next((a for a in info.args if a.name == name), None)
            if arg and arg.is_tensor_list:
                continue  # Tensor[] saved via Node.inputs, not _save()
            save_kw.append(f"{_safe_param(name)}={_safe_param(name)}")
        for name in saved_outputs:
            # Multi-output: result0 -> result[0], result1 -> result[1], etc.
            if name == "result":
                save_kw.append(f"{_safe_param(name)}=result")
            elif name.startswith("result") and name[6:].isdigit():
                idx = name[6:]
                save_kw.append(f"{_safe_param(name)}=result[{idx}]")
            else:
                save_kw.append(f"{_safe_param(name)}=result")
        if save_kw:
            lines.append(f"        grad_fn._save({', '.join(save_kw)})")

    # Save non-tensor args
    for arg in non_tensor_args:
        lines.append(f"        grad_fn._{arg.name} = {_safe_param(arg.name)}")

    # Attach grad_fn
    if info.is_multi_output:
        out_diff = info.output_differentiability
        for i in range(info.num_outputs):
            if out_diff is None or (i < len(out_diff) and out_diff[i]):
                lines.append(f"        result[{i}].grad_fn = grad_fn")
                lines.append(f"        result[{i}].requires_grad = True")
    else:
        lines.append("        result.grad_fn = grad_fn")
        lines.append("        result.requires_grad = True")
    lines.append("    return result")

    return "\n".join(lines)


def _gen_one_post_wrapper(info: DifferentiabilityInfo) -> str:
    """Generate a _post variant that only does autograd post-processing.

    The _post variant receives the already-computed result and the original
    args, plus raw_keyset and active_keyset.  It attaches grad_fn if needed
    and returns the result.
    """
    cls_name = info.backward_name
    op_name = info.op_name
    func_name = f"{info.generated_func_stem}_autograd_post"

    non_tensor_args = info.non_tensor_args
    saved_inputs = info.all_saved_inputs
    saved_outputs = info.all_saved_outputs
    diff_inputs = info.differentiable_inputs

    lines: list[str] = []

    # Build function signature: result, <original params>, *, raw_keyset, active_keyset
    params = []
    for arg in info.args:
        param_name = _safe_param(arg.name)
        if arg.default is not None:
            params.append(f"{param_name}={arg.default}")
        else:
            params.append(param_name)
    sig = ", ".join(["result"] + params + ["*", "raw_keyset", "active_keyset"])
    lines.append(f"\n\ndef {func_name}({sig}, **_kwargs):")

    # No differentiable inputs — nothing to do, just return result
    if not diff_inputs:
        lines.append("    return result")
        return "\n".join(lines)

    # Check requires_grad — same logic as the regular wrapper
    if len(diff_inputs) == 1:
        a = diff_inputs[0]
        safe_name = _safe_param(a.name)
        if a.is_tensor_list:
            rg_check = f"any(getattr(t, 'requires_grad', False) for t in {safe_name})"
        elif a.is_optional_tensor:
            rg_check = f"({safe_name} is not None and {safe_name}.requires_grad)"
        else:
            rg_check = f"{safe_name}.requires_grad"
    else:
        parts = []
        for a in diff_inputs:
            safe_name = _safe_param(a.name)
            if a.is_tensor_list:
                parts.append(f"any(getattr(t, 'requires_grad', False) for t in {safe_name})")
            elif a.is_optional_tensor:
                parts.append(f"({safe_name} is not None and getattr({safe_name}, 'requires_grad', False))")
            else:
                parts.append(f"getattr({safe_name}, 'requires_grad', False)")
        rg_check = " or ".join(parts)

    lines.append(f"    if GradMode.enabled and ({rg_check}):")

    # Build inputs list for Node — same logic as regular wrapper
    has_tensor_list = any(a.is_tensor_list for a in diff_inputs)
    if has_tensor_list:
        unpack_parts = []
        for a in diff_inputs:
            safe_name = _safe_param(a.name)
            if a.is_tensor_list:
                unpack_parts.append(f"*{safe_name}")
            else:
                unpack_parts.append(safe_name)
        inputs_expr = f"({', '.join(unpack_parts)},)"
    elif any(a.is_optional_tensor for a in diff_inputs):
        input_names = [_safe_param(a.name) for a in diff_inputs]
        lines.append(f"        _inputs = [x for x in ({', '.join(input_names)},) if x is not None]")
        inputs_expr = "_inputs"
    else:
        input_names = [_safe_param(a.name) for a in diff_inputs]
        inputs_expr = f"({', '.join(input_names)},)"

    lines.append(f"        grad_fn = _F.{cls_name}({inputs_expr}, raw_keyset=raw_keyset, active_keyset=active_keyset)")
    lines.append(f"        annotate_node_creation(grad_fn)")

    # Save tensors
    if saved_inputs or saved_outputs:
        save_kw = []
        for name in saved_inputs:
            arg = next((a for a in info.args if a.name == name), None)
            if arg and arg.is_tensor_list:
                continue
            save_kw.append(f"{_safe_param(name)}={_safe_param(name)}")
        for name in saved_outputs:
            if name == "result":
                save_kw.append(f"{_safe_param(name)}=result")
            elif name.startswith("result") and name[6:].isdigit():
                idx = name[6:]
                save_kw.append(f"{_safe_param(name)}=result[{idx}]")
            else:
                save_kw.append(f"{_safe_param(name)}=result")
        if save_kw:
            lines.append(f"        grad_fn._save({', '.join(save_kw)})")

    # Save non-tensor args
    for arg in non_tensor_args:
        lines.append(f"        grad_fn._{arg.name} = {_safe_param(arg.name)}")

    # Attach grad_fn to result(s)
    if info.is_multi_output:
        out_diff = info.output_differentiability
        for i in range(info.num_outputs):
            if out_diff is None or (i < len(out_diff) and out_diff[i]):
                lines.append(f"        result[{i}].grad_fn = grad_fn")
                lines.append(f"        result[{i}].requires_grad = True")
    elif info.returns and info.returns[0].type.endswith('[]'):
        lines.append("        for i in range(len(result)):")
        lines.append("            result[i].grad_fn = grad_fn")
        lines.append("            result[i].requires_grad = True")
    else:
        lines.append("        result.grad_fn = grad_fn")
        lines.append("        result.requires_grad = True")
    lines.append("    return result")

    return "\n".join(lines)


# ---------------------------------------------------------------------------
# Task 3: Cython forward wrapper generator
# ---------------------------------------------------------------------------

_PYX_HEADER = '''\
# cython: language_level=3, wraparound=False
# Auto-generated Cython forward autograd wrappers — DO NOT EDIT.
# Generated by tools/autograd/gen_variable_type.py from derivatives.yaml.

# Module-level cached references to avoid repeated attribute lookup.
# Populated lazily on first call via _ensure_refs().
cdef object _GradMode
cdef object _annotate_node_creation
cdef object _strip_autograd_keys
cdef object _current_dispatch_keyset
cdef object _redispatch
cdef object _F

_GradMode = None
_annotate_node_creation = None
_strip_autograd_keys = None
_current_dispatch_keyset = None
_redispatch = None
_F = None


cdef inline void _ensure_refs():
    global _GradMode, _annotate_node_creation, _strip_autograd_keys
    global _current_dispatch_keyset, _redispatch, _F
    if _GradMode is None:
        from ..autograd.grad_mode import GradMode as _gm
        _GradMode = _gm
        from ..autograd.anomaly_mode import annotate_node_creation as _anc
        _annotate_node_creation = _anc
        from .._backends.autograd import _strip_autograd_keys as _sak
        _strip_autograd_keys = _sak
        from .._dispatch.dispatcher import current_dispatch_keyset as _cdk
        _current_dispatch_keyset = _cdk
        from .._dispatch.dispatcher import redispatch as _rd
        _redispatch = _rd
        from . import functions as _f
        _F = _f
'''


def _gen_one_wrapper_pyx(info: DifferentiabilityInfo) -> str:
    """Generate a single Cython `def` forward wrapper for one op.

    Mirrors _gen_one_wrapper exactly but:
    - uses `def` (not `cpdef`) because signature ends with **_kwargs
    - calls _ensure_refs() at top and uses cached module-level names
    - avoids negative indexing (wraparound=False footgun)
    """
    cls_name = info.backward_name
    op_name = info.op_name
    func_name = f"{info.generated_func_stem}_autograd"

    diff_inputs = info.differentiable_inputs
    non_tensor_args = info.non_tensor_args
    saved_inputs = info.all_saved_inputs
    saved_outputs = info.all_saved_outputs

    lines: list[str] = []

    # Build function signature — same as Python version
    params = []
    for arg in info.args:
        param_name = _safe_param(arg.name)
        if arg.default is not None:
            params.append(f"{param_name}={arg.default}")
        else:
            params.append(param_name)
    sig = ", ".join(params)
    lines.append(f"\n\ndef {func_name}({sig}, **_kwargs):")
    lines.append("    _ensure_refs()")
    lines.append("    active_keyset = _current_dispatch_keyset()")
    lines.append("    raw_keyset = _strip_autograd_keys(active_keyset)")

    # Forward call
    fwd_args = ", ".join([f'"{op_name}"', "raw_keyset"] + [_safe_param(a.name) for a in info.args])
    lines.append(f"    result = _redispatch({fwd_args}, **_kwargs)")

    # Check requires_grad — identical logic to Python version
    if len(diff_inputs) == 1:
        a = diff_inputs[0]
        safe_name = _safe_param(a.name)
        if a.is_tensor_list:
            rg_check = f"any(getattr(t, 'requires_grad', False) for t in {safe_name})"
        elif a.is_optional_tensor:
            rg_check = f"({safe_name} is not None and {safe_name}.requires_grad)"
        else:
            rg_check = f"{safe_name}.requires_grad"
    elif len(diff_inputs) > 1:
        parts = []
        for a in diff_inputs:
            safe_name = _safe_param(a.name)
            if a.is_tensor_list:
                parts.append(f"any(getattr(t, 'requires_grad', False) for t in {safe_name})")
            elif a.is_optional_tensor:
                parts.append(f"({safe_name} is not None and getattr({safe_name}, 'requires_grad', False))")
            else:
                parts.append(f"getattr({safe_name}, 'requires_grad', False)")
        rg_check = " or ".join(parts)
    else:
        # No differentiable inputs
        lines.append("    return result")
        return "\n".join(lines)

    lines.append(f"    if _GradMode.enabled and ({rg_check}):")

    # Build inputs list for Node — same logic as Python version
    has_tensor_list = any(a.is_tensor_list for a in diff_inputs)
    if has_tensor_list:
        unpack_parts = []
        for a in diff_inputs:
            safe_name = _safe_param(a.name)
            if a.is_tensor_list:
                unpack_parts.append(f"*{safe_name}")
            else:
                unpack_parts.append(safe_name)
        inputs_expr = f"({', '.join(unpack_parts)},)"
    elif any(a.is_optional_tensor for a in diff_inputs):
        input_names = [_safe_param(a.name) for a in diff_inputs]
        lines.append(f"        _inputs = [x for x in ({', '.join(input_names)},) if x is not None]")
        inputs_expr = "_inputs"
    else:
        input_names = [_safe_param(a.name) for a in diff_inputs]
        inputs_expr = f"({', '.join(input_names)},)"

    lines.append(f"        grad_fn = _F.{cls_name}({inputs_expr}, raw_keyset=raw_keyset, active_keyset=active_keyset)")
    lines.append("        _annotate_node_creation(grad_fn)")

    # Save tensors
    if saved_inputs or saved_outputs:
        save_kw = []
        for name in saved_inputs:
            arg = next((a for a in info.args if a.name == name), None)
            if arg and arg.is_tensor_list:
                continue
            save_kw.append(f"{_safe_param(name)}={_safe_param(name)}")
        for name in saved_outputs:
            if name == "result":
                save_kw.append(f"{_safe_param(name)}=result")
            elif name.startswith("result") and name[6:].isdigit():
                idx = name[6:]
                save_kw.append(f"{_safe_param(name)}=result[{idx}]")
            else:
                save_kw.append(f"{_safe_param(name)}=result")
        if save_kw:
            lines.append(f"        grad_fn._save({', '.join(save_kw)})")

    # Save non-tensor args
    for arg in non_tensor_args:
        lines.append(f"        grad_fn._{arg.name} = {_safe_param(arg.name)}")

    # Attach grad_fn to result(s)
    if info.is_multi_output:
        out_diff = info.output_differentiability
        for i in range(info.num_outputs):
            if out_diff is None or (i < len(out_diff) and out_diff[i]):
                lines.append(f"        result[{i}].grad_fn = grad_fn")
                lines.append(f"        result[{i}].requires_grad = True")
    elif info.returns and info.returns[0].type.endswith('[]'):
        lines.append("        for i in range(len(result)):")
        lines.append("            result[i].grad_fn = grad_fn")
        lines.append("            result[i].requires_grad = True")
    else:
        lines.append("        result.grad_fn = grad_fn")
        lines.append("        result.requires_grad = True")
    lines.append("    return result")

    return "\n".join(lines)


def _gen_one_post_wrapper_pyx(info: DifferentiabilityInfo) -> str:
    """Generate a Cython `def` _post forward wrapper.

    Mirrors _gen_one_post_wrapper exactly but uses cached module-level refs.
    Uses `def` (not `cpdef`) because signature ends with **_kwargs.
    Avoids negative indexing (wraparound=False footgun).
    """
    cls_name = info.backward_name
    func_name = f"{info.generated_func_stem}_autograd_post"

    non_tensor_args = info.non_tensor_args
    saved_inputs = info.all_saved_inputs
    saved_outputs = info.all_saved_outputs
    diff_inputs = info.differentiable_inputs

    lines: list[str] = []

    # Build function signature: result, <original params>, *, raw_keyset, active_keyset
    params = []
    for arg in info.args:
        param_name = _safe_param(arg.name)
        if arg.default is not None:
            params.append(f"{param_name}={arg.default}")
        else:
            params.append(param_name)
    sig = ", ".join(["result"] + params + ["*", "raw_keyset", "active_keyset"])
    lines.append(f"\n\ndef {func_name}({sig}, **_kwargs):")

    if not diff_inputs:
        lines.append("    return result")
        return "\n".join(lines)

    lines.append("    _ensure_refs()")

    # requires_grad check — same as Python version
    if len(diff_inputs) == 1:
        a = diff_inputs[0]
        safe_name = _safe_param(a.name)
        if a.is_tensor_list:
            rg_check = f"any(getattr(t, 'requires_grad', False) for t in {safe_name})"
        elif a.is_optional_tensor:
            rg_check = f"({safe_name} is not None and {safe_name}.requires_grad)"
        else:
            rg_check = f"{safe_name}.requires_grad"
    else:
        parts = []
        for a in diff_inputs:
            safe_name = _safe_param(a.name)
            if a.is_tensor_list:
                parts.append(f"any(getattr(t, 'requires_grad', False) for t in {safe_name})")
            elif a.is_optional_tensor:
                parts.append(f"({safe_name} is not None and getattr({safe_name}, 'requires_grad', False))")
            else:
                parts.append(f"getattr({safe_name}, 'requires_grad', False)")
        rg_check = " or ".join(parts)

    lines.append(f"    if _GradMode.enabled and ({rg_check}):")

    # Build inputs list for Node
    has_tensor_list = any(a.is_tensor_list for a in diff_inputs)
    if has_tensor_list:
        unpack_parts = []
        for a in diff_inputs:
            safe_name = _safe_param(a.name)
            if a.is_tensor_list:
                unpack_parts.append(f"*{safe_name}")
            else:
                unpack_parts.append(safe_name)
        inputs_expr = f"({', '.join(unpack_parts)},)"
    elif any(a.is_optional_tensor for a in diff_inputs):
        input_names = [_safe_param(a.name) for a in diff_inputs]
        lines.append(f"        _inputs = [x for x in ({', '.join(input_names)},) if x is not None]")
        inputs_expr = "_inputs"
    else:
        input_names = [_safe_param(a.name) for a in diff_inputs]
        inputs_expr = f"({', '.join(input_names)},)"

    lines.append(f"        grad_fn = _F.{cls_name}({inputs_expr}, raw_keyset=raw_keyset, active_keyset=active_keyset)")
    lines.append("        _annotate_node_creation(grad_fn)")

    # Save tensors
    if saved_inputs or saved_outputs:
        save_kw = []
        for name in saved_inputs:
            arg = next((a for a in info.args if a.name == name), None)
            if arg and arg.is_tensor_list:
                continue
            save_kw.append(f"{_safe_param(name)}={_safe_param(name)}")
        for name in saved_outputs:
            if name == "result":
                save_kw.append(f"{_safe_param(name)}=result")
            elif name.startswith("result") and name[6:].isdigit():
                idx = name[6:]
                save_kw.append(f"{_safe_param(name)}=result[{idx}]")
            else:
                save_kw.append(f"{_safe_param(name)}=result")
        if save_kw:
            lines.append(f"        grad_fn._save({', '.join(save_kw)})")

    # Save non-tensor args
    for arg in non_tensor_args:
        lines.append(f"        grad_fn._{arg.name} = {_safe_param(arg.name)}")

    # Attach grad_fn to result(s)
    if info.is_multi_output:
        out_diff = info.output_differentiability
        for i in range(info.num_outputs):
            if out_diff is None or (i < len(out_diff) and out_diff[i]):
                lines.append(f"        result[{i}].grad_fn = grad_fn")
                lines.append(f"        result[{i}].requires_grad = True")
    elif info.returns and info.returns[0].type.endswith('[]'):
        lines.append("        for i in range(len(result)):")
        lines.append("            result[i].grad_fn = grad_fn")
        lines.append("            result[i].requires_grad = True")
    else:
        lines.append("        result.grad_fn = grad_fn")
        lines.append("        result.requires_grad = True")
    lines.append("    return result")

    return "\n".join(lines)


def gen_variable_type_pyx(infos: list) -> str:  # type: ignore[type-arg]
    """Generate _variable_type_cy.pyx — Cython compiled forward autograd wrappers."""
    parts = [_PYX_HEADER]
    for info in infos:
        parts.append(_gen_one_wrapper_pyx(info))
    for info in infos:
        parts.append(_gen_one_post_wrapper_pyx(info))
    seen_ops: set[str] = set()
    alias_lines = []
    for info in infos:
        if info.op_name in seen_ops:
            continue
        seen_ops.add(info.op_name)
        canonical = f"{info.op_name}_autograd"
        canonical_post = f"{info.op_name}_autograd_post"
        specific = f"{info.generated_func_stem}_autograd"
        specific_post = f"{info.generated_func_stem}_autograd_post"
        if canonical != specific:
            alias_lines.append(f"{canonical} = {specific}")
        if canonical_post != specific_post:
            alias_lines.append(f"{canonical_post} = {specific_post}")
    if alias_lines:
        parts.append("\n\n# Canonical overload aliases")
        parts.extend(alias_lines)
    return "\n".join(parts) + "\n"
